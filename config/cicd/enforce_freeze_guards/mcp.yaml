per_file_rules:
- pattern: mcp/server.py
  rules:
  - FG1
  reason: _ToolDef.schema_properties uses a direct MappingProxyType wrap over recursively copied JSON schema mappings while preserving JSON Schema list shape.
  expires: null
  max_hits: 1
