# Architecture Overview Has Moved

The maintained ELSPETH architecture overview is now the repository-root
[ARCHITECTURE.md](../../ARCHITECTURE.md).

This file remains only as a compatibility pointer for older links. The previous
content here was an early design snapshot and is no longer the current
architecture entry point.
