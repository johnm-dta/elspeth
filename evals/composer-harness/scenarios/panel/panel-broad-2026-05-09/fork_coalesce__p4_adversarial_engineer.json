{
  "scenario_id": "fork_coalesce__p4_adversarial_engineer",
  "cohort": "panel-broad-2026-05-09",
  "example": "fork_coalesce",
  "variant": null,
  "persona": "p4_adversarial_engineer",
  "task_class": "panel",
  "task_summary": "Panel cohort cell: p4_adversarial_engineer asks composer to build the fork_coalesce pipeline shape. Generated from panel-broad-2026-05-09.yaml on the panel-broad cohort.",
  "opening_prompt": "I need a fork/coalesce setup. CSV source with product data (id, product, price, category, description), truncate the description field to 40 chars, then fork every row into two parallel paths. Merge them back with nested strategy \u2014 I want both branches' results under separate keys in the output. Route the merged data to a JSONL sink. Dump the audit log to SQLite.",
  "structural_target": {
    "example_name": "fork_coalesce",
    "variant": null,
    "settings_path": "/home/john/elspeth/examples/fork_coalesce/settings.yaml",
    "source": {
      "plugin": "csv",
      "shape_token": "csv",
      "path": "examples/fork_coalesce/input.csv",
      "schema_mode": "fixed",
      "columns": [
        "id",
        "product",
        "price",
        "category",
        "description"
      ]
    },
    "transforms": [
      {
        "name": "preprocess",
        "plugin": "truncate",
        "shape_token": "truncate"
      }
    ],
    "gates": [
      {
        "name": "fork_gate",
        "condition": "True",
        "is_fork": true,
        "fork_paths": [
          "path_a",
          "path_b"
        ],
        "routes": {
          "true": "fork",
          "false": "output"
        }
      },
      {
        "name": "route_output",
        "condition": "True",
        "is_fork": false,
        "fork_paths": [],
        "routes": {
          "true": "output",
          "false": "output"
        }
      }
    ],
    "aggregations": [],
    "coalesce_nodes": [
      {
        "name": "merge_results"
      }
    ],
    "sinks": [
      {
        "name": "output",
        "plugin": "json",
        "shape_token": "json"
      }
    ]
  },
  "red_criteria": {
    "passivity_phrases": [
      "if you want, i can",
      "if you'd like, i can",
      "should i ",
      "do you want me to",
      "would you like me to",
      "shall i ",
      "let me know if"
    ],
    "build_failure_sentinels": [
      "i cannot mark this pipeline complete",
      "runtime preflight failed"
    ],
    "must_be_valid": true,
    "passivity_phrases_note": "Lower-case substring match against final assistant content. The skill (anti-passivity section) explicitly forbids these phrases \u2014 any hit is RED. Standard list shared with the convergence suite.",
    "build_failure_note": "These strings are server-injected by service.py:_build_runtime_preflight_message() when the model declares completion but the pipeline fails preflight. Their presence is definitive evidence the model failed to converge."
  },
  "green_criteria": {
    "must_be_valid": true,
    "must_not_contain_passivity": true,
    "must_have_outputs_min": 1,
    "must_have_node_kinds_substring_any_of": [
      [
        "csv",
        "truncate",
        "fork",
        "gate",
        "coalesce",
        "json"
      ]
    ],
    "must_have_node_chain_in_order": [
      "csv",
      "truncate",
      "fork",
      "gate",
      "coalesce",
      "json"
    ],
    "must_include_observed_columns": [
      "id",
      "product",
      "price",
      "category",
      "description"
    ]
  }
}
