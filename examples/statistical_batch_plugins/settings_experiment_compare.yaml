sources:
  primary:
    plugin: csv
    on_success: experiment_in
    options:
      path: examples/statistical_batch_plugins/experiment_scores.csv
      schema:
        mode: fixed
        fields:
        - 'id: int'
        - 'prompt_variant: str'
        - 'score: float'
      on_validation_failure: discard
aggregations:
- name: prompt_experiment
  plugin: batch_experiment_compare
  input: experiment_in
  on_success: output
  on_error: discard
  trigger:
    count: 8
  output_mode: transform
  options:
    schema:
      mode: fixed
      fields:
      - 'id: int'
      - 'prompt_variant: str'
      - 'score: float'
    variant_field: prompt_variant
    score_field: score
    baseline_variant: control
sinks:
  output:
    plugin: json
    on_write_failure: discard
    options:
      path: examples/statistical_batch_plugins/output/experiment_compare.jsonl
      schema:
        mode: observed
      format: jsonl
landscape:
  url: sqlite:///examples/statistical_batch_plugins/runs/experiment_compare.db
