# Reaudit report

## Summary

| Divergence | Count |
| --- | --- |
| SOURCE_EXCERPT_REJECTED | 0 |
| FUTURE_DATED_ENTRY | 0 |
| JUDGE_CALL_FAILED | 1 |
| JUDGE_CLASSIFICATION_FAILED | 0 |
| AMBIGUOUS_FINDING_MATCH | 0 |
| WAS_ACCEPTED_NOW_BLOCKED | 0 |
| PRE_JUDGE_FRESH_BLOCK | 290 |
| OVERRIDE_NO_LONGER_NEEDED | 0 |
| ENTRY_OBSOLETE | 0 |
| OVERRIDE_STILL_NEEDED | 0 |
| PRE_JUDGE_FRESH_ACCEPT | 245 |
| STILL_AGREES | 0 |
| _entries dispatched_ | 536 / 536 |
| _entries skipped by rule_filter_ | 0 |
| _judge calls attempted_ | 536 |
| _prompt tokens_ | total=4304082; cached=3595735; uncached=708347 |

## JUDGE_CALL_FAILED (1)

| Entry | Original Verdict | Fresh Verdict | Fresh Model | Cause | Notes |
| --- | --- | --- | --- | --- | --- |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=c0f2bbfd5e6c57ed` | <pre-judge> | <no judge call> | &lt;no judge call&gt; | JUDGE_BOUNDARY_FAILURE | JudgeContractError: judge returned non-JSON response; refusing to coerce. raw: '{"verdict": "BLOCKED", "rationale": "The function _inline_blob_content_for_proposal takes \`arguments: Mapping\[str, Any\]\` which is external tool-call data \(Tier-3\), the finding is R1 \(in the suppressible set\), and the subject \`arguments.get\(\\\\"source\\\\"\)\` is rooted directly at the external parameter. All three decorator-test conditions are met. The agent\\'s rationale is also boilerplate \(\\'ALLOWLIST-FRESH\\'\) and provides no site-specific justification' |

## PRE_JUDGE_FRESH_BLOCK (290)

| Entry | Original Verdict | Fresh Verdict | Fresh Model | Cause | Notes |
| --- | --- | --- | --- | --- | --- |
| `contracts/schema_contract_factory.py:R1:create_contract_from_config:fp=caa0070eb7edad8e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) here is on normalized_to_original, a process-local dict built two lines earlier from field_resolution — this is Tier-2 internal data, not Tier-3 external input. The rationale frames this as an 'intentional identity fallback', but that is exactly the defensive-with-default pattern the policy forbids outside trust boundaries: it silently masks the case where a declared field name is missing from the resolution map. If field_resolution is provided but lacks fd.name, that is an upstream invariant break that should surface, not be papered over with fd.name. The correct fix is a code change \(explicit conditional: use fd.name only when field_resolution is None/empty; otherwise index directly and let KeyError surface\), not an allowlist entry. The @trust_boundary decorator does not apply — no external parameter is involved. |
| `contracts/plugin_context.py:R1:PluginContext:record_call:fp=8f7477d482ca1864` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is inside PluginContext.record_call, which is engine/contract infrastructure receiving response_data from a plugin caller — not a function decorated as a Tier-3 boundary on an external parameter that flows directly from an external system at this site. Per the Plugin Ownership policy, plugin-returned data is system-owned: if a plugin claims to be an LLM call but returns a response_snapshot without a usage field, that's either legitimately absent \(record None\) or a plugin bug \(crash\). The correct shape here is direct access guarded by membership/type checks that fail loudly, or pushing the Tier-3 coercion into the LLM source plugin that constructs response_data from the raw provider response. Suppressing R1 here entrenches defensive .get\(\) in a contracts-layer helper, which is the wrong layer for boundary coercion. The function signature is not visible in the excerpt to confirm a source_param, so the decorator nudge is not safe to issue. |
| `core/canonical.py:R6:_normalize_value:fp=b26d50562c16f852` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a silent except: TypeError swallow on a numpy operation inside core canonicalization code, not a Tier-3 boundary. The rationale's claim \('non-numeric arrays can't contain NaN'\) is plausible but the suppression masks any TypeError from np.isnan/isinf, including bugs we'd want to surface. The offensive-programming fix is to dispatch on dtype explicitly \(e.g., check np.issubdtype\(obj.dtype, np.number\) or np.floating\) and only call isnan/isinf on numeric arrays, eliminating the try/except entirely. R6 \(silent exception handling\) is correctly flagging defensive control flow in code we own; the remediation is dtype-gated logic, not an allowlist entry. |
| `core/events.py:R1:EventBus:emit:fp=6f513a170f117689` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`self._subscribers.get\(type\(event\), \(\)\)\` inside EventBus.emit — \`self._subscribers\` is a process-local dict owned by EventBus itself, not external/Tier-3 data. The rationale 'optional field access at trust boundary' is a misapplication of the Tier-3 vocabulary: there is no external boundary here. The correct pattern is either direct subscript with a guaranteed-initialized dict, or use of a defaultdict/explicit branch, not a silent \`.get\(\)\` suppression. The rationale is also boilerplate and gives no site-specific reasoning. |
| `core/dag/graph.py:R6:ExecutionGraph:warn_divert_coalesce_interactions:fp=ea839197606998d2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale describes the code mechanics \(silently swallowing KeyError/GraphValidationError from _trace_branch_endpoints\) but does not justify why silent suppression is the correct response. This is a warning/analysis pass over internal DAG structures \(Tier-1/Tier-2 data we own\), not a Tier-3 boundary. Per the Decision Test, if _branch_info isn't populated when we're tracing branch endpoints, that's either an invariant violation \(let it crash\) or a known-legitimate state that should be checked explicitly rather than caught-and-passed. The rationale even admits the catch hides a 'forward reference or optional path' case — those are predicates that can be tested offensively, not exceptions to swallow. R6 \(silent exception swallow\) on internal graph traversal is exactly the pattern the policy forbids. |
| `core/retention/purge.py:R4:PurgeManager:purge_payloads:fp=ed928398c920ef43` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale invokes a real concern \(payloads already deleted, loop must continue\), but the chosen recovery channel violates the telemetry primacy order: pipeline-relevant events about stale reproducibility grades are being diverted to logger.warning instead of being recorded as an audit fact in the Landscape. Per policy, 'we log it anyway' is not a valid justification — audit fires first, sync, crash-on-failure; logging is last resort. Additionally, Tier-1 audit-DB grade updates failing silently \(caught by bare \`except Exception\`\) is precisely the scenario the auditability standard forbids: 'I don't know what happened' becomes the answer for the affected run's grade. The correct remediation is to record the failure as an audit event \(and accumulate failures in the PurgeResult, which the code partially does\) rather than suppress an R4 finding on the broad except. The agent should fix the channel, not allowlist the pattern. |
| `core/checkpoint/recovery.py:R6:RecoveryManager:get_resume_point:fp=c48ba8486b6c2e03` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The checkpoint is Tier-1 ELSPETH-owned data \(audit/recovery state we wrote ourselves\). Policy is explicit: 'crash on any anomaly — serialization doesn't change trust tier' and 'a defective plugin that silently produces wrong results is worse than a crash.' Catching IncompatibleCheckpointError and silently falling back to None converts a catastrophic invariant break \(persisted checkpoint incompatible with current code\) into a silent full-recompute, destroying the audit signal that a schema/migration mismatch occurred. The 'post-deploy migration' framing is precisely the No-Legacy-Code/transitional reasoning the policy rejects — if the checkpoint schema changed, the migration must be explicit, not papered over by a swallow-and-recompute path. Let it crash; surface the incompatibility to operators via the audit trail. |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:create_row:fp=90984799b1de3e99` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This code is inside the Landscape repository \(Tier-1, our data\) writing payload persistence. The rationale claims Tier-3 boundary, but by the time data reaches DataFlowRepository.create_row it has already been processed by sources; the 'quarantined' flag is metadata we set, not an external trust boundary. More importantly, the suppression is on a try/except that silently swallows ValueError/TypeError from canonical_json and fabricates a {'_repr': repr\(data\)} payload — this is exactly the silent-recovery-with-fabricated-default pattern the policy forbids on the audit-write path. Even for quarantined rows, the audit trail must be deterministic: either canonical_json succeeds, or the failure should surface, not be replaced with a repr\(\) blob that future auditors cannot reconcile against the original value. The decorator does not apply \(rule R6 is not in {R1,R5}; this is not a function-parameter Tier-3 boundary\). |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:record_transform_error:fp=b4edfad55e521778` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-1 write boundary \(DataFlowRepository writing to the Landscape audit database\), not a Tier-3 boundary. The rationale conflates the origin of the data \(transform results, which are Tier-2 pipeline data\) with the act of writing to the audit trail. Per policy, the audit trail must be 100% pristine and crash on anomalies — silently substituting a repr-based fallback when canonical_json rejects NaN/Infinity is exactly the 'evidence tampering' the Tier-1 policy forbids. If error_details can contain non-canonical floats, the fix is upstream: sanitize/coerce at the transform boundary or reject the error record loudly, not swallow ValueError at the write site. The rule_id is R6 \(not R1/R5\), so the @trust_boundary decorator is not applicable, and the enclosing function writes our own data anyway. |
| `engine/dependency_resolver.py:R1:_load_depends_on:fp=0ecc84f5b6b35d41` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(operator-authored YAML file\) and R1 is in the decorator's suppressible rule set. The subject \`loaded.get\("depends_on", \[\]\)\` is rooted in \`loaded\`, which is the parsed YAML produced inside this function from \`settings_path\`. The structural remediation is \`@trust_boundary\` on the enclosing function with \`source_param\` pointing at the external parameter \(the file path / loaded payload\) rather than a per-line allowlist entry. Since the excerpt shows \`settings_path\` as the external input parameter feeding \`loaded\`, the decorator should target that parameter; the agent should restructure so the suppression scope is the function, not this line. |
| `engine/executors/state_guard.py:R5:NodeStateGuard:_extract_audit_evidence_context:fp=81df711769fff87f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an isinstance check on the return value of a system-owned plugin/contract method \(AuditEvidenceBase.to_audit_dict\), not on Tier-3 external data. Per Plugin Ownership policy, a plugin returning the wrong type is a bug in our code that should crash, not be defensively type-checked. The rationale conflates 'contractually required to serialize to a mapping' with a trust boundary — the contract should be enforced by the type system and the plugin's correctness, not by runtime isinstance guards. Additionally, the surrounding try/except Exception swallows all failures into a returned exception, which is exactly the defensive pattern the policy forbids for system code. R5 here is not a Tier-3 boundary case; the decorator does not apply. |
| `engine/executors/state_guard.py:R4:NodeStateGuard:_extract_audit_evidence_context:fp=d9eb19b04d13628e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a broad \`except Exception\` around code we control: \`to_audit_dict\(\)\` is a method on our own AuditEvidenceBase plugin hierarchy \(Tier-1/system code, not Tier-3 external data\), and \`canonical_json\` is our serializer. Per Plugin Ownership policy, a defective plugin that returns the wrong shape should crash, not be silently swallowed — and per the Defensive vs Offensive test, swallowing exceptions from code-we-control with a None default is forbidden defensive programming. The rationale \('cannot strand node_states OPEN'\) describes a real concern, but the correct remediation is structural: the state-guard's terminal-failure path should be robust to exceptions in evidence extraction at the guard's own boundary \(e.g. the caller records the auto-fail regardless\), not by laundering arbitrary bugs into None here. The audit trail is the legal record; silently dropping evidence context on serialization bugs is exactly the 'I don't know what happened' outcome the Auditability Standard forbids. |
| `engine/orchestrator/core.py:R6:Orchestrator:_handle_quarantine_row:fp=49d051713c4a7398` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is in engine/orchestrator/core.py — engine code handling row data that has already passed through a source plugin. By the time the orchestrator is computing a content hash on source_item.row, the row is Tier-2 pipeline data, not Tier-3 external input; the source plugin is the Tier-3 boundary, and it is responsible for coercing/quarantining non-canonical floats before handing rows to the engine. If stable_hash raises ValueError/TypeError here, that indicates the source produced row contents stable_hash cannot handle — a plugin contract violation that should crash per the Plugin Ownership policy, not be silently rerouted to repr_hash with a slog.debug \(which also inverts the audit/telemetry/logging primacy order: this 'which hash path was used' fact is not in Landscape, it's in a debug log\). The rationale also misuses the Tier-3 label to justify a fallback inside the engine layer. Fix the root cause at the source/quarantine boundary, or make stable_hash's contract explicit and let violations surface. |
| `mcp/server.py:R6:_find_audit_databases:fp=badb2efd50be18f6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a single boilerplate sentence \('Silent recovery with explicit error handling path'\) that doesn't engage with this site. The code catches OSError on db_file.stat\(\) and substitutes mtime=0 — this is fabrication of a sort-key value when the filesystem call fails, and it silently swallows the error. Per the Defensive vs Offensive test, an OSError when stat'ing a file we just discovered is an invalid state worth surfacing, not papering over with a default. If the file truly vanished between discovery and stat, the honest path is to skip it \(record absence\) or let it crash, not synthesize mtime=0 which silently sorts the file as 'oldest possible'. The rationale also fails the auditability standard — a future reader cannot tell from 'silent recovery with explicit error handling path' why mtime=0 is the correct fabricated value. |
| `mcp/server.py:R6:_prompt_for_database:fp=250dc301eb3eda7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with this specific site. The code catches ValueError from Path.relative_to\(\) to fall back to the absolute path for display purposes — this is a legitimate offensive pattern \(using the exception as control flow to detect non-relative paths\), but the rationale provided fails to explain that. A suppression's rationale is itself an audit record; 'silent recovery' is precisely the defensive-programming language the policy forbids, and a future auditor reading this entry would have no idea why it was accepted. The agent should rewrite the rationale to name the actual semantics \(Path.relative_to raises ValueError when the path is not under search_path; the except branch is the documented API for that case\), or restructure to avoid the exception-as-control-flow if R6 forbids it here. |
| `mcp/server.py:R6:_prompt_for_database:fp=dbbeb81e161a9a1d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is generic boilerplate that does not justify the specific suppression. While catching EOFError/KeyboardInterrupt at an interactive-input boundary is plausibly legitimate \(user cancellation via Ctrl-C/Ctrl-D is a real Tier-3 input signal, not a bug to crash on\), the rationale fails the Auditability Standard: a future auditor reading 'silent recovery with explicit error handling path' learns nothing about why these two specific exceptions are caught here or what invariant the handler preserves. The handler is also not silent — it writes 'Cancelled' and returns None, which contradicts the rationale's own wording. The agent should rewrite the rationale to name the Tier-3 source \(interactive stdin\), the two specific signals \(EOF and SIGINT\) being treated as 'user cancelled selection', and why returning None is the correct contract here. |
| `mcp/server.py:R6:_prompt_for_database:fp=b20406c8a83aad1b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a one-line boilerplate phrase \('Silent recovery with explicit error handling path'\) that does not engage with the policy. While catching ValueError on int\(\) of user keyboard input at an interactive CLI prompt is plausibly a legitimate Tier-3 boundary pattern \(user input is external, untrusted\), the rationale provides no audit-quality justification: it doesn't identify the input as Tier-3, doesn't name the invariant, and doesn't explain why the error message + reprompt loop is the correct recovery. An allowlist entry is itself an audit record; this one is uninterpretable to a future auditor. The agent should either write a site-specific rationale naming the trust boundary \(interactive stdin\) and the recovery contract, or restructure so the suppression is unnecessary. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=054e76166ade23bc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale references csv.Error and skip_rows parsing, but the excerpt shows an \`except ValidationError as e:\` handler — the rationale does not match the code at the finding site. The handler catches a project-defined ValidationError \(Tier-2 schema validation outcome\), not a CSV parser error from malformed external bytes. R6 suppressions need a site-specific justification grounded in the actual exception type and trust tier; this rationale appears to describe a different except clause. The decorator nudge does not apply \(R6 not in the suppressible set {R1, R5}\). |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=0fb2b154fb016195` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale doesn't match the code shown. The excerpt at line 441 catches StopIteration in the main data-row processing loop \(after headers have already been discovered, per the surrounding context about ContractBuilder and 'first valid row'\), not during header discovery as the rationale claims. The catch is the standard idiom for terminating a manual next\(\)-driven iteration loop, which is a legitimate offensive pattern, but the rationale describes a different site entirely — a corrupted audit record under the Auditability Standard. The agent must rewrite the rationale to honestly describe what this StopIteration handler actually does \(loop termination on end-of-data-rows\) before this can be accepted. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=bb231ad98805d136` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale references 'ValidationError' and 'source schema validation', but the code excerpt shows an \`except csv.Error\` clause at a Tier-3 CSV parsing boundary — these are unrelated. The rationale does not describe what is happening at this site, so it fails the Auditability Standard: a future auditor reading this allowlist entry would not understand why catching csv.Error is legitimate here. Catching csv.Error at the external-data boundary may well be correct \(Tier-3, parser-state corruption documented inline\), but the agent must supply a rationale that actually matches the code. R6 is not in the @trust_boundary decorator's suppressible set \(R1/R5\), so the decorator nudge does not apply. |
| `plugins/sources/dataverse.py:R1:DataverseSource:_normalize_row_fields:fp=b9db16c1b599becb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R1 finding is on \`mapping.get\(k\)\` where \`mapping\` is \`self._field_resolution.resolution_mapping\` — a process-local dict built by our own code, not Tier-3 external data. The rationale conflates the row's Tier-3 origin with the mapping lookup, but the suppressed expression is a lookup in an internal data structure where \`None\` is a legitimate sentinel for 'key not present' and the code already handles it explicitly on the next line. This is a normal dict membership check, not defensive suppression of external-data shape uncertainty; the decorator would also not apply since the subject is not rooted at an external parameter. If R1 flags \`dict.get\` with explicit None-handling as defensive, the fix is either to use \`if k in mapping: ... else: ...\` / \`try/except KeyError\`, or to broaden the rule — not a per-line allowlist entry justified by a tier the expression doesn't operate on. |
| `plugins/infrastructure/manager.py:R4:PluginManager:register:fp=0e88b71c015f8aa0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is not a Tier-3 trust boundary — it's a rollback path inside our own plugin manager catching exceptions from pluggy operations on system-owned code. The rationale frames it as 'protecting rollback' but the effect is exactly what the policy forbids: silently swallowing an exception \(downgraded to a logger.error, which violates the telemetry primacy order\) from system code we control. If rollback fails, that is a catastrophic invariant break in pluggy state and should propagate, not be logged-and-dropped; Python's implicit chaining via \`raise\` after a failed except would correctly surface both exceptions. The 'might bury the original' concern is solvable structurally \(capture the original, raise it with \`from\`\) rather than via a broad \`except Exception\` suppression. R4 here flags real defensive programming on Tier-1/internal control flow. |
| `plugins/infrastructure/batching/mixin.py:R9:BatchTransformMixin:_release_loop:fp=2f93bd40222e5b89` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is internal pipeline state \(\`self._batch_submissions\`\), not Tier-3 external data — the dict is populated by code we control. The rationale invokes 'race' and 'optional scenarios' but doesn't explain the invariant: either submission tracking is consistent with the entries flowing through the buffer \(in which case \`.pop\(\)\` without default would surface the bug\), or it isn't \(in which case silently swallowing the miss hides a real defect\). Per the Defensive vs Offensive Decision Test, this is suppressing a KeyError from code we own — the fix is to establish and assert the invariant, not to mask it. A vague rationale like 'may not exist in race scenarios' is exactly the corrupted audit record the policy forbids. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=188d682d00e17ea1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate that does not engage with the specific finding \(R6 on a try/except in a release loop\). The excerpt shows three except clauses; the judge cannot tell which one R6 targets or why suppression is warranted at this specific site. A legitimate suppression rationale must name what is being caught, why it is safe to recover, and what audit/telemetry channel records the event. 'Silent recovery' is in fact the defensive pattern the policy forbids — the rationale describes the violation, not a justification. This is not a Tier-3 external-data boundary; the data is internal queue state, so the @trust_boundary decorator does not apply. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=24666a1e729638d7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is a one-line boilerplate that does not identify what is being suppressed, why it is legitimate at this site, or which tier of data is involved. The excerpt shows a broad \`except Exception\` in a worker loop — exactly the kind of catch-all that the policy treats as forbidden defensive programming unless tightly justified. The audit-record standard requires a site-specific explanation \(what invariant the catch preserves, why re-raise is not appropriate for the non-None-token branch, how the wrapped exception reaches the waiter\), and this rationale provides none of that. The decorator nudge does not apply: R6 is not in {R1, R5} and there is no external Tier-3 parameter. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=153b600f7b824428` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding occurs inside a function that takes external provider data as a parameter \(\`usage\`\), R1 is in the decorator's suppressible set, and the subject \`usage.get\(...\)\` is rooted directly at that parameter. This is a textbook structural Tier-3 boundary case: the correct remediation is \`@trust_boundary\(tier=3, source='LLM provider usage payload', source_param='usage', suppresses=\('R1',\), invariant='returns TokenUsage with None for missing/non-int fields', ...\)\` on \`_extract_usage_from_provider_response\`, not a per-line allowlist entry. The rationale itself is honest and correct about the tier and absence-preservation, but the suppression mechanism is wrong. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=54f676db05cacf08` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case that matches all three decorator criteria: the function takes external provider usage data as the \`usage\` parameter, the finding is R1 \(in the suppressible set\), and the subject \`usage.get\(...\)\` is rooted directly at that parameter. The rationale is substantively correct about the tier and absence-vs-fabrication handling, but the correct remediation is \`@trust_boundary\` on \`_extract_usage_from_provider_response\` with \`source_param='usage'\`, not a per-line allowlist entry. The decorator would also cover the symmetric \`getattr\` calls in the else branch under the same invariant. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=3ce6385129d45fcc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook structural Tier-3 boundary case: the function takes external provider usage data \(Tier 3\), R1 is in the decorator's suppressible set, and the subject \`usage.get\(...\)\` is rooted at the \`usage\` parameter. The correct remediation is to apply \`@trust_boundary\(tier=3, source='LLM provider usage payload', source_param='usage', suppresses=\('R1',\), ...\)\` on \`_extract_usage_from_provider_response\` rather than adding a per-line allowlist entry. The rationale itself is honest and policy-aligned \(preserving absence as None, no fabrication\), but the structural alternative supersedes the YAML entry. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=ac3495bbb7ec9639` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case for R1: the function takes external Azure Search API response data via the response_data parameter, R1 is in the decorator's suppressible set, and the subject item.get\("@search.score"\) is rooted in response_data \(item comes from response_data\["value"\]\). The correct remediation is the @trust_boundary decorator on _parse_response with source_param="response_data", not a per-line allowlist entry. The rationale itself is sound \(Tier-3 boundary, absence recorded in skipped_items rather than fabricated\), but the structural fix supersedes the allowlist approach. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=866a8f498d10e0fe` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is R1 on an external Azure Search API response \(Tier 3\), and the data flow is rooted at the function parameter response_data → results → item. This is exactly the structural Tier-3 boundary pattern the @trust_boundary decorator exists to handle: _parse_response is the boundary function, R1 is in the suppressible set, and item derives from response_data via iteration over response_data\['value'\]. The correct remediation is decorating _parse_response with @trust_boundary\(tier=3, source_param='response_data', suppresses=\('R1','R5'\), ...\) rather than a per-line allowlist entry, which would also cover the sibling item.get calls on lines 264 and 271. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=d3ae8b449f567803` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: _parse_response processes external Azure Search API JSON, the rule is R1 \(suppressible by @trust_boundary\), and the subject item.get\('id'\) is rooted in iteration over response_data\['value'\] which originates from an external parameter. The correct remediation is to apply @trust_boundary\(tier=3, source='Azure AI Search API response', source_param=&lt;the response parameter&gt;, suppresses=\('R1','R5'\), ...\) to _parse_response rather than per-line allowlist entries — there are multiple R1/R5 sites in this function and a per-line approach will not scale. The excerpt does not show the function signature, but the agent should identify the response parameter and apply the decorator. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=1a56c5169fca221b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is a structural Tier-3 boundary case: the function parses an external Azure AI Search JSON response, R1 is in the decorator's suppressible set, and the subject \`item.get\("content"\)\` is rooted in data flowing from the external API response parameter. The correct remediation is to apply \`@trust_boundary\(tier=3, source='Azure AI Search response', source_param=&lt;param&gt;, suppresses=\('R1','R5'\), ...\)\` to the enclosing parser rather than a per-line allowlist entry. The excerpt does not show the function signature, so the agent must identify the correct external parameter name; the rationale itself is accurate about the tier but the structural fix supersedes the line suppression. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=46bde1db12749de8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is a structural Tier-3 boundary case: the function parses an Azure AI Search API response \(external data\), R1 is in the decorator's suppressible set, and the subject \`item.get\("id"\)\` is rooted at an item from the external response payload. The correct remediation is \`@trust_boundary\` on the enclosing parse function with \`source_param\` pointing at the response/items parameter, not a per-line allowlist entry. The rationale itself is site-appropriate \(Tier-3 external API, recording absence honestly\), but the structural decorator is the policy-preferred mechanism for this pattern. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=3c62e2c3ff824fd7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary parsing an Azure Search API response, and the finding subject \(item.get\('id'\)\) is rooted at an external item dict — the canonical R1-on-external-data pattern. The structural remediation is @trust_boundary\(tier=3, source_param=...\) on _parse_response \(or whatever method iterates the response items\), not a per-line allowlist entry. The excerpt does not show the function signature, but the symbol AzureSearchProvider._parse_response and the data-flow pattern strongly indicate an external-response parameter is in scope. Emit the decorator nudge so this and the sibling item.get\(...\) sites are covered structurally rather than line-by-line. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=423ca2aea34a9f56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function parses an Azure AI Search API response \(external Tier-3 data\), the rule is R1 \(silent .get\(\) on external data\), and the subject \`item.get\("id"\)\` is rooted in iteration over the external response payload. The correct remediation is \`@trust_boundary\` on \`_parse_response\` with the appropriate source_param \(the parameter carrying the Azure response\), not a per-line allowlist entry. Note: the excerpt does not show the function signature, so the exact parameter name must be confirmed by the agent — but the pattern \(loop variable \`item\` derived from an external response\) is the canonical decorator case. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=8c1f201c7b5ddd49` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(Azure AI Search API response\) and R1 is in the suppressible set, with the subject rooted in the per-item dict iterated from the response. The correct remediation is the @trust_boundary decorator on _parse_response \(or its iteration scope\) with source_param naming the response/item parameter, not a per-line allowlist entry. The excerpt does not show the function signature or decorators, but the data-flow pattern \(item.get on external response items, with explicit skip-on-absence honoring the no-fabrication rule\) is exactly the structural case the decorator exists to cover. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=f9be71eef8e59cb3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is at line 273 on \`item.get\("id"\)\` inside what appears to be a loop iterating over Azure Search API response items — genuine Tier-3 external data. This is exactly the structural pattern the @trust_boundary decorator exists to handle: R1 on subscript/\`.get\(\)\` access rooted at an external parameter \(the parsed Azure response\). The agent should decorate \`_parse_response\` with \`@trust_boundary\(tier=3, source='Azure AI Search query response', source_param=&lt;the response param&gt;, suppresses=\('R1',\), ...\)\` rather than adding per-line allowlist entries — there are multiple \`.get\(\)\` calls visible in this excerpt alone \(lines 264, 271, 273, 282, 287\), confirming the decorator is the correct structural fix. I cannot see the function signature in the excerpt so I am guessing the parameter name; the agent should substitute the actual external-data parameter. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_complete_ticket:fp=9b67bbe4194832ff` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is too terse to function as an audit record. It asserts a race exists \('completing a worker result can race with timeout eviction or shutdown'\) but does not explain why KeyError and ShutdownError are the legitimate, expected signals of those races, why swallowing them \(rather than letting them surface\) is safe, or what invariant guarantees no other KeyError source is being masked. This is system-owned plugin code, not a Tier-3 boundary — the @trust_boundary decorator does not apply \(rule R6 is not in its suppressible set, and there is no external parameter\). A future auditor reading this allowlist entry would not be able to confirm the suppression is narrow and correct. The agent should rewrite the rationale to name the specific producers of each exception \(buffer.complete raises KeyError when the ticket was already evicted by the timeout sweeper; ShutdownError when the buffer was closed\), and why debug-logging is the correct response in both cases. |
| `plugins/infrastructure/config_base.py:R1:_format_validation_error_cause:fp=06bc6e6d57f87e71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is the output of Pydantic's ValidationError.errors\(\) — a library we control the version of via pyproject, not Tier-3 external data. Pydantic's documented schema for error dicts includes 'type'; if a version omits it, that's a dependency-contract break that should crash loudly \(offensive programming\), not be silently treated as 'not missing'. Note also that the same function uses bare error\['loc'\] and error\['msg'\] two lines above — singling out 'type' for defensive .get\(\) is inconsistent and reveals this is not a real trust boundary. The decorator nudge does not apply: the function parameter is a Pydantic ValidationError \(our dependency's typed object\), not external user input, so R1 here is a code-fix task \(use error\['type'\]\), not a suppression candidate. |
| `plugins/transforms/rag/transform.py:R2:RAGRetrievalTransform:_configured_collection_name:fp=757eb1e99aea1a3e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-2 pipeline concern, not a Tier-3 boundary: provider_config is a validated provider-specific model from our own config system, which means the set of fields is known statically. The correct offensive pattern is explicit attribute access on the concrete union members \(e.g. isinstance/match on the model type, then direct .collection or .index access\), or a protocol/abstract property that every provider config must implement. Using getattr\(..., None\) to probe two mutually exclusive field names suppresses the very interface violation R2 is designed to surface — a new provider config type that exposes neither would silently fall through to the FrameworkBugError instead of failing at type-check time. The @trust_boundary decorator does not apply \(R2 is not in its suppressible set, and provider_config is not external Tier-3 data\). |
| `plugins/transforms/rag/transform.py:R5:RAGRetrievalTransform:_configured_collection_name:fp=b22badfe2569d3ea` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is Tier-2 internal data, not an external trust boundary. The provider_config is a validated pydantic model produced by ELSPETH's own configuration system — accessing its fields should be direct attribute access on the typed model, not a getattr+isinstance dance over a tuple of possible field names. The isinstance check is defensive shape-guarding against our own validated config, which the policy forbids. Additionally, getattr\(..., None\) on a typed config object is itself a defensive-programming smell that should be addressed at the root: either the config schema guarantees one of these fields \(then access it directly\) or the provider config types should expose a uniform property. The R5 finding is correct; the fix is structural, not a suppression. |
| `plugins/sinks/chroma_sink.py:R6:ChromaSink:write:fp=8501da8c0e5bf4c0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale describes 'per-row KeyError catch for optional metadata fields', but the code excerpt at the finding site catches KeyError on REQUIRED fields \(id_field and document_field\) — not optional metadata. This is a Tier-2 pipeline-data context \(rows arriving at a Sink\); per policy, sinks expect type/shape conformance and should not coerce or recover from missing required fields. If id_field or document_field is missing, that is an upstream schema violation that should surface, not be diverted row-by-row. The rationale also does not match the code it is attached to, which is itself disqualifying as an audit record. Decorator nudge does not apply: this is a Sink on Tier-2 data, not a Tier-3 external boundary, and the rule is R6 \(not R1/R5\). |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=12aaa412bd91a810` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Sink operating on Tier-2 pipeline data, not a Tier-3 external boundary. Per policy: 'Sink: no coercion, expect types.' If a row reaches the sink with a non-str id or document, that is an upstream source/transform bug and should crash, not be silently diverted. The rationale also misdescribes the finding \(claims it guards float NaN/Infinity, but the flagged isinstance checks are on raw_id/raw_doc against str\). The decorator nudge does not apply: this is not a Tier-3 boundary function and \`rows\` is Tier-2 pipeline data, not an external parameter being validated at ingress. |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=87156d7ec508a40b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims this is a Tier-2→external trust boundary, but the cited finding at line 311 is an isinstance\(str\) check on raw_doc, not a float/NaN guard. The rationale describes a different code site \(non-finite float metadata\) than the one flagged. Furthermore, a sink operates on Tier-2 pipeline data per policy \('Sink: no coercion, expect types'\) — if upstream produced a non-str document, that is an upstream plugin bug, not a per-row data problem to divert. The rationale's text doesn't match the code, and the trust-tier framing misapplies the manifesto. |
| `plugins/sinks/chroma_sink.py:R6:ChromaSink:write:fp=fe6fa1b761d7dd9c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Sink operating on Tier-2 pipeline data \(rows from upstream transforms/sources\). Per the Three-Tier Trust Model, sinks must not coerce or defensively recover — if a configured metadata field is missing from a row, that's an upstream schema/source bug and should surface, not be silently skipped. The rationale 'metadata is optional per-field' is a configuration claim, not a tier claim: if optionality is real, the source should emit None for absent fields \(recording absence honestly\) and the sink should access row\[field\] directly, treating None as 'not present.' Swallowing KeyError fabricates the absence signal at the sink layer, violating the no-coercion-at-sink rule and the fabrication-decision test. The decorator does not apply: this is a sink on Tier-2 data, not a Tier-3 external boundary, and R6 is not in the decorator's suppressible set. |
| `plugins/transforms/web_scrape.py:R5:WebScrapeHTTPConfig:_validate_allowed_hosts:fp=922ac0bb4c76376b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R5 finding is on \`isinstance\(v, str\)\` inside a Pydantic field_validator whose type annotation is \`str \| list\[str\]\`. This is a union-type discriminator, not a Tier-3 shape guard against malformed external data — Pydantic has already validated that \`v\` conforms to the union before the validator runs. The rationale misidentifies the purpose of the isinstance check \(it's branching between the two legal shapes of a validated union, not defending against arbitrary input\) and also mentions \`ipaddress.ip_network\` which is on a different line than the flagged one. Additionally, the @trust_boundary decorator's source_param mechanism is designed for function-parameter rooted access in regular functions, not classmethod field_validators where the discriminator is intrinsic to the declared union type; the correct disposition is to handle this via a per-file or rule-level policy for union-discriminator isinstance, not a per-line allowlist with a mismatched rationale. |
| `plugins/transforms/web_scrape.py:R5:WebScrapeTransform:get_post_call_hints:fp=f919002b3beb8525` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function signature shows config_snapshot typed as Mapping\[str, object\] — this is a pipeline configuration snapshot, not an LLM-supplied tool-call payload. Configuration is system-owned \(Tier 1/2\), not Tier-3 external data. The rationale mischaracterizes the tier to justify the isinstance guard. If text_separator is typed/validated upstream as str by the plugin's config schema, accessing it directly and letting type errors surface is the correct offensive posture; if it's genuinely 'object' because the schema is loose, the fix is to tighten the schema, not suppress R5 at the use site. The decorator nudge doesn't apply because config_snapshot is not an external-data parameter in the Tier-3 sense. |
| `plugins/sinks/azure_blob_sink.py:R6:AzureBlobSink:write:fp=304f148f13df7df3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH ... review whether this is new debt'\) that explicitly admits it has not justified the suppression. That fails the auditability standard — a future auditor reading this allowlist record would find no site-specific justification. Separately, the code itself looks defensible \(catching json.dumps failures on Tier-2/3 row data and diverting per on_write_failure is a legitimate per-row trust-boundary handling pattern\), but the judge does not write the rationale; the agent must. Resubmit with a rationale that names the tier of the data, why exception-catching is the correct channel here \(per-row divert vs. crash\), and why narrowing to ValueError/TypeError is appropriate. |
| `plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_with_per_row_diversion:fp=9e9d1f67fecbd6f2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly asking the reviewer to determine whether this is new debt or needs justification. That is not a justification at all; it is a non-answer that cannot serve as an audit record. The code catches IntegrityError to perform per-row diversion of user data on INSERT, which may well be a legitimate Tier-3 boundary pattern \(quarantine bad user rows, continue\), but the agent must articulate that reasoning explicitly with reference to the trust tier and the diversion contract. Without a site-specific rationale, the suppression cannot land. |
| `plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_with_per_row_diversion:fp=d64a7ca89b0cfe0e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH... review whether this is new debt'\) and provides no site-specific justification at all — it explicitly asks for review rather than asserting why suppression is correct. Per the Auditability Standard, this is a corrupted audit record. On the merits, the code catches IntegrityError from a database write to route the offending row to the diversion path; this may well be a legitimate trust-boundary catch \(database is an external system, per-row attribution is the documented behavior\), but the agent must write that rationale explicitly rather than submit a placeholder. The decorator nudge does not apply: R6 is not in the {R1, R5} suppressible set, and the subject is an exception from a DB call, not a parameter-rooted external dict. |
| `plugins/sinks/csv_sink.py:R6:CSVSink:_stage_rows_per_row:fp=73ebd4f4b271eb4f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) rather than a site-specific justification. It explicitly asks for review and provides no argument that the suppression is legitimate. Per the Auditability Standard, an allowlist entry with no real rationale is a corrupted audit record and must be refused regardless of whether the underlying try/except around csv serialization might be defensible on its own merits. The agent must supply a real justification \(or remove the suppression\) before this can be reconsidered. |
| `plugins/sinks/json_sink.py:R6:JSONSink:write:fp=70e172c6d4480200` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is the placeholder boilerplate \('ALLOWLIST-FRESH — no stale entry matched...'\) which is explicitly an instruction to review, not a justification. It contains no site-specific reasoning about why catching ValueError/TypeError around json.dumps is legitimate here. While the code itself looks defensible \(per-row Tier-2 serialization fault routed to a divert channel rather than silently swallowed, preserving auditability\), the audit-record standard requires the rationale itself to explain that — a vague placeholder is a corrupted audit record. The agent must replace the rationale with a site-specific justification naming the Tier-2 per-row fault, the divert path, and why crashing the batch would be wrong. |
| `plugins/sinks/json_sink.py:R6:JSONSink:_write_jsonl_content:fp=7f2a8740a6f881fa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH — review whether this is new debt'\) and provides no site-specific justification for suppressing R6 at this location. Per the Auditability Standard, an allowlist entry with no substantive reasoning is a corrupted audit record. The code itself may well be legitimate \(catching ValueError/TypeError from json.dumps on per-row Tier-2 data and diverting to the failsink is a sensible row-quarantine pattern at a sink boundary\), but the judge cannot accept a suppression on the basis of code-shape alone when the operator has explicitly flagged this as unreviewed debt. The agent must write a real rationale explaining why this except clause is the correct row-quarantine boundary, or fix the root cause. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=e59ce88529f38df2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is pure boilerplate \('ALLOWLIST-FRESH — no stale entry matched...'\) and provides no site-specific justification for suppressing R6 at this location. The code excerpt shows a targeted except ExternalHeaderError handler at a Tier-3 boundary \(external blob headers\) that records and quarantines — this may well be legitimate defensive code at a trust boundary, but the agent must articulate that reasoning, not paste a fingerprint-rotation note. An allowlist entry is itself an audit record; a future auditor cannot interpret 'ALLOWLIST-FRESH' as a justification. The agent should either write a real per-site rationale explaining why catching ExternalHeaderError here is the correct Tier-3 boundary handling, or restructure the function under @trust_boundary if appropriate. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=7b8583638b096231` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is the ALLOWLIST-FRESH placeholder text — it explicitly says 'review whether this is new debt that needs a source fix or a more specific justification' and provides no site-specific justification. An audit record consisting of a 'please review' placeholder is exactly the corrupted audit record the Auditability Standard forbids. Additionally, I cannot determine the nature of R6 here or whether the suppression is warranted without a real rationale; the burden is on the proposer to supply one. The agent must either remove the entry \(let the code stand without suppression\) or write a concrete, site-specific justification grounded in the Tier-3 header-fault handling visible in the excerpt. |
| `plugins/infrastructure/validation.py:R1:_extract_errors:fp=9240c6f6517a323b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | Pydantic's ValidationError.errors\(\) is a stable, well-documented contract from a library we depend on — not Tier-3 external data. The surrounding code already accesses err\['loc'\], err\['msg'\], and err\['input'\] directly \(correctly\), trusting Pydantic's schema. Singling out err\['type'\] for defensive .get\(\) is inconsistent and invokes a 'version compatibility' concern that resembles the forbidden backwards-compatibility/shim pattern. If a future Pydantic version omitted 'type', that's a dependency-pinning issue and an interface-violation crash is the correct response, matching how the adjacent keys are handled. The decorator does not apply: this is not function-parameter external data, it's a library return value. |
| `telemetry/manager.py:R6:TelemetryManager:handle_event:fp=64535719a1fd4215` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with the policy. This is in telemetry/manager.py — telemetry is best-effort per the primacy order, but the finding \(R6\) catches queue.Full and increments a drop counter while only logging an error. Per the Telemetry primacy order, the correct channel for a pipeline-relevant event \(dropped telemetry\) is not logger.error — and more importantly, the rationale offers no site-specific justification for why catching queue.Full here is correct. The agent must write a rationale that explains why this specific catch is the right boundary handling for a bounded queue under BLOCK-mode timeout, not a generic phrase reusable on any try/except. |
| `telemetry/manager.py:R6:TelemetryManager:_drop_oldest_and_enqueue_newest:fp=1c75bf1b3c8e4241` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Queue overflow handling at telemetry backpressure boundary' is a category label, not a site-specific justification. It does not identify what R6 is flagging, why the suppression is correct at this exact location, or what invariant the code maintains. The excerpt shows internal queue management on process-local state \(self._queue\), not a Tier-3 external-data boundary — telemetry backpressure is an internal concurrency concern, not an untrusted-input boundary. Without knowing what R6 reports here, and given the rationale provides no site-specific reasoning, this is an uninterpretable audit record. The agent must either justify the specific construct R6 flagged or restructure the code. |
| `telemetry/manager.py:R6:TelemetryManager:_drop_oldest_and_enqueue_newest:fp=07c7b40d564bb0bc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Queue overflow handling at telemetry backpressure boundary' is a topic label, not a justification. It does not explain why catching RuntimeError and logging+returning is correct here rather than defensive swallowing. The code catches an exception from an internal helper \(_requeue_shutdown_sentinel_or_raise\) on process-local state — this is not a Tier-3 external boundary. Per the Telemetry primacy order, diverting events to logger.warning and incrementing a drop counter is itself questionable, and the suppression must be defended on its merits with a site-specific argument about why propagation would be wrong and why the recovery semantics are correct. A future auditor reading 'backpressure boundary' as the audit record cannot reconstruct the decision. |
| `telemetry/manager.py:R6:TelemetryManager:_requeue_shutdown_sentinel_or_raise:fp=9a2e20048626df2b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a one-line slogan \('Sentinel reinsertion loop handles bounded queue contention'\) that names what the code does but provides no audit-grade justification for why R6 should be suppressed at this site. I cannot tell from the rationale what R6 flags here \(likely the broad except queue.Full / queue.Empty handlers\), why catching them is correct rather than defensive, or what invariant the function preserves on failure. The docstring already explains the design — the allowlist entry must explain why the static rule is wrong for this specific construct, not restate the function's purpose. An auditor reading this entry later would not be able to reconstruct the decision. The data is not Tier-3 external input; this is internal queue-contention handling, so the @trust_boundary decorator does not apply. |
| `telemetry/manager.py:R6:TelemetryManager:close:fp=79190bde3fa222f5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with the actual code or policy. This is not a Tier-3 trust boundary — it's internal queue management on a process-local queue we own. The queue.Full and queue.Empty handlers are part of a deliberate shutdown drain algorithm, not defensive suppression of unexpected errors; the rationale should explain that the exceptions are expected control-flow signals in Python's queue API used to implement the drain-and-retry loop, and that the loop bound plus the final 'if not sentinel_sent' error log preserves auditability. As written, the rationale fails the audit standard: a future auditor reading 'silent recovery with explicit error handling path' cannot tell why this site is legitimate. Agent should rewrite the rationale to describe the specific invariant \(sentinel-insertion guarantee during shutdown\) and why queue.Full/queue.Empty are expected signals here. |
| `tui/widgets/lineage_tree.py:R1:LineageTree:_build_tree:fp=60a7a8bf5283d1ba` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) here is on \`tokens_by_sink\`, a process-local dict built moments earlier in the same function \(Step 1\), not on Tier-3 external data. The sparseness is a property of code we control: not every sink has tokens, so the dict legitimately lacks some keys. The correct offensive pattern is direct membership check or building the dict with all sink_node_ids pre-populated, but more directly: this is a dict-lookup-with-default on an internal data structure, which is idiomatic Python — if this is being flagged as R1, the resolution is to use \`tokens_by_sink\[sink_node_id\] if sink_node_id in tokens_by_sink else \(\)\` or restructure, not to suppress. The rationale describes the data shape but does not establish a trust boundary, and the decorator does not apply \(subject is not rooted at an external parameter\). |
| `web/auth/oidc.py:R1:JWKSTokenValidator:decode_token:fp=c10514f46a44024c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: decode_token receives an external JWT string, parses its header, and the R1 finding on header.get\("kid"\) reads from data rooted at that externally-derived header. All three decorator conditions are met — external-data parameter \(token, from which header is derived\), rule_id R1, and subject rooted in the parsed external payload. The correct remediation is @trust_boundary on decode_token rather than a per-line allowlist entry. Note: the decorator's source_param matches a function parameter; since header is derived from token, the agent should pass source_param="token" \(or restructure so the header dict is the parameter\). |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_validate_discovery_document:fp=e21f32481fc9784c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(OIDC discovery document from an external IdP\) and R1 is in the decorator's suppressible set, and the subject \`discovery.get\('jwks_uri'\)\` is rooted at the function parameter \`discovery\`. All three conditions for the @trust_boundary structural alternative are met, so the correct remediation is to decorate \`_validate_discovery_document\` with \`@trust_boundary\(tier=3, source='OIDC discovery document from IdP', source_param='discovery', suppresses=\('R1',\), invariant=..., test_ref=..., test_fingerprint=...\)\` rather than add a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_validate_jwks_document:fp=6494c39fcc6d20fc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary \(JWKS document fetched from an external OIDC provider\) and R1 is in the suppressible set, with the subject rooted at the \`jwks\` parameter. All three structural conditions are met, so the correct remediation is \`@trust_boundary\(tier=3, source='OIDC provider JWKS endpoint', source_param='jwks', suppresses=\('R1',\), ...\)\` on \`_validate_jwks_document\`, not a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_token_algorithm:fp=08a628af082bd78b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes external JWT header data as parameter \`\`header\`\`, the rule is R1 \(the decorator's suppressible set\), and the subject \`\`header.get\('alg'\)\`\` is rooted directly at the \`\`header\`\` parameter. The correct remediation is \`\`@trust_boundary\(tier=3, source='JWT header from external token', source_param='header', suppresses=\('R1',\), invariant='returns validated non-empty alg string or raises AuthenticationError', ...\)\`\` on \`\`_get_token_algorithm\`\`, not a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_jwk_algorithm:fp=57b015b09222f296` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`raw_key.get\("kid"\)\` inside \`_get_jwk_algorithm\`, which operates on \`jwks\` — data fetched from an external OIDC provider, genuinely Tier-3. However, the function takes \`jwks\` as a parameter and the suppressed expression is rooted at that parameter, so the structural remedy is \`@trust_boundary\(tier=3, source_param='jwks', suppresses=\('R1','R5'\), ...\)\` on the function, not a per-line allowlist entry. The decorator would also cover the \`raw_key.get\("alg"\)\` access on line 138 and the \`isinstance\` shape guard on line 134, consolidating the boundary into one auditable record. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_jwk_algorithm:fp=7ad8f0d01efdbd81` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`raw_key.get\("alg"\)\` inside \`_get_jwk_algorithm\`, which iterates over \`jwks\["keys"\]\` — JWKS data fetched from the external OIDC IdP, which is genuine Tier-3 data. The rule \(R1\) is in the decorator's suppressible set, and the subject is rooted at the \`jwks\` parameter. Per the three-question structural test, the correct remediation is \`@trust_boundary\(tier=3, source_param='jwks', suppresses=\('R1',\), ...\)\` on the function, not a per-line allowlist entry. The rationale itself is reasonable on the merits, but the structural decorator is the policy-preferred path. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:authenticate:fp=5b6d0378675eb2f2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 OIDC token payload boundary and R1 is in the suppressible set, and the subject \`payload.get\("preferred_username"\)\` is rooted at the \`token\`-derived \`payload\` local. However, the suppressible decorator binds to a function *parameter* \(source_param\), and here the external data enters via \`payload\`, which is produced inside the function from \`token\`. The structural remediation is still preferable: refactor so the decoded payload is the source_param \(or apply @trust_boundary with source_param='token' if the decorator's flow analysis follows token→payload\). Regardless, the per-line allowlist is the wrong unit — and additionally, \`payload.get\(...\) or sub\` performs fabrication-style fallback that the rationale should justify against the fabrication test \(here arguably acceptable since username is cosmetic, but the one-line rationale doesn't engage with it\). Fix structurally rather than allowlisting. |
| `web/auth/oidc.py:R1:optional_profile_claim:fp=4c7ec97e16ccebc9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case suitable for the @trust_boundary decorator. The function takes external OIDC token payload data as the \`payload\` parameter, the finding is R1 \(one of the suppressible rules\), and the subject \`payload.get\(claim_name\)\` is rooted directly at that external parameter. The decorator is the correct remediation here, not a per-line allowlist entry. Apply @trust_boundary\(tier=3, source='OIDC token payload', source_param='payload', suppresses=\('R1',\), invariant='returns None for missing/non-string/blank claims', ...\) to optional_profile_claim. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=b8b1270a71192d97` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(decoded OIDC token payload\), the rule is R1, and the subject is rooted at the function parameter \`token\` -&gt; \`payload\`. All three decorator-test conditions are met: the function should be marked with @trust_boundary\(tier=3, source_param='token' or the payload parameter\) rather than carrying a per-line allowlist entry. The structural decorator is the project's prescribed remediation for this exact pattern; a YAML entry here is the wrong shape of fix. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=cd4ba5b028a35026` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 OIDC token boundary and R1 is in the decorator's suppressible set, but the subject \`payload.get\("preferred_username"\)\` is rooted at \`payload\`, which appears to be a local variable inside \`get_user_info\` rather than a direct parameter. If \`payload\` is the decoded token derived from a parameter of \`get_user_info\`, the structural fix is \`@trust_boundary\(tier=3, source_param=&lt;the token/payload parameter&gt;, suppresses=\('R1',\), ...\)\` on the enclosing function rather than a per-line allowlist entry. The rationale also conflates fallback-to-sub with Tier-3 coercion: substituting \`sub\` when \`preferred_username\` is missing is arguably fabrication under the policy's test \(the IdP did not assert that username equals sub\), which deserves explicit treatment rather than a silent \`.get\(\) or sub\`. |
| `web/auth/entra.py:R1:EntraAuthProvider:authenticate:fp=564181fecfacb986` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(decoded JWT payload from an external IdP\), R1 is in the decorator's suppressible set, and the subject \`payload.get\("preferred_username"\)\` is rooted at the \`payload\` local derived from the token parameter. The structural remediation is \`@trust_boundary\` on \`authenticate\` with \`source_param\` pointing at the parameter carrying the token/payload, not a per-line allowlist entry. Additionally, the \`or sub\` fallback edges toward fabrication \(substituting \`sub\` for an absent username changes meaning\) — that concern should be addressed in code, not entrenched via suppression. Excerpt does not show the function signature, so the exact parameter name is inferred; agent should confirm before applying. |
| `web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=9ef9844bc003576e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on payload.get\("preferred_username"\) inside get_user_info. The function's parameter \(the decoded JWT payload\) is Tier-3 external data, and R1 is in the decorator's suppressible set — this is a structural @trust_boundary case, not a per-line allowlist case. Additionally, the excerpt shows the codebase already has an optional_profile_claim helper for exactly this pattern, suggesting the access should either go through that helper or be covered by a function-level decorator. Emit should_use_decorator on the payload parameter; the per-line allowlist entry should not land. |
| `web/execution/progress.py:R6:ProgressBroadcaster:_safe_put:fp=b0813b2652b434b9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale describes intended runtime behavior but doesn't engage with policy. This is a backpressure/queue-management site, not an external Tier-3 data boundary, and the suppressed except routes a pipeline-relevant event \(subscriber drop\) to slog.warning rather than to the Landscape audit trail — that inverts the telemetry primacy order. Dropping a non-terminal progress event silently with only a log line means an auditor asking 'what happened to event X' gets 'I don't know' from the audit trail, which the Auditability Standard forbids. The remediation is structural \(record the drop in Landscape/telemetry, or guarantee bounded-but-recorded delivery\), not a per-line suppression. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=6529682f4f63b877` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a one-line description of what the code does \('Idle-timeout status recheck — run may be deleted between heartbeat polls'\), not a justification for suppressing an R6 finding. It does not identify what R6 flags here, why catching _RunStatusNotFoundError at this site is a legitimate trust-boundary action rather than defensive swallowing, or why the exception cannot be handled by the offensive path. The run-status data being loaded is Tier-1 \(our audit/Landscape data\), where the policy is explicit: crash on anomaly, no silent recovery — yet the handler maps a not-found to a clean websocket close, which needs an explicit auditability argument the rationale never makes. Without a site-specific justification tying the suppression to a policy-sanctioned boundary, this is an unacceptable audit record. |
| `web/middleware/rate_limit.py:R9:ComposerRateLimiter:_sweep_stale_entries:fp=50c8c5807a0f8fe3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is internal process-local state \(self._user_locks and self._buckets\), not Tier-3 external data. The rationale claims a bucket may exist without a corresponding lock as an 'edge case', but the code excerpt shows locks are created via _get_user_lock during check\(\) and buckets are populated in check\(\) as well — the invariant between them is fully under our control. If the invariant can be violated, that's a bug in code we own and should crash \(KeyError on del self._user_locks\[uid\]\), not be silently swallowed with pop\(...,  None\). Per the Defensive vs Offensive decision test, this is 'something might be None/missing in code we control' — fix the root cause or assert the invariant, don't suppress. The decorator does not apply: no external parameter, and R9 is not in {R1, R5}. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=d0e7e2507b117f2d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`source.get\("options"\)\` where \`source\` is a dict parsed from YAML produced by an internal YamlGenerator — this is system-produced configuration, not Tier-3 external data. The function already raises TypeError on shape violations, which is correct offensive programming for an internal contract. The \`.get\(\)\` here is being used as a presence check on a key that may legitimately be absent in a well-formed config; if 'options' is a contract-required key, the correct fix is direct subscript and let it KeyError. Either way, 'keys may be absent' is the canonical defensive-programming rationale the policy forbids — the right shape is \`if 'options' in source:\` followed by direct subscript, or treating absence as a contract violation. The decorator nudge does not apply: this function does not take an external parameter \(input is internally-generated YAML\), so R1 suppression via @trust_boundary would misrepresent the trust tier. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=5f1f81fee41e3e1a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding at line 74 is on \`config.get\("sinks"\)\`, but the agent's rationale describes 'sink options keys \(path/file\)' — which is a different location entirely \(line 82, \`sink_cfg.get\("options"\)\`, or the \`key in opts\` checks\). The rationale does not address the actual flagged expression. Beyond the mismatch: \`config\` here is the parsed YAML output from a generator — if this is treated as Tier-3 external data, the function should be refactored to use \`@trust_boundary\` with \`source_param='config'\` rather than per-line allowlist entries, since R1 is in the decorator's suppressible set and the subject is rooted at the \`config\` parameter. However, since the excerpt is truncated and I cannot see the function signature or existing decorators, I emit the nudge cautiously. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=c876a96667c425f7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`sink_cfg.get\("options"\)\` where sink_cfg is YAML-generator-produced config navigated within elspeth's own runtime preflight. This is not Tier-3 external data at a boundary — it's pipeline configuration that the code explicitly type-checks with isinstance/TypeError just lines above. The rationale 'keys may be absent' is the textbook forbidden defensive pattern: using .get\(\) to silently tolerate a missing key. If 'options' is absent the loop should simply not execute; the correct code is \`if "options" in sink_cfg:\` followed by direct subscript, or to let a KeyError surface. The decorator nudge does not apply: this function does not take an external parameter that flows into the subject, and the surrounding code already enforces strict shape with raises rather than silent recovery. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=42996be95a89dd56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is the output of our own YamlGenerator, parsed back via yaml.safe_load. That is system-produced data, not Tier-3 external input — the generator is code we control. The rationale's claim that 'source key may be absent in generated YAML' describes an invariant of our own generator; if it's optional by design, direct subscript with a KeyError-on-bug is fine, or the generator's contract should make absence explicit. The @trust_boundary decorator does not apply: pipeline_yaml is not external untrusted data, it is the return value of an in-process plugin call \(system code\). Per the Plugin Ownership policy, a plugin returning malformed structure should crash, not be defensively navigated. Fix the root cause: either document 'source' as optional and use direct access guarded by 'in', or have the generator always emit the key. |
| `web/middleware/request_id.py:R1:RequestIdMiddleware:dispatch:fp=a17a1317c0fa8e7b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary finding \(R1\) inside a function whose external data parameter is \`request\` — specifically \`request.headers\`, which is HTTP-protocol input from arbitrary clients. The three-question decorator test is satisfied: the function takes external data, R1 is in the decorator's suppressible set, and the subject is rooted at the external \`request\` parameter \(via \`request.headers.get\(...\)\`\). The correct remediation is \`@trust_boundary\` on \`dispatch\` with \`source_param='request'\`, not a per-line allowlist entry. The rationale itself is substantively correct about the trust tier and the protocol semantics; it is just proposing the wrong mechanism. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=fdc4a3946c001515` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being read here is from the audit database \(transform_errors_table via db.read_only_connection\) — this is Tier-1 ELSPETH-owned data, not Tier-3 external input. The policy is explicit: 'A defensive .get\(\) on a row read out of the audit database is forbidden — if the field is missing, that is a catastrophic invariant break and must surface.' The rationale's claim that 'error_details JSON shape varies by transform' is itself a description of an audit-trail invariant problem: if different plugins write inconsistent keys into error_details_json, that's an upstream schema bug to fix, not a shape to paper over with 'UnknownError' fabrication. Defaulting absent error_type to the string 'UnknownError' is fabrication per the test — an auditor querying this field gets a value no transform actually wrote. The 'caller wraps with broad-except' justification compounds the violation rather than excusing it. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=b09747c7efb37444` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This reads from the audit database \(transform_errors_table via landscape_run_id\) — that is Tier 1 data, not Tier 3. The rationale frames JSON shape variance as a boundary concern, but error_details_json was written by our own transform code; if some transforms write 'error' and others write 'reason', that is an upstream schema inconsistency that should be normalized at write time, not silently smoothed over at read time. The fabrication test also fails: collapsing absence to empty string changes 'unknown' into 'no message'. Per the Auditability Standard and Tier-1 rules, the audit trail must be pristine — fix the writers to use a single canonical key, then access it directly. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=1f176fb7969fbed2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being read is from \`transform_errors_table.c.error_details_json\` — a column in our own audit/landscape database \(Tier 1, FULL TRUST\), not external Tier-3 data. The policy is explicit: 'A defensive .get\(\) on a row read out of the audit database is forbidden — if the field is missing, that is a catastrophic invariant break and must surface.' Fabricating empty-string defaults for missing \`error\`/\`reason\` keys silently masks audit-trail corruption. The remediation is to fix the writer to guarantee the schema and access keys directly, letting KeyError surface if the invariant breaks — not an allowlist entry. The @trust_boundary decorator does not apply since this is not Tier-3 data from a function parameter. |
| `web/composer/guided/steps.py:R1:handle_step_1_source:fp=d12bd40a848a8b71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding R1 on resolved.options.get\("path"\) is exactly the Tier-3 boundary pattern the @trust_boundary decorator was built for: handle_step_1_source receives external SchemaForm data and the suppressed expression is rooted at a function parameter \(resolved\). The structural remediation is to decorate handle_step_1_source with @trust_boundary\(tier=3, source_param='resolved', suppresses=\('R1',\), ...\) rather than add a per-line allowlist entry. The rationale itself is substantive and accurate about the tier, but per policy the decorator path supersedes the per-line entry when the three-question test passes. |
| `web/composer/guided/recipe_match.py:R1:_classify_slot_resolver:fp=6726fb5580a0cb92` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims sink.outputs\[0\].options is Tier-3 external data, but this resolver receives a SinkResolved object — that is post-source pipeline data \(Tier 2\), not raw external LLM input. Even granting the data is operator-influenced, the .get\(\) with a fabricated default path 'outputs/classified.jsonl' is exactly the fabrication pattern the policy forbids: it changes 'unknown/absent' into a synthetic value that the operator never asserted, and propagates that synthetic path downstream. If 'path' is required, the resolver should assert it \(like the blob_ref check immediately above\) or the predicate should require it; if it is genuinely optional, absence should be represented as None and handled explicitly at the recipe_offer turn, not filled in here. The function also doesn't appear to be a @trust_boundary-decorated entry point taking the external dict as a named parameter, so the decorator nudge doesn't apply. |
| `web/composer/guided/recipe_match.py:R1:_split_threshold_slot_resolver:fp=e71b13ac68a94bb6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale invokes Tier-3 but the data flow doesn't support it: \`sink.outputs\[0\].options\` is a field on an internal sink configuration object that has already been constructed and validated by upstream code — this is Tier-2 pipeline data, not raw external input. The function signature shown does not take an external \`arguments\`-style parameter; it operates on already-constructed Source/Sink objects. Furthermore, the \`.get\("path", "outputs/above.jsonl"\)\` fabricates a default output path rather than recording absence as None — this is fabrication per the fabrication-decision test \(if the operator's options change to include an explicit path later, the audit trail will silently disagree about which path was used\). The correct fix is either a predicate that requires \`path\` to be present \(mirroring the \`blob_ref\` invariant check directly above\) or direct subscript access that crashes on absence. Decorator nudge does not apply: the subject is not rooted at an external function parameter. |
| `web/composer/guided/recipe_match.py:R1:_split_threshold_slot_resolver:fp=fd8beb31159c31ca` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims sink.outputs\[1\].options is Tier-3 external data, but the excerpt shows this is a slot resolver operating on a parsed/validated sink configuration object with structured .outputs\[\].options attributes — this is Tier-2 pipeline data, not raw external input. The fabrication-decision test fails: substituting 'outputs/below.jsonl' when the user did not supply a path invents a value the external system never asserted, and would silently mask a configuration that was supposed to carry an explicit path. If the predicate registry guarantees the shape \(as the InvariantError docstring above asserts for blob_ref\), then 'path' should likewise be required and accessed directly; if it is genuinely optional, that belongs in the typed schema with an explicit default, not a silent .get\(\) at the resolver. The structural decorator does not apply: the subject is rooted at 'sink', not at an external-parameter Tier-3 payload. |
| `web/blobs/service.py:R6:BlobServiceImpl:finalize_run_output_blobs:_sync:fp=0f0584634973a5ac` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a narrow except clause catching BlobQuotaExceededError — a specific, named exception — not the broad exception handling that R6 typically flags. However, the rationale is vague \('narrow filesystem/database failures are collected'\) and doesn't match what's visible: the visible handler catches one specific quota-exceeded exception with clear remediation \(delete file, mark error\). The rationale references broader 'filesystem/database failures' suggesting there are wider handlers below the visible window that the rationale is actually trying to justify, but I cannot see them. Without visibility into the actual R6-flagged handler and with a rationale that gestures at unspecified broad catches, the audit record would be uninterpretable to a future auditor — exactly what the Auditability Standard forbids. |
| `web/composer/implicit_decisions.py:R5:_flatten_options:fp=24bbd76d54ccfe33` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is being used to discriminate between a Mapping \(recurse\) and a leaf value \(append\) during recursive flattening. This is control-flow type dispatch on Tier-2 pipeline data — not a defensive shape guard, but the policy's defensive-programming rule R5 still flags it. However, the proper remediation is not a per-line allowlist entry: the values in a free-form options mapping are genuinely heterogeneous \(nested mappings vs scalars vs sequences\), and the recursion needs structural dispatch. The rationale correctly identifies the tier but doesn't justify why allowlisting is preferable to restructuring \(e.g., a typed visitor, or normalising options to a flat representation upstream\). Given ELSPETH's conservative prior against suppressions and the absence of any argument that restructuring is infeasible, this should be blocked so the agent considers a structural fix. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=ebdc5429b73a3656` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims 'Tier 3 boundary' but the data being processed is a JSON schema produced by Pydantic — code we control — not external untrusted input. A Pydantic-generated schema missing a 'required' list is either a valid JSON Schema shape \(in which case the correct semantic is 'no required fields', i.e. an empty set, which is what the .get default produces but should be expressed honestly\) or a Pydantic bug \(in which case it should crash like the sibling dangling-$ref case explicitly does on line 473\). Note the inconsistency: the author already chose direct subscript for \`defs\[...\]\` calling it a 'Pydantic-schema bug', but uses defensive .get\(\) for 'required'/'properties' on the same Pydantic-owned object. This is Tier-2-or-better data, not Tier 3; the decorator does not apply \(subject is not rooted at an external function parameter\). Fix by either trusting the JSON Schema spec \(absent 'required' legitimately means empty — document and use direct access with a typed fallback at the schema-contract layer\) or by crashing on the malformed-Pydantic case. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=627b5114d5635ef7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being accessed \(\`variant\`\) comes from a Pydantic-generated JSON Schema's \`$defs\` table — this is data produced by code we control \(Pydantic\), not Tier-3 external data. The rationale mislabels it as 'Tier 3 boundary'. Per the Plugin Ownership and Tier-2 rules, if Pydantic produces a variant schema, we should trust its structure; if \`properties\`/\`required\` keys are genuinely optional in JSON Schema, the correct offensive pattern is \`variant.get\('properties', {}\)\` only if the absence is semantically meaningful \(empty properties = no fields\), which is defensible — but the rationale must say so accurately, not invoke a fictitious Tier-3 boundary. The decorator does not apply: this is not Tier-3 external input rooted at a function parameter. The rationale is a corrupted audit record \(wrong tier label\) and must be rewritten to honestly describe the JSON Schema spec permitting absence of \`required\`. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=0fcb6d233deaafbe` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is a JSON-Schema property dict assembled from plugin schemas that ELSPETH itself produces/loads — this is system-owned plugin metadata, not Tier-3 external data from an untrusted system. The rationale's 'Tier 3 boundary' framing misapplies the trust model: plugin schemas are Tier-1/2 system artifacts, and a missing 'default' or 'description' is an expected schema shape \(optional keys\), not external trash to defend against. Even granting the optional-key argument, the correct construct is direct conditional access or trusting the schema contract, not silent .get\(\) suppressed via allowlist. The function takes no external parameter, so @trust_boundary does not apply. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=d98fcb19a086f35c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on field_schema.get\("default"\) inside _field_summary, which operates on a JSON-Schema property dict. This is plugin/system-owned schema data \(built from plugin specs\), not external Tier-3 input — there is no external parameter at this function boundary, and the data has already been processed by code we control. The rationale's invocation of 'Tier 3 boundary' is a misapplication of the trust model. Even granting that 'default' is an optional JSON-Schema key, the correct expression is still direct access patterns appropriate to a typed schema dict; if the agent wants to express 'this key is optional', that is a code shape question, not an allowlist justification. The decorator nudge does not apply since _field_summary takes no external parameter. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=dbd0c2b47b09652b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The reported finding is on \`branch.get\("type"\)\` inside iteration over \`field_schema\["anyOf"\]\`. The rationale claims 'JSON schema field default is optional' — but that refers to line 512, not the flagged line 504. More importantly, \`field_schema\` here is not Tier-3 external data: it comes from plugin-registered JSON schemas, which are system-owned code per the Plugin Ownership policy. Schemas are authored by us; a malformed \`anyOf\` branch missing \`type\` is a plugin bug that should surface, not be defensively suppressed. The function is not decorated as a trust boundary, and the parameter is not rooted at external input — it flows from internal plugin class introspection. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=9e86d37d099865b9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line is \`branch.get\("type", "object"\)\` inside \`_field_summary\`, which processes a JSON Schema dict. The rationale claims 'Tier 3 boundary — JSON schema field default is optional', but the finding is on \`branch.get\("type", ...\)\`, not on \`default\`, and the JSON schemas being summarized here are produced by ELSPETH's own plugin classes \(system-owned code\), not external Tier-3 input. That makes this Tier-2 data at best, and the \`.get\("type", "object"\)\` substitutes a fabricated default \('object'\) for an absent type rather than letting a plugin-schema invariant break surface. The decorator nudge does not apply: \`field_schema\` is not an external parameter to a boundary function — it's derived from internal plugin schemas — so R1 here should be fixed by trusting the schema invariant, not suppressed. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=811406999d977a9f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims a Tier-3 boundary, but field_schema here is a JSON-Schema dict assembled/registered by plugin code we own — that's Tier-2 \(system-internal\) data, not external untrusted input. JSON-Schema property entries have well-defined optional keys \('description', 'default'\); the correct idiom is direct access with documented optionality, but that is a code-design question, not a suppression. Even granting that 'default' and 'description' are legitimately optional in JSON-Schema, this is not a Tier-3 external-data boundary as the rationale asserts; the data flows from our own plugin schema registry. The decorator nudge does not apply: _field_summary's input is not an external-source parameter and R1 here is not the Tier-3 boundary pattern the decorator targets. Fix by establishing a typed schema-entry model or by accepting the rule's guidance at the call site, not by allowlisting with a mislabeled tier. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=cad629e5a725a37c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`field_schema\` parameter is a JSON-Schema dict produced by ELSPETH's own plugin descriptors — this is system-owned plugin metadata, not Tier-3 external data. The rationale's invocation of 'Tier 3 boundary' misapplies the trust model: plugin schemas are code we control, and a missing 'default' key in our own schema is an interface contract, not external malformation. The function does not take an external/untrusted parameter, so the @trust_boundary decorator is not the right structural fix either. If 'default' is genuinely optional in the schema convention, the correct fix is to type the input \(e.g. a TypedDict with NotRequired\) or document the contract — not a per-line R1 suppression with a mischaracterized tier. |
| `web/composer/state.py:R1:_validate_web_scrape_abuse_contact_not_reserved:fp=1be9831e03d16765` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`node.options.get\("http"\)\` inside \`_validate_web_scrape_abuse_contact_not_reserved\`, which takes \`node\` as a parameter — but \`node\` is not the external-data parameter, and the excerpt does not show that this function takes an external mapping as a named parameter suitable for \`@trust_boundary\(source_param=...\)\`. More importantly, \`node.options\` is composer state — by the time it reaches this validator, it has flowed through the composer's own data model; the rationale's claim that it is "opaque LLM/user-supplied" conflates Tier-3 ingress with Tier-2 in-pipeline state. Even granting Tier-3 framing, the rationale that "the plugin's pydantic schema reports the absence elsewhere" is exactly the defensive pattern the policy forbids: deferring to another reporter via silent \`.get\(\)\` rather than letting the schema be the single point of validation. If the http block's shape is genuinely the plugin schema's responsibility, this validator should not be reaching into \`node.options\["http"\]\["abuse_contact"\]\` at all — it should run after schema validation on a typed structure. The remediation is structural \(run this check post-schema on typed data, or move it into the plugin's own validation\), not a per-line suppression. |
| `web/composer/state.py:R1:_validate_web_scrape_http_identity_not_placeholder:fp=bea652fff29d7051` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case suitable for the @trust_boundary decorator. The function takes node \(whose .options dict carries user/LLM-authored composer state — Tier-3 external data\), the finding is R1 \(in the suppressible set\), and the subject http = node.options.get\('http'\) is rooted at the external parameter via attribute access. The correct remediation is @trust_boundary\(tier=3, source='composer node options authored by user/LLM', source_param='node', suppresses=\('R1',\), ...\) on the enclosing function, not a per-line allowlist entry. |
| `web/composer/state.py:R5:_validate_web_scrape_http_identity_not_placeholder:fp=d7b50b5c57e5c231` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`node: NodeSpec\`, not a raw external parameter — \`node.options\` is composer-state data already past the source boundary, so this isn't a function-scoped Tier-3 boundary in the sense the @trust_boundary decorator covers. More importantly, R5 here is checking the shape of a nested value read from a typed NodeSpec; if \`options\['http'\]\` is not a Mapping that's either a schema violation \(let it surface\) or a legitimate skip case that should be expressed via typed access, not an isinstance narrow. The rationale conflates 'wire-visible HTTP identity' \(the downstream effect\) with the trust tier of the data being inspected here. The agent should fix this by typing \`node.options\` properly or by restructuring the validator to consume a typed http config, not by allowlisting the isinstance. |
| `web/composer/state.py:R1:_validate_web_scrape_http_identity_not_placeholder:fp=976e77012c7cb2f6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case suitable for the @trust_boundary decorator. The function takes external/operator-supplied data via the \`node\` parameter \(node.options being raw composer/LLM-authored HTTP options\), the rule is R1, and the suppressed expression \`http.get\(field_name\)\` is rooted in that external parameter via node.options. The correct remediation is to apply @trust_boundary\(tier=3, source_param='node', suppresses=\('R1',\), ...\) rather than a per-line allowlist entry. |
| `web/composer/state.py:R5:_validate_web_scrape_http_identity_not_placeholder:fp=8e9f7b0aaa21d365` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on node.options, which is pipeline configuration data \(NodeSpec\) inside the composer state — not Tier-3 external data freshly arrived from an external system. Even if options are 'untyped until schema validation', the data is internal pipeline state, not an external HTTP/LLM payload, so the trust_boundary decorator's Tier-3 framing does not apply. Additionally, the function parameter is 'node', not the http mapping itself, so the three-question decorator test fails on parameter-rooting. The agent should either rely on schema validation having run first \(let access crash if types are wrong = offensive\) or treat node.options as a documented Tier-3-like boundary by restructuring — not allowlist this isinstance check site-by-site. |
| `web/composer/state.py:R1:_check_schema_contracts:_producer_emit_set:fp=553d63b88e277081` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale's premise is contradicted by the code: line 1372 already short-circuits the source case \(\`producer.producer_id == "source"\` returns early\), so by line 1375 the producer is not a source. The data is Tier-2 pipeline state \(node_by_id is an internally-constructed dict of NodeSpecs\), not Tier-3 external data, so the @trust_boundary decorator does not apply. If a non-source producer_id is missing from node_by_id, that is an internal invariant violation and should crash, not silently fall back via .get\(\). The legitimate remediation is direct subscript access \(or an explicit membership check with a meaningful raise\), not a defensive .get\(\). |
| `web/composer/state.py:R1:_check_schema_contracts:fp=c09544503f550037` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | node.options here is plugin configuration data sourced from the pipeline config \(Tier-2 within ELSPETH's plugin layer\), not Tier-3 external user data flowing through a trust boundary function. The function _check_schema_contracts is not a Tier-3 boundary, and the rationale's appeal to 'matches the pydantic default' is exactly the defensive pattern the policy forbids: if the config schema defines select_only with default False, the correct path is to parse the options through FieldMapperConfig and read the typed attribute, not to silently mirror the default with .get\(\). The rationale even admits it's avoiding constructing the plugin to stay out of the plugin layer — that's a layering/refactor problem \(extract the config parse, or move the gate into the plugin\), not a suppression case. The decorator does not apply: this is not a function-parameter-rooted Tier-3 boundary. |
| `web/composer/_required_paths_validator.py:R5:_optional_ancestor_present:fp=b07f5f9d3d7a04bf` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the walker inspects external LLM-supplied JSON shape, R5 is in the decorator's suppressible set, and the subject \`cursor\` is rooted at the function parameter \`value\` \(Tier-3 data\). The correct remediation is to apply @trust_boundary\(tier=3, source_param='value', suppresses=\('R5',\), ...\) on the enclosing function rather than land a per-line allowlist entry. The excerpt does not show the function signature/decorators, but the symbol name and rationale make the data flow clear enough to nudge toward the decorator. |
| `web/composer/llm_response_parsing.py:R2:_provider_details_payload:fp=2a1833c0a52a5e5a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2, which is not in the decorator's suppressible set {R1, R5}, so the structural @trust_boundary nudge does not apply. The rationale invokes a Tier-3 provider-metadata boundary, which is plausible \(LiteLLM/provider response shapes are genuinely external and heterogeneous\), but the rationale is generic boilerplate that doesn't explain why getattr-with-None across an arbitrary field tuple is the right normalization here rather than, e.g., explicit per-field extraction. More importantly, the getattr\(..., None\) pattern fabricates a mapping whose keys may not have been asserted by the provider — the absence-vs-fabrication test from the Data Manifesto is not addressed in the rationale. Without a site-specific justification tying each field to a documented provider contract and explaining why None-on-absent is meaning-preserving here, this is a shallow audit record. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=129470a225d57f27` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R2 \(getattr finding\) is not in the decorator's suppressible set {R1, R5}, so the structural nudge doesn't apply. The function takes a \`response\` parameter from LiteLLM/provider — genuinely Tier-3 external data — and the getattr is at that boundary, which would normally be defensible. However, the rationale is generic boilerplate that doesn't engage with why getattr \(rather than direct attribute access plus a typed boundary parse\) is the right shape here, and doesn't justify why the response object's structure can't be modeled as a known Mapping/object union with explicit handling. Per the auditability standard, 'provider responses may be mapping-shaped, object-shaped, absent, or provider-specific' is too vague to serve as a durable audit record — a future auditor cannot tell from this rationale what invariant the suppression protects. The agent should either tighten the rationale to cite the specific provider-shape contract being accommodated, or restructure to parse the response into a typed shape at the boundary. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=019b920aea416acb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(LiteLLM/provider response\), R1 is in the decorator's suppressible set, and the subject \(\`usage.get\(...\)\`\) is rooted at the function's \`response\` parameter via \`usage = getattr\(response, 'usage', None\)\`. The correct remediation is \`@trust_boundary\(tier=3, source='LiteLLM/provider response', source_param='response', suppresses=\('R1','R5'\), ...\)\` on \`token_usage_from_response\`, not a per-line allowlist entry. The data-flow from \`response\` -&gt; \`usage\` -&gt; \`.get\(...\)\` is exactly the structural pattern the decorator was designed to cover. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=8a4d5742fc096a4c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(LiteLLM provider response, shape varies by provider\) and R1 is in the decorator's suppressible set. The subject \`usage.get\(...\)\` is rooted in \`response\` \(the function parameter\) via \`usage = getattr\(response, 'usage', None\)\`. All three conditions for the structural @trust_boundary remediation are met: the function \`token_usage_from_response\` takes external \`response\`, the rule is R1, and the data flow is direct from the parameter. The correct fix is to decorate the function with @trust_boundary\(source_param='response', suppresses=\('R1','R5'\), ...\) rather than land a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=7a51927419dde97b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external-data boundary \(LiteLLM provider response parsing\), and the function does take the external data as a parameter \(\`response\`\). The finding's subject \(\`usage.get\(...\)\`\) is rooted at \`response\` via \`getattr\(response, 'usage', None\)\`, and R1 is in the decorator's suppressible set. The structural remediation is \`@trust_boundary\(tier=3, source='LiteLLM provider response', source_param='response', suppresses=\('R1','R5'\), ...\)\` on \`token_usage_from_response\`, not a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=23654a5020554ff2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes an external LLM provider response as a parameter, the finding is R1 \(silent .get\(\) on external data\), and the subject chain is rooted at that parameter \(response -&gt; usage -&gt; .get\(...\)\). The correct remediation is the @trust_boundary decorator on token_usage_from_response with source_param set to the response parameter, not a per-line allowlist entry. The rationale itself is accurate about the trust tier, but the structural fix supersedes the line-level suppression. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=40e4830b7986f089` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 external-data boundary: the function parses a LiteLLM/provider response whose shape varies by provider, and the suppressed expression \(usage.get\("completion_tokens"\)\) is rooted at the function's \`response\` parameter via \`usage = getattr\(response, "usage", None\)\`. The rule_id is R1 and all three decorator conditions are met, so the correct remediation is @trust_boundary on the enclosing function rather than a per-line allowlist entry. The excerpt does not show the function's decorator line, but the conservative path is to nudge toward the decorator; if it is already decorated, the agent can confirm and the allowlist entry remains unnecessary. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=14c33dfbf868059d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is a textbook Tier-3 external-data boundary case suitable for the @trust_boundary decorator. The function token_usage_from_response takes the provider response as a parameter, the rule is R1 \(silent .get\(\) on external data\), and the suppressed subject \(usage.get\('total_tokens'\)\) is rooted in a data-flow path from that external parameter. The correct remediation is to decorate the function rather than land a per-line allowlist entry. The excerpt does not show the function signature/decorators, but the parameter is clearly named 'response' based on the body's reference to it. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=01435ad22c7f7946` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 LLM provider response boundary \(R1 on usage.get derived from response.usage\), but the function 'token_usage_from_response' takes 'response' as a parameter and the .get\(\) calls flow from that external parameter. This is the textbook structural case for @trust_boundary\(tier=3, source_param='response', suppresses=\('R1','R5'\), ...\) rather than per-line allowlist entries. The decorator excerpt is not visible above line 108, but the function signature clearly takes the external 'response' object, so the correct remediation is to apply the decorator \(if not already present\) instead of accumulating per-line entries. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=fa9224835d150b3f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external-data boundary \(LiteLLM provider response\), R1 is in the decorator's suppressible set, and the subject is rooted at the function's external parameter \`response\` \(via \`usage = getattr\(response, 'usage', None\)\`\). The structural remediation is \`@trust_boundary\(tier=3, source='LiteLLM provider response', source_param='response', suppresses=\('R1','R5'\), ...\)\` on \`token_usage_from_response\`, not a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=5cd0d50b12dd73ad` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function processes external LiteLLM/provider response data, the rule is R1 \(silent .get\(\) on external data\), and the subject is rooted at the \`response\` parameter \(via \`usage = getattr\(response, 'usage', None\)\`\). The correct remediation is to apply @trust_boundary\(tier=3, source_param='response', suppresses=\('R1','R5'\), ...\) on the enclosing function rather than a per-line allowlist entry. The decorator excerpt is not visible in the +-30 line window, but the function signature suggests \`response\` is the external parameter. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=b05d403d618526a2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr with default\), which is not in the decorator's suppressible set \(R1, R5\). The code parses LiteLLM/provider response objects which are genuinely Tier-3 external data with provider-specific shapes \(object vs mapping\), so defensive access at this boundary is legitimate in principle — but R2 specifically flags fabrication-via-default risk, and the rationale does not address why None defaults here are coercion rather than fabrication of token counts. A future auditor reading 'reasoning_tokens: None' cannot distinguish 'provider didn't report it' from 'we couldn't find the attribute' — that's exactly the fabrication-test failure mode. The rationale is also generic boilerplate \('may be mapping-shaped, object-shaped, absent'\) with no per-field justification of why None-on-absence preserves meaning for each token-count field. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=c745f85451112e20` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr usage\), which is NOT in the decorator-suppressible set {R1, R5}. However, the rationale is plausible on its face: provider response objects from LiteLLM are Tier-3 external data whose shape varies by provider \(object-shaped vs mapping-shaped\). The problem is that the function-level structure here is the right remediation — this entire function is a Tier-3 boundary parsing provider metadata, and the appropriate response is either to model the provider response with a typed adapter at the boundary \(so internal code sees a normalized shape\) or to scope a per-file rule. A per-line R2 allowlist on getattr-with-default for provider-shape probing entrenches the defensive pattern without the structural audit record the policy requires. The rationale also doesn't explain why getattr-with-None is preferable to a typed normalization step at the LiteLLM boundary. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=633b8b555a08a0d5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on getattr\(\) calls against a provider response object \(LiteLLM/provider usage object\), which is genuinely Tier-3 external data. However, R2 is not in the decorator's suppressible set \(R1/R5\), so the structural nudge does not apply. The rationale is also thin: it gestures at provider variability but does not explain why getattr-with-None-default is the correct boundary handling here versus, e.g., a single typed adapter that normalizes the provider object once. Absent fields are being recorded as None \(not fabricated\), which is policy-consistent, but the per-line allowlist entry for nine getattr calls on the same object suggests the right remediation is a structural one \(a typed boundary adapter or a broader per-file/per-function rule\), not a fingerprinted suppression on one line. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=cb1c4be269a3e9f0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr with default\), which is not in the decorator's suppressible set {R1, R5}, so the structural decorator nudge does not apply. The rationale invokes Tier-3 provider metadata, which is genuine — LiteLLM/provider response objects are external and shape-variable — but the rationale text is a generic one-liner that does not explain why a default of None is meaning-preserving here \(recording absence as None\) rather than fabrication, nor why each specific getattr is needed at this site. Per the fabrication test, None for an absent token-count field is the honest 'unknown' marker, which would justify the pattern, but the rationale must say that explicitly to land as an audit record. As written, the rationale is too vague to serve as a durable audit record; the agent should either tighten the rationale to name the boundary \(LiteLLM ModelResponse.usage attribute access\) and assert that None preserves absence semantics, or restructure to a parsing helper with a per-file rule. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=320755415534b8b2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R2 \(getattr defensive access\) on a LiteLLM provider response object is a Tier-3 boundary concern, but the finding is not in the suppressible rule set for @trust_boundary \(R1/R5 only\). However, the function takes \`usage\` \(derived from an external provider response\) and the getattr calls are rooted in that external object — the structural remediation is to decorate the enclosing function \`token_usage_from_response\` with @trust_boundary\(tier=3, source_param='usage', suppresses=\('R2',...\)\) if R2 were in scope, OR to refactor to parse the provider object through an explicit boundary adapter. The rationale itself is also thin: it asserts the tier but does not identify the source_param, the invariant on malformed input, or why getattr per-field is preferable to a single normalization step at the boundary. A per-line allowlist entry here entrenches scattered defensive access instead of consolidating the boundary. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=0d833fdefa90f98f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on getattr\(\) against a LiteLLM provider response object — Tier 3 external data, which is a legitimate trust boundary. However, the structural remedy is @trust_boundary\(tier=3, source_param='usage', suppresses=\('R2',\)\) on the enclosing function token_usage_from_response, not a per-line allowlist entry. The excerpt does not show the function signature or any existing decorator, so I cannot confirm whether the function is already decorated; given R2 is not in the decorator's documented suppressible set {R1, R5}, the decorator nudge does not cleanly apply either. The rationale is also boilerplate-shaped \('may be mapping-shaped, object-shaped, absent'\) without explaining why each individual getattr default of None is meaning-preserving rather than fabrication for this specific cache_read_input_tokens field — auditors reading this entry won't be able to distinguish 'provider omitted the field' from 'provider returned None'. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=b62e64bc2d61e617` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R2 \(getattr with default\) on a provider response object is a Tier-3 boundary concern, but the function-level @trust_boundary decorator is the structural remediation. However, R2 is not in the decorator's suppressible rule set \(R1, R5 only\), so the decorator nudge does not apply. The rationale is also generic boilerplate that doesn't site-justify why getattr-with-None-default is correct here versus, say, validating the shape once at function entry. The excerpt shows roughly ten getattr-with-default calls clustered together; a per-line allowlist entry on just line 147 is the wrong granularity — either broaden via a per-file rule scoped to this boundary helper or restructure to validate the usage object's shape explicitly at the boundary. |
| `web/composer/llm_response_parsing.py:R2:_provider_cost_from_response:fp=00217f7aa707a30d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`getattr\(response, "usage", None\)\` inside a function that takes \`response\` as a parameter carrying Tier-3 external provider metadata from LiteLLM. The function's docstring explicitly documents the trust-boundary semantics \(absent/malformed values treated as unavailable\). This is a textbook structural Tier-3 boundary case: the parameter is external, and the defensive access is rooted at that parameter. The correct remediation is the \`@trust_boundary\` decorator with \`source_param='response'\`, not a per-line allowlist entry. Note: R2 \(getattr shape guard\) may not be in the decorator's suppressible set {R1, R5}; if R2 is outside that set, the agent must either extend the policy properly or restructure rather than allowlist with this shallow rationale. |
| `web/composer/llm_response_parsing.py:R2:safe_response_model:fp=0c7de3a07a40310f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R2 here is a getattr\(\) call on a provider response object. While LiteLLM/provider responses are genuinely Tier-3 external data, the function takes \`response\` as a parameter, making this a structural @trust_boundary candidate — but R2 is not in the decorator's suppressible set \({R1, R5}\), so the decorator nudge does not apply. More importantly, the rationale is generic \('may be mapping-shaped, object-shaped, absent'\) and doesn't justify why getattr-with-default is the right shape here versus direct attribute access with a typed protocol. Provider SDKs \(LiteLLM, OpenAI\) return typed response objects where \`.model\` is a documented field; if it's missing that's an SDK contract break worth crashing on, not fabricating None. The rationale also doesn't address why hasattr-style defensive access is needed rather than narrowing the response type at the actual boundary where the SDK call returns. |
| `web/composer/llm_response_parsing.py:R2:_safe_provider_request_id:fp=25a663b069e6a21e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | Rule R2 \(getattr with default\) is not in the decorator's suppressible set {R1, R5}, so the structural nudge does not apply. The rationale invokes Tier-3 provider data but the function takes \`\`response: Any \| None\`\` — a LiteLLM provider response object. While provider responses are external, the function is a small helper and getattr with default on attributes \`\`id\`\`/\`\`request_id\`\` is exactly the defensive \`\`getattr\(x, 'attr', default\)\`\` pattern the policy forbids when used to handle ‘missing expected attribute’. If LiteLLM's response contract guarantees these attributes, access them directly and let it crash; if it doesn't, the variation should be handled once at a documented boundary, not by silently returning None deep in a parser. The rationale is generic boilerplate \(‘may be mapping-shaped, object-shaped, absent’\) and doesn't justify this specific site. |
| `web/composer/llm_response_parsing.py:R1:_response_field:fp=216e94b72103f074` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a generic helper function \(_response_field\) that takes arbitrary \`value: Any\` and a field name — it is not itself a trust-boundary function with a named external-data parameter. The Tier-3 boundary should be the function that actually receives the LiteLLM/provider response \(e.g., the caller invoking the provider\), and that function should be decorated with @trust_boundary so the suppression scope is clear and auditable. Suppressing R1 on a low-level dispatch helper hides which call site is the actual external boundary and which is internal reuse; the helper may also be called on already-internalized data. The decorator nudge does not cleanly apply here because the parameter \`value\` is not rooted at an identifiable external source within this function — the boundary is upstream. |
| `web/composer/llm_response_parsing.py:R2:_json_safe_provider_artifact:fp=a50249b4c425b80e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr with default\), which is not in the decorator's suppressible set {R1, R5}, so the @trust_boundary decorator cannot help here. The function takes a single \`value: Any\` parameter that is genuinely Tier-3 provider data \(LiteLLM/OpenAI/Anthropic response objects of varying shapes\), and duck-typing across model_dump/to_dict/dict is the canonical way to normalize such artifacts — getattr-with-None-default followed by callable\(\) is the offensive idiom for 'probe for a method', not defensive error-swallowing. However, the rationale is too generic: it says 'may be mapping-shaped, object-shaped, absent, or provider-specific' without naming why probing for to_dict specifically requires getattr \(vs the documented LiteLLM ModelResponse contract\), and an auditor reading this entry later won't be able to tell which provider shapes are in scope. The rationale needs to be site-specific enough to survive future inquiry. |
| `web/composer/llm_response_parsing.py:R1:apply_anthropic_cache_markers:fp=37f7de34a9e33a02` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`message.get\("role"\)\` where \`message\` comes from iterating \`new_messages\`, which is \`list\(messages\)\` — the \`messages\` parameter to this function. This is data internal to the composer pipeline \(messages we construct to send to LiteLLM\), not Tier-3 provider response metadata as the rationale claims. The rationale mischaracterizes the data direction \(outbound request payload, not inbound provider response\) and the tier. Messages we build ourselves should have known shape; if a message lacks 'role', that's an upstream bug in our code, not external malformed data. Direct subscript \`message\["role"\]\` is the correct offensive pattern here. |
| `web/execution/schemas.py:R1:RunEvent:_resolve_data_from_event_type:fp=020df37cc90810ad` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Pydantic before-validator operating on raw JSON input — a genuine Tier-3 boundary case. R1 is in the suppressible set and the subject \(\`values\`\) is the function's external-data parameter. The correct remediation is the @trust_boundary decorator with source_param='values', not a per-line allowlist entry. The decorator is the structural replacement for exactly this pattern. |
| `web/execution/schemas.py:R1:RunEvent:_resolve_data_from_event_type:fp=946fe25115431a7a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Pydantic before-validator running on raw deserialization input — a legitimate Tier-3 boundary pattern. However, the structural remedy is the @trust_boundary decorator: the function takes external input as its \`values\` parameter, the rule is R1, and the suppressed subject \(\`values.get\("data"\)\`\) is rooted at that parameter. The correct remediation is to decorate \`_resolve_data_from_event_type\` with @trust_boundary\(tier=3, source_param='values', suppresses=\('R1',\), ...\) rather than add a per-line allowlist entry. |
| `web/composer/tools/_dispatch.py:R1:execute_tool:fp=64df0c1899440c17` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) here is on a process-local registry dict \(all_handlers\) built from module-level constants — that's Tier-1/code-controlled data, not Tier-3. The rationale conflates the *key* being LLM-supplied \(tool_name\) with the *dict* being external; only the latter would justify a Tier-3 boundary suppression. The correct idiom for 'unknown key routes to failure' is an explicit membership check or try/except KeyError with a meaningful error — or keep .get\(\) but justify it as a lookup-miss pattern, not as a trust-boundary suppression. The decorator nudge does not apply: the subject \(all_handlers\) is not rooted at an external parameter, even though tool_name is. The rationale misapplies the Tier-3 vocabulary. |
| `web/composer/yaml_generator.py:R1:_strip_web_metadata:fp=d266b01a6f1f2583` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder generated during fingerprint rotation \('ALLOWLIST-FRESH'\), not a site-specific justification. The code operates on an \`options\` dict inside a web-layer helper that converts CompositionState to engine YAML — this is internal pipeline data assembled by code we control \(CompositionState.to_dict produces these options\), not Tier-3 external input. Per the Defensive Programming policy, \`.get\(\)\` here is forbidden: if \`blob_ref\`/\`mode\` semantics are part of the contract, access them directly or restructure; if they are optional fields on a typed structure, use explicit key-in checks or a dataclass. The decorator does not apply \(not a Tier-3 boundary function, and \`options\` originates from internal state\). |
| `web/composer/yaml_generator.py:R1:_strip_web_metadata:fp=553a2bc2f7828b83` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly asks for review rather than justifying the suppression. The code is not at a Tier-3 external boundary: \`options\` is an internal dict built by the composer's own CompositionState, i.e. Tier-2 pipeline data, and \`_strip_web_metadata\` is plain internal code. The \`.get\("blob_ref"\)\`/\`.get\("mode"\)\` calls are defensive reads on data we control; per the Decision Test, the fix is direct access or an explicit schema, not suppression. The decorator does not apply \(not a Tier-3 boundary, options is not an external parameter\). |
| `web/composer/yaml_generator.py:R9:_strip_web_metadata:fp=8aa1876af2dcdcd5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification was supplied — it explicitly asks the reviewer to determine whether this is new debt. That is not an audit-grade rationale; per the Auditability Standard, a vague or non-rationale is a corrupted audit record. Separately, the suppressed line operates on an \`options\` dict that is internal pipeline composition state \(Tier-2-ish, ELSPETH-controlled\), not Tier-3 external data, so the @trust_boundary decorator does not apply. The agent must supply a site-specific justification or fix the root cause. |
| `web/sessions/routes/_helpers.py:R1:_patched_routes_attr:fp=468f024091698e3f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`sys.modules.get\("elspeth.web.sessions.routes"\)\`, which is not external Tier-3 data — \`sys.modules\` is a Python-internal mapping of our own package namespace. The rationale invokes 'HTTP/session route boundary' and 'request payloads', but the code excerpt shows neither; it shows a test-patching indirection helper that looks up a module by name. Worse, this pattern \(checking if a module attribute has been monkey-patched and forwarding to it\) is exactly the kind of test/legacy shim the No Legacy Code Policy forbids — the helper exists to support patching at a non-canonical location. The decorator nudge does not apply: there is no external parameter, and the subject is not rooted at one. Suppression is not the remedy; fix the patch target or delete the indirection. |
| `web/sessions/routes/_helpers.py:R2:_patched_routes_attr:fp=7d9b5c1063c91bfc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale invokes HTTP/session boundaries and persisted-state shape checks, but the actual code is a test-patching shim: \`_patched_routes_attr\` uses \`getattr\` with a default to discover whether the \`elspeth.web.sessions.routes\` module has been monkeypatched. This is not a Tier-3 external-data boundary — it's introspection on a process-local module object that ELSPETH controls. Worse, the function exists to support patched-vs-unpatched dispatch, which reads as a test/compatibility shim pattern that the No Legacy Code policy disfavors; the rationale's invocation of 'audit-drain fallbacks' does not describe what this code does. R2 \(and \`getattr\` with default generally\) on a code-controlled module is the defensive pattern the policy forbids — fix the indirection rather than suppress. |
| `web/sessions/routes/_helpers.py:R2:_get_session_compose_lock_registry:fp=5bf9de9dea5e3690` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(not R1/R5, so the @trust_boundary decorator doesn't apply\) on a getattr\(\) call against request.app.state — an internal FastAPI/Starlette app state object that ELSPETH controls, not external Tier-3 data. The rationale invokes 'HTTP/session route boundary' but the actual subject \(request.app.state.session_compose_lock_registry\) is process-local state we populate ourselves; this is a lazy-init pattern using getattr-with-default to check first-use, which is exactly the defensive idiom the policy forbids. The correct fix is to initialize the registry at app startup \(lifespan\) so the attribute is always present and can be accessed directly, or to use a try/except AttributeError if lazy-init is truly required — not to suppress the rule. The rationale's appeal to 'audit-drain fallbacks' and 'request payloads' is generic boilerplate that doesn't describe this specific site. |
| `web/sessions/routes/_helpers.py:R5:_litellm_error_detail:fp=1effdbb33b2605f1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check at line 488 is not a Tier-3 external-data shape guard — it's a type discrimination between two internally-known exception classes \(_BadRequestLLMError vs LiteLLM-native exceptions\) to decide which attributes to read. This is legitimate polymorphic dispatch, not a defensive boundary check, and R5 should not be suppressed here on 'trust boundary' grounds. The rationale \('HTTP/session route boundary; persisted composer state, request payloads, and audit-drain fallbacks require explicit shape checks'\) is generic boilerplate that doesn't describe what's happening at this site — the exc is an exception raised by our own code paths, not external input. If R5 is firing on a legitimate type-dispatch pattern, the fix is either to restructure \(e.g., a method on the exception class\) or to address the rule's scope, not to allowlist with a misleading rationale. The decorator nudge does not apply: this is not R1/R5 on an external parameter, and 'exc' is not Tier-3 data. |
| `web/sessions/routes/_helpers.py:R5:_extract_runtime_model_snapshot:fp=c7a42ffc3485b7c0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate \('HTTP/session route boundary...'\) that doesn't match the code. The excerpt shows a function reading composition_state \(Tier-1 'our own writer' data\) and raising AuditIntegrityError on shape mismatch — this is offensive programming on Tier-1 data, not a Tier-3 HTTP boundary. The isinstance\(options, Mapping\) check is being used to detect-and-crash, which is exactly the offensive pattern policy encourages; if R5 fires here, the right move is either to keep the offensive check and refine the rule's scope \(or use a per-file exception that documents Tier-1 anomaly detection\), not to accept a misleading 'HTTP boundary' rationale. The rationale misrepresents the tier and the purpose of the check, making it a corrupted audit record. |
| `web/sessions/routes/_helpers.py:R1:_extract_runtime_model_snapshot:fp=a1dd46422475f434` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code operates on Tier 1 audit data \(composition_state node options, with AuditIntegrityError raised on shape anomalies\), not Tier 3 external input. The rationale invokes 'HTTP/session route boundary' and 'request payloads' generically, but the excerpt shows the .get\(\) calls reading from state.nodes\[i\]\['options'\] — a structure the surrounding code explicitly describes as guaranteed by the composition_state writer \(Tier 1\). Per policy, Tier 1 data must crash on anomaly; using .get\('model'\) here silently treats absence as None when the writer's contract should be asserted offensively \(e.g., explicit key check with AuditIntegrityError on absence, matching the style of the type checks below\). The rationale misclassifies the tier and the decorator does not apply \(no external parameter, R1 subject not rooted in untrusted input\). |
| `web/sessions/routes/_helpers.py:R1:_extract_runtime_model_snapshot:fp=dc84631f2ee5050f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale describes the file in broad strokes \('HTTP/session route boundary; persisted composer state, request payloads, audit-drain fallbacks'\) but the actual finding is on \`options.get\("model_version"\)\` where \`options\` has just been validated to be a Mapping read out of composition_state — that's Tier-1 'Our Data' per the surrounding AuditIntegrityError messages which explicitly invoke 'Tier 1 audit anomaly' and 'the composition_state writer guarantees'. On Tier-1 data, \`.get\(\)\` for an optional key is itself wrong: the policy says crash on any anomaly, and the function already does exact-type offensive checks against the writer's guarantee. If 'model_version' is documented as str-or-absent, the correct access is a membership check then direct subscript, or direct subscript with KeyError surfacing — not silent \`.get\(\)\` that conflates 'absent' with 'present-but-None'. The rationale's tier claim does not match the code's own tier self-description. |
| `web/sessions/routes/_helpers.py:R5:_composer_persisted_validation:fp=92aa5cf0eede32c5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check at line 1151 is not a Tier-3 boundary guard — it is a type-discrimination dispatch on an internal sentinel type \(\`_RuntimePreflightFailed\`\) versus a normal preflight result. This is a sum-type/union dispatch pattern on system-owned data structures \(Tier-2 at most\), not an external-data shape check. The rationale invokes 'HTTP/session route boundary' but the code shows discrimination between two internal result types we constructed ourselves; nothing here is parsing an external payload. The decorator does not apply: this is neither operating on a Tier-3 external parameter nor an R1/R5 boundary defense — it is legitimate type narrowing that should either use a structural match or a tagged-union discriminator, but the suppression rationale misrepresents the tier. |
| `web/sessions/routes/_helpers.py:R6:_load_canonical_mapping:fp=3fb4e4fbf22e6de6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly flagging this as un-justified new debt, not an actual justification. Per policy, an allowlist entry is itself an audit record and a vague/absent rationale is a corrupted audit record. Separately, while the function does handle potentially external JSON \(canonical_payload\), the rule is R6 \(not R1/R5 in the decorator's suppressible set\), so the @trust_boundary decorator is not the structural fix here. The agent must supply a site-specific justification \(e.g., explaining why catching JSONDecodeError and returning None is correct at this Tier-3 boundary\) or fix the root cause. |
| `web/sessions/routes/_helpers.py:R5:_load_canonical_mapping:fp=365956fb402548eb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder boilerplate \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly asks the reviewer to determine whether this is legitimate. That is not a justification; it is an admission that no site-specific reasoning has been provided. Separately, while the function does parse external/serialized data \(canonical_payload, potentially Tier-3-ish JSON\) and R5 is in the decorator's suppressible set, the parameter is a string and the isinstance check is on json.loads output — the proper remediation here is likely the @trust_boundary decorator on the function with source_param='canonical_payload', but I cannot confidently recommend that without seeing whether the decorator is already present and given the rationale itself does not argue the boundary case. |
| `web/sessions/routes/_helpers.py:R6:_state_data_from_composer_state_impl:fp=ce6f7b423897ff71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code catches ValueError/TypeError/KeyError from state.validate\(\) where state is a CompositionState — a typed system object, not Tier-3 external data. If validate\(\) raises TypeError or KeyError, that's a bug in our plugin/code \(per Plugin Ownership policy: 'Plugin method throws exception → CRASH'\). The rationale invokes 'persisted composer state' and 'request payloads' generically but the actual suppressed line operates on an already-constructed CompositionState whose validate\(\) method is system code. Swallowing TypeError/KeyError here converts code bugs into a synthetic 'validation_failed' summary, which is fabrication of audit data. ValueError might be legitimately raised by validate\(\) for bad authoring data, but the broad catch including TypeError/KeyError is defensive programming against bugs in our own code. The decorator nudge does not apply: the subject is not rooted at an external parameter \(it's a method call on a typed object\), and R6 is not in the decorator's suppressible set. |
| `web/sessions/routes/_helpers.py:R5:_state_data_from_composer_state_impl:fp=be2139c04c7b110c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance shape guard\) on \`runtime\`, a local variable produced by \`_capture_runtime_preflight_failure\(exc\)\` — internal code we own, not external Tier-3 data. The rationale gestures vaguely at 'HTTP/session route boundary' but the actual subject of the isinstance is an internally-constructed value whose type is determined by our own helper. Per the Plugin Ownership and Defensive Programming sections, if \`_capture_runtime_preflight_failure\` can return either \`ValidationResult\` or something else, the fix is to tighten that helper's return type \(or use a typed union dispatch\), not to suppress the isinstance check as a 'boundary'. The decorator nudge does not apply: \`runtime\` is not rooted at an external parameter of this function. |
| `web/sessions/routes/_helpers.py:R6:_handle_convergence_error:fp=00b7ed549b93bf34` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' that doesn't engage with this specific finding. The code shows a SQLAlchemyError catch around a save operation — this is catching errors from our own database layer \(Tier-1 audit data territory\), not from external untrusted input. Per the Auditability Standard and Plugin Ownership policy, SQLAlchemy errors on persistence operations should typically surface; if there's a legitimate reason to degrade gracefully here \(returning a 422 with partial_state_save_failed flag rather than 500\), the rationale must explain why this specific recovery preserves the audit trail. The comment in the code itself is far more specific and policy-aware than the agent's rationale, suggesting the rationale was written generically rather than for this site. Additionally, the slog.error call without escalation to Landscape audit may violate the telemetry primacy order. |
| `web/sessions/routes/_helpers.py:R6:_handle_plugin_crash:fp=10a0a4c1f8e0af26` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and doesn't actually justify the specific R6 finding \(broad exception catch on SQLAlchemyError\). While the inline code comments explain why SQLAlchemyError is caught broadly \(to handle the full family rather than just IntegrityError\), that justification belongs in the rationale field itself, not deferred to in-source comments. The submitted rationale conflates 'request payloads' \(Tier-3 input\) with 'audit-drain fallbacks' \(our own DB writes, Tier-1\), which is exactly the tier-confusion the policy warns about — a failed save to our own database is not a Tier-3 boundary. The agent should resubmit with a site-specific rationale explaining why catching the full SQLAlchemyError family here is correct \(e.g., this is a best-effort partial-state persist on an already-failing path where re-raising would mask the primary plugin crash\), rather than the current generic boundary-language. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=a024c22b062f016e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R5 isinstance check is on \`plugin_name = edited\["plugin"\]\`, where \`edited\` comes from \`turn_response\["edited_values"\]\` — an HTTP request payload \(Tier-3 external data\). This is a classic Tier-3 boundary shape-guard pattern that the @trust_boundary decorator is designed to suppress. The rationale describes the boundary correctly but the structural remediation is the decorator on the enclosing function \(with source_param pointing at the parameter carrying the request payload\), not a per-line allowlist entry. The excerpt does not show the function signature, but the symbol \`_dispatch_guided_respond_impl\` and the data flow indicate an external request parameter is in scope. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=cc710463b50e2ed0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape-guard on external HTTP request data, which is exactly the Tier-3 boundary pattern the @trust_boundary decorator was built to suppress. The value \`options_raw\` is rooted in \`edited\` \(a parsed request payload from the composer response\). All three conditions are met: function processes external data, rule_id is R5, and the subject traces back to the external payload. The correct remediation is to apply @trust_boundary on the enclosing function rather than land a per-line allowlist entry. The excerpt does not show the function signature, so the parameter name is inferred from context as the payload/edited-values argument. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=f07a10c9d12a6ba4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape-guard on data flowing from an HTTP request payload \(edited\['observed_columns'\]\) — a textbook Tier-3 boundary case. The function appears to dispatch a guided response from external request data; if the function takes the request/payload as a named parameter, the structural remediation is @trust_boundary\(tier=3, source_param=&lt;the payload param&gt;, suppresses=\('R5',\), ...\) rather than a per-line allowlist entry. The rationale is also generic boilerplate covering three distinct concerns \(persisted state, request payloads, audit fallbacks\) rather than site-specific justification, which is itself a corrupted audit record per the Auditability Standard. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=bab71348af41ba49` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape-guard on external HTTP request data at a trust boundary — the canonical case the @trust_boundary decorator was built to suppress. The excerpt shows sample_rows_raw flowing from 'edited' \(a parsed JSON payload from the composer's schema_form response\), and the function name suggests it takes the request payload as a parameter. The structural remediation is @trust_boundary\(tier=3, ..., suppresses=\('R5',\), ...\) on the enclosing function, not a per-line allowlist entry. I cannot confirm the exact parameter name from the truncated excerpt, but 'edited' or its parent request body is the likely source_param. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=d6b20d8b8dacf40d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 HTTP boundary R5 isinstance check on data rooted in an external request payload \(sample_rows_raw extracted from edited, itself from a parsed HTTP body\). The three-question test passes: the enclosing function handles external request data, the rule is R5, and the subject _sr_elem is rooted in the external payload via iteration over sample_rows_raw. The correct remediation is @trust_boundary\(tier=3, ..., suppresses=\('R5',\), ...\) on the enclosing function, not a per-line allowlist entry. I cannot see the parameter name from the truncated excerpt, so I cannot name the source_param definitively; the agent should identify the request/payload parameter on _dispatch_guided_respond_impl and apply the decorator there. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=248c9c25b0846ebf` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape guard on data coming from an HTTP request \(turn_response\['edited_values'\]\['columns'\]\), which is a legitimate Tier-3 external boundary. However, the structural remediation is @trust_boundary on the enclosing function rather than a per-line allowlist entry: R5 is in the decorator's suppressible set, and the subject \(columns_raw\) is rooted in the request payload. I cannot see the function signature in the excerpt to confirm the exact parameter name, but the function symbol _dispatch_guided_respond_impl and the data flow through turn_response indicate the external payload is a function parameter. The agent should apply @trust_boundary with source_param set to whichever parameter carries turn_response \(likely 'turn_response' or the request body parameter\) rather than landing a YAML entry. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=209b201c4d6f6543` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data derived from an HTTP request payload \(Tier-3\), which is exactly the pattern the @trust_boundary decorator is designed to handle structurally. However, the excerpt does not show the function signature or its decorators, so I cannot confirm the external parameter name with certainty — the data flows through 'turn_response'/'edited' which appear to be derived from a request payload parameter. The rationale is also generic boilerplate covering multiple unrelated concerns \('persisted composer state, request payloads, and audit-drain fallbacks'\) rather than site-specific justification, which fails the auditability standard. The agent should identify the external parameter at the function boundary and apply @trust_boundary\(tier=3, suppresses=\('R5',\), ...\) rather than a per-line allowlist entry. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=22c4187cc4e2b19c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data rooted in an external HTTP request payload \(\`edited\`/\`options_raw\`\), which is the canonical Tier-3 boundary pattern the @trust_boundary decorator was designed to cover. The excerpt doesn't show the enclosing function signature, but the symbol \`_dispatch_guided_respond_impl\` and the pattern of validating request-derived \`edited_values\` strongly indicates the external payload enters as a function parameter. The structural remediation is to decorate the function with @trust_boundary\(tier=3, suppresses=\('R5',\), ...\) naming the request-payload parameter as source_param, not to add a per-line allowlist entry. The rationale's generic invocation of 'HTTP/session route boundary' is exactly the boilerplate the decorator nudge exists to redirect. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=d753a4a63fb314d4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape guard at an HTTP request boundary — exactly the pattern the @trust_boundary decorator is designed to suppress structurally. The function processes external client-supplied turn_response/edited_values data, and the isinstance check on recipe_name is rooted in that external payload. The excerpt does not show the function signature or decorators, but the rule_id is R5 and the subject is clearly external request data, so the correct remediation is to apply @trust_boundary\(tier=3, source_param=&lt;the request payload parameter&gt;, suppresses=\('R5',\), ...\) rather than a per-line allowlist entry. The rationale is also generic boilerplate covering multiple concerns rather than site-specific. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=4d71e28876dcb4ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance shape-guard on external HTTP request payload data \(edited_values\['slots'\]\) — a textbook Tier-3 boundary case. The function appears to process request payload data; the subject slots_raw is rooted in client-supplied 'edited' content. The structural remediation is @trust_boundary on the enclosing function rather than a per-line allowlist entry. However, I can't see the function signature in the excerpt to confirm the parameter name with certainty — the agent should identify the parameter carrying the request payload \(likely the edited_values or request dict\) and apply the decorator there. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=312b6f0675209ff6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks at lines 3561 and 3564 are guarding values pulled from guided.step_3_proposal.steps — that is persisted composer/pipeline state \(Tier-2 'probably OK' data\), not a Tier-3 external payload. Per the Three-Tier model, transforms/sinks operating on pipeline data should expect type conformance and let mismatches crash as upstream bugs; the isinstance guards here are defensive shape-checks on data we control. The rationale gestures at 'HTTP/session route boundary' but the actual subject \(\`step\['plugin'\]\`, \`step\['options'\]\`\) is rooted in our own staged proposal structure, not an inbound request body. The decorator nudge does not apply because the subject is not rooted at an external parameter of this function. Fix the root cause: type the step structure \(TypedDict/dataclass\) so the access is type-safe, or raise InvariantError unconditionally on schema drift without an isinstance gate. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=16d626de6112ffc8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is on \`options = step\["options"\]\` where \`step\` is derived from \`guided.step_3_proposal.steps\[edit_step_index\]\` — this is persisted composer/guided session state, which is our own data \(Tier 1/2\), not Tier-3 external input. If the persisted proposal has a non-Mapping \`options\` field, that is an invariant break in code we control and should crash loudly, not be shape-guarded with an InvariantError raise pattern at this site \(the raise is fine, but R5 reports the isinstance as defensive — the rationale must justify why a shape check on internal state is needed, and 'persisted composer state requires explicit shape checks' is precisely the Tier-2 misapplication the policy warns about\). The rationale also lumps three unrelated justifications together \(HTTP payloads, persisted state, audit-drain fallbacks\) which is boilerplate covering for a site-specific judgment. Decorator does not apply: subject is not rooted at an external function parameter. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=7b7d6c8ca4388365` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`arguments: Mapping\[str, Any\]\` which is Tier-3 external data \(tool-call arguments from the composer model\), the rule is R5 \(isinstance shape guard\), and the subject \`source\` is rooted at \`arguments\` via \`arguments.get\("source"\)\`. All three decorator conditions are met. The agent rationale is a placeholder \('ALLOWLIST-FRESH'\) with no site-specific justification, which is itself a corrupted audit record. The correct remediation is \`@trust_boundary\` on the function with \`source_param='arguments'\`, not a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=feb1bbf03070b119` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`_inline_blob_content_for_proposal\` takes \`arguments: Mapping\[str, Any\]\` which is external tool-call data \(Tier-3\), the finding rule_id is R1, and the subject \`source.get\("inline_blob"\)\` is rooted in \`arguments\` via \`source = arguments.get\("source"\)\`. All three decorator-nudge conditions are met. The rationale itself \('ALLOWLIST-FRESH'\) is a placeholder admitting no real justification was supplied. The correct remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1','R5'\), ...\)\` on the function rather than a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=09b43dc60ebd6036` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _inline_blob_content_for_proposal takes \`arguments: Mapping\[str, Any\]\` which is external Tier-3 tool-call data, the finding is R5 \(isinstance shape guard\) — one of the two rules the @trust_boundary decorator is allowed to suppress, and the subject \`inline_blob\` is rooted in \`arguments\` via \`arguments.get\('source'\).get\('inline_blob'\)\`. The agent's rationale is a placeholder \('ALLOWLIST-FRESH'\) with no site-specific justification, which is itself grounds to reject. The structural remediation is to apply @trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1','R5'\), ...\) to the function rather than a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=5e022f0ca07cbf2e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`arguments: Mapping\[str, Any\]\` which is external Tier-3 data \(composer tool-call arguments\), and the finding R1 on \`inline_blob.get\("content"\)\` is rooted in that parameter via \`arguments.get\("source"\).get\("inline_blob"\)\`. All three decorator-nudge conditions are met: external parameter, suppressible rule \(R1\), and clear data-flow path from \`arguments\`. The rationale itself is a placeholder admitting no real justification was provided. The correct remediation is \`@trust_boundary\` on this function with \`source_param='arguments'\`, not a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=f210c162f5a60357` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes external \`arguments: Mapping\[str, Any\]\` \(tool-call payload, same pattern as the composer set_pipeline path\), the finding rule_id is R5 \(isinstance shape guard\) which is in the decorator's suppressible set, and the subject \`content\` is rooted in \`arguments\` via \`arguments\['source'\]\['inline_blob'\]\['content'\]\`. The correct remediation is \`@trust_boundary\(tier=3, source='composer tool-call arguments for set_pipeline inline blob recovery', source_param='arguments', suppresses=\('R5',\), invariant='returns None on any malformed shape; never raises', test_ref=..., test_fingerprint=...\)\` on \`_inline_blob_content_for_proposal\`. The agent's rationale is also empty boilerplate \(ALLOWLIST-FRESH placeholder\) and does not justify a per-line entry on its own merits. |
| `web/sessions/routes/composer.py:R1:register_composer_routes:accept_composition_proposal:fp=ec8182b9ca85932f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`result.data.get\(_DATA_ERROR_KEY\)\`, where \`result\` is a ToolResult returned by a system-owned plugin/tool. Per Plugin Ownership policy, plugin return values are not Tier-3 data — they are system code, and \`result.data\` should have a known type. The defensive \`isinstance\(result.data, Mapping\)\` + \`.get\(\)\` pattern is exactly the forbidden 'plugin might return None/wrong-type' shape. The rationale gestures broadly at 'HTTP/session route boundary' but the suppressed expression operates on plugin-produced data, not on the request payload. The decorator nudge also does not apply: the subject is not rooted at an external function parameter, it's rooted at a tool result. Fix the root cause by typing \`result.data\` properly and accessing it directly. |
| `web/sessions/routes/composer.py:R6:register_composer_routes:accept_composition_proposal:fp=97184ca5456a173f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches KeyError and ValueError from an internal service call \(service.reject_composition_proposal\) — this is Tier-1/Tier-2 internal code, not a Tier-3 external boundary. The rationale claims 'narrow recovery catches' at a route boundary, but the catch is around our own service method, and the recovery is to silently add a note and continue. Per Plugin Ownership and the Defensive Programming policy, internal service exceptions should crash or be handled via offensive assertions, not swallowed with add_note. If concurrent state transitions are a real domain concern, the service should return a typed result or raise a domain-specific exception that the route handles explicitly; catching generic KeyError/ValueError conflates 'already terminal' with genuine bugs \(e.g., a KeyError from a typo in the service implementation\). |
| `web/sessions/routes/composer.py:R7:register_composer_routes:recompose:fp=b22f01dc72b4660b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and does not address what R7 is actually flagging at this specific line — a contextlib.suppress\(asyncio.CancelledError\) wrapping a shielded progress publish during cancellation cleanup. The agent must explain why suppressing CancelledError specifically at this site is correct \(e.g., the shielded coroutine may itself be cancelled by an outer task and we are already in the cancellation path re-raising below\), not invoke a blanket 'boundary requires narrow catches' justification. Additionally, the rule R7 is not in the decorator's suppressible set {R1, R5}, so the structural alternative doesn't apply; the agent should either write a site-specific rationale naming the exact invariant \(cancellation cleanup mirrors send_message; outer raise preserves cancellation semantics\) or restructure. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:get_guided:fp=f09febcda74c85ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a bare \`except Exception\` swallowing audit-persistence failures, which directly violates the Auditability Standard and the Telemetry/Logging primacy order. The Landscape audit trail is the source of truth and must fire sync, crash-on-failure — diverting a persist failure to \`slog.error\` is exactly the inverted channel the policy forbids. The rationale invokes 'audit-drain fallbacks' as if logger-of-last-resort were a legitimate channel for a primary audit write, but this is the primary write, not a tertiary fallback after audit+telemetry have both failed. The decorator nudge does not apply: R4 is not in the suppressible set {R1, R5}, and the suppressed expression is not rooted at an external request parameter — it's wrapping our own audit write. Remediation is structural \(let the persist failure propagate, or route through the proper crash-on-failure audit primitive\), not an allowlist entry. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:get_guided:fp=a89801c4ea858a56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and does not address this specific site. The code shows contextlib.suppress\(Exception\) wrapping a slog.error call inside an except handler that is already handling a persist failure. Per the Telemetry primacy order, the Landscape audit is the primary channel and has already failed here; logging is the last resort, but silently swallowing all exceptions from the logger \(rather than letting them crash or being narrowly scoped\) is exactly the defensive-programming pattern the policy forbids. The rationale claims 'no safer channel exists if structlog itself raises', but does not justify why a bare Exception suppression — which would also hide bugs like NameError or TypeError in the slog call itself — is correct rather than a narrower catch or letting it propagate. The audit-persist failure should surface, not be doubly suppressed. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:get_guided:fp=3596e2d1f66b3f99` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on a bare \`except Exception\` \(R4\) wrapping \`_persist_llm_calls\`, which writes to the Landscape audit trail — Tier 1 data. Per the Auditability Standard and Telemetry primacy order, audit persistence fires sync and crashes on failure; swallowing the exception and diverting to \`slog.error\` is exactly the inversion the policy forbids. The rationale's appeal to 'audit-drain fallbacks' misidentifies the tier: an audit-write failure is a Tier-1 invariant break, not a Tier-3 boundary recovery. The nested \`contextlib.suppress\` around the logger call compounds the problem by hiding even the diagnostic. Remediation is structural \(let the audit failure propagate, or surface it through the documented crash path\), not a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:get_guided:fp=7357db09796362cd` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate \('HTTP/session route boundary...requires narrow recovery catches'\) and does not justify this specific construct: \`contextlib.suppress\(Exception\)\` wrapping a \`slog.error\` call inside an except handler. Per the Telemetry primacy order, logging is the last-resort channel, and per policy, silently swallowing all exceptions from a logging call is exactly the defensive pattern the rules target. The rationale invokes 'audit-drain fallbacks' but the suppressed call is \`slog.error\`, not a Landscape audit write — so the 'boundary' framing misapplies. If slog.error can raise, that's a logger bug to fix at the root, not to blanket-suppress; at minimum the rationale must name why a broad Exception swallow on a logger call is the right shape here. Not a Tier-3 R1/R5 case, so the decorator does not apply. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_respond:fp=65b078af067fd26a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R4 here is a broad \`except Exception\` around \`_persist_tool_invocations\` followed by \`contextlib.suppress\(Exception\)\` around the logging — this is exactly the audit-channel swallow that the policy forbids. The Landscape audit trail is the source of truth and must fire sync with crash-on-failure; suppressing a persistence failure and diverting to \`slog.error\` inverts the primacy order \(audit first, telemetry second, logging last resort\). The rationale's gesture at 'audit-drain fallbacks' is a misapplication: a failure to persist the audit record is not a trust-boundary event to recover from, it is a catastrophic invariant break that must surface. The fact that this lives in an HTTP route doesn't make audit-persistence Tier-3; the data being persisted is our own Tier-1 audit data. No decorator nudge — R4 is not in the {R1,R5} suppressible set and the subject is not a Tier-3 parameter. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_respond:fp=90d1459942d2afe8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is R7 \(silent exception handling\) inside what appears to be a fallback path that swallows audit-persistence failures with contextlib.suppress\(Exception\) around a slog.error call. Per the Auditability Standard, audit \(Landscape\) fires first and crashes on failure; logging is last resort. Suppressing exceptions around the logger call itself may be defensible for the logging fallback, but the rationale is generic boilerplate \('HTTP/session route boundary... narrow recovery catches'\) that does not explain why this specific suppress is at a trust boundary — the data being persisted is Tier-1 audit data, not Tier-3 external input. The decorator does not apply \(R7 not in {R1,R5}, and no external parameter flow\). The agent must justify per-site why swallowing is correct, or restructure so the audit-persist failure surfaces. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_respond:fp=459203baa79b869f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R4 \(broad except Exception\) wrapping an audit persistence call. Per the Telemetry and Logging primacy order, audit \(Landscape\) fires first and must crash on failure — it is sync, crash-on-failure by design. Swallowing exceptions from _persist_llm_calls with a bare \`except Exception\` and downgrading to slog.error inverts the primacy order: audit failures become log entries, which the policy explicitly identifies as a defect rather than a justification. The rationale's appeal to 'HTTP route boundary' conflates the Tier-3 request-payload boundary with audit-write failures, which are Tier-1 \(our data\) and must surface. The decorator nudge does not apply \(R4 is not in {R1, R5}, and the suppressed subject is not an external parameter\). |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_respond:fp=647eaaa3142057a0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a broad \`except Exception\` followed by \`contextlib.suppress\(Exception\)\` around a \`slog.error\` call — this is exactly the silent-swallow pattern the policy forbids. The rationale is generic boilerplate about 'HTTP/session route boundary' and does not address why this specific audit-persist failure may be swallowed. Per policy, audit \(Landscape\) failures fire sync and crash-on-failure; logging is last resort, and suppressing the logger call here means the failure becomes invisible. If the concern is that we are already in an exception-handling path and a second persist failure must not mask the original, that needs to be stated explicitly with the invariant being preserved and the channel of record — not a vague boundary claim. R7 is not in the decorator's suppressible set {R1, R5}, so the structural nudge does not apply. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_chat:fp=4aac0c4c0b044ec2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on a bare \`except Exception\` that catches a failure from \`_persist_tool_invocations\` — an audit persistence call. Per the Auditability Standard and Telemetry primacy order, audit \(Landscape\) fires first and must crash-on-failure; swallowing audit-persist exceptions and diverting them to \`slog.error\` inverts the primacy order and corrupts the audit trail. The rationale's appeal to 'HTTP/session route boundary' conflates the Tier-3 request boundary with internal audit-write operations on Tier-1 data — those are not the same trust surface. This is exactly the 'we log it anyway' anti-pattern called out in policy; the resolution is to let the audit failure propagate, not to allowlist the suppression. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_chat:fp=fda975b0625f46e8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R7 \(silent exception suppression via contextlib.suppress\) on slog.error during exception handling. Per the Telemetry/Logging primacy order, audit \(Landscape\) is the source of truth and fires sync/crash-on-failure; logging is a last resort. This site is already in a degraded-path branch where audit persistence failed, but swallowing all exceptions from slog.error is generic defensive programming — not a Tier-3 external-data boundary. The rationale invokes 'HTTP/session route boundary' as a blanket justification but R7 here is about suppressing logger failures, not validating external request data; the decorator would not apply \(R7 is not in {R1,R5}\). If slog.error can fail in ways that must be tolerated, that should be encoded in the logging adapter itself, not by wrapping every call site in contextlib.suppress\(Exception\). |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_chat:fp=6a812675536c1066` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a bare \`except Exception\` \(R4\) wrapping \`_persist_chat_turns\`, which writes to ELSPETH-owned state. Persistence of chat turns is Tier-1 audit-trail-adjacent work, not a Tier-3 external boundary; swallowing the exception \(even with a slog.error\) violates the Audit primacy order — pipeline-relevant persistence failures must surface, not be diverted to logger. The rationale's appeal to 'HTTP/session route boundary' conflates the inbound request boundary \(already past\) with internal persistence calls, and 'audit-drain fallbacks' is precisely the anti-pattern the policy forbids. The decorator does not apply: R4 is not in {R1, R5}, and the subject is an internal service call, not an external parameter. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_chat:fp=89713c5bac9cbaa5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a broad \`except Exception\` followed by \`contextlib.suppress\(Exception\)\` around a structured logger call during exception handling. This is silent error swallowing in code we control, not a Tier-3 boundary on external request data — the suppressed operation is \`slog.error\`, our own logging call. Per the Telemetry primacy order, pipeline-relevant persistence failures belong in the Landscape audit trail, not buried under nested \`suppress\(Exception\)\` over a logger. The rationale invokes 'HTTP/session route boundary' generically without explaining why this specific double-suppression is legitimate; 'narrow recovery catches' is contradicted by the bare \`Exception\` catches. Fix the root cause \(route the persist failure through audit, or let it surface\) rather than allowlist R7 here. |
| `web/composer/tools/blobs.py:R1:_set_nested_option:fp=9606870cec567eb9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) that explicitly says review is needed — it is not a justification. The .get\(\) here is on a container dict being mutated by a recursive helper; container is constructed/owned by the caller path \(composition state\), not Tier-3 external data at this site. The correct pattern is direct access with explicit absence handling \(e.g., \`head not in container\` or try/except KeyError\), or use the existing None-check via a sentinel — let it crash if invariants break. The decorator does not apply: container is not a function-scoped external parameter \(it's a recursive accumulator\), and the subject is not rooted at a Tier-3 boundary input. |
| `web/composer/tools/blobs.py:R5:_set_nested_option:fp=984ed68ceb1df11f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder note from fingerprint rotation \('ALLOWLIST-FRESH — review whether this is new debt or needs a more specific justification'\), not an actual justification for suppression. It explicitly defers the decision rather than making the case. Additionally, the isinstance check here is an offensive validation: the code raises ValueError when the existing container value is not a Mapping, which is exactly the encouraged 'detect invalid state and throw meaningful exception' pattern — it is not a defensive shape guard that should be suppressed at all. The container originates from composition state manipulation, not directly from an external parameter on this function, so the @trust_boundary decorator is not the right structural fix either. |
| `web/composer/tools/blobs.py:R1:_execute_wire_blob_inline_ref:fp=b158f3692e8bbf5d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`arguments.get\("encoding", "utf-8"\)\` inside \`_execute_wire_blob_inline_ref\`, which takes \`arguments\` as an external tool-call payload \(Tier-3\). All three decorator-nudge conditions are met: the function consumes external data via a parameter, the rule is R1 \(in the suppressible set\), and the subject is rooted at \`arguments\`. The correct remediation is \`@trust_boundary\(tier=3, source=..., source_param='arguments', suppresses=\('R1',\), ...\)\` on the function, not a per-line allowlist entry. Additionally, the agent's rationale is a placeholder \("ALLOWLIST-FRESH"\) with no site-specific justification, which fails the auditability standard on its own. |
| `web/composer/tools/blobs.py:R5:_execute_wire_blob_inline_ref:fp=3658d54a8721ac96` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance shape guard\) on \`encoding_value\`, which is rooted at \`arguments.get\("encoding", "utf-8"\)\` — i.e. data taken from the LLM tool-call \`arguments\` dict, a Tier-3 external source. The enclosing function \`_execute_wire_blob_inline_ref\` takes \`arguments\` as a parameter, R5 is in the decorator's suppressible set, and the subject flows directly from that parameter. All three structural conditions are met, so the correct remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R5',\), ...\)\` on the function rather than a per-line allowlist entry. The agent's rationale is also a placeholder \("ALLOWLIST-FRESH"\) that does not justify the suppression on its merits. |
| `web/composer/tools/blobs.py:R1:_execute_wire_blob_inline_ref:fp=79391f3b16bd3db6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: the function operates on \`arguments\` \(an LLM tool-call dict\), the finding is R1 \(silent .get on external data\), and the subject \`arguments.get\("sha256_override"\)\` is rooted directly at the external parameter. The correct remediation is the @trust_boundary decorator on the enclosing function with source_param='arguments', not a per-line allowlist entry. Additionally, the agent rationale is a placeholder \('ALLOWLIST-FRESH'\) with no site-specific justification, which is independently insufficient as an audit record. |
| `web/composer/tools/blobs.py:R1:_state_source_blob_ref:fp=9f3a080fbddec2a0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) from fingerprint rotation, not a site-specific justification — it explicitly admits review is needed. On the merits: state.source.options is a dict on a CompositionState object \(Tier-2 pipeline data we control\), not Tier-3 external input. The .get\('blob_ref'\) followed by isinstance check is defensive programming against our own data structure. If options is expected to potentially contain blob_ref, the correct fix is a typed accessor on CompositionState; if not, let it KeyError. The decorator does not apply: 'state' is not an external parameter and CompositionState options is not a Tier-3 boundary. |
| `web/composer/tools/blobs.py:R5:_state_source_blob_ref:fp=e8ea24c6f35c3e3c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits no site-specific justification has been provided. That is a corrupted audit record by the Auditability Standard. Separately, the code reads from state.source.options, which is pipeline-internal CompositionState \(Tier 1/2 owned data\), not an external Tier-3 parameter — an isinstance shape guard there is forbidden defensive programming; the options dict should have a typed contract and direct access should crash on contract violation. The @trust_boundary decorator does not apply: state is not an external-data function parameter and there is no Tier-3 boundary here. |
| `web/composer/tools/blobs.py:R1:_prepare_blob_create:fp=df3f9d3f9790ca88` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is an R1 silent .get\(\) on an external 'arguments' dict \(LLM tool-call payload, Tier-3\). The function _prepare_blob_create takes 'arguments' as a parameter and the suppressed expression is rooted directly at that parameter, so all three structural conditions for the @trust_boundary decorator are met. The correct remediation is the decorator with source_param='arguments', not a per-line allowlist entry. Additionally, the rationale \('AST shift after refactor merge'\) is not a substantive justification — it merely notes that a prior suppression's location moved, which is not policy-grounded evidence that suppression is correct at the new site. |
| `web/composer/tools/generation.py:R1:_csv_source_delimiter:fp=a46f1ac16266c1ba` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH'\) and provides no site-specific justification — it explicitly asks the reviewer to determine whether this is debt or needs a better reason, which is not a valid audit record. Separately, this is a strong structural candidate for @trust_boundary: the function _csv_source_delimiter takes an external options mapping \(composer tool input / user-supplied configuration\), R1 is in the decorator's suppressible set, and the finding's subject \(options.get\('delimiter'\)\) is rooted directly at the source_param. The correct remediation is to decorate the function with @trust_boundary\(tier=3, source_param='options', suppresses=\('R1',\), ...\) rather than land a per-line allowlist entry with a placeholder rationale. |
| `web/composer/tools/generation.py:R1:_csv_source_skip_rows:fp=5e95739f36d9eda1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly asks for review rather than justifying the suppression. It provides no site-specific reasoning. Separately, this finding is a strong candidate for the @trust_boundary decorator: the function takes an external \`options\` mapping \(Tier-3 config input\), the rule is R1, and the .get\(\) subject is rooted at the \`options\` parameter. The structural remediation is to decorate \`_csv_source_skip_rows\` \(and its siblings\) with @trust_boundary\(source_param='options', suppresses=\('R1',\), ...\) rather than land a per-line allowlist entry. |
| `web/composer/tools/generation.py:R1:_csv_source_columns:fp=556a65c1e6ad5068` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder template \("ALLOWLIST-FRESH — ... review whether this is new debt"\) and offers no site-specific justification. The function _csv_source_columns parses user-supplied options \(a Tier-3 config boundary\), and the R1 finding is rooted at the \`options\` parameter via options.get\("columns"\). This is a structural Tier-3 boundary case: R1 is in the decorator's suppressible set, and the subject derives directly from the function parameter. The correct remediation is @trust_boundary\(tier=3, source_param='options', suppresses=\('R1',\), ...\) rather than a per-line allowlist entry. |
| `web/composer/tools/generation.py:R5:_csv_source_columns:fp=f89d44e37975a3cc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH'\) with no site-specific justification — that alone fails the auditability standard. Looking at the code: this is an R5 isinstance check inside a function that takes an \`options\` Mapping parameter carrying external user-config data \(Tier-3\), and the subject \`raw\` is rooted at \`options.get\('columns'\)\`, i.e. derived directly from the external parameter. All three conditions for the @trust_boundary decorator are met \(boundary function, R5 is in the suppressible set, subject traces to the external parameter\). The structural remediation is to decorate \`_csv_source_columns\` with @trust_boundary\(tier=3, source_param='options', suppresses=\('R5',\), ...\) rather than a per-line allowlist entry. |
| `web/composer/tools/generation.py:R5:_csv_source_field_mapping:fp=176985867214e183` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is the literal placeholder 'ALLOWLIST-FRESH' boilerplate from fingerprint rotation — it explicitly admits no site-specific justification was written. The code-comment justification \(isinstance needed because MappingProxyType is not dict\) is plausible on its merits, and this is an R5 structural guard on Tier-3 external/LLM-authored config inside what appears to be a source helper. However, the function takes \`options: Mapping\[str, Any\]\` and the subject \`raw\` is rooted at that external parameter, so the structurally correct remediation is \`@trust_boundary\(tier=3, source_param='options', suppresses=\('R5',\), ...\)\` on the enclosing function \(or its caller\), not a per-line allowlist entry — and even setting that aside, a placeholder rotation-stub rationale is not an acceptable audit record on its own. |
| `web/composer/service.py:R5:_request_interpretation_review_kind_from_arguments:fp=ee9dfe639c1746a6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes \`arguments: Mapping\[str, Any\]\` derived from LLM tool-call data, the rule is R5 \(isinstance shape guard\), and the subject \`raw_kind\` is rooted at the \`arguments\` parameter via subscript access. The decorator is precisely the correct remediation here — it would suppress R5 on names derived from \`arguments\` with a documented invariant rather than a per-line allowlist entry. The excerpt does not show a pre-existing \`@trust_boundary\` decorator on the function definition line 171, so the decorator nudge is appropriate. |
| `web/app.py:R1:_BodySizeLimitMiddleware:dispatch:fp=9519401fd195b0ac` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: dispatch\(\) receives an external HTTP request, R1 is in the suppressible set, and the subject request.headers.get\('content-length'\) is rooted at the request parameter. The correct remediation is the @trust_boundary decorator on dispatch with source_param='request', not a per-line allowlist entry. |
| `web/app.py:R1:create_app:fp=7ce3ee8e08561e45` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims this is for WEB_CONCURRENCY, but the actual code at the finding reads ELSPETH_ENV — the rationale describes a different site than the code. That alone makes this allowlist entry a corrupted audit record. Separately, while os.environ is legitimately Tier-3-ish, the decorator nudge doesn't apply \(create_app takes no external parameter carrying this data\), and the rationale is too thin/wrong to accept on its own merits. Agent should rewrite the rationale to match the actual expression and justify why .get\(\) \(vs direct os.environ\[\] with a KeyError contract, or a default via os.environ.get\(..., 'something'\)\) is correct here. |
| `web/app.py:R2:create_app:_request_id:fp=318afae72ce2947e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a defensive getattr with a fabricated default 'unset' substituting for an absent correlation id. The rationale claims a trust boundary, but request.state is internal application state populated by our own middleware \(Tier-1/code-controlled\), not Tier-3 external data. Per the fabrication test, if RequestIdMiddleware didn't run that absence is a fact — the honest representations are None or letting AttributeError surface \(offensive: the invariant 'middleware ran' is broken and the exception handler should reveal that\), not synthesising the string 'unset' into the audit/response. The decorator nudge doesn't apply: R2 is not in {R1, R5} and request.state is not an external source_param. Fix the root cause by either guaranteeing middleware order or recording request_id=None explicitly. |
| `web/composer/source_inspection.py:R1:_facts_from_objects:fp=839f884f581b26d0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R1 finding on \`obj.get\(h\)\` where \`obj\` comes from \`objects\` — likely a function parameter rooted in operator-supplied JSON. If \`_facts_from_objects\` takes the objects list as a parameter representing Tier-3 external data, the structural remediation is \`@trust_boundary\` on the function, not a per-line allowlist entry. The excerpt does not show the function signature or decorators, but the symbol name and the iteration pattern over \`objects\` strongly suggest the parameter is the external-data carrier. Agent should apply \`@trust_boundary\(tier=3, source_param=&lt;the objects parameter&gt;, suppresses=\('R1',\), ...\)\` instead. |
| `web/composer/source_inspection.py:R1:_declared_field_name:fp=ec99754b8b1fc32a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`field.get\("name"\)\` inside \`_declared_field_name\(field: DeclaredFieldSpec\)\`. The function takes operator-supplied declared-field data \(Tier-3 external\) as its parameter, R1 is in the decorator's suppressible set, and the subject is rooted directly at the \`field\` parameter. All three conditions for the structural @trust_boundary remediation are satisfied; the per-line allowlist entry is the wrong shape of fix. The agent should decorate \`_declared_field_name\` \(and similarly \`_declared_field_is_required\`\) with @trust_boundary\(tier=3, source_param='field', suppresses=\('R1','R5'\), ...\) rather than land a YAML suppression. |
| `web/composer/source_inspection.py:R5:_declared_field_name:fp=c2d07dc1c5c6d068` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance shape guard\) on data rooted in the \`field\` parameter of \`_declared_field_name\`. If \`field\` is operator-supplied Tier-3 data \(DeclaredFieldSpec from an external config\), this is exactly the structural Tier-3 boundary pattern the @trust_boundary decorator was built for: R5 is in the suppressible set, the subject \`name_raw\` flows from the \`field\` parameter via \`.get\("name"\)\`, and the function returns None on malformed input as its invariant. The remediation is the decorator on \`_declared_field_name\` \(and likely its sibling \`_declared_field_is_required\`\), not a per-line allowlist entry. |
| `web/composer/source_inspection.py:R5:_declared_field_is_required:fp=61f9e4be883aff78` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) that explicitly asks for review rather than justifying the suppression. It provides no site-specific reasoning about why an isinstance check at this location is legitimate. While the code itself appears to be discriminating between a string-form and dict-form DeclaredFieldSpec union type \(a plausible legitimate use of isinstance for sum-type dispatch\), the agent has not made that argument, and an audit record reading 'ALLOWLIST-FRESH — review whether this is new debt' is not an interpretable audit record. The function parameter is a DeclaredFieldSpec union, not external Tier-3 data flowing from a decorated boundary parameter, so the @trust_boundary decorator is not the right remedy either. |
| `web/composer/source_inspection.py:R1:_declared_field_is_required:fp=431bc71e5d5e137e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) generated by fingerprint rotation, not a site-specific justification. It explicitly flags itself as needing review and offers no policy-grounded reason for the suppression. The function _declared_field_is_required appears to operate on a DeclaredFieldSpec — a typed configuration/spec object — not a Tier-3 external parameter, so the decorator path doesn't obviously apply, and even if 'field' originates externally the rationale does not establish that. A suppression without a real justification is a corrupted audit record per the Auditability Standard. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=3aba7cf3d76c43d9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`node.options.get\("schema"\)\` inside \`_find_identity_node_advisories\`. The rationale claims Tier-3 operator-supplied config, which is plausible. However, this is a structural Tier-3 boundary pattern: the suppressed access is rooted at a function-local data structure derived from pipeline-config nodes, and R1 is in the decorator's suppressible set. If \`node\` \(and \`sink\`\) flow from a parameter of this function, the correct remediation is \`@trust_boundary\(tier=3, source_param=&lt;the nodes/config parameter&gt;, suppresses=\('R1','R5'\), ...\)\` rather than per-line allowlist entries — especially since the excerpt shows multiple R1/R5 sites in the same function that would all be covered structurally. The excerpt does not show the function signature, so I cannot name the parameter with certainty, but the pattern strongly indicates decorator remediation over allowlisting. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=ec32acfc57602caa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`schema_block.get\("fields"\)\` inside \`_find_identity_node_advisories\`. The rationale claims this is a Tier-3 config boundary, but the function signature and parameter flow are not visible in the excerpt — the data is accessed via \`node.options\` and \`schema_block\`, which appear to be attributes of structured node objects already past initial validation, not a raw external parameter. Even if the data is genuinely Tier-3 operator-supplied config, the correct remediation is to apply \`@trust_boundary\` to the enclosing function with the appropriate \`source_param\`, not a per-line allowlist entry. Since I cannot see the function signature to confirm the parameter name, I cannot confidently nominate one for the decorator. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=a1d34b9af2f7e3db` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding at line 559 \(\`sink.options.get\("schema"\)\`\) operates on data reached via \`sink.options\`, where \`sink\` is a parameter/local in \`_find_identity_node_advisories\` — not a function decorated for the external boundary visible in the excerpt. The rationale claims Tier-3 config-boundary handling, which may be true at the original config-parse boundary, but by the time data reaches this validation helper it has been parsed into node/sink objects and is Tier-2 pipeline data; defensive \`.get\(\)\` here is policy-forbidden. If the operator-supplied options dict truly remains Tier-3 at this layer, the structural remedy is \`@trust_boundary\` on the enclosing function with \`source_param\` rooted at the external input — not a per-line allowlist. I cannot see the function signature or decorators in the excerpt to confirm a parameter name, so I am not emitting a decorator nudge. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=7022ce32f26888c2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 \(.get on external data\) inside a function that processes operator/LLM-authored pipeline configuration — a legitimate Tier-3 boundary case. However, the subject \`sink_schema_block\` derives from \`sink.options\`, which traces back to function parameters \(likely \`nodes\`/pipeline config\), not from \`self\` state. The structural remediation is \`@trust_boundary\` on \`_find_identity_node_advisories\` with the appropriate \`source_param\`, not a per-line allowlist entry. I cannot see the function signature in the excerpt to name the exact parameter with certainty, but the pattern \(R1 on operator-supplied options dicts\) is exactly what the decorator exists to cover. |
| `web/execution/validation.py:R1:validate_pipeline:fp=733611406e3fefe3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) is on source_options, which is derived from state.source.options — a typed CompositionState domain object \(Tier-2 pipeline data\), not Tier-3 external input. The rationale's 'Tier 3 boundary' framing is a misapplication: by the time validate_pipeline runs, the YAML has been parsed into a typed SourceSpec with a typed Mapping options attribute. The correct pattern for 'key may or may not be present in a mapping' is direct membership testing \(\`if key in source_options\`\) or unconditional access with explicit handling, not silent .get\(\) defaulting. Even setting tier aside, the function is not decorated as a trust boundary in the visible excerpt and the subject is not rooted at an external function parameter, so the @trust_boundary nudge does not apply either. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=a12ab183df5dc425` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance\(\) check here is not a Tier-3 boundary guard on external data — it is a discriminated-union return-type check on the result of an internal helper \(_resolve_source_blob\) that can return either a ToolResult or a resolved source blob. This is system-controlled code returning a union type; the correct fix is to refactor the helper's return type \(e.g. separate success/failure channels, or use a typed sum\) rather than suppress R5. The rationale only describes an AST shift from a refactor merge and asserts the pattern is 'preserved' without justifying why isinstance on an internal return value is legitimate. The decorator does not apply: 'resolved' is not rooted at an external parameter. |
| `web/composer/tools/sessions.py:R1:_execute_set_pipeline:fp=a9b4b8adc81c4f9b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged .get\(\) is on _MIME_TO_SOURCE, a process-local lookup dict we control, not on external/Tier-3 data. The subject is not rooted at an external parameter \(no data flow from arguments into _MIME_TO_SOURCE\), so the @trust_boundary decorator does not apply. The rationale only describes an AST shift after a merge — it provides no policy justification for why suppressing R1 at this site is legitimate. The correct pattern here is a normal dict lookup with None-check \(which the code already does\), and if R1 flags this lookup-table idiom legitimately, the resolution is a code-level fix \(e.g., explicit membership check or restructuring\) — not a per-line allowlist entry preserved across a refactor. |
| `web/composer/tools/sessions.py:R1:_execute_set_pipeline:fp=dbb2f95a63783167` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 LLM tool-call boundary case: the function receives raw LLM-supplied \`\`args\`\` and the finding is R1 on \`\`args.get\("outputs"\)\`\` — subject rooted directly at the external parameter. All three decorator-nudge conditions are met \(external parameter, R1 is suppressible, subject rooted at the parameter\). The correct remediation is \`\`@trust_boundary\`\` on the enclosing function with \`\`source_param='args'\`\`, not a per-line allowlist entry. Additionally, the rationale's AST-shift framing describes mechanical reapplication after a refactor, which is not a site-specific justification. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=8a8cb7d027f0f2f1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook structural Tier-3 boundary case for the @trust_boundary decorator. The finding is R5 \(isinstance shape guard\), the data subject \`raw_outputs\` is rooted at the function parameter \`args\` \(via \`args.get\('outputs'\)\`\), and the comment block explicitly identifies this as an LLM tool-call payload boundary read. All three decorator-suitability conditions are met. The remediation is to apply @trust_boundary\(tier=3, source='LLM tool-call arguments', source_param='args', suppresses=\('R1','R5'\), ...\) on the enclosing function rather than land a per-line allowlist entry. The rationale's invocation of an AST-shift/merge artifact is not a justification on its own merits — it explains why the fingerprint moved, not why the suppression is correct here. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=f8d3f1fc01dbefb4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance shape guard\) inside \`_execute_set_pipeline\`, which the policy's examples explicitly identify as a Tier-3 boundary function receiving external tool-call arguments. The subject \`raw_entry\` is rooted in \`raw_outputs = args.get\("outputs"\)\` where \`args\` is the external arguments mapping, so all three decorator-suggestion conditions are met: external-data parameter, R5 is in the suppressible set, and the data-flow path is via subscript from the external parameter. The correct remediation is \`@trust_boundary\` on the enclosing function with \`source_param\` set to the external arguments parameter, not a per-line allowlist entry. The agent's rationale \(AST shift after refactor\) is not a substantive justification — it documents a re-fingerprint, not why suppression is legitimate. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=87b1f78f92cca424` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code operates on \`validated.nodes\`, which is a Pydantic-validated model \(\`_PipelineNodeModel\`\) produced inside this function. By that point the data is Tier-2 \(post-source-validation\), not Tier-3 raw LLM input — the schema declares the type of \`n.branches\`, so an isinstance shape guard at this site is defensive programming against our own validated model rather than a trust boundary check. If the Pydantic model permits a union type for \`branches\`, the correct fix is to narrow the schema or branch on a discriminator field, not to suppress R5. The rationale \('AST shift after refactor, pattern preserved'\) is a merge-bookkeeping note, not a substantive justification, and it does not engage with whether the isinstance is actually at a Tier-3 boundary. The decorator nudge does not apply because the subject \`n.branches\` is rooted in a validated Pydantic object, not in an external parameter at this function's signature. |
| `web/composer/tools/sessions.py:R5:_is_full_state_component_alias:fp=a583fa7b91f2f5b4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is essentially a changelog note about an AST shift after a refactor merge — it does not justify why isinstance\(\) is appropriate here. The function takes \`component: Any\` which appears to flow from external tool-call arguments \(Tier-3\), so R5 at a Tier-3 boundary is the kind of suppression the @trust_boundary decorator is designed to cover. The parameter is named \`component\`, and the isinstance subject is rooted at that parameter. The correct remediation is to apply @trust_boundary\(tier=3, source_param='component', suppresses=\('R5',\), ...\) on this function \(or on the enclosing boundary function that receives the tool arguments and passes \`component\` in\), not a per-line allowlist entry whose rationale doesn't even articulate the trust-tier reasoning. |
| `web/composer/tools/sessions.py:R1:_execute_get_pipeline_state:fp=53cdd7b125696d4a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`\`_execute_get_pipeline_state\`\` takes \`\`args: dict\[str, Any\]\`\` which is a Tier-3 external payload from the composer model's tool call, the finding is R1 \(suppressible by the decorator\), and the subject \`\`args.get\("component"\)\`\` is rooted directly at the external parameter \`\`args\`\`. All three decorator-test conditions are met, so the correct remediation is \`\`@trust_boundary\`\` on the function, not a per-line allowlist entry. The rationale itself is also weak — it only describes an AST shift from a merge and preserves an old pattern, which is not a site-specific justification. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=b19e372e1ba24e9a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is meaningless boilerplate — it cites a merge/AST shift and quotes a generic 'prompt_template' pattern that has nothing to do with this site \(which reads INTERPRETATION_REQUIREMENTS_KEY from state.source.options\). That references commit hashes and unrelated keys; it does not justify suppression here. The data is state.source.options, which is internal pipeline source configuration \(Tier 1/2\), not an external Tier-3 parameter — three lines above, the code does an \`in\` check on the same key, so direct subscript access is the correct offensive pattern. The decorator does not apply: options is not an external parameter to this function. Fix the code \(use direct subscript since presence is already asserted\) rather than allowlisting. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=9821fdd3b0a0e36a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a merge-bookkeeping note \('AST shift after ToolContext refactor', 'pattern preserved'\) and does not justify the suppression on its merits. The isinstance check at line 1152 operates on \`state.source.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\` — that is data read from ELSPETH's own source state object, not a function parameter carrying external Tier-3 input into _assert_affected_component. If \`options\` is meant to hold a list of requirement mappings, the schema should be trusted \(Tier-2: let it crash on type mismatch\); if it is genuinely Tier-3, the boundary belongs where options is populated, not buried in a tool helper. The decorator nudge does not apply because the subject is not rooted at an external parameter of this function. BLOCK; the agent must justify the shape-guard on policy grounds or fix the upstream typing. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=cddef38c47eddcee` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) indicating the suppression was carried forward during fingerprint rotation without a substantive justification. Per the Auditability Standard, a vague or self-referential rationale is a corrupted audit record and must be refused. Additionally, the suppressed isinstance check inspects values pulled from state.source.options — which is pipeline/configuration data, not a Tier-3 external parameter the function takes — so the @trust_boundary decorator is not the structural remedy here either. The agent must either write a site-specific rationale tied to the actual trust tier of state.source.options or fix the root cause \(tighten the schema of interpretation_requirements so the isinstance shape guard becomes unnecessary\). |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=a3aa2e4be368b676` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) indicating fingerprint rotation found no matching prior entry — it is not a justification at all. It explicitly asks the reviewer to determine whether this is new debt; that is the agent's job, not a basis for acceptance. Separately, the code accesses \`requirement.get\(...\)\` where \`requirement\` comes from \`state.source.options\` — a session/state object that is Tier-2 pipeline data \(or Tier-1 our-data if it's session state we own\), not Tier-3 external input rooted at a boundary function parameter. The function \`_assert_affected_component\` is not visibly an external-data trust boundary, so the decorator does not apply; the agent must either justify the suppression site-specifically or fix the root cause. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=afcde30a5ec360ea` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) indicating the agent has not actually justified this suppression at all. It explicitly flags that the entry may be new debt needing a source fix. Per the Auditability Standard, a vague/non-rationale is a corrupted audit record and must be rejected. Separately, the data here \(state.source.options requirements\) is pipeline/internal Tier-1/2 state, not Tier-3 external input arriving as a function parameter, so the @trust_boundary decorator is not the right structural fix either; the agent needs to either trust the schema and use direct access or properly validate at the actual external boundary upstream. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=d92cc8f903f46fb1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder fingerprint-rotation marker \('ALLOWLIST-FRESH — no stale entry matched...'\), not a site-specific justification. Per policy, a vague or boilerplate rationale is a corrupted audit record and must be refused. Additionally, the suppressed code is iterating over \`state.source.options\` requirements — Tier-2 pipeline state we control, not Tier-3 external input — so the \`.get\(\)\` calls on \`requirement\` Mappings are defensive programming on data whose shape we own; the fix is to define the requirement structure as a typed object and access fields directly, letting malformed entries crash. The decorator nudge does not apply: the subject is not rooted at an external parameter of this function. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=fcc8acd21c8d9c6b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) that explicitly asks the reviewer to determine whether this is new debt — it provides no site-specific justification. On the merits: the .get\(\) call is on a 'requirement' Mapping that came from state.source.options\[INTERPRETATION_REQUIREMENTS_KEY\], which is pipeline/source-options data, not a Tier-3 external payload at this function boundary. Even if the options originated externally, this function is not the boundary; the source plugin should have validated/normalized the requirement shape. The decorator nudge does not apply: _assert_affected_component does not take the external data as a parameter — 'requirement' is reached via state.source.options, not via a function parameter. The correct remediation is to fix the upstream schema \(typed requirement records\) or let the access surface the invariant break, not a per-line suppression. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=b3cc1f6e14caea35` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly notes this is potentially new debt needing review — it provides no site-specific justification at all. The code at line 1161 performs an isinstance check on draft_value extracted from state.source.options, which is pipeline/source-options data \(Tier-2 at best\), not data flowing directly from a function parameter that is an external boundary. Even if one argued the source.options structure is loosely typed, the rationale itself disclaims being a real justification, so it fails the audit-record standard regardless of the underlying merit. A placeholder rationale is by definition a corrupted audit record. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=42309d8e9c36b9ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder boilerplate \('ALLOWLIST-FRESH — review whether this is new debt'\) that explicitly admits it is not a site-specific justification. That alone fails the auditability standard: a future auditor reading this allowlist entry learns nothing about why the suppression is legitimate. Separately, the suppressed expression is \`node.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\` where \`node\` came from \`_find_node_or_raise\(state, affected_node_id\)\` — i.e. a node retrieved from internal pipeline state, not from the external tool-call arguments. That makes this Tier-1/Tier-2 data, and the defensive \`.get\(\)\` plus subsequent \`isinstance\` shape guards represent forbidden defensive programming on data we control. The decorator nudge does not apply because the subject is not rooted at an external parameter of this function. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=5e4977581a3e3e2f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) with no site-specific justification — it explicitly says review is needed, not that suppression is warranted. The R5 isinstance guard is operating on \`node.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\` where \`node\` comes from internal pipeline state \(Tier-2 data\), not from an external parameter of this function. If the options schema guarantees a list there, the isinstance check is defensive against a typed invariant and should be removed in favor of direct iteration; if the schema doesn't guarantee it, that's a Tier-2 contract problem to fix upstream. The decorator does not apply since the subject is not rooted in an external parameter of this function. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=23a8268fd0c90047` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder template \('ALLOWLIST-FRESH — no stale entry matched...'\) with no site-specific justification — it explicitly asks for review rather than providing one, which is an inadmissible audit record. The code operates on \`node.options\`, which is internal pipeline/session state \(Tier-2 at best, likely Tier-1 if it comes from our session store\), not Tier-3 external input from the tool-call \`arguments\` parameter, so the @trust_boundary decorator nudge does not apply here. The isinstance shape-guarding of \`requirement\` \(a Mapping inside our own node.options structure\) is exactly the defensive pattern policy forbids: if node.options\['interpretation_requirements'\] can contain non-Mapping entries, that's an invariant break to fix at the writer, not to guard at the reader. Agent must either fix the root cause \(type the requirements structure and let access crash\) or supply a real, site-specific rationale tied to a documented trust boundary. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=100a8d01f438c0e6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder fingerprint-rotation marker, not a site-specific justification — it explicitly says 'review whether this is new debt that needs a source fix'. That alone fails the auditability standard. On the merits: the code is iterating over \`node.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\` where \`node\` came from \`_find_node_or_raise\(state, ...\)\` — this is Tier-2 pipeline/session state, not Tier-3 external input. The \`.get\(\)\` calls on \`requirement\` Mappings inside node options are defensive reads on data we control; if the schema for interpretation requirements is malformed, that's an upstream bug and should crash, not be silently skipped. The decorator nudge does not apply: this is not a function-scoped Tier-3 boundary on an external parameter. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=d4327137d876f74a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder admitting it's a fresh fingerprint with no real justification \('review whether this is new debt'\). That is not a valid audit record. Additionally, the code operates on \`node.options\` — Tier-2 pipeline/state data read from our own session state, not an external boundary parameter. The \`.get\(\)\` and \`isinstance\` checks here are defensive guards against malformed internal state, which the policy forbids: trust the schema and let it crash, or validate at the source that populates \`node.options\`. The decorator does not apply \(data is not rooted at an external function parameter\). |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=2a12bf7ffce7858d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder template \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits it is not a site-specific justification and asks for review. That is not an audit-quality rationale; it is the absence of one. Furthermore, the excerpt shows operations on \`node.options\` and the \`requirements\` list within it — this is Tier-2 pipeline/config data \(node options from the composer's internal state\), not external Tier-3 input rooted at a function parameter, so the @trust_boundary decorator is not the right structural fix either. The agent must either fix the root cause \(trust the schema and access directly, or validate \`requirements\` shape once and crash on violation\) or supply a real per-site justification. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=87a636f73cd97447` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly flags this as unreviewed debt rather than a site-specific justification — that is not an acceptable audit record. The code accesses \`requirement.get\("draft"\)\` on a Mapping element pulled from \`node.options\`, which is internal pipeline state \(Tier 1/2\), not Tier-3 external input arriving as a function parameter. The decorator nudge does not apply: the subject is not rooted at an external parameter of \`_assert_affected_component\`, it is rooted at \`node.options\`. The remediation is a code fix \(trust the schema, access directly, or validate upstream\), not a per-line suppression. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=32dc09ac1df53ab3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH ... review whether this is new debt'\) and provides no site-specific justification — that alone fails the auditability standard. On the merits: the R5 isinstance check is on \`requirement.get\('draft'\)\` where \`requirement\` comes from \`node.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\`. \`node.options\` is pipeline/config data we control \(Tier 1/2\), not Tier-3 external input from a tool-call parameter, so the @trust_boundary decorator does not apply. If the schema guarantees draft is a str, access it directly and let it crash; if it legitimately may be absent or non-str by design, the type discipline should be enforced at the source/schema layer, not patched with a per-line isinstance guard. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=316d0487bb1d8b7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder fingerprint-rotation marker \('ALLOWLIST-FRESH'\), not a site-specific justification — that alone fails the auditability standard. On the merits: \`options\` comes from \`node.options\`, which is internal pipeline/composer state \(Tier 2 at best, not Tier 3 external input\), so the Tier-3 trust-boundary decorator does not apply. The R1 finding on \`options.get\("prompt_template"\)\` is defensive access on data we control; the correct fix is direct access \(\`options\["prompt_template"\]\`\) with an offensive check, or a typed accessor on the node — not a per-line suppression. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=fd42b91d6bc188f8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) and provides no site-specific justification at all — it explicitly asks the reviewer to determine whether this is new debt. That is a corrupted audit record by the Auditability Standard. Additionally, the isinstance check is on \`options.get\('prompt_template'\)\` where \`node.options\` is Tier-2 pipeline/session state \(a composer session node's options dict\), not an external Tier-3 parameter; the decorator nudge does not apply because the subject is not rooted at an external parameter of this function. The agent must supply a real rationale or fix the root cause. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=400e7fa8db4f1ead` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a merge/AST-shift note — 'pattern preserved' is not a policy justification. The function takes \`nodes\` as a parameter; if this is a standalone helper called from production with internal Tier-2 pipeline data \(node specs the system constructed\), then isinstance shape-guarding is defensive on data we control and should crash instead. If \`nodes\` is genuinely Tier-3 external data \(e.g. from a tool-call payload\), the structural remedy is \`@trust_boundary\(tier=3, source_param='nodes', suppresses=\('R5',\), ...\)\` on the enclosing function, not a per-line allowlist entry. R5 is in the decorator's suppressible set and the subject is rooted at the \`nodes\` parameter, so the decorator path applies if the boundary claim is real. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=edf7fab42da3610b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a meta-note about an AST shift after a refactor merge — it explains why the finding moved, not why suppression is legitimate at this site. The function operates on a \`nodes\` mapping whose values are untyped dicts \(Mapping\), and uses \`.get\(\)\` on those node dicts. This is Tier-2 pipeline data \(composer node specs\) being handled defensively because the structure is untyped — the correct fix is to use the typed variant \(\`_detect_unresolved_interpretation_placeholders_typed\` exists right below\) or to access fields directly and let malformed nodes crash. No external parameter boundary is established here, so the @trust_boundary decorator is not the right structural fix either. A 'pattern preserved across refactor' rationale is not a site-specific justification. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=bd3440d42f497594` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on a \`nodes\` mapping iterated as \`nodes.values\(\)\`, treating each \`node\` as a Mapping and calling \`.get\("options"\)\`. The excerpt does not show the function signature or any \`@trust_boundary\` decorator, and the rationale only says 'AST shift after refactor merge' — it does not establish what tier \`nodes\` is. If \`nodes\` is composer/pipeline configuration parsed from external input passed through this helper, the structural remediation is \`@trust_boundary\` on the enclosing function with \`source_param\` set to the nodes parameter, not a per-line allowlist entry. The rationale is purely a fingerprint-shift housekeeping note with no site-specific justification of why \`.get\(\)\` is correct here, which is an inadequate audit record under the Auditability Standard. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=d9b8b97d7f2fd4e7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on a \`nodes\` mapping that appears to be composition state internal to ELSPETH — not a freshly received Tier-3 external payload at a function boundary. The \`options\` value being type-guarded here is read out of an already-parsed node dict, and the function signature in the excerpt is not visible enough to confirm a Tier-3 external parameter; even if it were, R5 suppression via @trust_boundary requires the subject to be rooted at the decorated source_param, whereas here the subject is \`options\`, derived from \`node.get\("options"\)\` inside a loop. The rationale gives no Tier-3 justification — it only narrates an AST shift from a merge \('pattern preserved'\) which is not a substantive defense of the suppression. Per policy, an audit record reading 'AST shift after refactor merge' is exactly the corrupted-rationale shape the gate must reject; if the shape guard is legitimate the agent must articulate the trust tier of \`nodes\`/\`options\` and why a Mapping check at this site is a boundary rather than a defensive shape-test on Tier-2 pipeline data. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=ff5b63e0153dcdb3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on a \`nodes\` mapping which appears to be composition state \(sibling function takes \`Sequence\[NodeSpec\]\`, suggesting this is typed/semi-typed pipeline state, not raw external Tier-3 input\). The rationale is purely procedural \('AST shift after refactor merge'\) and does not justify why defensive \`.get\(\)\` plus \`isinstance\` guards are correct at this site — it only asserts the pattern was preserved. Per policy, 'pattern preserved across a refactor' is not a rationale; the judge must evaluate the suppression on its merits. The excerpt does not show a \`@trust_boundary\` decorator and \`nodes\` is not a parameter named as an external source on this function — the three-question decorator test fails on subject-rooting \(subject is \`options\` from \`node.get\(...\)\`, not a decorated external parameter\). The rationale provides no audit-grade explanation of the data tier or invariant; future auditors cannot interpret 'AST shift' as a justification. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=329fb69e5f766ce6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a non-justification: it merely notes that an AST shift occurred after a refactor and that the isinstance\(\) pattern was preserved. It does not explain why the suppression is legitimate at this site — i.e., why the function is operating on Tier-3 data, what the trust boundary is, or what invariant is being defended. Moreover, the data being inspected \(\`nodes\`, a parameter passed in\) appears to be composition state internal to the engine, and there is no visible decorator context or argument-flow analysis in the rationale. The R5 isinstance check on \`prompt_template\` derived from \`options.get\(...\)\` looks like defensive shape-guarding on internal pipeline data, which is forbidden under Tier-2 handling rules. The fix is to tighten upstream types \(NodeSpec / typed options\) so the isinstance becomes unnecessary, not to entrench it via allowlist. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders_typed:fp=b1ca3b580e951eeb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is an R5 isinstance check inside a function iterating over \`nodes\` — likely external tool-call data — making it a structural Tier-3 boundary candidate. The subject \`prompt_template\` is rooted at \`node.options\[...\]\` which flows from the \`nodes\` parameter. The rationale \('AST shift after refactor merge'\) is a churn-tracking note, not a substantive justification: it doesn't explain why this site warrants per-line suppression rather than the @trust_boundary decorator. The excerpt does not show the function signature or decorators, but the suppressible rule \(R5\) and the parameter-rooted data flow argue for the decorator path with source_param='nodes'. |
| `web/composer/tools/generation.py:R1:_schema_required_fields:fp=170da3b1e8b91a7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder boilerplate string \('ALLOWLIST-FRESH — no stale entry matched...'\) that does not justify the suppression at all — it explicitly asks the reviewer to determine whether this is new debt. That is not an audit-quality rationale. Separately, while the function does appear to validate an external/LLM-authored schema mapping \(plausibly Tier-3\), the correct remediation for an R1 finding on a parameter-rooted .get\(\) in a boundary helper is the @trust_boundary decorator, not a per-line allowlist entry. The subject schema.get\('required_fields'\) is rooted at the schema parameter, R1 is in the suppressible set, and the function performs Tier-3 structural validation. |
| `web/composer/tools/generation.py:R5:_schema_required_fields:fp=42a2049f51f41ad5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly acknowledges it is not a justification but a prompt to write one. That is a corrupted audit record by the Auditability Standard. Separately, although the function does validate external schema data \(Tier-3-ish config input\) and R5 is in the decorator-suppressible set, the subject \`raw\` is rooted at \`schema.get\(...\)\` where \`schema\` is the function parameter — so a structural \`@trust_boundary\(source_param='schema', suppresses=\('R5',\), ...\)\` is the correct remediation rather than a per-line allowlist entry. |
| `web/composer/tools/generation.py:R1:_execute_get_plugin_assistance:fp=be409e72085e2f59` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`args.get\("issue_code"\)\` inside \`_execute_get_plugin_assistance\`, where \`args\` is the LLM tool-call arguments dict — a Tier-3 external boundary. All three decorator-nudge conditions are met: the function takes external \`args\`, the rule is R1 \(in the suppressible set\), and the subject is rooted at the \`args\` parameter. The correct remediation is \`@trust_boundary\(tier=3, source_param='args', suppresses=\('R1',\), ...\)\` on the function, not a per-line allowlist entry. The rationale itself only describes an AST shift from a refactor and provides no site-specific justification. |
| `web/composer/tools/generation.py:R1:_execute_list_models:fp=abc6ef78029ec544` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding occurs in \`_execute_list_models\`, a tool handler whose \`args\` parameter is a Tier-3 LLM tool-call arguments dict. The finding is R1 on \`args.get\("provider"\)\`, and the subject is rooted directly at the external \`args\` parameter — all three conditions for the @trust_boundary decorator are met. The agent rationale merely describes an AST shift after a refactor merge and does not justify a per-line allowlist entry; the structural remediation is to apply @trust_boundary\(tier=3, source_param='args', suppresses=\('R1','R5'\), ...\) to the function and delete the allowlist entry. |
| `web/composer/tools/generation.py:R1:_execute_list_models:fp=1f07af64510ad515` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case for the @trust_boundary decorator, not a per-line allowlist. The function _execute_list_models takes external tool-call arguments \(args\), the rule is R1 \(silent .get\(\) on external data\), and the subject args.get\('limit', 50\) is rooted directly at the args parameter. All three decorator-nudge conditions are met. Additionally, the rationale itself is procedural \('AST shift after refactor merge'\) rather than a site-specific justification — it explains why the fingerprint moved, not why the suppression is legitimate. The correct remediation is @trust_boundary\(tier=3, source_param='args', suppresses=\('R1',\), ...\) on the function. |
| `web/composer/tools/generation.py:R5:_execute_list_models:fp=2d3a8b87d9ca51f3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance shape guard\) on \`limit\`, which is rooted in \`args\` — an external tool-call arguments dict \(Tier-3\). All three decorator conditions are met: the function takes external \`args\`, the rule is R5 \(in the suppressible set {R1,R5}\), and \`limit\` flows directly from \`args.get\('limit', 50\)\`. The correct remediation is \`@trust_boundary\(tier=3, source_param='args', suppresses=\('R1','R5'\), ...\)\` on the enclosing function, not a per-line allowlist entry. The rationale also offers no policy justification — it only describes an AST shift from a merge, which is not a reason to suppress. |
| `web/composer/tools/generation.py:R5:_execute_list_models:fp=9e1243b28dd8b86f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a classic Tier-3 boundary case: _execute_list_models takes \`args\` \(an external tool-call arguments dict\), the finding is R5 \(isinstance shape guard\), and the subject \`provider\` is rooted at \`args.get\("provider"\)\`. All three decorator-nudge conditions are met. The correct remediation is to apply @trust_boundary\(tier=3, source_param='args', suppresses=\('R1','R5'\), ...\) on the function rather than a per-line allowlist entry. The rationale also smells of copy-paste boilerplate \('AST shift after ToolContext refactor merge'\) rather than a site-specific justification. |
| `web/composer/tools/generation.py:R1:_source_schema_mode:fp=e0bc2bf3936637b4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`\`_source_schema_mode\`\` takes a \`\`SourceSpec\`\` parameter, not an external dict directly — \`\`source.options\`\` is an attribute access on a typed dataclass-like object. While the rationale claims the values are LLM-supplied \(Tier-3\), the function parameter itself is \`\`SourceSpec\`\`, so the data-flow from an external parameter is via an attribute chain rather than the parameter root. The @trust_boundary decorator's source_param mechanism suppresses on names rooted at the named parameter; \`\`source.options.get\(...\)\`\` is rooted at \`\`source\`\`, which qualifies. The correct remediation is @trust_boundary\(tier=3, source_param='source', suppresses=\('R1','R5'\), ...\) on this function rather than a per-line allowlist entry — this is exactly the structural pattern the decorator exists to handle. |
| `web/composer/tools/generation.py:R5:_source_schema_mode:fp=30e852fafe54278d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`\`_source_schema_mode\`\` does not take external data as a named parameter rooted at the suppressed expression — it takes a \`\`SourceSpec\`\` \(a typed object from our own code\) and reads \`\`source.options.get\('schema'\)\`\`. The \`\`options\`\` dict is Tier-2 pipeline configuration data assembled into a SourceSpec, not a Tier-3 LLM tool-call argument dict at this call site. The decorator nudge therefore doesn't apply \(no external param to name\), and the rationale's framing as 'LLM-authored schema' is not evidenced by the excerpt — the function signature shows a typed SourceSpec. If schema values inside SourceSpec.options can genuinely be arbitrary JSON, the proper fix is to type/validate them at the source boundary that constructs the SourceSpec, not to allowlist a shape guard at this read site. |
| `web/composer/tools/generation.py:R1:_source_schema_mode:fp=23513e8813c297f6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`\`_source_schema_mode\`\` takes a \`\`SourceSpec\`\` parameter, not raw external data; \`\`source.options\`\` is Tier-2 pipeline configuration data, and the \`\`schema\`\` Mapping was extracted from it on line 847. The rationale calls this 'Tier-3 LLM-authored', but by the time it reaches this helper it has already passed through SourceSpec construction — that's Tier-2 at best. Even granting a Tier-3 framing, the helper does not take the external payload as a parameter \(it takes \`\`SourceSpec\`\`\), so the @trust_boundary decorator is not the right structural fit either. The correct remediation is to validate schema shape at the actual Tier-3 boundary \(where the LLM tool-call arguments are parsed into SourceSpec\) and let this helper access \`\`schema\['mode'\]\`\` directly, treating absence as the upstream contract violation it is. |
| `web/composer/tools/generation.py:R5:_source_schema_mode:fp=a198571c1b9ad526` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes a SourceSpec \(a typed/Tier-2 object\), not an external parameter — the LLM-authored content lives inside source.options\['schema'\], several layers down. The @trust_boundary decorator's source_param mechanism keys on names rooted at a function parameter; 'source' is the parameter here but it is not itself Tier-3, and the decorator's R5 suppression would not naturally scope to schema.get\('mode'\). More importantly, R5 isinstance shape-guards on deeply-nested LLM-authored payloads inside an options bag suggests the Tier-3 boundary should live at the function that originally ingests the LLM tool-call arguments and normalizes them into SourceSpec, not at this downstream accessor. Allowing a per-line suppression here entrenches Tier-3 handling deep in helper code rather than at the actual external boundary. |
| `web/composer/tools/generation.py:R5:_sample_csv_rows:fp=20dc866451ce18cc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 CSV-row boundary \(csv.DictReader output of external file content\), and R5 is in the decorator's suppressible set. The isinstance\(key, str\) check operates on keys derived from the function parameter \`\`content\`\`, which is the external bytes payload being parsed. All three decorator-eligibility conditions are met: external parameter \(content\), suppressible rule \(R5\), and data-flow from content through csv.DictReader into the comprehension. The correct remediation is @trust_boundary\(tier=3, source_param='content', suppresses=\('R5',\), ...\) on _sample_csv_rows, not a per-line allowlist entry. |
| `web/composer/tools/generation.py:R1:_value_transform_preserves_field:fp=e2665599a03a84d5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case for the @trust_boundary decorator. The function takes a NodeSpec parameter whose .options is LLM-supplied \(Tier-3\) data; the finding is R1 \(in the suppressible set\); and the subject node.options.get\("operations"\) is rooted at the node parameter. The correct remediation is to decorate _value_transform_preserves_field with @trust_boundary\(tier=3, source='LLM-authored NodeSpec.options', source_param='node', suppresses=\('R1',\), invariant='returns False on any non-field-preserving or malformed operations payload', ...\) rather than a per-line allowlist entry. |
| `web/composer/tools/generation.py:R5:_value_transform_preserves_field:fp=7236ffc455b6c7be` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes a NodeSpec \(an internal pipeline data structure\), not an external parameter. The data being inspected — node.options\['operations'\] — is LLM-authored composer state that flows through internal structures, but the function's parameter is the internal NodeSpec, not the raw external payload. The decorator's source_param mechanism keys off a parameter name, and there is no external parameter here to root the suppression at. Moreover, R5 isinstance checks on internal NodeSpec.options suggest the schema/typing of NodeSpec.options is too loose — the proper fix is to type 'operations' precisely at the NodeSpec validation boundary \(where the LLM output first becomes a NodeSpec\) and let downstream code trust the type. This is Tier-2 data being treated as Tier-3 mid-pipeline. |
| `web/composer/tools/generation.py:R5:_value_transform_preserves_field:fp=aa5808f615d3af9a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes a NodeSpec \(Tier-2 pipeline data\), not external/Tier-3 data. While node.options may originate from LLM authorship upstream, by the time it reaches this helper it is internal composition state being inspected by analysis code we control. More importantly, the @trust_boundary decorator's source_param must name a function parameter that directly carries Tier-3 data; here the parameter is 'node', and the isinstance check is on an element of node.options\['operations'\], not rooted at an external parameter. The R5 isinstance shape-guard on a list element from a nested options dict is a defensive shape probe that should either be replaced by trusting the upstream schema \(let it crash if operations contains non-mappings\) or, if this truly is a boundary, the boundary needs to be drawn at the point where LLM output enters the system, not buried inside an analysis predicate. |
| `web/composer/tools/generation.py:R1:_value_transform_preserves_field:fp=c1946cc969215bd2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _value_transform_preserves_field takes a NodeSpec \(Tier-2 pipeline configuration data\), not a Tier-3 external parameter. The operation.get\("target"\) access is on data extracted from node.options, which is system-managed composition state, not LLM-authored input at this boundary. Even if the composer model authored upstream, by the time it reaches a NodeSpec it has passed source validation and is Tier-2; transforms must not defensively .get\(\) Tier-2 data. The decorator nudge also doesn't apply because the subject is not rooted at an external function parameter — it's rooted at node.options. The correct fix is direct access or schema enforcement at the NodeSpec boundary. |
| `web/composer/tools/generation.py:R1:_numeric_aggregation_diagnostics_for_observed_csv:fp=9c1695c48f0efa04` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) is on 'options', which is the return value of get_aggregation_contract_options\(node.options, ...\) — a helper operating on pipeline state.nodes data, not on raw LLM-authored input at this call site. Even if the contract options ultimately originate from an LLM, by the time this function consumes them they have passed through a contract-options helper and are Tier-2 pipeline data; the rationale's 'Tier-3 read' framing misapplies the trust tier. Additionally, the enclosing function does not take the external payload as a parameter — it takes structured 'state', 'inferred_types', 'observed_headers' — so the @trust_boundary decorator with source_param is not the right structural fix either. The correct remediation is to either have get_aggregation_contract_options return a typed object with a typed optional value_field attribute \(access directly, let absence be None\), or validate at the actual Tier-3 boundary where the LLM contract is first parsed. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=78003676a01b62e7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale's tier claim is internally contradictory and ultimately wrong. The inline code comment states source.options is Tier-1 Mapping\[str, Any\], but the agent's rationale calls this a Tier-3 walk of LLM-authored data. Either way, R1 \(.get on external data\) is being invoked on something the code itself identifies as a structured config object, not Tier-3 input from an external boundary. If 'schema' is an optional config key on a typed options mapping, the correct pattern is direct access with a typed accessor or an explicit absence check — not a per-line R1 suppression. If source.options truly is LLM-authored Tier-3 data, the remediation is a @trust_boundary decorator on the enclosing compute_proof_diagnostics function with source_param pointing at the external parameter, not a one-off allowlist entry. I cannot see the function signature in the excerpt to confirm a parameter root, so I am not emitting a decorator nudge. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=f0e165ac927832cd` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims the schema Mapping is Tier-3 LLM-authored data, but the excerpt shows \`\`schema = source.options.get\('schema'\)\`\` where the surrounding comment explicitly states \`\`source.options is Tier-1\`\`. A \`\`Mapping\`\` extracted from Tier-1 \`\`source.options\`\` is system-owned configuration, not Tier-3 external data — the tier claim is the inverted from what the code shows. Per policy, defensive \`\`.get\(\)\`\` on Tier-1/Tier-2 data is forbidden; if \`\`mode\`\` is absent, the comparison naturally yields False without needing suppression, and if the schema dict shape is wrong that's a config/plugin bug that should surface. The decorator nudge does not apply because \`\`schema\`\` is not rooted in an external function parameter. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=65bfa9c0c7279e67` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly asks for review rather than justifying the suppression. It contains no site-specific reasoning about why .get\(\) on source.options is legitimate here. While the surrounding comment suggests source.options is treated as Tier-1 with an unstructured 'schema' value, the proposed entry itself provides no defensible audit record. The agent must write a real rationale tying this site to policy \(or fix the root cause\), not land a self-described placeholder. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=fd05b8a30100c68c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly inviting review, not a justification. It provides no site-specific argument for why R6 should be suppressed here. Per the Auditability Standard, an allowlist entry with a non-rationale is a corrupted audit record and must be refused. The agent must either supply a real justification tied to the code at this site or remediate the finding directly. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=0939dce5f06ef7cb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is a placeholder admitting this is a fresh finding with no real justification \('ALLOWLIST-FRESH — review whether this is new debt'\). That is not a legitimate suppression rationale; it is a deferral. The code shows a try/except ValueError around derive_required_header_mismatch_risk that converts the exception into a diagnostic — whether this is appropriate trust-boundary handling or defensive swallowing of a system-internal bug requires a site-specific argument the agent has not provided. Per the Auditability Standard, an allowlist entry is itself an audit record and this rationale would be uninterpretable to a future auditor. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=2d23380aa7ea7a24` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) rather than a site-specific justification. It explicitly says 'review whether this is new debt that needs a source fix or a more specific justification' — that is a request for review, not a defensible audit record. The finding is \`schema.get\("mode"\) == "fixed"\` where \`schema\` appears to be pipeline/config data inside the composer, not a clearly-rooted Tier-3 external parameter from the visible excerpt; without evidence of a trust-boundary function this looks like ordinary defensive \`.get\(\)\` on data we control. A future auditor reading 'ALLOWLIST-FRESH' would have no idea why the suppression was granted. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=2dadfe65b888003f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly flags this as potentially new debt needing review, not a substantive justification. Per the Auditability Standard, an allowlist entry is an audit record and a placeholder rationale is a corrupted audit record that future auditors cannot interpret. The code shows a try/except ValueError around derive_extra_column_risk that converts the exception into a diagnostic — this may well be legitimate \(converting an internal validation failure into a structured diagnostic\), but the agent must articulate that reasoning site-specifically. The finding is R6 \(not R1/R5\), and the suppressed expression is not rooted at an external function parameter, so the @trust_boundary decorator is not applicable. |
| `web/composer/tools/transforms.py:R5:_execute_upsert_node:fp=28b82b3b788af6e4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a churn-bookkeeping note \('AST shift after refactor merge'\) and does not justify why an isinstance\(\) shape guard is correct at this site. The subject is \`validated.branches\`, which is a field on an already-validated object \(Tier-2 pipeline data\), not a parameter rooted at an external Tier-3 input — so the @trust_boundary decorator path does not apply, and the R5 suppression on a validated-model attribute looks like defensive shape-sniffing on data whose type the validator should already have pinned down. The correct remediation is to make the validated model's \`branches\` field have a single concrete type \(Mapping or sequence, not a union requiring runtime discrimination\), so the isinstance branch disappears. A merge-conflict rationale is not a substitute for a site-specific trust-tier argument. |
| `web/composer/tools/transforms.py:R1:_execute_upsert_edge:fp=8e25103e55154751` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` call is on \`routes\`, a dict constructed locally from \`node.routes\` — Tier-2 pipeline/state data we own, not Tier-3 external input. The rationale only describes an AST-shift after a refactor merge \('pattern preserved'\), which is not a substantive justification under the Auditability Standard; 'we used to suppress this' is not a reason. The decorator nudge does not apply because the subject is not rooted at an external parameter \(it's derived from \`node.routes\`, internal state\). The correct fix is to use \`routes.get\(route_key\)\` only if it's semantically correct against a typed schema — and since \`routes\` is a freshly built dict where \`route_key\` may legitimately be absent \(no edge yet set\), this is a membership/lookup question that should be written as \`routes.get\(route_key\)\` only with explicit intent expressed in code, not suppressed by allowlist. Resolve in code \(e.g., explicit \`route_key not in routes or routes\[route_key\] != to_node\`\), not via allowlist entry. |
| `web/execution/service.py:R5:ExecutionServiceImpl:set_openrouter_catalog_snapshot:fp=f622220671ecdcda` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly admitting it is not a site-specific justification but a request for review. That is not an acceptable audit record. On the merits: the function is an internal API called once from the lifespan with values produced by code we control \(read_openrouter_catalog_snapshot_id\), so the sha256/source arguments are not Tier-3 external data — they are Tier-1/2 internal invariants. The isinstance check is offensive validation of an internal contract that the docstring says 'never returns None', so the correct fix is to delete the isinstance guard \(the type annotation and producer invariant already guarantee str\) or keep it without suppression. The decorator is not appropriate \(not Tier-3, no external source_param\). |
| `web/execution/service.py:R8:ExecutionServiceImpl:execute:fp=ae8955d9fc074acc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is essentially a fingerprint-shift note \('Fingerprint shifted from ... by the F-17/F-21 pre-execute placeholder gate ImportFroms'\) with a one-line description of what the code does. It does not explain why rule R8 should be suppressed at this site, what tier of data is involved, or why the offensive/defensive decision test favours suppression. A bare 'this is what the code does' is not a legitimate audit record — future auditors cannot reconstruct the policy reasoning. Fingerprint churn is not a justification; if the rule fires on lazy lock creation, the agent must articulate the policy basis \(or fix the code\), not re-stamp the allowlist. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=a91add5d5970814d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) is being called on composition_state.source.options, which is pipeline configuration that has been materialized and validated upstream — this is Tier-2 data, not Tier-3 external input at a boundary. The rationale 'optional keys in user-authored config' conflates user-authored config \(which is validated at parse time\) with raw external untrusted input. The correct pattern here is either direct access if the schema guarantees the key, or explicit membership check; but a per-line R1 suppression on Tier-2 config access is not justified. The decorator nudge does not apply: _execute_locked does not take the external source data as a parameter \(the options dict reaches it via self/state, not as a function argument\). |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=f6eeabcecd608c77` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\("blob_ref"\) is on composition_state.source.options — this is user-authored configuration data flowing through the pipeline, but the finding is inside ExecutionServiceImpl._execute_locked, which appears to be an execution-service method, not a Tier-3 source-parameter trust boundary. The rationale 'optional keys in user-authored config' describes a Tier-2/config-shape concern; if options is a typed structure, the access should be direct \(options\['blob_ref'\] or options.blob_ref\) and missing keys handled by schema, not silent .get\(\). If options is genuinely an untrusted external dict at this boundary, the structural fix is @trust_boundary on the enclosing function with source_param pointing to the external parameter — but composition_state is not a direct function parameter visible here, so a decorator nudge isn't clearly applicable. The rationale also volunteers a fingerprint-drift explanation, which is metadata, not justification. Agent should either type the options structure or restructure the boundary. |
| `web/execution/service.py:R6:ExecutionServiceImpl:validate_state:_blob_get_metadata:fp=07f7b1d9599f9e03` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder fingerprint-rotation note \('ALLOWLIST-FRESH'\) that explicitly says 'review whether this is new debt' — it provides no site-specific justification. R6 is not in the decorator's suppressible set \({R1, R5}\), and the code shows a try/except around an internal blob-service call \(Tier-1/2 system code, not a Tier-3 external boundary\). Catching BlobNotFoundError to return None on an internal service lookup may be a legitimate pattern, but the agent must articulate why absence-as-None is the correct contract here rather than letting the caller handle the exception. A non-rationale cannot become an audit record. |
| `web/execution/service.py:R1:ExecutionServiceImpl:cancel:fp=3195518183f4604c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is \`self._shutdown_events.get\(str\(run_id\)\)\` on a process-local dict that the service itself populates. This is not Tier-3 external data — it's internal state, and \`.get\(\)\` here is used as a presence check \(\`if event is not None\`\). The agent's rationale doesn't address the policy at all: it just describes a fingerprint change and restates what the code does. The decorator does not apply \(no external parameter; \`run_id\` is a UUID, not the suppressed subject's root\). The correct remediation is a code change — e.g., use \`in\`/\`KeyError\`-based access or restructure — not a per-line allowlist entry. |
| `web/execution/service.py:R4:ExecutionServiceImpl:_run_pipeline:fp=d291d0da191e7e8e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-1 audit-DB read \(load_top_failure_samples on landscape_db\). Per the Auditability Standard and the Data Manifesto, Tier-1 anomalies must crash — 'bad data in the audit trail = crash immediately.' The rationale inverts this: it argues that swallowing audit-DB exceptions is preferable because run-status persistence matters more, and points to slog.warning as the compensating channel. But the logging primacy order explicitly rejects slog as the channel for pipeline-relevant events, and 'we log it anyway' is called out in policy as a misunderstanding. If the audit DB raises on malformed JSON, that is a catastrophic invariant break, not an enrichment inconvenience to be degraded around. The decorator nudge does not apply \(not R1/R5, no external parameter\). Remediation is structural: either the enrichment read must not be able to fail \(narrow query, typed schema enforced\) or the failure must surface — not a broad except Exception with a logger fallback. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_finalize_output_blobs:fp=76aac7aeed5d2790` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is too cryptic to serve as an audit record — 'failure must not mask run outcome' gestures at intent but doesn't justify catching a broad exception class and routing it solely to slog.error. Per the Telemetry/Logging primacy order, pipeline-relevant failures \(blob finalization is pipeline state\) belong in the audit trail \(Landscape\), not slog as the sole channel. Additionally, blob finalization operates on our own data \(run output blobs\), which is Tier-1-adjacent infrastructure, not a Tier-3 external boundary — the rationale doesn't establish why a broad except is appropriate here rather than letting it crash or recording it to the audit trail. The fingerprint-shift note explains why a prior entry no longer matches but is not itself a justification for the suppression at this site. |
| `web/composer/service.py:R1:_last_mutation_was_pending_proposal:fp=b8c97a159c375db2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is non-substantive — it merely describes an AST shift from a merge and preserves an old pattern, without justifying why \`.get\(\)\` is correct at this site. The data flows from \`invocation.result_canonical\`, which is a JSON string stored in our own audit/state model — that is Tier-1/Tier-2 data we control, not Tier-3 external input, so R1's defensive \`.get\(\)\` is not justified by a trust boundary. If the payload schema is \`{data: {status: ...}}\`, direct subscript access \(\`payload\["data"\]\`\) is the offensive-programming choice; a missing key indicates an upstream bug, not a recoverable condition. The decorator nudge does not apply: this function takes no external parameter, and \`payload\` is derived from internally-canonicalised tool-invocation results. |
| `web/composer/service.py:R1:_last_mutation_was_pending_proposal:fp=2a6ac3e449ff4daa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is non-substantive — it merely notes an AST shift after a merge and asserts 'pattern preserved'. That's a changelog note, not a justification. The code reads payload from json.loads of a tool invocation's result_canonical; this is internally-produced data \(a result canonicalized by our own composer tool pipeline\), not Tier-3 external data, so the .get\('status'\) on data is operating on Tier-2 pipeline data. Per policy, transforms on Tier-2 data should not use defensive .get\(\) — if the contract says result_canonical for these tools carries a dict with 'status', trust the schema. The remediation is a code fix \(direct access, or assert the shape\), not an allowlist entry. The decorator nudge does not apply: the function parameter is a tuple of ComposerToolInvocation, not an external dict, and the subject is rooted in json.loads output, not a decorated source_param. |
| `web/composer/service.py:R5:ComposerServiceImpl:_serialize_response_via_walker:fp=cd655256ca15f7bb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) explicitly asking for review, not a justification. On the merits: this is an R5 isinstance check on a value derived from internal outcome objects \(Tier-2 pipeline data\), not an external Tier-3 boundary. The function does not take an external parameter; outcome.response is produced by our own tool-execution path. If the type discipline matters, encode it in the type system or assert offensively — don't suppress. The decorator nudge does not apply because the subject is not rooted at an external function parameter. |
| `web/composer/service.py:R1:ComposerServiceImpl:_cached_runtime_preflight:fp=4ba60774ae10f0f5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged expression is \`cache.get\(key\)\` where \`cache\` is a \`_RuntimePreflightCache\` passed in by internal code — this is Tier-1/Tier-2 system data, not Tier-3 external input. The \`.get\(\)\` is a normal dict-lookup-with-absence-semantics \(returns None when uncached\), not defensive suppression of malformed external data. The rationale doesn't engage with the trust tier at all; it just notes an AST shift after a refactor and describes the pattern, which is not a policy-grounded justification. If R1 is firing on legitimate cache-miss semantics, the fix is to narrow the rule or use a typed cache API, not to add an allowlist entry with a merge-history rationale. |
| `web/composer/tool_batch.py:R5:run_tool_batch:fp=9a7b25e2fc96514d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on decoded_arguments inside run_tool_batch, which processes LLM tool-call arguments — a Tier-3 external-data boundary. The finding's subject \(decoded_arguments\) appears rooted in a value derived from external LLM output, and R5 is in the decorator's suppressible set. The correct remediation is to apply @trust_boundary\(tier=3, ...\) to run_tool_batch with source_param pointing at the external parameter that carries the decoded arguments \(or restructure so the decoded value is the named source_param\), rather than a per-line allowlist entry. I cannot see the function signature in the excerpt, so I'm naming the most likely parameter; the agent should pick the actual external parameter name when applying the decorator. |
| `web/composer/service.py:R2:ComposerServiceImpl:_call_text_llm:fp=9475aed316c9be0b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`getattr\(exc, 'status_code', None\)\` where \`exc\` is a \`LiteLLMBadRequestError\` — an exception from an external library \(litellm\). This is exactly the plugin-return-value / external-library-interface case the policy addresses: if LiteLLMBadRequestError's interface doesn't guarantee \`status_code\`, the correct fix is to check the library's actual exception contract and access it directly \(let it AttributeError if absent — that's a real interface violation we want surfaced\), or use a typed accessor. The rationale describes the mechanical AST-shift context but does not justify why fabricating \`None\` for a missing status_code is meaning-preserving rather than masking an interface change. The @trust_boundary decorator does not apply: R2 is not in the suppressible set {R1, R5}, and \`exc\` is not an external-data parameter rooted at a function arg. |
| `web/composer/redaction.py:R5:_coerce_stringified_json_object:fp=4f5bfe819cd1532d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale text 'ALLOWLIST-FRESH — no stale entry matched...' is a placeholder/meta-comment about fingerprint rotation, not a site-specific justification for the suppression. Policy requires allowlist entries to be audit records future reviewers can interpret; this entry would be a corrupted audit record. Additionally, this is an R5 \(isinstance\) finding inside a Pydantic BeforeValidator function operating on LLM-supplied data — a Tier-3 boundary case where the structural remedy is the @trust_boundary decorator on the validator function \(with source_param='value', suppresses=\('R5',\)\) rather than a per-line YAML entry. The agent must either supply a real rationale tied to this site or apply the decorator. |
| `web/composer/redaction.py:R5:_coerce_stringified_json_object:fp=22c2fc298648b291` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is a placeholder \('ALLOWLIST-FRESH — review whether this is new debt'\) that explicitly admits no site-specific justification has been provided. That is not a valid audit record. Separately, while this function does coerce LLM-supplied \(Tier-3\) data and the rule is R5, the function's parameter is named \`value\`, not a clearly external source name, and the function is a pydantic BeforeValidator helper rather than a tool-call boundary handler — the @trust_boundary decorator nudge is not a clean fit, and the excerpt does not show the function signature/decorators in full. The agent must either write a real rationale tying the isinstance check to the Tier-3 coercion contract or restructure. |
| `web/composer/redaction.py:R1:redact_source_storage_path:fp=2d2e6b24c7aafd25` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function receives a serialized state dict produced by our own code \(Tier 1/2 — output of to_dict\(\) or component dicts from _execute_get_pipeline_state\), not external Tier-3 data. The rationale admits this: it's handling 'multiple dict shapes' from internal callers. The correct fix is to make the function's contract explicit \(e.g., accept only the well-defined shape, or split into two functions for the two shapes\) so direct subscript access either succeeds or crashes loudly on an interface violation. Suppressing R1 here entrenches polymorphic-dict-shape handling rather than fixing the upstream callers to pass a consistent contract. The @trust_boundary decorator does not apply: state_dict is not external input. |
| `web/composer/redaction.py:R1:redact_source_storage_path:fp=f98ce159ca5d32fa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is sourced from CompositionStateRecord.source \(a deep_thaw'd JSON blob from our own DB / Landscape-adjacent state\). Per the Three-Tier Trust Model, data we wrote and stored is Tier-1 \(or at best Tier-2 pipeline data once deserialized\) — not Tier-3 external input. The rationale conflates 'raw JSON from the DB' with external untrusted data; deserialization does not change trust tier. If 'options' is a documented part of the source contract, missing it should crash; if it is genuinely optional per schema, the function should accept a typed model and branch on a documented optional field rather than .get\(\) a dict. The @trust_boundary decorator does not apply: the parameter is not external input and R1 here is not at a Tier-3 boundary. |
| `web/sessions/service.py:R6:SessionServiceImpl:persist_compose_turn:fp=1708f112cf7702e2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on a try/except RuntimeError block that detects whether an event loop is running — this is offensive programming used to enforce an invariant \(must not be called from a coroutine\), not a defensive Tier-3 boundary pattern. The agent's rationale about 'session persistence boundary' and 'optional envelope, fork, archive, and lock state' is generic boilerplate that does not describe what is happening at this specific line. The excerpt shows no Tier-3 external data here; it shows asyncio loop detection. The rationale fails the audit-record test: a future auditor reading 'session persistence boundary' against an asyncio.get_running_loop\(\) guard would be unable to reconstruct why R6 was suppressed. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=bd8888c405f0f2b5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppressed expression is \`options.get\("model"\)\`, where \`options\` is the node's stored configuration — Tier-2 pipeline/system state, not Tier-3 LLM input. The rationale conflates the LLM-supplied \`llm_draft\` \(which is genuinely Tier-3\) with \`options\`, which is our own node configuration. Node options should conform to a schema; if \`options\['model'\]\` is missing for an LLM_MODEL_CHOICE node, that is an invariant violation in our own state and should crash via KeyError, not be silently coerced to None and re-validated. Additionally, the excerpt does not show the function signature/decorators, but \`options\` is not the external parameter here \(\`llm_draft\`/\`arguments\` would be\), so the @trust_boundary decorator is not the right structural fix for this specific access. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=d096c7dfd60bc4c9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is operating on \`current_model = options.get\('model'\)\`, where \`options\` is the node's own configuration data from CompositionState — not the Tier-3 LLM tool argument \`llm_draft\`. Node options are Tier-1/Tier-2 internal state we control; if a node declared as LLM_MODEL_CHOICE doesn't have a string \`options.model\`, that's an upstream invariant break \(bug in our code or schema\) and should crash, not be defensively narrowed. The rationale misclassifies the data tier: \`llm_draft\` is the Tier-3 input, but the isinstance is guarding \`options\['model'\]\`. The decorator nudge doesn't apply because the subject is not rooted at an external parameter — it's rooted at \`options\`, which comes from internal node state. Fix the root cause by trusting the schema invariant for InterpretationKind.LLM_MODEL_CHOICE nodes. |

## PRE_JUDGE_FRESH_ACCEPT (245)

| Entry | Original Verdict | Fresh Verdict | Fresh Model | Cause | Notes |
| --- | --- | --- | --- | --- | --- |
| `__init__.py:R6:_module_:fp=1669d46742317730` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate package-metadata boundary at module import time. PackageNotFoundError from importlib.metadata is a narrow, specific exception \(not a broad swallow\), and the fallback value 'UNKNOWN-dev-uninstalled' is honest sentinel marking, not fabrication — it explicitly records the dev-uninstalled state in any audit trail rather than inventing a fake version string. Crashing on import in editable-but-not-installed dev environments would prevent the package from loading at all, and production installs always have metadata so the except branch is dev-only. The rule_id is R6 \(not R1/R5\), so the @trust_boundary decorator is not applicable. |
| `composer_mcp/server.py:R6:create_server:call_tool:fp=bbe2757f46dc0db3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 trust boundary: canonical_json/stable_hash operate on LLM-supplied tool-call arguments, and rfc8785 canonicalization legitimately raises ValueError on non-finite floats and TypeError on non-serializable types. The except clause does not silently swallow — it captures the exception, builds an explicit forensic sentinel via a shared helper, recomputes canonical+hash on the sentinel, and records the failure in canonicalization_failed for the downstream ARG_ERROR audit path. This satisfies audit primacy \(row always lands\) without fabrication: the sentinel is explicitly marked as a canonicalization failure, not synthesized 'real' arguments. The decorator nudge does not apply: R6 is not in the decorator's suppressible set {R1, R5}, and the pattern is exception recovery at a boundary, not subscript/get access. |
| `composer_mcp/server.py:R6:create_server:call_tool:fp=98007a473ebfd71b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate trust-boundary recovery at the audit-serialization layer, not defensive programming on Tier-2 data. The try/except wraps canonical_json/stable_hash in a finally-block that computes the audit row on the success path; without it, a non-finite float or non-serializable type in result_dict would raise from finally and mask the LLM's success return. The recovery preserves the audit row via a shared sentinel \(build_canonicalization_sentinel\), which is the project's documented discipline for canonicalization failures, rather than swallowing the error silently. R6 \(broad exception handling\) is appropriate here because the alternative is losing both the success response to the caller and the audit record — exactly the auditability failure the policy forbids. |
| `contracts/url.py:R6:SanitizedDatabaseUrl:from_raw_url:fp=a8a9139e2ff09e37` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of try/except as control flow at a configuration boundary: get_fingerprint_key\(\) raises ValueError specifically to signal 'no key configured', and the code explicitly branches on that signal to distinguish dev mode from production. The exception is narrowly scoped \(ValueError only\), not swallowed \(have_key drives a subsequent explicit branch that either fingerprints, raises SecretFingerprintError, or intentionally proceeds in dev mode\), and the behavior is documented in the docstring. This is offensive programming around an environment-configuration check, not defensive suppression of a bug. |
| `contracts/url.py:R6:SanitizedWebhookUrl:from_raw_url:fp=ec8dd835c4c43c96` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is offensive programming, not defensive: the code calls get_fingerprint_key\(\) specifically to probe whether a key is configured, and the ValueError branch is an explicit control-flow signal distinguishing dev mode \(no key configured\) from production \(where fail_if_no_key triggers a SecretFingerprintError below\). The exception is not being swallowed silently — have_key=False is set and the subsequent branch either fingerprints or raises a meaningful, chained error. This is the canonical 'detect invalid state and act meaningfully' pattern at a configuration boundary, not error suppression. The rationale accurately describes the two-mode branch shown in the excerpt. |
| `contracts/data.py:R6:_get_allow_inf_nan:fp=a073efa3e28410f3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is introspection over Pydantic's FieldInfo.metadata, which is a heterogeneous list whose element types are determined by the Pydantic library — not external/user data and not a plugin return value. vars\(item\) legitimately raises TypeError on items without __dict__ \(e.g., plain strings\), and the only correct response is to skip them and continue scanning for the metadata object that carries allow_inf_nan. This is an iteration/type-shape concern over a third-party library's documented heterogeneous structure, not defensive coercion of Tier-2/Tier-3 data, and there is no fabrication: absence still returns None. The suppression is narrow and the rationale is site-specific. |
| `contracts/transform_contract.py:R2:_get_python_type:fp=24df5cf5593bf5fa` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The getattr here is on a Python type-annotation object \(e.g. list\[str\], dict\[str, Any\]\) used solely to construct an error message before raising TypeError. Generic alias objects legitimately may lack __name__, and falling back to str\(unwrapped\) is meaning-preserving for error reporting — not fabrication of data. This is offensive programming: the code is raising a meaningful exception about an unsupported annotation, and the getattr exists to make that crash maximally informative. No trust-tier or layer concern applies; the subject is a developer-supplied type annotation, not Tier-2/3 row data. |
| `contracts/transform_contract.py:R2:_get_python_type:fp=37ef1f2fbd33537f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is getattr on Python type-annotation objects, not on Tier-2/Tier-3 pipeline data. Generic aliases like list\[str\] and Union members genuinely lack __name__ while plain classes have it — this is a structural feature of the typing system, not defensive programming against missing data. The getattr is used purely to construct a human-readable error message in a TypeError that is itself being raised \(offensive programming about schema misuse\). The fallback to str\(a\) is meaning-preserving for display purposes, not fabrication of a domain value. |
| `contracts/schema_contract.py:R5:PipelineRow:to_dict:fp=19c383f767c4ed8c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an offensive programming guard, not defensive suppression. The isinstance check is paired with an immediate TypeError raise that crashes with a meaningful message if deep_thaw violates its return-type contract on Tier-1 internal data \(PipelineRow._data\). This matches the 'detect invalid state and throw a meaningful error' pattern from the Decision Test, and aligns with Tier-1 crash-on-anomaly handling. R5 typically flags defensive shape guards, but here the isinstance precedes a raise, not a silent fallback. |
| `contracts/schema_contract.py:R5:PipelineRow:to_checkpoint_format:fp=13c2f8a3bfbe5f2a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is offensive programming, not defensive. The isinstance check immediately raises TypeError on type violation rather than suppressing it — it's an invariant assertion on the return type of deep_thaw, which operates on Tier-1 data \(PipelineRow._data destined for checkpoint storage\). The Decision Test points to 'detect invalid state and throw meaningful error' \(offensive, encouraged\). The code excerpt confirms the isinstance is paired with an immediate raise, matching the offensive-guard pattern the policy endorses. |
| `contracts/contract_propagation.py:R1:merge_contract_with_output:fp=b77bb8f4e1cd3d1d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` here is not defensive error suppression — it's a legitimate dictionary lookup with an explicit, meaningful fallback. \`input_originals\` is a lookup table built one line above from the input contract's fields; the code is asking 'does this output field correspond to a known input field?' and falling back to the output field's own original_name when it doesn't \(e.g., a new field introduced by the transform\). Both branches are intentional semantic outcomes, not absence-hiding. The two-argument dict.get pattern for lookup-with-default on a code-controlled dict is idiomatic and outside the defensive-programming prohibition, which targets silent absence-coercion on data fields. |
| `contracts/tier_registry.py:R5:tier_1_error:fp=360d94dd51a67c28` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance\(reason, str\) check is at a decorator API boundary validating caller-supplied kwargs before they're stored in the Tier-1 errors registry. This is offensive programming — detecting an invalid state \(non-string reason\) and raising a meaningful ValueError, exactly the pattern the policy encourages. The check is not swallowing errors with a default; it explicitly raises. The decorator nudge does not apply: R5 is in the suppressible set, but the function does not take external Tier-3 data — it takes decorator kwargs from system code \(other modules using @tier_1_error\), and the validation is the contract enforcement itself, not a Tier-3 shape guard. |
| `contracts/tier_registry.py:R5:tier_1_error:fp=b6d17d7d22d3b69b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an offensive-programming guard at a decorator API boundary: the isinstance check validates a caller-supplied kwarg \(caller_module\) that feeds into a security-relevant module-prefix allowlist, and the function raises ValueError on malformed input rather than silently accepting it. The pattern matches the policy's encouraged use of 'proactively detect invalid states and throw meaningful exceptions' — R5 \(isinstance shape guard\) here is exactly the type-validation form, not a defensive shape-narrowing that swallows errors. The @trust_boundary decorator does not apply: caller_module is not Tier-3 external data, it is a developer-supplied decorator argument, and the guard is an assertion that crashes rather than coerces. Rationale is specific to the site and references the security implication \(allowlist spoofing\). |
| `contracts/tier_registry.py:R5:_register_with_module_prefix:fp=ce98be6cf59df4d5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is an offensive invariant guard at a decorator API boundary: it validates that the decorated object is actually a class and a BaseException subclass, immediately raising TypeError with a meaningful message on failure. This is precisely the 'detect invalid state and throw meaningful error' pattern the policy encourages, not a defensive shape guard that swallows errors with a default. The R5 rule targets defensive isinstance shape-guards on Tier-3 data; this site uses isinstance to assert a precondition and crash loudly, which matches offensive programming. |
| `contracts/tier_registry.py:R5:_register_with_module_prefix:fp=18d4ee87363f8490` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a decorator-registration API boundary that validates a registered exception class before admitting it to the Tier-1 registry. \`cls.__module__\` is technically typed as \`str\` but Python permits arbitrary values there \(classes can set \`__module__\` to anything\), so this is offensive validation that raises TypeError with a meaningful message on invalid input — exactly the encouraged pattern. The isinstance check is not suppressing an error with a default; it's detecting an invalid state and crashing informatively. The decorator nudge does not apply: this function does not take external/Tier-3 data via a parameter, and the subject \`class_module\` is derived from a class object's dunder, not from an external source_param. |
| `contracts/declaration_contracts.py:R5:_normalise_row_tuple:fp=6cd7a87f9b40bc62` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is offensive programming, not defensive: it explicitly validates the type of an internal bundle field and raises TypeError on anything other than list/tuple. This is the 'detect invalid state and throw a meaningful error' pattern the policy encourages, not a shape-guard that silently swallows mismatches. The function is a normalization helper for our own contract-layer bundles \(Tier-1/Tier-2 internal data\), not an external boundary, so the @trust_boundary decorator does not apply. The R5 rule fires on isinstance shape-checks, but this site's isinstance is the discriminator for a meaningful crash, which aligns with policy. |
| `contracts/declaration_contracts.py:R5:_normalise_row_tuple:fp=2be5b2edada71a02` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is offensive programming, not defensive: the helper explicitly enumerates accepted shapes \(list vs already-normalized tuple\) and raises TypeError on anything else. This is a type-dispatch discriminator that ends in an explicit raise, which is exactly the offensive-programming pattern the policy encourages. The data is internal contract bundle material in contracts/ \(L0\), not Tier-3 external input, so the @trust_boundary decorator is not the right structural fix. R5's 'shape guard' concern does not apply when the alternative branch crashes with a meaningful error. |
| `contracts/declaration_contracts.py:R2:_collect_contract_sites:fp=8d6e48bba4566067` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a marker-discovery pattern: the code walks every attribute of every class in the MRO and probes for a decorator-applied marker attribute. The absence of the marker is semantically meaningful \('this method is not a dispatch site'\) and is the documented contract of @implements_dispatch_site. Direct attribute access would crash on every undecorated method \(dunders, helpers, dataclass fields\), which is the wrong behavior — the None branch is the dominant code path, not an error case. This is offensive use of getattr-as-probe, not defensive suppression of an error, and it operates on internal framework code \(not Tier-1 audit data, not a plugin return\). |
| `contracts/declaration_contracts.py:R5:register_declaration_contract:fp=e770539007d53958` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an offensive-programming isinstance check at a registry entry point that raises TypeError with a self-diagnosing message — exactly the pattern the policy encourages. The check enforces ADR-010's nominal-typing requirement \(rejecting structural Protocol matches\) and crashes loudly on framework bugs rather than silently coercing. R5 targets isinstance used as a defensive shape guard; here isinstance is used as an offensive type assertion that raises, not to suppress or branch around bad data. The rationale is site-specific and ties directly to the visible code. |
| `contracts/declaration_contracts.py:R5:register_declaration_contract:fp=28b565efc229de07` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check at line 1188 is offensive programming: it validates that a registered contract's payload_schema is actually a type \(TypedDict class\), raising TypeError with a clear diagnostic if not. This is a registry-entry invariant check that crashes loudly on framework bugs, exactly matching the 'detect invalid state and throw meaningful exception' pattern encouraged by policy. R5 \(isinstance shape guard\) here is not Tier-3 boundary suppression but an assertion-style type gate; the suppression is legitimate and the rationale accurately describes the code. |
| `contracts/declaration_contracts.py:R5:register_declaration_contract:fp=88e060f1276007c6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is an offensive type-gate at contract registration: it validates that caller-supplied violation_class is actually a type object before subsequent issubclass\(\) and identity checks. This is the encouraged pattern of proactively detecting an invalid state and raising a meaningful TypeError — exactly what the offensive-programming policy endorses. R5 targets defensive shape-guards on Tier-3 data, but this is registry validation of framework-internal API contracts, raising loudly rather than coercing or silently swallowing. The sibling payload_schema gate above uses the identical pattern. |
| `contracts/plugin_context.py:R6:PluginContext:record_call:fp=b105d00f0725cd72` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a narrow set of transport-layer exceptions \(OSError, ConnectionError, TimeoutError\) around a telemetry emit call that occurs AFTER the primary Landscape call recording has already succeeded. This aligns with the telemetry primacy order: audit fires first and crashes on failure; telemetry fires second as best-effort. The comment correctly notes that all other exceptions \(including programming errors\) propagate naturally, so the suppression is appropriately scoped to transport failures only. The rationale accurately describes the policy basis \(telemetry must not corrupt already-recorded audit state\). |
| `contracts/plugin_context.py:R6:PluginContext:record_validation_error:fp=5e7b2d9d784b791f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary handling: record_validation_error is the quarantine path for external row data that has already failed validation, and stable_hash legitimately raises ValueError/TypeError on non-canonical inputs like NaN or non-serializable types. Catching those specific exceptions to fall back to repr_hash preserves the audit trail \(recording what we actually saw\) rather than fabricating or dropping the record, which aligns with the Tier-3 'record absence/malformation as fact' principle. The except is narrow \(ValueError, TypeError\), uses \`from\`-free re-raise unnecessary here because the row is being quarantined not re-raised, and the warning log is informational alongside the Landscape record. Decorator nudge does not apply: R6 is not in the {R1,R5} suppressible set. |
| `contracts/security.py:R1:get_fingerprint_key:fp=e763a5c875c64b76` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary read: os.environ is external process state, and os.environ.get\(\) is the idiomatic way to test for an env var's presence. The code immediately validates absence with an explicit ValueError raise — it does not fabricate a default or silently recover, which is exactly the offensive pattern the policy encourages at a trust boundary. The decorator nudge does not apply: the external data is not a function parameter, so @trust_boundary\(source_param=...\) has no parameter to bind to. |
| `contracts/composer_interpretation.py:R5:_validate_enum_member:fp=ffdde8578491891c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an offensive-programming validator: \`_validate_enum_member\` takes \`value: object\` and is explicitly designed to detect invalid states and raise a meaningful ValueError with type and repr context. The \`isinstance\` check here is not a defensive shape-guard suppressing an error — it IS the error detection, the assertive 'crash with informative message' pattern the policy encourages. R5 targets isinstance used to silently branch around malformed data, but here the negative branch raises with full diagnostic context. The decorator nudge does not apply: \`value\` is a generic object parameter to a reusable validator, not a Tier-3 external-source parameter with a clear external origin in this scope. |
| `core/payload_store.py:R6:FilesystemPayloadStore:store:fp=7ffad90e4cb20922` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate offensive-programming pattern: EAFP at a filesystem boundary where TOCTOU races are real. The try/except narrowly catches FileNotFoundError \(not a blanket except\), and the semantic is correct — file absence at this point means we proceed to the write path, which is the intended control flow, not silent error suppression. The filesystem is an external resource where concurrent purge is a legitimate concern, and the alternative \(exists\(\)+read_bytes\) is explicitly worse per the inline comment. The rationale accurately describes the code's intent and aligns with the offensive-programming guidance about meaningful, narrowly-scoped exception handling at I/O boundaries. |
| `core/payload_store.py:R6:FilesystemPayloadStore:delete:fp=5e6dea501dc28457` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate trust boundary at the filesystem — the delete\(\) method's contract explicitly returns False if not found, making it an idempotent operation. The FileNotFoundError handling is at an external system boundary \(filesystem\), which the policy explicitly permits for defensive handling. The caught exception is narrow \(FileNotFoundError specifically, not bare except\), the return value honestly signals absence rather than fabricating success, and the documented contract makes the behavior auditable. R6 \(exception handling\) at a filesystem boundary with a narrow exception class and a documented idempotent contract is exactly the offensive-at-boundary pattern the policy allows. |
| `core/landscape/query_repository.py:R6:QueryRepository:explain_row:fp=5b5158345e746330` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a specific, named exception \(PayloadNotFoundError\) that represents a legitimate expected state — the payload store has a retention policy that purges payloads, and a query for an explained row after purge is a normal occurrence, not corruption. The handler correctly records the absence \(payload_available=False, source_data=None\) rather than fabricating data, consistent with the 'record absence as None' guidance. The exception is narrow \(not bare except\), the logged event is at debug level for an expected condition, and the audit-relevant outcome is encoded in the returned RowLineage fields. This is offensive narrow exception handling at a real boundary \(payload store retention\), not defensive swallowing. |
| `core/rate_limit/limiter.py:R6:RateLimiter:try_acquire:fp=3e1bc5e9e76ba71f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a specific, named exception \(BucketFullException\) from a third-party rate-limiter library and converts it into the documented False return value. This is not silent recovery or defensive error swallowing — BucketFullException is the library's in-band signal for 'rate limited', and the function's contract explicitly returns False in that case. The exception type is narrow \(not bare except or Exception\), the handling is the function's primary semantics, and there is no fabrication or data coercion. R6 \(silent exception handling\) is misapplied here; this is offensive use of a typed exception as control flow at a library boundary. |
| `core/rate_limit/limiter.py:R6:RateLimiter:close:fp=3aef03c2689d8024` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 framework boundary: pyrate-limiter is a third-party library whose internal \`_leaker\` attribute on \`bucket_factory\` is undocumented and may change across versions. The code is reaching into library internals \(a private underscore-prefixed attribute\), which is genuinely external/untrusted territory where defensive AttributeError handling is appropriate. The fallback behavior \(skip graceful cleanup, rely on excepthook safety net\) is documented and meaning-preserving — no fabrication occurs. R6 suppression at this specific call site is narrowly scoped to the known-fragile internal access. |
| `core/security/secret_loader.py:R6:CompositeSecretLoader:get_secret:fp=cb4d8fc57f9917d4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a specific, narrow exception \(SecretNotFoundError\) as part of an intentional fallback iteration across prioritized backends — this is the documented composite/chain-of-responsibility pattern, not silent error swallowing. The exception type is precise \(not a bare except or broad Exception\), and exhaustion of all backends still raises SecretNotFoundError, preserving the audit trail. R6's concern about swallowed exceptions does not apply when the swallow is structurally required by the iteration semantics and the terminal case re-raises. |
| `core/retention/purge.py:R6:PurgeManager:purge_payloads:fp=0d32fa231299f2d6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 appears to be a catch-specific-exception rule applied at an external I/O boundary. PayloadStore.delete\(\) performs filesystem/blob storage operations which legitimately raise OSError, and the handler does the right thing: it records the failure per-ref into failed_refs \(preserved in the PurgeResult audit record\), logs structured context, and continues purging other refs. This matches the trust-boundary policy — external I/O failures are expected operational conditions, not bugs in code we control, and the handling is per-item bounded rather than a broad swallow. The rationale correctly identifies the tier and the code excerpt independently supports it. |
| `core/landscape/execution_repository.py:R6:ExecutionRepository:begin_node_state:fp=6e0f00f363e89415` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code path is explicitly gated on the quarantined=True branch, which the docstring documents as Tier-3 external data that may contain non-canonical floats \(NaN/Infinity\) that stable_hash legitimately rejects with ValueError/TypeError. The fallback is not silent recovery of our own data — it's a documented Tier-3 boundary handler that records a hash \(via repr_hash\) rather than fabricating one, preserving auditability of malformed external input. The exception types are narrow \(ValueError, TypeError\), not a bare except, and the non-quarantined path still crashes on anomaly as Tier-1 policy demands. R6 is not in the decorator's suppressible set \(R1/R5\), so the structural alternative does not apply. |
| `core/security/web.py:R4:validate_url_for_ssrf:_resolve_worker:fp=bdf07052a9f75058` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of BaseException catch in a thread-pool worker: the worker runs on a separate thread and must relay ALL exceptions \(including KeyboardInterrupt, SystemExit, and unexpected errors\) back to the caller thread through the result queue. Failing to catch BaseException would cause the exception to be swallowed by the executor and the caller would hang on result_queue.get until timeout. The caller thread re-raises the exception with proper typing \(SSRFBlockedError, NetworkError\) preserving the exception chain via \`from err\`, so no error is silently swallowed — this is offensive programming relayed across a thread boundary, not defensive suppression. The code excerpt confirms the exception is faithfully propagated, satisfying the auditability standard. |
| `core/security/secret_loader.py:R6:KeyVaultSecretLoader:get_secret:fp=e77b2a357f43e9a2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R6 finding flags an \`except ImportError\` clause that is not a runtime error handler in the defensive sense — it is a compile-time fallback that defines a sentinel exception class so the later \`except AzureResourceNotFoundError\` clause has a valid name to bind when the optional \`azure-keyvault-secrets\` dependency is absent. The sentinel is an unreachable exception type \(never raised anywhere\), so it cannot silently swallow real errors; \`_get_client\(\)\` raises ImportError first if Azure is unavailable. This is a legitimate optional-dependency pattern, not defensive programming against runtime failures, and the rationale accurately describes the construct. |
| `core/checkpoint/recovery.py:R5:RecoveryManager:get_unprocessed_row_data:fp=7a2f9a2b860707a7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an offensive-programming integrity check on Tier-1 data \(deserialized checkpoint payload from our own payload store\). The isinstance check is not silently coercing or suppressing — it raises AuditIntegrityError with a clear Tier 1 violation message, which is exactly the policy's prescribed crash-on-anomaly behavior for our own data. The R5 rule targets defensive shape guards at boundaries; here the guard exists precisely to surface corruption rather than tolerate it. The decorator nudge does not apply: this is not a Tier-3 external-parameter boundary, and the suppression is the rare legitimate use of isinstance as an offensive assertion. |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:create_row:fp=8ca3a7ee3cc84783` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppression is on a try/except around stable_hash for quarantined rows, which by definition carry Tier-3 external data that failed source validation and may contain non-canonical values \(NaN/Infinity\) that stable_hash legitimately rejects. The except is narrow \(ValueError, TypeError\), the fallback \(repr_hash\) is documented in canonical.py as the intended escape hatch, and the branch only fires when quarantined=True — the non-quarantined path still crashes offensively on bad hashing. This is a genuine trust-boundary case inside core/landscape, not a transform-layer .get\(\) pattern; the @trust_boundary decorator doesn't apply \(R6 isn't in its suppressible set, and the data isn't function-parameter-rooted in the Tier-3 sense the decorator expresses\). |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:record_validation_error:fp=79239e021b66fa02` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppression covers a try/except around canonical_json/stable_hash of row_data — Tier-3 external data being recorded into the audit trail. The fallback is not silent recovery: it constructs a NonCanonicalMetadata record that preserves the type information and uses repr_hash, which is the honest way to record 'this row was not canonically serializable' rather than fabricating a value or dropping the audit entry. This is exactly the trust-boundary case where wrapping is policy-encouraged: external data with non-canonical values \(NaN/Infinity\) must still be recorded faithfully. The decorator nudge does not apply because R6 is not in the decorator's suppressible set {R1, R5}. |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:record_transform_error:fp=cf71d23d49a057eb` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on a try/except wrapping canonical_json serialization of row_data being recorded into the audit trail. row_data is Tier-2 pipeline data that may legitimately contain NaN/Infinity \(valid Python floats that canonical_json rejects with allow_nan=False\). The except clause catches ValueError/TypeError narrowly and falls back to a documented NonCanonicalMetadata record — this is not silent error swallowing; the audit trail records the serialization failure explicitly with repr-based hash and metadata. Losing the error record entirely would be worse for auditability than recording it in non-canonical form. The pattern mirrors an established sibling \(record_validation_error\) and the rationale honestly identifies the tier and the failure mode. |
| `engine/coalesce_executor.py:R5:CoalesceExecutor:_execute_merge:fp=4a8461c859544771` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is offensive programming, not defensive — it's detecting a specific exception type to re-raise it and bypass the FAILED-record writing path, preserving audit integrity. R5 flags isinstance as a shape guard, but this usage is exception-type discrimination at an error-handling boundary, which is a legitimate pattern \(equivalent to \`except AuditIntegrityError: raise\` restructuring\). The rationale correctly identifies that writing FAILED to a compromised audit DB would be evidence tampering, aligning with the Auditability Standard. The suppression is site-specific and the code excerpt independently supports it. |
| `engine/executors/sink.py:R4:SinkExecutor:_best_effort_cleanup:fp=1b066df6010227f2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate broad-except in a best-effort cleanup path that explicitly re-raises Tier-1 audit errors first \(the higher-priority crash\) and only swallows the residual cleanup failure to let the original FrameworkBugError/AuditIntegrityError propagate. Suppressing the cleanup exception here is necessary precisely to preserve the original error's audit signal — re-raising would mask the root cause. The logger.warning is appropriate as a last-resort channel for the cleanup-failure detail since the primary error path \(the original exception\) continues to propagate to its audit destination. The structure \(specific Tier-1 catch + re-raise, then broad catch with informative log\) matches the offensive-programming pattern of preserving maximum diagnostic information. |
| `engine/executors/sink.py:R4:SinkExecutor:write:fp=7484e4d5a8bb7618` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate broad-except in a cleanup path during exception handling: the original error is being preserved \(re-raised at line 487\), Tier-1 audit corruption errors are explicitly re-raised first \(line 478-479\), and only non-Tier-1 cleanup exceptions are caught and logged so they don't mask the primary failure. The narrow alternative — letting cleanup_exc propagate — would lose the original begin_node_state error, which is exactly the failure mode the comment at line 479 distinguishes from. Logging here is acceptable because the primary error is still raised and will reach the audit trail via the normal path; this logger.warning is a true last-resort channel for the secondary cleanup failure, consistent with the primacy order. The rationale is site-specific and accurate. |
| `engine/executors/sink.py:R5:SinkExecutor:write:fp=8ae24dda2209d609` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook offensive isinstance check: it validates the return value of a plugin method \(sink.write\(\)\) and raises PluginContractViolation with an informative message identifying the bug. Per the Plugin Ownership policy, plugin returning wrong type must crash with a clear contract violation, which is exactly what this code does. The isinstance is not suppressing an error or providing a default — it is detecting an invalid state and throwing meaningfully, matching the 'offensive programming' encouraged pattern. R5 here is a false positive against legitimate contract enforcement. |
| `engine/executors/sink.py:R5:SinkExecutor:write:fp=333193edcab82132` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check validates a plugin return value \(Tier-2 system code boundary\) and raises PluginContractViolation with a clear message identifying the plugin bug — this is textbook offensive programming per the Plugin Ownership policy \('Plugin returns wrong type → CRASH'\). The check converts an opaque downstream AttributeError into an informative crash naming the offending sink. The decorator nudge does not apply: R5 here is not on an external Tier-3 parameter, it's on a plugin return value, and the function is not a Tier-3 boundary. |
| `engine/executors/sink.py:R5:SinkExecutor:write:fp=6bf4f058589b3c6f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check validates the return type of a plugin call \(failsink.write\(\)\) and raises PluginContractViolation identifying the buggy plugin if the contract is violated. This is offensive programming per the policy's plugin-ownership section: 'Plugin returns wrong type — CRASH — bug in our code' with a meaningful error. The check converts an opaque downstream AttributeError into a precise contract-violation diagnostic naming the offending plugin. R5 is not applicable here because the subject is a plugin return value, not external Tier-3 data, and the decorator would be the wrong remediation. |
| `engine/executors/sink.py:R5:SinkExecutor:write:fp=00755313e7e139d5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is offensive isinstance validation at a plugin contract boundary, not defensive shape-guarding of external data. The failsink is system-owned plugin code \(per Plugin Ownership policy\), and the check raises PluginContractViolation with a precise message identifying the buggy plugin if it returns the wrong artifact type — exactly the 'detect invalid states and throw meaningful exceptions' pattern the policy encourages. R5's decorator nudge does not apply: the subject is a plugin return value, not an external Tier-3 parameter, so @trust_boundary would be the wrong remediation. Without this check, a later attribute access would produce an opaque AttributeError that loses the plugin-bug attribution. |
| `engine/commencement.py:R5:evaluate_commencement_gates:fp=18ca009ccd49b5e0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook offensive isinstance check: the code detects an invalid state \(gate expression returned non-bool\) and raises an informative CommencementGateFailedError with full context snapshot rather than coercing or silently accepting truthiness. The Decision Test maps cleanly to 'Can I detect an invalid state and throw a meaningful err? → Assert it \(offensive\).' The subject is not external Tier-3 data rooted at a parameter — it's the return value of an internal ExpressionParser — so the @trust_boundary decorator is not applicable. The rationale accurately describes the policy basis. |
| `engine/executors/state_guard.py:R5:NodeStateGuard:_extract_audit_evidence_context:fp=86d1554e15f3f1f6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is a nominal type discrimination on an exception object to decide whether to extract structured audit context — not a defensive shape guard on Tier-3 data. ADR-010 explicitly chose nominal inheritance over structural Protocol to prevent accidental-match spoofing of audit evidence, which makes isinstance the correct mechanism. The exception always propagates regardless; the isinstance only gates conditional enrichment, so this is offensive type-driven dispatch rather than swallowing. The decorator nudge does not apply: exc_val is an in-process exception object from code we control, not an external parameter, and the suppression scope is a single discriminator line. |
| `engine/executors/declaration_dispatch.py:R6:_dispatch:fp=15d9ff75661bacf0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a dispatcher that catches a narrow, domain-specific exception type \(DeclarationContractViolation\), enriches it with the authoritative contract name from the registry, collects it, and re-raises either the original instance \(N=1, reference-equality preserved\) or an AggregateDeclarationContractViolation \(N&gt;=2\) at loop end. This is not silent swallowing — it is the audit-complete invariant the docstring documents: every applicable contract's method runs exactly once, and all violations surface. The catch is narrow \(not bare/Exception\), the exception is not discarded, and the structural reason \(must iterate all contracts before raising\) cannot be expressed as a simple let-it-crash. This matches offensive-programming intent: maximally informative crash containing every violation, not just the first. |
| `engine/_best_effort.py:R4:best_effort:fp=c48026df1d344433` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is the canonical post-audit ceremony helper whose sole purpose is to consolidate broad-except handling at a legitimate boundary: secondary work that runs AFTER the primary audit event has been recorded. The policy's logging-as-last-resort applies here because telemetry/ceremony itself is the failing subsystem, and the original audit event is already preserved. The single broad-except is the helper's reason for existing — concentrating 14 scattered catches into one reviewable surface improves auditability rather than hiding bugs. The code excerpt confirms the docstring articulates the invariant and the warning log carries structured context for triage. |
| `engine/_best_effort.py:R7:best_effort:fp=437c71e49d86fee5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is the logger-of-last-resort inside an already-secondary ceremony boundary. The primary audit event has already been recorded; the suppress\(Exception\) here exists specifically so that a failure in the warning-logging path cannot mask or replace the original audit-load-bearing event. This matches the policy's primacy order \(audit first, telemetry second, logger last\) — at the logger-last tier, swallowing logger failures is the correct behavior, not a defensive anti-pattern. The rationale honestly names the boundary and the invariant being protected. |
| `engine/orchestrator/cleanup.py:R4:cleanup_plugins:run_hook:fp=3f14e10c45af58b3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate cleanup-phase broad catch consolidated into a single helper. The contract requires attempting every plugin hook even when individual hooks fail, so a broad except is structurally necessary at this point. The excerpt shows that record_cleanup_error re-raises Tier-1 errors \(FrameworkBugError, AuditIntegrityError\) before downgrading anything, and non-Tier-1 failures are collected and surfaced via a chained RuntimeError after all hooks run — so errors are neither silently swallowed nor fabricated away. The decorator nudge does not apply \(R4, not R1/R5; no external parameter; this is lifecycle orchestration, not a Tier-3 boundary\). |
| `plugins/sources/csv_source.py:R6:CSVSource:load:fp=efb42c44febf0c23` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a CSV Source plugin at the Tier-3 external boundary catching UnicodeDecodeError from decoding raw file bytes. Per the Data Manifesto, sources MAY catch external parse failures and quarantine them; the code records the failure via ctx.record_validation_error and yields a quarantined SourceRow rather than silently swallowing. This is the canonical Tier-3 boundary pattern for source plugins \(validate at boundary, quarantine failures, don't crash on external malformedness\). The decorator nudge does not apply since R6 is not in the {R1, R5} suppressible set and the subject is an exception type, not a parameter-rooted expression. |
| `plugins/sources/csv_source.py:R6:CSVSource:load:fp=feb345d977d9991e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary: CSVSource.load is decoding external file bytes, and UnicodeDecodeError is precisely the kind of malformed-external-input failure that sources are expected to catch, record via ctx.record_validation_error, and quarantine. The except clause does not swallow silently — it records a parse-level validation error with the encoding context and stops iteration, which matches the Tier-3 'validate at the boundary, record what we got' guidance. R6 \(broad/exception-handling rule\) at this site is the legitimate trust-boundary pattern, not defensive programming on Tier-2 data. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=122485b351a8f172` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a CSV source plugin catching csv.Error at the Tier-3 external-data boundary — exactly where defensive handling is policy-mandated. The except clause records the parse failure via record_validation_error and routes to quarantine per _on_validation_failure, which is the prescribed pattern for malformed external input. R6 \(likely a broad-except or exception-handling rule\) is not in the decorator's suppressible set {R1, R5}, so the @trust_boundary nudge does not apply. The handler is narrow \(csv.Error specifically\), preserves the exception message in the audit record, and does not swallow silently. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=21b69c7fe66e0a1b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a CSV Source plugin \(Tier-3 boundary\) catching StopIteration from next\(\) on a file iterator — the canonical Python idiom for detecting end-of-stream. The except clause does not silently swallow the error; it records a validation error to the audit trail \(ctx.record_validation_error\) so the absence is observable. R6 \(likely broad-except or exception-handling rule\) at a Tier-3 source boundary catching the specific StopIteration signal is exactly the kind of boundary handling the policy permits. The decorator nudge does not apply: R6 is not in {R1, R5}. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=55805867c8844172` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a CSV source plugin \(Tier-3 boundary\) catching csv.Error during row iteration over external file input. The exception is recorded via ctx.record_validation_error \(audit trail\) and processing stops to prevent silent data loss — this is exactly the boundary-validation pattern the policy endorses for Tier-3 sources. The rationale correctly identifies the trust tier and the specific failure mode \(parser state corruption from unmatched quotes\). R6 \(exception handling\) at a Tier-3 source boundary with proper audit recording is legitimate. |
| `plugins/sources/dataverse.py:R1:DataverseSource:load:fp=5e69d62057bf8bb7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`entity_elem.get\("name"\)\` where \`entity_elem\` is an XML element parsed from operator-supplied FetchXML configuration — genuinely Tier-3 external data at a source plugin. Critically, the \`.get\(\)\` is not silently swallowing absence: the very next line checks \`if entity_name is None\` and raises a meaningful RuntimeError naming the exact malformed-input condition. This is offensive programming at a trust boundary, not defensive suppression — the code detects the invalid state and crashes informatively. The pattern matches Tier-3 source handling: validate at boundary, record/raise on malformed input. |
| `plugins/sources/dataverse.py:R6:DataverseSource:load:fp=b8ab7237d10474f3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary in a Source plugin: rows come directly from the Dataverse API, and the try/except catches a specific ValueError raised by _strip_odata_metadata when external OData formatted-value metadata would collide with real fields. The handler quarantines the row via ctx.record_validation_error and yields a SourceRow.quarantined — recording the failure rather than silently swallowing it, consistent with the Tier-3 'validate at boundary, quarantine failures' policy. The exception is narrow \(ValueError, not bare Exception\), the data flow is genuinely external, and this is exactly where defensive handling is permitted. |
| `plugins/sources/dataverse.py:R6:DataverseSource:load:fp=dfad59c5d97e602a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source plugin \(Dataverse API\) catching a specific ValueError from field-name normalization at the external boundary. The handler quarantines the row with audit recording via ctx.record_validation_error and emits a quarantined SourceRow — this is exactly the prescribed Tier-3 pattern: validate at the boundary, quarantine failures, record the absence. The exception type is narrow \(ValueError\), the error message is preserved, and the audit trail captures the failure. R6 \(exception handling\) suppression is appropriate here; the @trust_boundary decorator only covers R1/R5, not R6, so a per-line entry is the correct mechanism. |
| `plugins/sources/dataverse.py:R6:DataverseSource:load:fp=d31e59f727e94dff` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary catching ValidationError from Pydantic schema validation of external Dataverse API rows. The except clause does exactly what the policy prescribes for Tier-3 boundaries: it quarantines the malformed row, records the validation error in the audit trail via ctx.record_validation_error, and continues processing. This is the canonical 'validate at the boundary, quarantine failures' pattern for source plugins handling external data. The error is not silently swallowed — it is recorded and the row is routed to the configured failure destination. |
| `plugins/sources/json_source.py:R6:JSONSource:_load_jsonl:fp=cd3cc8680d23c268` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 trust boundary: JSONSource is reading external file input, and the except clause catches JSONDecodeError plus ValueError \(which the parse_constant hook raises for NaN/Infinity per canonical JSON policy\). The malformed rows are quarantined with raw line + line number for audit traceability, not silently swallowed. R6 \(broad exception catch\) is legitimately suppressed here because both exception types are concrete parse-failure signals from the external boundary, and the recovery path is audit-recording quarantine, not fabrication. The decorator nudge does not apply: R6 is not in the {R1, R5} set the decorator suppresses. |
| `plugins/sources/json_source.py:R6:JSONSource:_load_jsonl:fp=ce254b64675c2272` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Source plugin reading external JSONL file content — Tier-3 data at the boundary. The UnicodeDecodeError catch handles malformed external byte sequences, quarantines the offending line with audit metadata \(path, line number, error message\), and yields the quarantined record rather than silently swallowing. This matches the Tier-3 source contract: validate at the boundary, record what we got, quarantine failures. The rationale correctly identifies the tier and the failure mode, and the code shows proper audit-trail preservation rather than silent recovery. |
| `plugins/sources/json_source.py:R6:JSONSource:_load_json_array:fp=a8886f7f301af61e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is at a Tier-3 file-parsing boundary in a Source plugin, where the policy explicitly permits defensive handling: external JSON files can be malformed, and the code correctly quarantines the parse error rather than crashing the pipeline. The rationale accurately identifies the trust tier and the specific failure modes \(JSONDecodeError, non-finite ValueError\) being handled. The excerpt does not show the function signature/decorators, so I cannot recommend the @trust_boundary decorator path with confidence; additionally R6 is not in the decorator's suppressible set {R1, R5}. This is a legitimate Source-level boundary suppression consistent with the Three-Tier Trust Model. |
| `plugins/sources/json_source.py:R6:JSONSource:_load_json_array:fp=ad1b700baaf7e161` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary: JSONSource reads an external file and the except clause catches parse/decode failures from json.load to quarantine malformed input rather than crash. The handling is exactly what the Three-Tier Trust Model prescribes for source-level Tier-3 input — record the failure, quarantine the row, continue. The rule is R6 \(not R1/R5\), so the @trust_boundary decorator does not apply here. The rationale accurately identifies the tier and the operation. |
| `plugins/sources/json_source.py:R6:JSONSource:_validate_and_yield:fp=624f4fc7fffa80de` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary: JSONSource ingests external JSON rows and is precisely the layer where validation/quarantine is policy-correct. The except ValueError wraps a normalization call on external row data and routes the failure to _record_validation_failure which quarantines the row — that is the prescribed Tier-3 handling \(validate at boundary, quarantine failures, record absence\), not silent recovery. The exception type is narrow \(ValueError\), the failure is recorded rather than swallowed, and the code does not fabricate a default. R6 suppression here matches the documented source-boundary contract. |
| `plugins/sources/json_source.py:R6:JSONSource:_validate_and_yield:fp=6650d40e380ee359` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a JSON Source plugin at the Tier-3 external data boundary catching ValidationError from schema validation of external row data. R6 \(exception handling at trust boundary\) is exactly the offensive-programming-at-the-boundary pattern: the source validates external input, records the validation failure to the audit trail via ctx.record_validation_error, and quarantines the row rather than crashing the pipeline — which matches the policy's prescription that user data with wrong type/missing fields should quarantine and continue. The excerpt shows proper audit recording before quarantine, not silent swallowing. |
| `plugins/infrastructure/schema_factory.py:R6:_find_non_finite_value_path:fp=b5937945437c8045` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except TypeError is narrowly scoped around np.isfinite on a NumPy array whose dtype may not be numeric \(e.g., string/object arrays\). This is not silent error suppression of a bug in our code — it's a type-dispatch check on a library call where TypeError is the documented signal that the array contains no floats to check, hence no non-finite values to find. The function correctly returns None \(falls through\) in that case, which is the honest answer: a string array cannot contain NaN/Infinity. The rule R6 finding flags the bare except-pass, but the rationale matches the code shape and the semantics are correct. |
| `plugins/infrastructure/clients/http.py:R1:AuditedHTTPClient:_follow_redirects_safe:fp=10ab112db646388f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`response.headers.get\("location"\)\` inside an HTTP client that processes responses from external servers — this is a genuine Tier-3 boundary \(external HTTP response\). The Location header is legitimately optional per HTTP semantics; a redirect response without a Location is malformed external behavior, and the code correctly handles absence by breaking the loop rather than fabricating a value. The subject is \`response.headers\`, not a function parameter, so the @trust_boundary decorator's source_param mechanism does not cleanly apply here \(the external data enters via the response object from httpx, not via a named parameter\). The rationale accurately identifies the trust boundary and the suppression scope is narrow and site-appropriate. |
| `plugins/infrastructure/discovery.py:R9:_discover_in_file:fp=12a2a0eef980437f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code registers a module in sys.modules before exec_module \(required for Python 3.13+ dataclass resolution\) and on exec failure must clean up the registration. The .pop\(name, None\) with default is a legitimate cleanup pattern: the module was just registered on line 174, so it should be present, but using None default makes the cleanup idempotent and avoids masking the original exception \(which is re-raised immediately on line 180\). This is not Tier-3 defensive coding on external data, nor swallowing an error — the real exception propagates via \`raise\`. The default prevents a KeyError in cleanup from shadowing the underlying loader failure, which matches offensive-programming principles \(preserve the informative exception\). |
| `plugins/infrastructure/pooling/executor.py:R5:PooledExecutor:_build_retry_timeout_result:fp=5ce04fa54ab80b94` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate discriminated-union narrowing, not a defensive shape guard. The parameter type is explicitly \`PluginRetryableError \| CapacityError\`, and \`status_code\` exists only on \`CapacityError\`; \`isinstance\` is the idiomatic way to narrow a union so the type checker permits the attribute access. This is system-internal Tier-2 control flow on a typed union, not Tier-3 boundary validation, and the alternative \(getattr with default\) would be the defensive anti-pattern the policy forbids. R5 is firing on a structurally correct use. |
| `plugins/infrastructure/pooling/executor.py:R6:PooledExecutor:_execute_single:fp=71e586ef9fbea49d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except CapacityError block is part of an explicit retry-orchestration control flow: the exception is captured into retryable_error, then the worker checks timeout/interruption, throttles, releases the semaphore, and retries within the capacity budget. This is not silent swallowing — the error becomes load-bearing state for subsequent timeout/interrupt result construction \(_build_retry_timeout_result\(retryable_error, ...\)\). The excerpt independently corroborates the rationale: the caught exception is used, not discarded, and the surrounding code shows a deliberate retry loop rather than a defensive try/except. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:accept_row:fp=e7a832f8c64e2c4e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except RuntimeError catches a specific, well-understood race condition: ThreadPoolExecutor.submit raises RuntimeError when the pool has been shut down between row admission and submission. This is not defensive suppression of an unknown error — it's offensive handling of a known concurrency boundary where the row is already committed to the FIFO buffer and must be resolved into an explicit error TransformResult to avoid stranding the ticket. The handler does meaningful work \(cleans up submission tracking, emits a structured shutdown_requested error result\) rather than silently swallowing. R6 \(broad exception handling\) is not in the decorator's suppression set, and this is a shutdown-race boundary, not a Tier-3 external-data boundary. |
| `plugins/infrastructure/batching/mixin.py:R4:BatchTransformMixin:_process_and_complete:fp=44635c7d2b4b541c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate thread-boundary catch: worker threads cannot propagate exceptions directly to the orchestrator, so the broad except captures the exception \(including plugin bugs\) and transports it via ExceptionResult to be re-raised in the orchestrator thread. This preserves the crash-on-plugin-bug semantics required by the Plugin Ownership policy rather than swallowing the error — the exception and traceback are forwarded intact and re-raised. The broad except is structurally necessary at a thread boundary, analogous to a trust/transport boundary, and the code comment plus surrounding docstring make the audit record clear. |
| `plugins/infrastructure/batching/mixin.py:R9:BatchTransformMixin:evict_submission:fp=cb11639929a462ac` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code is an eviction routine that explicitly handles the case where the key is absent: pop\(key, None\) is followed by an explicit \`if ticket is None: return False\`. This is offensive programming — the function's contract documents 'False if not found or already completed' — not silent error swallowing. The data is internal process state \(Tier-2 process-local dict\) under a lock with a documented race \(evict may run after completion\), so pop-with-default is the correct idiom to express the documented invariant. No fabrication occurs; absence is recorded as a False return. |
| `plugins/infrastructure/clients/http.py:R1:AuditedHTTPClient:_parse_response_body:fp=c10e1a372d3013e8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`response.headers.get\("content-type", ""\)\` inside \`_parse_response_body\`, which is explicitly documented as a Tier-3 boundary parsing an external HTTP response. HTTP headers are genuinely external data where content-type may legitimately be absent, and defaulting to empty string is meaning-preserving for the subsequent content-type dispatch \(no fabrication of a field value, just routing logic\). The function takes \`response\` not a dict parameter rooted at an external argument in a way the decorator's \`source_param\` mechanism handles — the subject is \`response.headers\`, an attribute chain, not a parameter-rooted subscript, so the @trust_boundary decorator's R1 suppression on \`source_param\`-derived names is not the cleaner structural fit here. Per-line allowlist is appropriate. |
| `plugins/infrastructure/clients/http.py:R4:AuditedHTTPClient:_emit_telemetry_after_audit:fp=588df931d0f0d1f1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function is named _emit_telemetry_after_audit and the excerpt shows the policy primacy order applied correctly: Tier-1 errors re-raise, programmer-bug exceptions \(TypeError/AttributeError/KeyError/NameError\) re-raise, and only then does a broad catch convert remaining telemetry failures to a logger.warning. This matches the project's primacy order — audit fires first \(already succeeded by this point\), telemetry is best-effort, logging is last resort for telemetry-channel failures. The broad except is narrowly scoped to telemetry emission after audit success, which is exactly the scenario where logger fallback is sanctioned. |
| `plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=92c580e9594a61bd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function extracts token usage from a provider SDK response object — Tier-3 external data crossing the boundary from an LLM provider into ELSPETH. The getattr calls with None defaults honestly record absence \(TokenUsage.unknown / explicit None\) rather than fabricating values, consistent with the Tier-3 'record absence as None' policy. R2 \(getattr suppression\) is not in the trust_boundary decorator's suppressible set \(R1/R5 only\), so the per-line allowlist is the correct mechanism here. The rationale is site-specific and explains the SDK-shape variability that motivates the access pattern. |
| `plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=f8ac3c8b6b9c5f85` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function extracts usage data from a provider SDK response object — Tier-3 external data crossing into the system at the LLM client boundary. The rule is R2 \(getattr with default\), and the rationale correctly identifies that provider SDKs vary in shape and that missing attributes should become honest \`None\` \(recorded absence\), not fabrication — TokenUsage.from_dict performs the single normalization. This matches the Tier-3 boundary pattern: coerce/validate at the external boundary, record absence as None. The decorator nudge does not apply because R2 is not in the decorator's suppressible set {R1, R5}. |
| `plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=d741dbbe7cf1857f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary: the \`usage\` object is returned by a provider SDK \(OpenAI, Anthropic, etc.\) whose schema varies by version and provider. The getattr calls with None defaults preserve honest absence \(per the fabrication-decision test: missing -&gt; None, not fabricated zero\), which TokenUsage.from_dict then normalizes. The Mapping branch already uses .get\(\) symmetrically, confirming the boundary intent. The decorator nudge does not apply: R2 is not in the decorator's suppressible set {R1, R5}, and the subject is rooted at a provider SDK return value, not a named external parameter the decorator targets. |
| `plugins/infrastructure/clients/llm.py:R4:AuditedLLMClient:chat_completion:fp=bcb37b188197d7f9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook telemetry primacy-order suppression: Landscape audit has already fired \(and Tier-1/programmer errors are explicitly re-raised on lines 403-405\), so the remaining broad except guards only best-effort async telemetry emission, which by policy must not corrupt the error-handling path. The code carefully filters out the categories that must crash before catching, and the logger.warning fallback is the documented last-resort channel when telemetry breaks. R4 \(broad except\) at this exact shape — post-audit, post-Tier1-reraise, post-bug-reraise — is the legitimate trust-boundary case for telemetry. No decorator applies \(R4 is not in the decorator's suppressible set, and this is not a Tier-3 input boundary\). |
| `plugins/infrastructure/clients/llm.py:R4:AuditedLLMClient:chat_completion:fp=6f49fd2afcb62733` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The broad except sits after explicit re-raises for Tier-1 audit/integrity errors and for programming-error exception types \(TypeError, AttributeError, KeyError, NameError\), so the remaining catch covers only operational telemetry failures. Per the primacy order, telemetry is best-effort and must not corrupt the already-recorded audit path or mask the LLMClientError being raised to the caller. The catch logs with exc_info and then control proceeds to re-raise the underlying LLM error, preserving the audit-first, telemetry-second, logging-last-resort ordering. This is a legitimate best-effort telemetry boundary, not silent error swallowing. |
| `plugins/infrastructure/clients/llm.py:R4:AuditedLLMClient:chat_completion:fp=369b5c198983a3e5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows the broad except is the final clause after Tier-1 audit-integrity errors and programmer-bug exceptions \(TypeError/AttributeError/KeyError/NameError\) have already been re-raised. What remains is genuinely Tier-2 telemetry-emission failure \(the async, best-effort channel per the primacy order\), occurring after the Landscape audit path. Per policy, telemetry is best-effort and must not corrupt the error-handling flow, and logger.warning is the correct last-resort channel when telemetry itself is broken. The suppression scope is narrow and the rationale matches the code structure. |
| `plugins/infrastructure/clients/llm.py:R4:AuditedLLMClient:chat_completion:fp=9f00f16205160b4f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code shows correctly-ordered exception handling at a telemetry boundary: Tier-1 audit integrity errors re-raise, programming errors \(TypeError/AttributeError/KeyError/NameError\) re-raise, and only then does the broad catch handle residual telemetry-emission failures. This matches the primacy order — Landscape audit already fired \(sync, crash-on-failure\) before this block, and telemetry is explicitly the best-effort second tier per policy. Letting a telemetry transport hiccup corrupt a successful, already-audited LLM call would invert the primacy. The logger.warning fallback is the documented last resort when telemetry itself is the broken channel, which is exactly this scenario. |
| `plugins/transforms/rag/query.py:R5:QueryBuilder:_build_field_only:fp=8690adb65aebc738` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is offensive programming, not defensive — it detects an invariant break in Tier-2 pipeline data and raises TypeError with a descriptive message naming the upstream plugin bug. There is no silent recovery, no coercion, no default value substitution. This matches the Decision Test's 'detect an invalid state and throw a meaningful error' branch. The decorator nudge does not apply: extracted is Tier-2 pipeline data \(post-source\), not a Tier-3 external parameter, and the rule is being used offensively rather than as a shape guard at a boundary. |
| `plugins/transforms/rag/query.py:R5:QueryBuilder:_build_regex:fp=6fa9771f101ecc99` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook offensive isinstance check, not a defensive shape guard. The code raises TypeError with a precise diagnostic message when a Tier-2 invariant is violated, explicitly identifying it as an upstream plugin bug. R5 targets defensive isinstance-as-coercion patterns at boundaries; here the isinstance is the assertion mechanism for a crash-loud invariant, matching the 'Offensive Programming: Encouraged' policy. The data is Tier-2 pipeline data \(not Tier-3 external\), so the @trust_boundary decorator is not applicable. |
| `plugins/transforms/rag/config.py:R6:_get_providers:fp=3371a100de2c9052` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate optional-dependency boundary: the code catches ModuleNotFoundError specifically \(not a broad Exception\) around an import statement for an optional provider whose SDK may not be installed. This is the canonical pattern for optional-feature registration — the catch is narrowly scoped to import failure, and the consequence \(provider absent from registry\) is the correct semantic. It is not defensive programming against logic errors or Tier-2 data; it's structural handling of an environment fact \(package presence\) at import time. R6 \(silent exception handling\) is being applied to a case where 'silent' is the correct behavior because absence-of-optional-dep is not an error condition. |
| `plugins/transforms/rag/config.py:R6:_get_providers:fp=56767cfd4c9ab197` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate optional-dependency pattern: the try/except wraps an import of an optional extra \(\[rag\]/chromadb\), and on ModuleNotFoundError the provider is simply omitted from the registry. This is not silencing a bug in code we control — it is the standard idiom for gating registration of an optional plugin on whether its third-party SDK is installed. The exception type is narrow \(ModuleNotFoundError, not bare Exception\), and the failure mode is observable \(the 'chroma' key is absent from PROVIDERS, so any config referencing it will fail loudly at config validation\). No fabrication, no Tier-tier confusion, no layer violation. |
| `plugins/transforms/rag/config.py:R1:RAGRetrievalConfig:validate_provider_config:fp=8dcd5a41a5d0705b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is offensive programming, not defensive: PROVIDERS.get\(\) is used to look up a registry entry, with an explicit None check that raises a meaningful ValueError naming the unknown provider and listing available options. The .get\(\) here is not swallowing absence — it's the mechanism for detecting it and converting it into a loud, informative crash at config-validation time. This is exactly the pattern the policy encourages \(proactively detect invalid state, throw meaningful exception\). The R1 rule is matching the syntactic .get\(\) pattern but missing that the very next line converts None into a raise. |
| `plugins/infrastructure/preflight.py:R1:plugin_preflight_mode_enabled:fp=2019727813f38ff9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged \`.get\(\)\` is \`ContextVar.get\(\)\`, not \`dict.get\(\)\`. \`_PLUGIN_PREFLIGHT_MODE\` is declared on line 9 as \`ContextVar\[bool\]\`, and \`.get\(\)\` on a ContextVar returns the current context value \(with the declared default of False\) — it is not a defensive dict lookup and has nothing to do with Tier-3 data handling. The R1 rule is misfiring on a syntactic match; suppressing it at this site is correct and the rationale accurately identifies the false positive. |
| `plugins/infrastructure/config_base.py:R1:DataPluginConfig:__init_subclass__:fp=e9064dddb97b1895` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) is on cls.__dict__, a Python-internal mapping whose shape is guaranteed by the interpreter — this is Tier-1 trusted data, not external input. The pattern is the canonical idiom for reading a class-local opt-in marker without inheriting it from parent classes \(since \`cls._component_type_exempt\` would walk the MRO and defeat the non-propagation invariant documented in the docstring\). Using .get\(\) here is semantically required, not defensive: a missing key legitimately means 'not exempt'. The rationale correctly identifies the trust tier and the structural reason for the access pattern. |
| `plugins/sinks/azure_blob_sink.py:R5:AzureBlobSinkConfig:headers_mapping:fp=a46b952b7c0461a9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is type discrimination on a union-typed config field \(str \| dict\[str, str\] \| None\), not a defensive shape guard against external data. The field's type is validated by Pydantic; the property simply selects which branch of the union applies, returning the dict variant or None. This is legitimate union-type narrowing — equivalent to a structural match — not the kind of isinstance-as-shape-guard the R5 rule targets. The pattern is also internally consistent with SinkPathConfig.headers_mapping. |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=817edacea0609417` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-2→external trust boundary at a sink. ChromaDB is an external system with its own type constraints \(str\|int\|float\|bool\|None, finite floats only\); validating row metadata types before handing them to the external store is exactly the boundary-validation pattern the policy permits. The isinstance check here is not defensive suppression — it detects invalid states and diverts the row to a quarantine path rather than silently coercing, which matches the 'quarantine row, continue' rule for user-data validation failures. The decorator nudge does not apply: R5 is in scope but the data subject is \`row\[field\]\` \(Tier-2 pipeline data, not an external function parameter\), and this is a sink-side outbound boundary rather than the inbound Tier-3 source-parameter pattern the decorator targets. |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=d7e05b8fc5f239a9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-2→external trust boundary at a sink. ChromaDB is an external system with documented constraints \(rejects non-finite floats in metadata since they break canonical JSON serialization\). The isinstance\(value, float\) check is not a defensive shape guard on internal data — it's a type-discriminating branch needed to apply the float-specific finiteness check, and the surrounding code already diverts the row rather than crashing or fabricating. This matches the sink-boundary pattern: validate external-system constraints, divert non-conforming rows, continue the batch. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=7be036c024aeeefe` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a Tier-3 boundary: raw bytes from an Azure blob are being decoded, and UnicodeDecodeError is caught to record a validation error and quarantine the row — exactly the prescribed handling for external data per the Data Manifesto. The except clause does not silently swallow; it records the failure via ctx.record_validation_error and yields a quarantined SourceRow, preserving auditability. R6 \(exception-catch suppression\) at a true external-data boundary is legitimate here, and the rationale accurately identifies the tier and the failure mode. The decorator nudge does not apply: R6 is not in the {R1, R5} set the decorator suppresses. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=feb5a61fc7ca0a29` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary case: an Azure blob CSV source catching StopIteration from next\(reader\) on external file content. The handler does not silently swallow — it records a validation error, quarantines the row with diagnostic context, and routes to the configured failure destination. This is exactly the prescribed pattern for source-level handling of malformed external data \(record absence, quarantine, continue\). The decorator nudge does not apply here because R6 is not in the decorator's suppressible set \(R1/R5\). |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=97a1ef9c4fc676bd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary catching csv.Error from malformed external CSV header parsing — exactly the kind of trust-boundary exception handling the policy permits. The handler does not silently swallow: it records a validation error via ctx.record_validation_error and quarantines the row with a descriptive error message preserving the exception detail. R6 \(exception handling\) at a Source's external-data parse boundary is the textbook legitimate use. The decorator nudge does not apply — R6 is not in the {R1, R5} suppressible set. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=431d28180bca26dd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary case: the code reads from an external Azure blob CSV file, and StopIteration here is the natural end-of-iterator signal indicating an empty file. Catching it to return cleanly \(treating empty input as zero rows\) is meaning-preserving handling of external absence, not fabrication — no synthetic data is invented. The rule R6 \(likely silent exception handling\) is being applied narrowly to the specific iterator-exhaustion signal on external data at the source boundary, which is exactly where defensive handling is permitted by policy. The rationale is site-specific and accurately describes the external-data condition. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=39664fb9a4cb9fa2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a manual iterator pattern using next\(row_source\) inside a while True loop, where StopIteration is the canonical termination signal for the loop. Catching StopIteration here is not defensive error-swallowing — it's the documented control-flow mechanism for manually-driven iterators, structurally distinct from try/except around business logic. The surrounding code shows deliberate per-row error discipline \(csv.Error stops processing, column mismatches quarantine\), and StopIteration handling fits that pattern. This is offensive iteration control, not Tier-3 boundary defense, but the suppression is legitimate at this site. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=ebcdf378c1b17bc6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary case: the code is catching csv.Error from parsing an external Azure blob CSV, which is exactly the kind of malformed external input the trust-boundary policy permits wrapping. The handler does not silently swallow — it records a validation error and stops processing because csv parser state is corrupted, matching CSVSource behaviour. R6 \(presumably the try/except-on-external-parse rule\) at a source plugin's CSV ingest is the canonical defensive-at-boundary pattern. The decorator nudge does not apply: R6 is not in the decorator's suppressible set {R1, R5}, and this is exception handling rather than a subscript/get on a parameter. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_json_array:fp=f7674d7898065886` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is at a genuine Tier-3 boundary: blob_data is bytes read from Azure Blob Storage \(external system\), and decoding with a configured encoding can legitimately fail on malformed external content. The except clause catches the specific UnicodeDecodeError, records the failure via the file-level error path \(quarantine/audit\), and returns — this is exactly the prescribed boundary behavior: validate, record absence/failure, do not fabricate. The exception is narrowly scoped and not a silent swallow. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_json_array:fp=c861bcabbb4691df` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 trust boundary: Azure blob content is external data being parsed via json.loads, and the except clause catches JSONDecodeError plus ValueError \(raised by the _reject_nonfinite_constant hook for malformed numerics\). The handler does not silently swallow — it records a file-level parse error and returns, which is the correct quarantine-at-boundary pattern. The rationale accurately identifies both exception types and ties them to malformed external content. R6 is not in the decorator's suppressible set \(R1/R5\), so the @trust_boundary decorator nudge does not apply. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_jsonl:fp=72e6ca8223767db9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 trust boundary: the code parses raw JSONL lines from an external Azure blob source, and the except clause catches JSONDecodeError plus ValueError from the explicit _reject_nonfinite_constant hook. Both exception types are precisely the malformed-external-data failure modes, and the handler quarantines the row with full audit traceability \(raw line, line number, error message recorded via ctx.record_validation_error\) rather than silently swallowing. R6 \(broad exception\) is not in the decorator's suppressible set {R1, R5}, so the decorator nudge does not apply. The rationale accurately identifies the tier and the failure modes. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_jsonl:fp=501ead0ca19e6205` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary \(Azure blob external data\) catching UnicodeDecodeError from decoding external bytes — a textbook external parse failure that should be quarantined per the Tier-3 quarantine-on-malformed-input policy. The code records the validation error and routes to the configured failure destination rather than silently swallowing. R6 \(likely broad-except or exception-handling rule\) on a specifically-typed UnicodeDecodeError at a source decode boundary is exactly the offensive-at-boundary pattern the policy permits. The rationale accurately identifies the tier and the failure mode. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_validate_and_yield:fp=3c529a5cd371be8f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary \(Azure blob external data\) catching a specific ValueError from field-name normalization, routing it through the proper quarantine path with ctx.record_validation_error. This matches the policy's prescription for sources: validate at the boundary, quarantine failures, record what we got. The except is narrowly scoped to ValueError \(not bare except\), and the recovery path is the documented quarantine mechanism, not silent swallowing. R6 \(presumably narrow-exception or boundary try/except\) suppression is legitimate here. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_validate_and_yield:fp=06c8cb35688214ff` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary catching ValidationError from schema validation on external Azure blob row data. The except handler does exactly what the policy prescribes for source boundaries: records the validation failure in the audit trail \(ctx.record_validation_error\) and quarantines the row for routing, rather than swallowing the error silently. The exception type is narrow \(ValidationError, not bare Exception\), the rule is R6 \(not in the decorator's R1/R5 set\), and the handler is the legitimate Tier-3 validate-and-quarantine pattern at a source plugin's external boundary. |
| `plugins/sources/text_source.py:R6:TextSource:load:fp=75958c4926a6eef7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary: TextSource reads an external file, and UnicodeDecodeError is exactly the kind of malformed-input failure the source layer is supposed to catch, quarantine, and record. The except clause does not silently swallow — it records a parse error via _record_parse_error and yields a quarantined row, which is the policy-prescribed handling for Tier-3 boundary failures. R6 \(broad/silent except\) is legitimately suppressed here because the handler is narrowly scoped to UnicodeDecodeError and produces an audit record rather than a default value. |
| `plugins/sources/text_source.py:R6:TextSource:_validate_and_yield:fp=57eaf7e1d1239212` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 source boundary catching a specific ValidationError from schema validation of external row data — exactly the validate-at-boundary, record-and-quarantine pattern the Data Manifesto prescribes for Source plugins. The except clause is narrow \(ValidationError only, not bare Exception\), records the failure via ctx.record_validation_error \(audit trail fires\), and routes the row to quarantine rather than fabricating a default. The code excerpt independently supports the rationale: this is the canonical Tier-3 source-validation handler. |
| `plugins/infrastructure/pooling/executor.py:R6:PooledExecutor:_execute_batch_locked:fp=d90958e285a74a8e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate trust boundary at the threading/concurrency layer: ThreadPoolExecutor.submit\(\) raises RuntimeError when the pool is shut down concurrently, which is an external-state condition \(the pool's lifecycle\) not a code bug we can prevent by offensive checks. The handler does not silently swallow — it translates into a deterministic TransformResult.error with explicit reason 'shutdown_requested', completes the reserved buffer slot to preserve invariants, and propagates shutdown markers to remaining contexts. This is exactly the offensive-programming style: maximally informative error surfacing while preserving the buffer's submission-order contract. The rationale is site-specific and accurately describes the concurrency invariant being maintained. |
| `plugins/sinks/database_sink.py:R1:DatabaseSink:__init__:fp=4aa464556c62f83d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | os.environ is a genuine Tier-3 external boundary \(process environment from the operator\), and the .get\(\) with empty-string default is meaning-preserving here: absence of the env var is semantically equivalent to 'not set to true', and the immediate .lower\(\) == 'true' comparison gives a deterministic bool. This is not fabrication \(no field is being synthesized into pipeline data\) and not defensive suppression of a code bug — it's the documented pattern for an optional dev-only override. The decorator nudge doesn't apply: the subject is not rooted in a function parameter carrying external data, it's an environment read inside __init__. |
| `telemetry/manager.py:R1:TelemetryManager:_export_loop:fp=30047980e068a453` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged \`.get\(\)\` call is \`queue.Queue.get\(\)\`, a blocking method on the stdlib Queue object that retrieves the next item from the queue — it is not a dict-style defensive \`.get\(\)\` with a default. The R1 rule appears to be a false positive on this name collision. The rationale correctly identifies this distinction in one line, and the excerpt confirms \`self._queue\` is a Queue instance being consumed by a background thread. No defensive-programming concern applies. |
| `telemetry/manager.py:R6:TelemetryManager:_export_loop:fp=05590f539280dce7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a specific exception type \(TelemetryExporterError\) and stores it on self._stored_exception for re-raise on flush\(\), which is the documented mechanism for propagating background-thread exceptions to the main thread. This is not silent swallowing — the exception is preserved and will surface at the next flush/close boundary. The pattern is structurally required because background threads cannot raise into the main thread directly, and the code clearly implements deferred re-raise rather than suppression. R6 \(broad/silent exception handling\) is misapplied here; the rationale accurately describes the deferred-raise mechanism visible in the excerpt. |
| `telemetry/manager.py:R4:TelemetryManager:_export_loop:fp=eef48cd4ffa15e30` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate inverted transport-error pattern in a background export thread. The code uses isinstance defensively to distinguish transport errors \(logged, best-effort per telemetry primacy order\) from programming errors \(stored for re-raise on flush/close, breaking the loop to surface the bug\). This is offensive programming: non-transport exceptions are NOT swallowed — they're captured and re-raised to the main thread, which is the only mechanism a background thread has to surface programming errors. The narrow isinstance check on a known-bounded set of transport error types at an async-thread boundary is appropriate, and the rationale accurately describes the inverted pattern. |
| `telemetry/manager.py:R6:TelemetryManager:handle_event:fp=0a1bb9f3fb968fdf` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except queue.Full at line 363 is not silent error swallowing — it's a control-flow signal for the DROP backpressure mode. queue.Full is the documented, expected signal from put_nowait\(\) that the queue is at capacity, and the handler invokes _drop_oldest_and_enqueue_newest to execute the configured overflow policy. This is offensive programming on an expected concurrency boundary condition: the exception is the API contract for 'queue full', not an error being suppressed. Telemetry is also explicitly best-effort per the primacy-order policy, and the DROP mode is a documented user-configured behavior, not silent recovery from a bug. |
| `telemetry/manager.py:R6:TelemetryManager:_drop_oldest_and_enqueue_newest:fp=7b8c10be497703d4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate offensive-programming pattern at a backpressure boundary: the code attempts put_nowait on a bounded queue and catches queue.Full to account for a documented race \(queue refilled between eviction and re-enqueue\). The except is not swallowing a bug — it converts an unavoidable concurrency race into a counted drop event \(self._events_dropped\) with audit accounting, exactly the bounded behaviour expected at a telemetry queue overflow boundary. The rationale honestly identifies the boundary and the handling is narrow \(specific exception, increment+log+return, no fabrication\). No upward import, no Tier-1/Tier-2 data tampering, and the decorator nudge doesn't apply \(R6 is not in the suppressible set and there's no external parameter\). |
| `telemetry/manager.py:R6:TelemetryManager:_requeue_shutdown_sentinel_or_raise:fp=c7e62874628e94d1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(presumably a silent-except pattern\) is being applied to a tight producer/consumer race-handling loop on an internal bounded queue. The except queue.Empty here is not swallowing an error — it's handling an expected control-flow signal in a bounded-retry sentinel reinsertion algorithm where get_nowait racing with put_nowait can legitimately observe an empty queue between checks. The 'continue' is correct: retry the put. This is a concurrency primitive boundary, not defensive programming on data; letting it crash would break shutdown semantics. The rationale, while terse, accurately names the situation \(bounded retry under queue contention\) and the code excerpt independently supports it. |
| `telemetry/manager.py:R6:TelemetryManager:close:fp=c87058caa78a1776` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on a \`pass\` inside \`except queue.Empty\` during shutdown sentinel delivery. This is a legitimate race-condition handler: the code tries to put a sentinel, catches queue.Full, drains one item, but the drain may race with another consumer leaving the queue empty — in which case retrying the put on the next loop iteration is exactly correct. This is offensive programming around a concurrency primitive, not silent error swallowing: the outer loop bounds retries, and a terminal failure is explicitly logged at line 601. The exception is narrowly typed \(queue.Empty only\) and the recovery action \(loop and retry put\) is the documented correct response to the race. |
| `web/composer/guided/protocol.py:R1:validate_payload:fp=0aaf84e80b0f3a16` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | _NESTED_SHAPES is a system-owned, code-defined mapping \(not external data\) where keys are present only for TurnTypes that have nested shapes. The .get\(turn_type, \(\)\) idiom expresses 'this TurnType has no nested shapes, iterate zero times' — it's a lookup-with-default on a code-controlled table, not defensive suppression of external-data anomalies. The turn_type itself was already validated as a TurnType enum on line 257, so this is offensive code with a deliberate sparse-mapping design, not a fabrication or error-swallowing pattern. |
| `web/catalog/routes.py:R1:get_schema:fp=bc80b0854f0cb34e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` here is on a constant module-level dict \`_PLURAL_TO_SINGULAR\` used as a lookup table where None is the documented sentinel for 'unknown plugin type' — immediately checked and converted to a 404. This is not defensive suppression of a missing field; it is the idiomatic use of dict.get\(\) as a membership-and-fetch operation against a fixed mapping, with explicit handling of the absence case. The rationale's 'Tier 3' framing is slightly off \(the dict is ours, the key is external\), but the pattern is correct: validate the external \`plugin_type\` against an allowlist and raise a meaningful HTTPException on miss. R1's intent doesn't apply to constant-dict membership probes. |
| `web/execution/progress.py:R8:ProgressBroadcaster:subscribe:fp=80dd3c1d05f93c94` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line uses \`setdefault\` on an internal process-local dict \`self._subscribers\` to lazily create the per-run subscriber map on first subscription. This is not defensive handling of external data — it's the idiomatic use of setdefault for lazy initialization of an internal data structure under a lock. The data is Tier-1/internal \(a runtime registry the broadcaster owns\), not Tier-3 input. The rationale accurately describes the lazy-map pattern and the code excerpt supports it. |
| `web/execution/progress.py:R9:ProgressBroadcaster:unsubscribe:fp=d21d1c5a7f779ec1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R9 on \`self._subscribers\[run_id\].pop\(queue, None\)\` inside \`unsubscribe\`. The data here is internal process state \(Tier-1-ish in-memory bookkeeping, not external Tier-3 input\), but the operation is genuinely idempotent by design: unsubscribe is documented as being called from a finally block and may race with cleanup_run or a prior removal. \`pop\(queue, None\)\` here is not swallowing a bug — it encodes the idempotency contract that the docstring explicitly states. The rationale matches the code shape and the policy's offensive-vs-defensive test allows idempotent removal where 'already absent' is a legitimate post-state, not a hidden invariant break. |
| `web/execution/progress.py:R1:ProgressBroadcaster:broadcast:fp=895f7c6d83b0c1d9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` here is on \`self._subscribers\`, a process-local dict mapping run_id to subscriber maps. Absence of a run_id in this dict has a legitimate domain meaning: no subscribers have registered for that run, in which case the broadcaster has nothing to do and returns. This is not Tier-3 external data nor a defensive guard against a bug — it is using dict.get\(\) as the idiomatic way to distinguish 'key present' from 'key absent' where both are valid states. The alternative \(membership check then subscript\) would be equivalent but more verbose. The rationale accurately describes this semantics. |
| `web/execution/progress.py:R6:ProgressBroadcaster:_safe_put:fp=18e58b3e32b2e5fe` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(catching asyncio.QueueFull\) here is a legitimate offensive-programming response to a known backpressure condition on an asyncio.Queue, not silent error swallowing. The except clause does meaningful work: it distinguishes terminal vs non-terminal events, drains the queue to guarantee terminal-event delivery \(an audit-relevant invariant\), and logs the drain via slog.info. The caught exception is a specific, expected control-flow signal from the asyncio.Queue API at an internal concurrency boundary, not a Tier-3 data boundary or a defensive shape-guard. The rationale accurately describes the site-specific invariant being preserved. |
| `web/execution/progress.py:R7:ProgressBroadcaster:_safe_put:fp=4148cfe963eb1f65` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of contextlib.suppress\(QueueEmpty\) in a concurrency race window: the loop checks queue.empty\(\) then calls get_nowait\(\), and between those operations a concurrent consumer could drain the item. This is not Tier-3 external data handling nor defensive programming against our own bugs — it's the standard asyncio idiom for non-blocking queue drain where QueueEmpty is the expected signal that the drain is complete. The suppression is narrow \(single call, specific exception\) and the surrounding logic correctly handles the empty case. R7 \(silent exception suppression\) is genuinely the right tool here; the alternative would be a try/except with identical semantics. |
| `web/execution/progress.py:R7:ProgressBroadcaster:_safe_put:fp=4082e42d8f7ef55f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate concurrency race suppression, not a Tier-3 boundary or defensive-on-our-data case. The queue is an asyncio.Queue shared across coroutines; between the QueueFull exception path entering and the get_nowait\(\) drop-oldest call, another consumer may have drained it, making QueueEmpty a genuinely-expected benign race outcome. The suppression is narrowly scoped to a single get_nowait\(\) call whose only purpose is to free a slot, and the subsequent put_nowait failure is itself handled and logged. R7 \(silent exception suppression\) is correctly justified here because the swallowed condition is semantically a no-op for the algorithm's intent. |
| `web/execution/progress.py:R9:ProgressBroadcaster:cleanup_run:fp=bcf3a714f3cd29f0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R9 appears to flag the silent .pop\(run_id, None\) and .discard\(run_id\) — both idempotent removal operations on process-local dicts/sets that we control. The docstring explains cleanup_run is invoked from a finally block and may race with other cleanup paths \(unsubscribe\), so the run_id may or may not still be present. dict.pop with default and set.discard are the idiomatic idempotent-remove primitives in Python; the alternative \(KeyError-raising del\) would force a try/except that adds no information. This is not Tier-1/2/3 data fabrication — it's internal lifecycle bookkeeping where absence is a legitimate expected state, not a missing field. |
| `web/auth/middleware.py:R6:get_current_user:fp=85ef49ad42821944` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The token comes from the HTTP Authorization header — Tier-3 external data. The unverified JWT parse is explicitly a best-effort extraction of claims for downstream metadata \(iat for refresh chain\), and the actual trust decision happens in auth_provider.authenticate\(\) below. Catching PyJWTError and setting None \(not {}\) preserves the distinction between 'unparseable' and 'parsed but no iat', which matches the fabrication-decision test — absence is recorded as None, not synthesized. This is a legitimate Tier-3 boundary catch on an external library call. |
| `web/composer/recipes.py:R1:get_recipe:fp=2669127d1300bf00` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function's documented contract is \`\`RecipeSpec \| None\`\`; \`\`_RECIPES.get\(name\)\`\` is a membership probe on a system-owned registry, not a defensive default that masks malformed external data. The None return is a first-class signal that callers \(e.g. \`\`apply_recipe\`\`\) act on explicitly by raising \`\`RecipeValidationError\`\`. This is the legitimate \`\`dict.get\`\` as probe pattern, not Tier-3 defensive coercion, and the decorator does not apply \(no external parameter, registry is internal\). |
| `web/composer/recipes.py:R1:apply_recipe:fp=fb7764c2c4816ee2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` here is a membership probe on \`_RECIPES\`, a system-owned process-local registry, immediately followed by an explicit \`if recipe is None\` arm that raises \`RecipeValidationError\` with the available names. This is the offensive-programming pattern: detect the invalid state \(unknown recipe name\) and throw a meaningful error — not a silent default. The \`name\` parameter is Tier-3 but the \`.get\(\)\` operates on the system-owned registry, not on external data, so the @trust_boundary decorator doesn't apply. The rationale accurately describes the code. |
| `web/secrets/server_store.py:R1:ServerSecretStore:has_secret:fp=64afc66c27b37667` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary read: os.environ is external process state, and the .get\(\) is used to probe presence/non-emptiness for a boolean has_secret\(\) check. The docstring documents the deliberate semantics \(reserved names return False, allowlist+env+key gates the True path\), so the suppression is not fabricating data — it's recording absence as False at an external boundary. The decorator nudge does not apply: os.environ is not a function parameter, so @trust_boundary\(source_param=...\) cannot target it. The rationale is brief but site-specific and supported by the surrounding docstring. |
| `web/secrets/server_store.py:R1:ServerSecretStore:get_secret:fp=208ba630bb980dc3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary: os.environ is operating-system-supplied input, not data we control. The .get\(\) call reads an environment variable whose absence is explicitly handled on the next line by raising SecretNotFoundError — absence is recorded as a typed exception, not fabricated into a default value. The code excerpt confirms the rationale: line 92-93 converts the None/empty case into a meaningful error rather than silently substituting. The decorator nudge does not apply because os.environ is module-level state, not a function parameter \(source_param targets named parameters\). |
| `web/secrets/server_store.py:R1:ServerSecretStore:list_secrets:fp=19395c7ca9458241` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | os.environ is a Tier-3 external boundary \(process environment, populated outside our control\). The .get\(\) here is used solely for presence-detection to assign a structural reason code \(env_var_not_set\), not to fabricate a value into the inventory. This matches the policy's guidance for boundary handling: detect absence and record it honestly. The decorator nudge does not apply — the external data is os.environ, not a function parameter, so source_param-based suppression is not the right structural fit. |
| `web/secrets/user_store.py:R1:_fingerprint_key_available:fp=a93da1679d15525e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | os.environ is a Tier-3 external boundary \(deployment environment\), and this function's sole purpose is to probe presence of an env var — the .get\(\) returning None and being coerced to bool\(\) is the meaning-preserving check, not fabrication or defensive suppression of a bug. R1 on os.environ.get\(\) at a presence-check site is exactly the legitimate Tier-3 boundary use. The decorator nudge does not apply: the external data is not a function parameter \(source_param\) but a process-environment read, so @trust_boundary\(source_param=...\) cannot target it. |
| `web/secrets/user_store.py:R6:UserSecretStore:_row_is_resolvable:fp=43dce80c56a84dc0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate probe pattern: _row_is_resolvable's explicit purpose is to test whether a stored secret can be decrypted and return a boolean. SecretDecryptionError is a narrowly-typed exception raised by the immediately-adjacent _decrypt_secret_value method for exactly the conditions being probed \(key rotation, ciphertext corruption, unsupported binary types\). Catching it to return False is not silent error swallowing — it is the documented contract of a resolvability check, and any other exception type still propagates. The catch is narrow, typed, and semantically meaningful. |
| `web/secrets/service.py:R6:WebSecretService:resolve:fp=e201cb7204d089d8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(likely 'specific exception handler that returns None'\) is being applied to a typed exception class \(FingerprintKeyMissingError\) at a well-documented contract boundary: resolve\(\) promises 'unresolvable ref -&gt; None' so the caller's batch-aggregation in resolve_secret_refs can bucket all failures into one SecretResolutionError. The handler is not silently swallowing — it emits a rate-limited deployment breadcrumb specifically so the misconfiguration is visible, then returns None to honor the documented aggregation contract. The sibling handlers \(SecretDecryptionError, SecretNotFoundError\) follow the same typed-exception-to-None pattern, confirming this is a deliberate API shape rather than ad-hoc defensiveness. The rationale is site-specific, names the caller, and explains why letting the exception escape would break batch validation. |
| `web/secrets/service.py:R6:WebSecretService:resolve:fp=2661d9a427c7a309` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code catches a specific typed exception \(SecretDecryptionError\) raised by the server store, emits a rate-limited operator breadcrumb, and returns None to preserve the documented resolve\(\) contract that aggregates unresolvable refs. This is not silent swallowing: the failure class is distinct from the catch-all, the typed error is raised by code we control at a legitimate boundary \(decryption of stored ciphertext, where key rotation/corruption is an operational reality\), and the breadcrumb makes the event visible without breaking the higher-level aggregation contract that the pipeline-validation path relies on. The rationale ties the suppression to a specific failure mode with operator-visible signaling, satisfying auditability. |
| `web/secrets/service.py:R6:WebSecretService:resolve:fp=00a3b8aa94bc1df2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate TOCTOU trust-boundary suppression. The resolve\(\) method's documented contract is to return None for missing secrets so the caller can batch-aggregate failures into a single SecretResolutionError. Catching SecretNotFoundError here and returning None handles the race where a concurrent DELETE or env-var clear lands between probe and fetch — the absence is real at fetch time, and the None return preserves the aggregation contract rather than fabricating a value. The surrounding pattern \(FingerprintKeyMissingError, SecretDecryptionError already returning None with rate-limited operator breadcrumbs\) confirms this is a deliberate boundary design, and check_user_ref_resolvable is documented as the propagating variant for callers that need typed errors. R6 \(silent exception handling\) is not in the decorator's suppressible set {R1, R5}, so the decorator nudge does not apply. |
| `web/execution/routes.py:R1:create_execution_router:websocket_run_progress:fp=877d7cf203d87e44` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged expression is \`queue.get\(\)\` where \`queue\` is an \`asyncio.Queue\` — its \`.get\(\)\` is the async dequeue coroutine, not the defensive dict \`.get\(key, default\)\` pattern the rule targets. This is a true false positive of R1: there is no Tier-3 data access, no silent default, and no fabrication risk. The suppression records an honest reason a future auditor can verify by inspecting the type of \`queue\`. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=0f6db382cb729ec0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except TimeoutError here is not swallowing an error — asyncio.wait_for raises TimeoutError as its normal signal that the queue had no event within the idle window. The handler responds to that signal by re-checking authoritative run status via the audit-backed snapshot loader, and re-raises/closes on real errors \(ValidationError, integrity errors, not-found\). This is offensive use of a control-flow exception, not defensive suppression, and it does not fabricate data — terminal status is read from the source of truth. R6 here is a false positive at a legitimate idle-timeout control point. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=d0744adbecfc8dac` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(silent exception handling\) at this site catches WebSocketDisconnect, which is the framework-defined control-flow signal that the client closed the connection — not an error to recover from. The handler does not swallow the exception silently in a defective sense: it falls through to the finally block which performs proper cleanup \(broadcaster.unsubscribe\). This is a legitimate external boundary \(websocket protocol with remote client, Tier-3\) where disconnect is expected protocol flow, not a bug being hidden. The sibling except blocks correctly log and re-close on genuine errors, showing the author distinguishes expected from unexpected paths. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=322dc8f146c19780` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The handler catches \(ConnectionError, OSError\) at a WebSocket I/O boundary — these are legitimate external transport failures from the network/socket layer, not programming errors. The rationale accurately describes that the catch was narrowed from a broader handler so that programming errors propagate, which aligns with offensive programming at a real trust boundary \(network I/O\). Errors are logged via slog \(appropriate here since this is connection-failure context, not pipeline event data\), and the handler attempts a graceful close. R6 \(broad exception handling\) suppression is justified at this specific network boundary. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=3d78dd97fe37ba1b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a narrow exception handler around a websocket close\(\) call during error cleanup — a legitimate trust boundary at the network I/O edge. The catch is narrowed to \(WebSocketDisconnect, ConnectionError, OSError\), which are the specific transport-level failures that can occur when closing an already-broken websocket; the handler logs the failure via slog \(appropriate here since the original error was already structured-logged and the websocket is being torn down\). R6 \(broad/silent exception handling\) is satisfied by the explicit narrow tuple and the structured error logging — no silent swallowing. This is offensive programming at an external boundary, not defensive suppression of internal bugs. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=f803148b0c50ea15` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches only AuthenticationError — a specific, named exception from the auth provider — at a genuine external trust boundary \(WebSocket JWT validation from a query parameter\). The handler does not swallow the error silently: it closes the WebSocket with a specific code \(4001\) and reason, then returns. This is offensive handling of an expected failure mode at a Tier-3 boundary, not defensive suppression. The rationale accurately describes the narrowing from a broad except to a specific exception type, which is exactly the policy-preferred shape. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=a5b10b0844aaac53` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 typically flags broad exception handling; here the agent has narrowed from \`except Exception\` to \`except ValueError\`, which is the offensive-programming-compatible pattern — catching only the specific signaling exception that \`verify_run_ownership\` raises for malformed run_id input at the websocket trust boundary \(Tier-3 external input\). The handler converts it to a 4004 close, which is the correct boundary behavior for an unknown/invalid run identifier from an external client. This is a legitimate trust-boundary narrow catch, not silent swallowing of unrelated errors. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=738d4af40fe8e2af` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 finding is on the except clause catching _RunStatusNotFoundError after an ownership check has already passed. This is a legitimate TOCTOU race: between verify_run_ownership and the subsequent status load, the run record could be deleted \(or otherwise become unavailable\). Handling that race by closing the websocket with 4004 is the correct boundary behavior for a concurrent-deletion window, not a defensive swallow of a logic bug. The rationale is specific to the site and matches the code structure. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=03f80783c2d49864` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches two specific, named exceptions \(ValidationError from pydantic and an internal _RunStatusIntegrityError\) raised by the accounting/validation helper and converts them into an explicit WebSocket close with code 1011 — this is offensive programming at a protocol boundary, not silent swallowing. The handler does not fabricate data or hide the failure; it surfaces a distinct internal-error close code so the client sees an unambiguous signal rather than a malformed terminal event. R6 \(broad/exception-handling rule\) is appropriately suppressed here because the caught exceptions are narrow and named, and the WebSocket close-code contract requires translation rather than propagation. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=adb32a3f23e3de7b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(broad exception catch\) is suppressed here on a narrow, named tuple of exceptions \(ValidationError, _RunStatusIntegrityError\) at a WebSocket boundary. The handler does not swallow the error silently — it closes the socket with code 1011 and an explicit reason, then breaks the loop. This is offensive at the boundary: a validation/integrity failure on the authoritative run status is converted to an explicit protocol-level internal-error signal rather than fabricating a heartbeat or synthetic terminal event, which aligns with the no-fabrication and audit-honesty principles. The catch is bounded and meaningful, not a defensive sink. |
| `web/composer/audit.py:R6:finish_success:fp=12ea97ccaadab07e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate try/except at the audit-serialization boundary. The function must produce an audit record even when canonical_json encounters non-finite floats or non-serializable types in the tool result; without the fallback, finish_success would raise and bypass the recorder entirely — a genuine audit hole. The caught exceptions \(ValueError, TypeError\) are narrowly scoped to what rfc8785.dumps raises per spec, and the fallback does not silently swallow: it constructs a diagnostic sentinel via build_canonicalization_sentinel that preserves the failure shape for auditors. This preserves the Auditability Standard \(audit fires, crash-on-failure is avoided only because the alternative is losing the record entirely\) rather than violating it. |
| `web/secrets/user_store.py:R1:_compute_fingerprint:fp=35bf7c1e6d3def8f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 boundary read of an environment variable \(deployment configuration external to the process\). The .get\(\) is immediately followed by a check-and-crash via a typed exception \(FingerprintKeyMissingError\), which is exactly the offensive pattern the policy prescribes: detect absence and raise meaningfully rather than fabricate a default. The function is not a candidate for @trust_boundary because os.environ is a module-level external read, not a parameter-rooted dataflow the decorator targets. Rationale is site-specific and accurate. |
| `web/composer/skills/__init__.py:R6:load_deployment_skill:fp=edd927a95648e236` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 filesystem boundary: the function reads an optional deployment skill file, and FileNotFoundError is a meaningful semantic signal \('no skill configured'\) distinct from other OSErrors. The except clause is narrowly scoped to FileNotFoundError only — PermissionError, IsADirectoryError, and other OSErrors propagate as documented. Returning empty string on absence is not fabrication: absence of the file is the configured 'no skill' state, not a missing data field being filled with a default. The docstring explicitly documents the contract. |
| `web/blobs/sniff.py:R6:_detect_from_text:fp=a966bc58c3ffd3b7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 trust boundary: the function is sniffing MIME types from untrusted upload bytes, and csv.Error is exactly the malformed-input signal the parser raises on adversarial content. Catching it to 'continue' to the next delimiter candidate is the correct boundary behavior — it doesn't swallow a bug in our code, it handles the documented failure mode of parsing external data. The except clause is narrow \(csv.Error specifically, not bare Exception\) and the recovery path is meaningful \(try the other delimiter, then fall through to text/plain\). R6 \(broad exception handling\) is not in the decorator's suppressible set {R1, R5}, so the structural alternative does not apply here. |
| `web/secrets/service.py:R6:WebSecretService:check_user_ref_resolvable:fp=c1e9289fa7f9c2d9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The catch is narrowly scoped to SecretNotFoundError, which is the typed signal for 'absent' from the server store — converting it to False is the documented contract of this resolvability probe. FingerprintKeyMissingError and SecretDecryptionError are deliberately allowed to propagate so the HTTP layer can map them to 503/409, preserving deployment-diagnostic information. This is offensive programming around a typed-error API, not silent error swallowing, and the excerpt confirms the rationale matches the code \(the parallel try/except on the user store does the same and is documented in the surrounding comments\). |
| `web/secrets/service.py:R6:WebSecretService:check_user_ref_resolvable:fp=d86f66826e90d353` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except catches a specific typed exception \(SecretNotFoundError\), not a bare or broad Exception, and the rationale documents a real TOCTOU window between has_secret_record\(\) and get_secret\(\) where a concurrent delete must collapse to False per the method's contract. Other exceptions \(FingerprintKeyMissingError, SecretDecryptionError\) are deliberately allowed to propagate, so this is not silent-swallowing — it is offensive narrow handling of a known race. The code excerpt confirms the rationale: the narrow except is load-bearing for the documented 'unresolvable -&gt; False' contract, not a defensive blanket. |
| `web/async_workers.py:R6:run_sync_in_worker:fp=2f6935230e6215d4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The TimeoutError catch here is not defensive error-swallowing; it is the explicit control-flow primitive of asyncio.wait_for, which signals 'the shielded future has not completed within the polling interval'. The continue is the documented mechanism for keeping an event-loop timer active while awaiting the worker, and the shielded future propagates any real exception on its next iteration. This is offensive use of a timeout signal, not a Tier-anything data-handling concern, and no decorator nudge applies \(R6, not R1/R5; no external parameter\). |
| `web/execution/runtime_preflight.py:R1:RuntimePreflightCoordinator:run:fp=2a8571a12863dbd7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(key\) call is on self._inflight, a process-local dict of in-flight asyncio tasks keyed by an internal typed RuntimePreflightKey. This is not external data — it's a presence check on an internal cache where None legitimately means 'no task in flight yet', and the very next line branches on that absence to create the task. This is the canonical idiom for dict-based memoization/dedup, not defensive suppression of an error. No tier confusion, no fabrication, no upward import, no legacy shim. |
| `web/execution/runtime_preflight.py:R1:RuntimePreflightCoordinator:run:_evict:fp=3fdb1527637df68a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(key\)\` here is an identity-guard read of an internal dict \(\`self._inflight\`\) inside a done-callback, not defensive handling of external data. The key may legitimately be absent \(already evicted by cancellation or replaced by a later run\(\)\), and the \`is finished\` comparison is precisely the offensive check that distinguishes 'still ours' from 'replaced/gone'. Using direct subscript would raise KeyError in the normal eviction-race path, which is the expected control flow, not an invariant break. The rationale accurately describes the concurrency invariant and the comment in the code corroborates it. |
| `web/execution/runtime_preflight.py:R9:RuntimePreflightCoordinator:run:_evict:fp=a4269762ca0780b3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an internal concurrency invariant on a process-local dict \(self._inflight\), not Tier-3 external data. The identity guard on line 60 already verifies that the entry is the same task; the pop\(key, None\) is the offensive paired statement that tolerates a benign TOCTOU between the get-identity-check and the pop in a callback context where the dict could have been mutated by another run\(\) invocation. Using pop\(key\) with KeyError would force a try/except around a known-benign race; pop\(key, None\) is the idiomatic GIL-safe expression of 'remove if present'. The rationale accurately describes the concurrency scenario and the code shows the identity guard that makes the suppression scoped rather than blanket-defensive. |
| `web/execution/runtime_preflight.py:R4:RuntimePreflightCoordinator:_capture:fp=cbdc5bec7bc401ee` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The _capture method is a deliberate task-isolation boundary: it wraps a worker coroutine and converts any exception into a RuntimePreflightFailure entry so that the coordinator's asyncio.Task always resolves to a RuntimePreflightEntry rather than propagating an exception to all awaiters of the shielded task. This is offensive programming at a concurrency boundary — failures are captured as first-class result values \(RuntimePreflightFailure wraps exc, preserving the original exception for inspection\), not silently swallowed. The blanket except is structurally necessary because worker\(\) is arbitrary plugin code and any escape would corrupt the coordinator's invariant that cached entries represent terminal outcomes. The rationale accurately describes the code's behavior and the invariant it preserves. |
| `web/execution/fanout_guard.py:R6:_estimate_source_rows:fp=fdf1ab16ee8462d4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a pre-execution fanout estimation function reading external source files \(filesystem I/O on user-provided paths\) — a genuine Tier-3 boundary. The exception types caught are narrowly scoped to actual external-data failure modes \(OSError for filesystem, UnicodeDecodeError for encoding, csv.Error and json.JSONDecodeError for parse failures\), not a blanket except. Returning None to signal 'cardinality unknown' is honest absence-recording per the fabrication test, not silent recovery — the caller can decide how to handle unknown cardinality. The decorator nudge doesn't apply: R6 is not in the decorator's suppressible set {R1, R5}, and the boundary here is filesystem I/O rather than a parameter-rooted external dict. |
| `web/execution/outputs.py:R6:_is_path_in_sink_allowlist:fp=0cedc3792a8a7287` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate filesystem trust-boundary case. Path.resolve\(\) performs a syscall that can raise OSError for symlink loops, missing components, or permission errors — these are external \(filesystem\) conditions, not bugs in our code. The rationale correctly identifies that this is a UI hint function, not the security boundary itself \(the /content endpoint reapplies the check\), so conservatively returning False on resolution failure is the right semantic: an unresolvable path is not in the allowlist. The suppression scope is narrow \(single OSError on a single syscall\) and the rationale is site-specific. |
| `web/blobs/service.py:R6:BlobServiceImpl:finalize_run_output_blobs:_sync:fp=ea2f8e3e7ddf8c75` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(broad exception handling\) at a filesystem/database recovery boundary: the outer except catches a project-defined tuple \(self._PER_BLOB_SUPPRESSED\) to isolate per-blob failures so other blobs in the batch finalize, and the inner except narrows to \(SQLAlchemyError, OSError\) — programmer bugs would not be in that tuple. This is the legitimate use of bounded exception handling at an external I/O boundary \(filesystem unlink + DB update\), not silent swallowing: the failure is recorded by transitioning the row to 'error'. The rationale identifies the boundary correctly and the code shows narrow, named exception classes rather than bare except. |
| `web/blobs/service.py:R6:BlobServiceImpl:finalize_run_output_blobs:_sync:fp=5d74d3cb32617f1c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause narrowly catches SQLAlchemyError and OSError during a best-effort recovery write that transitions a failed blob to 'error' status. The code explicitly documents that programmer bugs \(TypeError, AttributeError, AssertionError\) propagate, and the recovery exception is captured into recovery_exc and appended to the errors list so both causes reach the audit trail rather than being swallowed. This is a legitimate trust-boundary handling of DB/IO faults during compensating cleanup, consistent with offensive-programming policy: the primary operation's inspectability is preserved while bug-class exceptions still crash. |
| `web/blobs/service.py:R6:BlobServiceImpl:copy_blobs_for_fork:fp=ffe8f02b27f08dea` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate cleanup-failure boundary during error recovery from a primary copy failure. The catch is narrowly scoped to \(SQLAlchemyError, OSError\) — programmer bugs \(TypeError, AttributeError, etc.\) still propagate. The failures are not silently swallowed: they are collected and attached as notes on primary_exc with explicit 'RecoveryFailed\[...\]' markers naming the orphan blob ID, making the cleanup failure visible to operators and auditors via the traceback. This matches the documented convention in finalize_run_output_blobs and preserves the original copy failure as the headline exception, satisfying offensive-programming principles at a genuine I/O recovery boundary. |
| `web/sessions/_auto_title.py:R6:maybe_auto_title_session:fp=6eeb90e8c33babf8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches a narrow, named tuple of exceptions \(LiteLLMAPIError, TimeoutError, asyncio.CancelledError\) at a genuine external boundary — an async call to a third-party LLM provider via litellm. This is Tier-3 boundary handling: provider failures are expected operational events for best-effort UI metadata. The failure is not silently swallowed — _record_auto_title_failure\(exc\) emits an operational signal, satisfying the audit/observability requirement. The narrow exception list \(no bare Exception\) and the auxiliary recording call distinguish this from forbidden defensive swallowing. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=b8d3d99b772457b8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) calls traverse a JSON Schema dict produced by Pydantic's model_json_schema\(\). Per the JSON Schema spec, 'properties' and 'required' are conditionally present keys — a schema with no properties legitimately omits 'properties', and a schema with no required fields legitimately omits 'required'. This is standard JSON Schema traversal, not defensive programming against a malformed Tier-2 row: the absent-key case is a well-defined spec state with a clear semantic \({} and \[\] respectively\), not a fabricated default papering over an upstream bug. The inline code comment correctly distinguishes this from forbidden defensive .get\(\) on row data. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_extract_config_fields:fp=35f19820d9e768ad` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code traverses a JSON Schema dict produced by Pydantic's model_json_schema\(\). The 'required' key is conditionally present per the JSON Schema specification — schemas with no required fields legitimately omit the key. This is standard JSON Schema traversal, not defensive programming against our own data: the .get\(\) with \[\] default correctly expresses 'no required fields declared', which is semantically distinct from fabrication. The inline comment at lines 432-434 documents this reasoning, and the same pattern is used for 'properties' on the preceding line. The decorator nudge does not apply: schema is not an external function parameter rooted at a Tier-3 boundary in the three-question sense — it's a structured dict from a Pydantic call — and the suppression is semantically about JSON Schema's optional-key spec, not about untrusted input. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=b3f6fc4a8d272276` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is a Pydantic-generated JSON Schema, which is an external/Tier-3-like structure where JSON Schema legitimately permits 'required' to be absent \(it's an optional keyword meaning 'no required fields'\). Using .get\('required', \[\]\) is meaning-preserving coercion of 'absent' to 'empty set of required fields', which matches JSON Schema semantics — not fabrication. The surrounding code shows offensive handling elsewhere \(direct subscript on $defs lookup to crash on dangling refs\), so the author has correctly distinguished schema-bug cases from schema-optional cases. The rationale is site-specific and identifies the exact JSON Schema invariant being relied upon. |
| `web/composer/state.py:R1:_validate_web_scrape_abuse_contact_not_reserved:fp=cacfb09cb1ee4078` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`http.get\("abuse_contact"\)\` inside a validator that inspects operator-supplied node options — genuine Tier-3 external configuration data. The code already guards the outer \`http\` block with isinstance, and \`abuse_contact\` is documented as optional, so \`.get\(\)\` returning None is the correct way to express 'absent optional field'. This is a legitimate trust-boundary read of an optional sub-key, not fabrication \(None is propagated, not coerced\) and not plugin-return handling. The excerpt is too short to confirm whether the enclosing function is already decorated with @trust_boundary, but the suppression at this site is policy-consistent on its merits. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=93e6d0f366d473f2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code catches ValueError from a contract-config parser and converts it into a structured ValidationEntry with severity 'high' that is appended to the errors list \(and continues to the next node\). This is the validation-aggregation pattern: the function's contract is to collect all schema-contract errors across nodes rather than fail-fast on the first one, which is appropriate for surfacing complete diagnostics to the operator. The exception is not swallowed — it is recorded with the node id and original message preserved via f-string interpolation, satisfying the auditability requirement. R6 \(broad/silent except handling\) does not apply when the exception is converted into a first-class error record consumed by the caller. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=f4ef2614439ba941` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except ValueError here catches a parse error from _consumer_locked_input_set and converts it into a structured ValidationEntry appended to errors with high severity, then continues to the next node. This is the correct pattern for a /validate endpoint: surface per-node configuration errors as structured validation results rather than crashing the whole validation request. The exception is narrowly scoped \(ValueError only\), the error is recorded \(not swallowed\), and the pattern mirrors the immediately-preceding consumer_required parse block. This is offensive-style error surfacing at a validation boundary, not silent recovery. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=5c3102391a7ef9dc` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code catches ValueError from _effective_producer_guarantees parsing and converts it into a structured ValidationEntry error with high severity, plus marks the producer as parse-failed so downstream checks skip it. This is legitimate validation-result accumulation at a configuration-parsing boundary, not silent error swallowing — the failure is surfaced through the errors list which is the function's contract for reporting issues. The exception is specific \(ValueError, not bare Exception\), chained context is preserved in the message, and the producer is tracked to prevent cascading false positives. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=3aff3816428e3590` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except ValueError: continue here is not defensive programming against external data — it's deduplication logic in a validator. The rationale explains that _effective_producer_guarantees \(called earlier in the same function\) already parses the producer and surfaces any ValueError as a ValidationEntry to the operator. Re-raising at line 1465 would duplicate the same diagnostic; skipping Rule A for this pair preserves the audit trail \(the error is already recorded\) while avoiding noise. The continue is a precise short-circuit, not a silent swallow — the diagnostic is recorded elsewhere, satisfying the auditability standard. The rationale is site-specific and references the exact upstream surfacing point. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=ad8e4160808d1d4d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The try/except catches a ValueError from parsing sink contract configuration and converts it into a structured ValidationEntry appended to errors with 'high' severity, then continues to validate other outputs. This is a legitimate validation-gathering pattern at a configuration-parsing boundary — the goal is to collect all schema contract errors and surface them as blocking validation results, not to silently swallow the exception. The error is recorded with the specific ValueError message preserved, satisfying auditability. R6 \(broad/silent exception handling\) does not apply here since the exception is narrowly typed \(ValueError\) and explicitly surfaced. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=0a40378bed99324c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(catching ValueError\) is being suppressed at a /validate endpoint boundary where schema-parse errors from _sink_locked_input_set must be converted into structured ValidationEntry results rather than crashing the validation request. The except clause is narrow \(ValueError only\), preserves the error message in a structured 'high' severity entry, and continues to the next output — this is offensive surfacing via the validation response channel, not silent swallowing. The pattern mirrors the immediately-preceding sink_required parse block, which is internally consistent with the function's contract of aggregating contract-config errors. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=1c4db9634cd49938` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 flags a try/except around _effective_producer_guarantees, which parses contract configuration. The except converts ValueError into a structured ValidationEntry \(errors.append with high severity\) and marks the producer as parse-failed to avoid downstream cascade — this is exactly the pattern of a validator collecting diagnostics rather than crashing. The function's job is to surface contract problems as results, not to propagate exceptions; swallowing here is the offensive-programming outcome \(a meaningful, attributed error record\), not silent recovery. Rationale matches the visible code shape. |
| `web/composer/state.py:R6:_check_schema_contracts:fp=0438e025d4ddc0b3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except ValueError: continue is suppressing a re-raise of a producer parse error that has already been surfaced as a blocking ValidationEntry by _effective_producer_guarantees earlier in the same check. This is deduplication of an already-recorded diagnostic, not silent error swallowing — the audit trail still contains the original error via the prior code path. The rationale is site-specific, names the symmetric Rule A path, and identifies the exact dedup invariant. This is not Tier-3 boundary data, not a layer violation, and not fabrication; R6 \(silent exception handler\) is justified here because the diagnostic is preserved upstream. |
| `web/composer/state.py:R6:CompositionState:validate:fp=d30f04f5c3109e3f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a validation routine that intentionally catches PydanticValidationError to convert it into a structured ValidationEntry for the composer UI. The exception is not silently swallowed — its details are extracted via exc.errors\(\) and surfaced as a high-severity blocking error that prevents malformed trigger.condition values from reaching runtime settings load. This is the legitimate trust-boundary pattern for user-supplied composition data \(Tier 3\): validate, record the failure with detail, and reject. The rule R6 \(try/except suppression\) is correctly applied here because the except clause is doing meaningful work, not recovery. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=9abdc8a826386eea` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is at a genuine Tier-3 boundary: LiteLLM/provider usage objects are external responses with provider-specific shapes \(mapping vs. object vs. absent\). The code already branches on Mapping vs. object form and uses getattr with None defaults \(recording absence, not fabricating values\), which matches the policy's Tier-3 'record what we didn't get as None' guidance. R2 \(likely the getattr-with-default rule\) is not in the decorator's suppressible set \(R1/R5\), so the @trust_boundary decorator nudge does not apply here. The rationale honestly identifies the tier and the provider-shape variability, and the code excerpt independently supports it. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=2bfecea3bd9c907b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`getattr\(usage, "total_tokens", None\)\` where \`usage\` is a provider-supplied response object from LiteLLM/various LLM providers — genuinely Tier-3 external data with provider-specific shapes \(mapping-shaped, object-shaped, or with varying field availability\). Recording \`None\` for absent fields is the policy-correct behavior per the fabrication-decision test: it preserves the fact that the provider did not supply the value rather than fabricating a zero. R2 \(getattr with default\) is not in the decorator's suppressible set {R1, R5}, so the structural alternative does not apply here. The rationale is site-specific and the code shape independently supports the Tier-3 boundary claim. |
| `web/composer/llm_response_parsing.py:R2:_provider_cost_from_response:fp=e68305badf66bd65` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external boundary: LiteLLM/provider response objects are external data with provider-specific shapes \(mapping vs object vs absent\). The function explicitly preserves absence semantics by returning None + NOT_AVAILABLE rather than fabricating a cost — fully compliant with the fabrication-decision test. The getattr on \`usage\` for the non-Mapping branch is the correct shape-tolerant access at the boundary. R2 is not in the decorator's suppressible set \(R1/R5\), so the decorator nudge does not apply; a per-site allowlist is the appropriate mechanism here. |
| `web/composer/llm_response_parsing.py:R2:_response_field:fp=66a71df341185085` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`getattr\(value, field, None\)\` \(R2\) inside \`_response_field\`, a helper that normalizes Tier-3 LiteLLM/provider response objects which may legitimately be either Mapping-shaped or object-shaped depending on provider/SDK version. This is a genuine external-data boundary: provider response shapes are outside our control, and the function's job is precisely to bridge the mapping/object duality. The decorator nudge does not apply because R2 is not in the decorator's suppressible set {R1, R5}, and the value parameter is not named as a Tier-3 source_param in the typical decorator pattern. Returning None on absence is honest recording of absence, not fabrication. |
| `web/composer/llm_response_parsing.py:R2:_json_safe_provider_artifact:fp=d613ce74e27d37d1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary: the function normalizes arbitrary provider response objects from LiteLLM/various LLM providers into JSON-safe form. Provider SDK objects legitimately expose serialization via heterogeneous duck-typed methods \(model_dump for Pydantic, to_dict, dict\), and probing for these methods via getattr is the appropriate boundary-coercion pattern — there is no shared interface to dispatch on. R2 \(getattr with default\) is exactly the rule that applies here, and the suppression is narrowly scoped to a serialization helper at the external-data boundary. The decorator nudge does not apply: the parameter is typed Any \(not a structured external payload bound to a named source_param\), R2 is not in the decorator's suppressible set {R1, R5}, and the pattern is recursive duck-typing rather than the function-scoped Tier-3 envelope the decorator targets. |
| `web/composer/llm_response_parsing.py:R2:_json_safe_provider_artifact:fp=c958b58e1cfbd5e2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding sits inside _json_safe_provider_artifact, a recursive serializer that normalizes arbitrary provider response objects \(LiteLLM/OpenAI/Anthropic SDK objects, pydantic models, dicts, etc.\) into JSON-safe form. The value parameter is genuinely Tier-3 external data of unknown shape, and the getattr probes for model_dump/to_dict/dict are duck-typing across heterogeneous provider SDKs — exactly the kind of shape-discovery the trust boundary permits. R2 \(getattr with default\) is not in the @trust_boundary decorator's suppressible set \(R1/R5 only\), so the decorator nudge does not apply. The rationale honestly identifies the tier and the structural reason for the probe. |
| `web/sessions/routes/messages.py:R6:register_message_routes:send_message:fp=6ffa502bacb1e244` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except TimeoutError catches only the scheduling-bound from asyncio.wait_for, not exceptions from within the auto_title_task itself \(those would re-raise through wait_for and propagate\). The handler's sole action is to cancel the runaway task — no silent swallowing of inner task errors, no fabricated state. The inline comment at L799-804 documents the trust boundary correctly: this is bounded scheduling against an unbounded background coroutine, an offensive-programming pattern \(fail-fast on wait timeout by cancelling\) rather than defensive recovery. R6 \(silent exception handling\) is appropriately suppressed here because the timeout is the expected signal, not an error condition. |
| `web/sessions/routes/messages.py:R7:register_message_routes:send_message:fp=be313c147c5a50be` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate asyncio cancellation idiom, not defensive error-swallowing. The pattern \`with contextlib.suppress\(asyncio.CancelledError\): await asyncio.shield\(...\)\` is the canonical way to let a shielded coroutine complete in the background while the outer task is being cancelled — the suppress only catches the re-raised CancelledError on the cancelling task, and the comment confirms \`raise\` two lines below restores the cancel chain. The shielded operation persists partial llm_calls audit records during client-disconnect, which directly serves the auditability standard \(audit must fire even on teardown\). The scope is tight \(only CancelledError, only around the shielded await\), the rationale is site-specific and technically precise, and no Tier-3 boundary refactor applies here. |
| `web/sessions/routes/messages.py:R7:register_message_routes:send_message:fp=1a85561c51205f0c` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The contextlib.suppress\(asyncio.CancelledError\) here wraps an asyncio.shield\(\) pattern at a cancellation boundary: the shielded _publish_progress must run to completion to record the terminal cancelled state in the progress registry \(an audit-relevant side effect\), while the outer await re-raises CancelledError on the cancelling task. Suppressing CancelledError locally is necessary so the explicit 'raise' two lines below can restore the original cancel chain — this is the documented asyncio idiom for 'shield a critical finalizer during cancellation'. This is not defensive error-swallowing; it's precise control-flow handling of a specific exception type at a structural cancellation boundary, mirroring the already-accepted pattern at L769. The rationale is site-specific and accurately describes the mechanism. |
| `web/sessions/routes/_helpers.py:R2:_litellm_error_detail:fp=6eff26e727be1d1a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`getattr\(exc, 'status_code', None\)\` where \`exc\` is a non-LiteLLM exception coming from arbitrary provider/transport libraries \(a Tier-3 external boundary — LLM provider exceptions\). The code is extracting an optional convention attribute \(\`status_code\`\) that some HTTP/provider exceptions expose and others don't, with strict type-checking on the result. This is the canonical legitimate \`getattr\` use: probing for an optional protocol-style attribute at an external boundary, not papering over a missing attribute on code we own. The decorator nudge doesn't apply: R2 isn't in the suppressible set {R1, R5}, and the subject is an exception object from a third-party stack rather than a function parameter carrying external row/payload data. |
| `web/sessions/routes/_helpers.py:R6:_handle_runtime_preflight_failure:fp=f1af9d80a5652f7d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except catches SQLAlchemyError \(a specific DB driver exception\) around a save_composition_state call — this is a genuine external-system boundary \(database I/O\) where narrow exception handling is appropriate per policy. The handler does not silently swallow: it logs structured error metadata and surfaces the failure into the response body via explicit fields, preserving auditability. The catch is narrowly typed \(not bare Exception\) and scoped to the single I/O call, matching the offensive-programming guidance for trust boundaries. The R6 finding on a typed SQLAlchemyError catch at a DB persistence boundary is a legitimate suppression site. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:recompose:fp=65afef4550e989c5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppression sits in a CancelledError handler that uses contextlib.suppress\(asyncio.CancelledError\) around asyncio.shield\(...\) to ensure llm_call persistence completes even if the request is cancelled mid-flight. This is the documented shielded-publish pattern \(mirrored from send_message\) — the suppression is narrow \(CancelledError only\), targets a specific async cancellation race, and exists to preserve audit-trail completeness during request teardown. R7 \(likely a broad-except/suppress rule\) fires structurally but the pattern is the correct offensive handling of cooperative cancellation at an HTTP boundary, not silent error swallowing. The rationale identifies the boundary nature accurately. |
| `web/composer/tools/blobs.py:R1:_session_blob_lock:fp=1b80b2f85d944ac6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook double-checked locking idiom on a process-local registry dict \(\`_SESSION_BLOB_LOCKS\`\). The \`.get\(session_id\)\` is not defensive handling of external/Tier-3 data — it's the canonical 'lookup-or-create' fast path, where absence is the expected branch that proceeds to acquire the registry mutex and install a new lock. The dict is internal state we control, not external input, so the trust-tier rules don't apply; using \`.get\(\)\` here is semantically correct \(returning None to signal 'not yet installed'\) rather than swallowing an error. The decorator would not apply: \`session_id\` is a str parameter, not an external data structure being subscripted. |
| `web/composer/tools/blobs.py:R1:_session_blob_lock:fp=1572f749e0721c8d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate double-checked locking pattern on a process-local registry dict \(_SESSION_BLOB_LOCKS\). The .get\(\) is not defensive coercion of external data — it is the idiomatic 'check if lock exists, else create under mutex' pattern. The session_id key is intentionally absent on first access; .get\(\) returning None is the expected control-flow signal, not an error to be swallowed. This is neither Tier-3 boundary code \(no external input being suppressed\) nor a missing-field bug; it's the correct primitive for get-or-create. The decorator does not apply \(no external source_param; rule applies to internal registry access\). The rationale text is sparse but the code excerpt independently justifies the suppression. |
| `web/composer/tools/blobs.py:R6:_execute_update_blob:fp=96d73b682ad0a226` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code shows a narrow OSError handler around a rollback attempt \(storage_path.write_bytes\) during an exception-handling path. The original exception \(primary_exc\) is preserved via add_note and re-raised via 'raise' on line 1351 — the OSError from the rollback is not swallowed in a way that hides failure; instead it's attached as a note to the primary exception. This is the correct pattern for rollback-during-cleanup: programmer-bug exceptions \(TypeError etc.\) still propagate because the handler is narrowed to OSError, and the primary exception is still raised. R6 \(exception swallowed without re-raise\) is technically present on the inner OSError but the surrounding semantics preserve auditability. The rationale accurately describes the AST shift and the offensive-programming-compliant narrow handler. |
| `web/composer/tools/blobs.py:R6:_execute_delete_blob:fp=bf8563c850ca9a87` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except OSError block does not swallow the exception — it attaches a forensic note to primary_exc via add_note\(\) describing the rollback failure \(divergence between blob row and storage, manual reconciliation required\), and then the outer handler re-raises primary_exc on line 1487. This is offensive programming at a filesystem boundary: the rollback is best-effort, and its failure is recorded in the audit-relevant exception chain rather than silently dropped. R6 \(exception swallowed without re-raise\) is a false positive here because the analyzer sees no raise inside the inner except but misses that the outer except re-raises after annotation. |
| `web/composer/redaction.py:R1:_build_substitute_provider:provider:fp=849a3ad887180a44` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The walker traverses a \`model_dump\(\)\` dict representation where Optional Pydantic fields surface as literal None values along legitimate typed paths. The \`.get\(name\)\` here is not defensive data-shape guessing on Tier-3 input — it is the walker's documented disposition for absent intermediate Optional arms, and the immediate \`if value is None: continue\` records absence rather than fabricating a default. The surrounding comment explicitly anchors this to the redactor contract: absence of the intermediate is itself the audit fact, no leaf to substitute. This is a legitimate structural traversal on a known schema's nullable arms, not silent error swallowing. |
| `web/app.py:R6:_periodic_orphan_cleanup:fp=2de630b45978d685` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The catch is narrow and intentional: SQLAlchemyError and OSError correspond to transient DB/IO failures from the audit-layer call cancel_all_orphaned_runs, which is exactly the kind of recoverable trust-boundary failure that should not kill a periodic background task. Programmer-bug exceptions \(AttributeError, TypeError, AssertionError\) are deliberately not caught and will terminate the task, surfacing at lifespan shutdown — consistent with the offensive-programming policy. This is not silent swallowing of arbitrary errors and not a Tier-mismatch; the rationale accurately describes the data flow and the failure modes. The rule R6 \(broad-except\) suppression is site-justified at this audit/IO boundary in a long-lived loop. |
| `web/app.py:R1:lifespan:_probe_get:fp=5a8b80ff0e908ab1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt confirms \`_probe_client\` is an \`httpx.AsyncClient\` instance \(line 341\), and \`_probe_client.get\(url\)\` at line 344 is the HTTP GET method, not a dict \`.get\(\)\` defensive read. This is a genuine false positive from a token-based pattern matcher that cannot distinguish \`dict.get\` from \`httpx.AsyncClient.get\`. The suppression does not entrench any defensive-programming antipattern; it only documents that R1's heuristic misfires on a legitimate HTTP client call. |
| `web/app.py:R6:lifespan:fp=961011006d9c0cda` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 external boundary: the code probes the live OpenRouter /models endpoint during lifespan boot, and the except clause is narrowly scoped to httpx.HTTPError and OSError — exactly the transport-level failure modes for an external HTTP call. Programming errors \(NameError, TypeError, etc.\) are deliberately not caught and will still crash boot, which matches offensive-programming policy. The fallback to the bundled litellm catalog is a documented design decision with audit-visible slog.warning recording the failure class, not silent swallowing. R6 \(broad exception suppression\) does not fire on the narrow exception tuple's intent here; the allowlist entry honestly records a trust-boundary wrap. |
| `web/app.py:R7:lifespan:fp=91b3559b942c0040` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is the canonical asyncio task-cancellation idiom: cancel a task, await it, and suppress the CancelledError that the await is guaranteed to raise as a result of our own cancel\(\) call. The suppressed exception is one we deliberately caused one line above; it is not silencing an unrelated error or masking a bug. R7 \(silent exception handling\) is correctly scoped here to exactly CancelledError on a self-cancelled task during lifespan shutdown, which is the documented pattern from the asyncio docs. |
| `web/app.py:R6:_settings_from_env:fp=7a5f5f8bd4a3ac25` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary: os.environ values are external operator-supplied data being parsed at ingest. R6 \(likely silent exception handling\) here catches JSONDecodeError/ValueError to fall back to treating the env var as a raw string, which is the documented contract for JSON-collection fields — a meaning-preserving coercion at the boundary, not fabrication or silent error swallowing. The except clause is narrow \(specific exception types\), the fallback is recording the literal string the operator provided \(not a fabricated default\), and Pydantic downstream will reject type mismatches. Decorator nudge does not apply: R6 is not in the {R1, R5} suppressible set, and the data source is os.environ rather than a function parameter. |
| `web/app.py:R1:create_app:fp=f228499fcfff4222` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on os.environ.get\("WEB_CONCURRENCY", "1"\) — reading an optional environment variable at a Tier-3 boundary \(process environment is external input\). A default of "1" is meaning-preserving for this case: absence semantically means single-worker, and the value is immediately parsed and used to detect a multi-worker misconfiguration. The rationale also doesn't claim this is a function-parameter boundary, and the data isn't rooted at a function parameter, so @trust_boundary doesn't apply. Reading os.environ with a default is the canonical Tier-3 source-coercion pattern. |
| `web/app.py:R4:create_app:_prometheus_metrics:fp=057597c004befd39` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate external-boundary suppression. The /metrics endpoint is an HTTP boundary where exposing a traceback in the response body would be an information-leak defect; the bare except returns a generic 503 and the traceback remains in the process log. The wrapped call is generate_latest\(\) over the Prometheus REGISTRY, which executes third-party/global collector code outside ELSPETH's plugin-ownership contract, so this is not a system-plugin call we should crash on. R4 \(broad exception\) is not in the decorator's suppressible set {R1,R5}, so @trust_boundary is not the structural alternative. The rationale is site-specific and audit-honest about failure being operational, not legal. |
| `web/composer/source_inspection.py:R6:_inspect_jsonl:fp=327fe9a5a3e59ac5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 trust boundary: operator-supplied blob content is being parsed line-by-line as JSONL, and JSONDecodeError on a single malformed line is exactly the expected failure mode at an external-data boundary. The exception is not silently swallowed — failures are counted and surfaced via a warning \('N jsonl line\(s\) failed to parse'\), preserving the 'record what we got' principle. R6 \(exception handling at a boundary\) is not in the decorator's suppressible set {R1, R5}, so the structural alternative does not apply here. The rationale is site-specific and matches the code excerpt. |
| `web/composer/source_inspection.py:R6:_inspect_json:fp=314ea348e863a6f4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary: the function parses operator-supplied blob content sampled from an external source, and json.JSONDecodeError on a truncated 8 KiB peek is an expected, well-understood failure mode. The handler does not fabricate data — it sets loaded=None and records a warning that distinguishes truncation from genuine malformation, preserving auditability. The rationale is site-specific and accurately characterizes both the trust tier and the two failure cases. R6 \(exception handling at external boundary\) is appropriately suppressed here; the decorator nudge does not apply because R6 is not in the {R1, R5} suppressible set. |
| `web/composer/source_inspection.py:R8:_facts_from_objects:fp=03c72d42852801e5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding sits inside a helper that consumes parsed external JSON sample rows — genuine Tier-3 data whose shape is operator-supplied and cannot be statically typed without enumerating all possible schemas. R8 \(presumably a typed-dict / Any-typing rule\) on \`list\[dict\[str, Any\]\]\` is the correct contract for arbitrary external JSON at this boundary; tightening the type would force fabrication of a schema the external source never asserted. The excerpt shows the function performing exactly the kind of shape-tolerant inference appropriate at a Tier-3 boundary \(union-of-keys, per-value type probing\). No legacy-shim or layer-violation concerns apply. |
| `web/composer/source_inspection.py:R5:_declared_field_name:fp=684a0b8827dc898e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is genuine union-arm discrimination on a Union type \`DeclaredFieldSpec = str \| Mapping\[str, Any\]\`, not a defensive shape guard. The two arms have structurally different extraction logic \(string split vs. mapping lookup\), which is precisely the offensive-programming use of isinstance — typed dispatch on a sum type. R5 \(isinstance shape guard at boundary\) is misfiring here; this is type-directed control flow that cannot be expressed otherwise without match/case \(which would have the same isinstance semantics\). The decorator nudge does not apply: the function does not appear to be a Tier-3 external boundary, and the parameter is a typed union, not raw external data. |
| `web/execution/validation.py:R1:validate_pipeline:fp=0c7d6399be7e8ecd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows iteration over user-authored sink options from pipeline YAML \(Tier-3 external configuration\), probing for either 'path' or 'file' keys which are mutually exclusive and both optional. The .get\(\) correctly returns None when the key is absent, and the immediate \`if value is not None\` guard skips the validation for that key — this is meaning-preserving absence handling at a trust boundary, not fabrication or silent error swallowing. The two-key probe pattern is a legitimate boundary check on external config. I cannot see the function's decorators in the +-30 line excerpt, so I do not nudge toward @trust_boundary here. |
| `web/sessions/routes/sessions.py:R6:register_session_routes:fork_from_message:fp=7db495b3c8fcc96a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R6 finding flags a narrow exception catch during cleanup after a BlobQuotaExceededError. The catch is deliberately narrowed to \(SQLAlchemyError, OSError\) — recoverable IO/DB failures — so that programmer bugs \(AttributeError, TypeError\) still propagate and crash. The cleanup failure is attached as a note on the propagating HTTPException, preserving the audit trail rather than masking the original error. This is offensive programming applied correctly: the narrow catch is a deliberate recovery boundary for a specific known-recoverable failure class, not silent error swallowing, and the rationale matches the code's actual structure. |
| `web/sessions/routes/sessions.py:R6:register_session_routes:fork_from_message:fp=77a40eab4296aab8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a narrow except clause catching \(SQLAlchemyError, OSError\) around a cleanup operation \(archive_session\) inside a broader except block that re-raises the original exception. The pattern explicitly mirrors the documented RecoveryFailed convention: cleanup failures must not mask the primary exception, and the catch is narrowed to specific I/O/DB error types so programmer bugs propagate. The original exception is preserved via bare \`raise\` and the cleanup failure is attached as a note for operator visibility — this is offensive programming around a genuine rollback boundary, not silent swallowing. R6 \(broad/recovery catch\) is legitimately scoped here. |
| `web/composer/tools/sources.py:R1:_resolve_source_blob:fp=5b29016c3826c2c7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate dict lookup pattern, not defensive Tier-2 field access. \`_MIME_TO_SOURCE\` is a constant lookup table; \`.get\(\)\` returning None is the documented 'unknown MIME type' signal, and the very next lines branch on None to produce a typed failure result naming the unsupported MIME. This is offensive programming with an explicit error message, not silent suppression. The subject is not rooted at an external function parameter \(it's an in-module constant table\), so the @trust_boundary decorator doesn't apply. |
| `web/composer/tools/sources.py:R5:_execute_set_source_from_blob:fp=b9536a22b9e37eda` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is sum-type discrimination on a return value typed as \`\`_ResolvedSourceBlob \| ToolResult\`\`, not a defensive shape-guard on external data. This is the encouraged offensive pattern for narrowing a union return: propagate the failure variant \(ToolResult\), unpack the success variant. R5's defensive-pattern rationale doesn't apply because the type is internally controlled and the discrimination is exhaustive over the declared union. The decorator nudge does not apply: \`\`resolved\`\` is not rooted at an external parameter, it's the return of an internal helper. |
| `web/composer/tools/sources.py:R6:_execute_inspect_source:fp=88b260427087c8ec` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R6 \(likely an exception-handling rule\) on a try/except around UUID\(\) parsing of an LLM-tool-call-supplied blob_id. This is a legitimate Tier-3 boundary: blob_id arrives from the composer model's tool call, UUID\(\) raises ValueError on unparseable input, and the except branch records the absence honestly \(blob_uuid = None\) plus emits a structured warning surfaced in the response — no fabrication, no silent swallow. R6 is not in the decorator's suppressible set {R1, R5}, so the @trust_boundary decorator does not apply here. The rationale is site-specific and the code excerpt independently supports it. |
| `web/composer/tools/sessions.py:R5:_execute_apply_pipeline_recipe:fp=6fb832f3172fdeb1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is offensive programming, not defensive: the code explicitly enumerates the two legitimate shapes \(None or Mapping\) from a system-code contract and raises AssertionError with a detailed diagnostic on any other type. This is exactly the 'detect invalid state and throw a meaningful error' pattern the policy encourages. The data is Tier-2 system code \(a ToolResult.data returned by another internal handler\), not Tier-3 LLM input, so the @trust_boundary decorator is inappropriate. R5 is reported on a shape discrimination that gates a crash, not a silent recovery. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=fb7e9f0ac5e38ba3` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks here are sum-type dispatch over ast.AST node variants produced by ast.walk, not defensive type guards on untrusted data. Filtering Subscript/Name/Constant/Call nodes is the canonical Python AST traversal pattern — there is no other way to discriminate node kinds from a walk. This is offensive structural matching, not defensive coercion, and R5 is misfiring on a legitimate use. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=1988212271415914` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks are discriminating AST node variants \(Subscript with Name value, Constant slice\) while walking a parsed AST — this is structural type discrimination on a sum-type-like tree, not a defensive shape guard on external Tier-3 data. The Python ast module legitimately returns heterogeneous node types and isinstance is the idiomatic way to narrow them. This is offensive programming \(positive identification of the desired node shape\), not the forbidden defensive pattern R5 targets. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=42dc2828b0e57306` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks here are discriminating AST node variants from ast.walk\(\) — classic offensive type-tagged discrimination on a sum type, not defensive shape-guarding of untrusted data. ast.Subscript.slice can legitimately be ast.Constant, ast.Name, ast.BinOp, etc.; the code is selecting the literal-constant case to extract the string field name. This is the correct idiom for walking a Python AST and is not the defensive pattern R5 targets. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=d8ccc95d7f3fafec` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of isinstance for AST node-type discrimination, not a defensive shape guard on external data. The code walks a parsed AST and must distinguish ast.Subscript/ast.Constant/ast.Name node variants — that is exactly what isinstance is for in Python's AST API, where node types are a sum type. The narrowing of node.slice.value to str ensures only string-keyed row references are collected, which is correct type-directed dispatch. R5's intent is to flag defensive shape-guarding of trusted data, not to forbid sum-type discrimination on ast nodes. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=3cf8de969f916272` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check operates on AST nodes from ast.walk, where node-type discrimination is the standard and correct idiom — these are not Tier-2 row values nor Tier-3 external data, but Python AST objects whose polymorphic nature requires type dispatch. R5 \(isinstance shape guard\) is structurally inapplicable here: there is no trust-tier coercion happening, just AST visitor pattern matching. The decorator nudge does not apply — the parameter is a string \`condition\`, not an external dict, and the isinstance targets are AST nodes derived from parsing, not from an external data parameter. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=788462ae239e2501` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of isinstance for AST node discrimination, not a defensive shape guard on Tier-3 data. The code walks an ast.parse tree where each node has a polymorphic type by design — distinguishing ast.Attribute from other ast.expr subtypes is the canonical way to match \`row.get\(...\)\` call shapes. This is offensive pattern-matching on a closed, well-defined type hierarchy \(the ast module\), not suppression of an error. The rationale accurately describes the code's purpose. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=8218c1e27d444b5d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is doing AST node-type discrimination while walking a parsed Python AST — this is the canonical legitimate use of isinstance for structural pattern-matching on a sum type \(ast.AST node variants\), not a defensive shape-guard on Tier-2/3 data. The check distinguishes \`\`row.get\(k\)\`\` from \`\`obj.row.get\(k\)\`\` by requiring the receiver be an ast.Name, which is necessary semantic discrimination, not error suppression. R5 \(isinstance shape guard at boundary\) does not apply: ast.walk yields heterogeneous node types and isinstance is the standard idiom for narrowing them. No fabrication, no upward import, no legacy shim. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=2780ab37f5f2d489` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is AST node discrimination, not defensive programming on row data. The \`isinstance\(node.args\[0\], ast.Constant\)\` check is the canonical Python pattern for narrowing AST node types — the code is walking a parsed AST and must distinguish literal-argument \`.get\(\)\` calls from variable-argument ones to extract referenced field names. R5 targets shape-guards on Tier-3 data; here the subject is an \`ast.AST\` node from \`ast.parse\(\)\` on code we control, and the isinstance is performing legitimate type discrimination on a sum type. No external data is being defensively probed. |
| `web/composer/tools/generation.py:R5:_row_fields_referenced_by_condition:fp=9481bb581f0a388f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is AST node-type discrimination, not a defensive shape guard on Tier-2/3 data. The code walks a parsed AST and must distinguish ast.Constant nodes whose .value is a str from those holding ints/floats/None — isinstance is the canonical Python idiom for AST visitor dispatch, and the policy's R5 prohibition targets defensive shape-checking of pipeline data, not structural pattern-matching on heterogeneous AST union types. Letting it crash isn't an option because ast.Constant.value is genuinely polymorphic by language design. The rationale correctly identifies this as field discrimination on a sum type. |
| `web/composer/tools/generation.py:R6:_gate_expression_type_diagnostics_for_observed_csv:fp=1a4634d0d4e540a1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(exception handling\) is being used here to convert a specific, typed exception \(ExpressionEvaluationError\) from evaluating an LLM-authored gate expression against a sampled CSV row into a structured _blocking_diagnostic that becomes the audit record. This is not silent swallowing: the exception type is narrow, the original error text is preserved in the diagnostic message, and the diagnostic carries an evidence_locator and suggested_repair. The gate condition is LLM-authored \(Tier-3\) and the row data is sampled external CSV \(Tier-3\), so catching a typed evaluation failure at this boundary to produce a diagnostic is appropriate offensive-at-the-boundary behavior. The decorator nudge does not apply: R6 is not in the {R1, R5} suppressible set. |
| `web/composer/tools/generation.py:R1:_numeric_aggregation_diagnostics_for_observed_csv:fp=f179b12ee165ade2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` is on \`inferred_types\`, a Mapping we computed ourselves from sampled CSV data — this is internal computed state, not external Tier-3 data. A missing key legitimately means 'no inference signal for this column', and the None return is propagated verbatim into the diagnostic's evidence_locator as honest absence \(not fabrication\). Direct subscript would crash on a legitimate 'no signal' case, which is the wrong semantics here; the \`.get\(\)\` faithfully represents absence. The rationale matches the code shape and the fabrication test is satisfied \(None recorded as None\). |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=0945f207a98cbbff` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`source.options.get\("blob_ref"\)\` where \`source.options\` is LLM-authored Tier-3 data passed through \`SourceSpec.options\` \(Mapping\[str, Any\]\). The \`blob_ref\` key is genuinely schema-optional — present for blob-backed sources, absent for path-based sources — and the function's documented contract is to abstain \(return empty diagnostics\) when absent. This matches the Tier-3 boundary read pattern with honest absence handling \(None propagates to early return, no fabrication\). However, this is a candidate for the @trust_boundary decorator structurally; I'm accepting the per-line entry because the external data is reached via \`state.source.options\` rather than a direct function parameter, so the decorator's source_param mechanism does not cleanly apply here. |
| `web/composer/tools/generation.py:R5:compute_proof_diagnostics:fp=2e46bb891c34ebf7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is an R5 isinstance check on the value at source.options\['schema'\], which the comment correctly identifies as unstructured LLM-authored content within an otherwise Tier-1 options mapping. The isinstance\(schema, Mapping\) guard is an offensive shape probe at a genuine trust boundary — it discriminates whether the diagnostic applies, rather than swallowing an error. The rationale is site-specific and accurately describes the narrow scope. The decorator nudge does not apply: source.options is not a function parameter rooted in an external call signature, and the function's broader inputs are Tier-1. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=157afff4c5b89efd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows \`schema\` is extracted from \`source.options\` where the comment notes the value at 'schema' is unstructured/LLM-authored — Tier-3 data. The \`.get\('fields'\) or \(\)\` normalizes an absent or falsy value to an empty tuple, which is immediately shape-checked by \`isinstance\(declared, \(list, tuple\)\)\` at line 1219. This is a legitimate Tier-3 boundary normalization pattern where the rationale honestly identifies the trust tier and the downstream guard. The function \`compute_proof_diagnostics\` does not appear to take \`schema\` as a named parameter \(it derives it from \`source.options\`\), so the @trust_boundary decorator's source_param mechanism would not cleanly apply here. |
| `web/composer/tools/generation.py:R5:compute_proof_diagnostics:fp=69025a61f1c28d9f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is an R5 isinstance check on \`declared\`, which is derived from \`schema.get\('fields'\)\` where \`schema\` itself is an unstructured/LLM-authored value pulled from \`source.options\`. The rationale correctly identifies this as a Tier-3 shape probe on operator/LLM-supplied configuration content — the isinstance gate exists precisely to narrow before iterating, which is the legitimate trust-boundary pattern. The decorator nudge does not apply cleanly here because the external data is not rooted at a function parameter but at \`source.options\`, an attribute traversal, so the function-scoped \`source_param\` mechanism would not bind. Per-line suppression is the appropriate channel. |
| `web/execution/service.py:R9:ExecutionServiceImpl:cleanup_session_lock:fp=c58de878b78c532e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppression is on \`dict.pop\(key, None\)\` for an internal process-local dict \`_session_locks\`, not external data. The default-None is the idiomatic non-raising removal pattern, and the rationale explains the legitimate invariant: cleanup is unconditional but a lock entry only exists if execute\(\) ran. This is the documented 'remove-if-present' semantic of dict.pop, not defensive masking of a bug — the alternative \(KeyError on never-executed sessions\) would be incorrect behaviour, not better diagnostics. Site-specific rationale, no fabrication, no tier confusion, no layer issue. |
| `web/execution/service.py:R5:ExecutionServiceImpl:_execute_locked:fp=e0c1cd46e0b80ba5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here discriminates arms of an explicitly-typed sum-type return \(CompositionState \| InterpretationReviewPending\) from materialize_state_for_execution\(\). This is the canonical Python idiom for union-type dispatch, not a defensive shape-guard against malformed external data — it's offensive programming on a typed internal API. R5's prohibition targets isinstance used to paper over uncertain types from untrusted data; here the type is fully known by construction and both arms are handled explicitly \(pending raises with telemetry, resolved continues\). The decorator nudge does not apply: this is not a Tier-3 boundary, the data is internal, and there is no external source_param. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=d458fff4dd2be32e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\('path'/'file'\) call is on output.options, which represents user-authored configuration submitted to the /execute endpoint — Tier-3 external data at a web request trust boundary. The code is explicitly enforcing a path allowlist before pipeline execution, treating absence \(None\) as 'no path supplied, skip check' rather than fabricating a value, which is the correct boundary handling. The keys 'path' and 'file' are genuinely optional across sink plugins, so .get\(\) is the honest expression of optionality here. The decorator nudge does not cleanly apply because the external data is reached via composition_state.outputs\[i\].options rather than rooted at a single named parameter of this method. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_execute_locked:fp=b4fdef35d6621b71` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate offensive-programming pattern at an error-recovery boundary: the cleanup path attempts a best-effort audit status update while an original exception is in flight \(the \`raise\` on line 703 re-raises it\). The rationale correctly narrows from bare Exception to \(SQLAlchemyError, OSError\) — the two expected failure modes of a DB write — so programmer bugs in update_run_status still propagate. The slog records only exc_class \(not str\) to avoid leaking DB credentials through SQLAlchemy's __cause__ chain, which aligns with the auditability + safety concerns. The decorator nudge does not apply: this is not a Tier-3 input boundary, R6 is not in the decorator's suppressible set, and the caught exceptions come from our own session service, not external parameters. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_run_pipeline:fp=23e49ac7e2c8a1c4` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate use of .get\(\) as a lookup on a code-controlled mapping dict, not defensive data access. The dict _SCOPE_TO_AUDIT_SOURCE is defined locally two lines above and contains a known finite enumeration. The .get\(\) pattern is paired with an explicit None-check that raises ValueError with an informative message — this is offensive programming \(proactively detecting an unmapped scope and crashing meaningfully\), not defensive suppression. The subject is not external Tier-3 data; rs.scope is a typed field on a ResolvedSecret returned by code we control, and the lookup target is a static literal dict. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_run_pipeline:_safe_broadcast:fp=6ae11216cb6427ab` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate narrow catch of a specific infrastructure failure mode: call_soon_threadsafe raises RuntimeError when the event loop is closed during shutdown. The code correctly catches only RuntimeError \(not bare Exception\), logs the exception class name rather than swallowing silently, and the surrounding comments explicitly document why broader bugs \(TypeError etc.\) are allowed to crash. Progress broadcasting is best-effort telemetry, not audit — the Landscape primacy order is preserved. The decorator does not apply \(R6, not R1/R5; no external parameter\). |
| `web/execution/service.py:R6:ExecutionServiceImpl:_run_pipeline:fp=59949748426d05a0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | GracefulShutdownError is a typed control-flow signal raised by the orchestrator to indicate a cooperative cancellation; catching it here is not silent exception swallowing but a structured handoff that finalizes output blobs and records cancellation metrics \(rows_processed, rows_succeeded, etc.\) onto the audit trail via update_run_status. This is offensive programming — the exception carries explicit accounting fields that are propagated to the session record, not discarded. The handler is narrow \(named exception class, not bare except\) and the rationale honestly identifies the control-flow contract. Fingerprint drift is explained by an unrelated import-gate change. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_run_pipeline:fp=fc0b10cc4927e3e0` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R6 \(narrow except clause around an audit-system call\) is correctly applied here: the probe catches only SQLAlchemyError and OSError, which are genuine audit-infrastructure degradation modes \(DB connectivity, filesystem\), while ValueError — which signals Tier-1 audit-data corruption per get_run's documented raise contract — is deliberately allowed to propagate and crash. This matches the policy's distinction between infrastructure faults at a boundary \(wrap narrowly\) and Tier-1 invariant violations \(must crash\). The inline comment block independently justifies the tier reasoning and ties the slog usage to the audit-degradation exception in the logging primacy policy. The decorator nudge does not apply: this is not an R1/R5 finding on an external-parameter subject. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_run_pipeline:fp=f75d502d14f8d197` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows a narrow except clause catching \(SQLAlchemyError, OSError\) around a status-update operation inside an outer exception handler — this is the canonical narrow-catch pattern for a database/IO trust boundary, not blanket exception swallowing. The handler logs class names only \(sanitization rationale is sound: avoiding SQL bound parameters and credential leakage from str\(status_err\)\), and the comment explicitly notes that programmer bugs propagate. The catch is appropriately narrow to a transient infrastructure failure boundary, and the rationale documents the fingerprint rotation chain tied to specific upstream changes, making this a site-specific audit record rather than boilerplate. |
| `web/execution/service.py:R9:ExecutionServiceImpl:_run_pipeline:fp=4112dae4da424dc9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .pop\(run_id, None\) in the finally block is genuinely idempotent cleanup: the run_id may or may not have been registered in _shutdown_events depending on how far execution progressed before reaching finally, and discarding a missing key is the correct semantic \(not fabrication, not silent error-swallowing on Tier-1/2 data\). This is process-local state, not external data, and pop-with-default is the idiomatic 'discard if present' pattern rather than defensive .get\(\) suppression. The rationale honestly identifies idempotent shutdown cleanup and explains the fingerprint shift via unrelated import changes. |
| `web/composer/service.py:R8:_apply_openrouter_app_identity:fp=9c37dc8cf6aa2386` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is outbound-request construction, not inbound data handling. The setdefault calls fill ELSPETH's own app-attribution headers on a Tier-3 external API call \(OpenRouter via LiteLLM\), preserving any caller-supplied values. Absence is the expected case — most callers don't pass extra_headers — so setdefault is meaning-adding default population at an outbound boundary, not silent coercion of inbound Tier-3 data into the pipeline. The fabrication test doesn't apply: these headers are ELSPETH's own identity assertions, not inferred values about an external system's data. |
| `web/composer/service.py:R8:_apply_openrouter_app_identity:fp=c28f5f9ec51d0f9a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is setdefault on an outbound headers dict being constructed for an external API call — not defensive reading of inbound external data. The pattern adds ELSPETH's app-attribution defaults while preserving caller overrides, which is meaning-adding outbound construction, not fabrication of audit-trail values. R8 \(setdefault\) here is at the outbound Tier-3 boundary where coercion/default-fill is explicitly permitted by policy. The decorator nudge doesn't apply: this isn't R1/R5, and the subject isn't an inbound external-data parameter. |
| `web/composer/service.py:R8:_apply_openrouter_app_identity:fp=5e18582300fe4db8` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is outbound header construction for an external API call, not inbound data handling. setdefault here adds ELSPETH's app-attribution defaults only when the caller did not supply them — caller-supplied values win, which is the correct precedence semantic. The 'absent' case is the normal path \(callers typically don't pass extra_headers\), so this is meaning-adding default population at an outbound Tier-3 boundary, not silent coercion of inbound data or fabrication of audit fields. The rationale accurately characterizes the data flow shown in the excerpt. |
| `web/composer/tool_batch.py:R4:run_tool_batch:fp=54356b4332bf5c63` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The broad except wraps an outbound LLM call \(request_advisor_hint\), which is a Tier-3 external boundary — provider/network failures are exactly the unpredictable failure modes that justify catch-and-convert at trust boundaries. The handler converts the failure into a structured tool-result error returned to the composer LLM, and the rationale notes the inner ComposerLLMCall audit record was already fired in the call's finally block, preserving the audit trail. This is the documented pattern for outbound Tier-3 calls \(record absence/failure, surface it as data\) rather than silent swallowing. R4 is not in the decorator-suppressible set {R1, R5}, so the decorator nudge does not apply. |
| `web/composer/tool_batch.py:R4:run_tool_batch:fp=1498557d416fbe12` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The broad except is the plugin-crash isolation boundary in the web server context: Tier-1 AuditIntegrityError and the narrow ToolArgumentError are re-raised by explicit handlers immediately above, so only genuine plugin bugs reach this catch. The excerpt shows the exception is converted into a ComposerPluginCrashError rather than swallowed silently, which preserves the crash signal while allowing the web layer to persist accumulated state — a documented web-server-specific deviation from the CLI 'abort the run' behavior. R4 \(broad-except\) is not in the decorator's suppressible set {R1, R5}, so the structural alternative does not apply. The rationale is site-specific, names the actual narrow-handler ordering visible in the excerpt, and is auditable. |
| `web/composer/service.py:R2:ComposerServiceImpl:_call_llm:fp=d6a5623c6b049c50` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The getattr\(\) is applied to a LiteLLM exception object — a Tier-3 external boundary where the third-party exception class is not under our control and the status_code attribute is not guaranteed across LiteLLM versions or underlying provider error types. Recording None when the provider exception doesn't carry a status code is meaning-preserving \(absence is recorded as absence, not fabricated to 0\), and the value flows into a structured error for the caller. R2 \(getattr with default\) is not in the decorator's suppressible set \(R1/R5\), and the subject is an exception object rather than a function parameter, so @trust_boundary doesn't apply. This is a legitimate trust-boundary suppression on a third-party exception surface. |
| `web/sessions/service.py:R6:SessionServiceImpl:archive_session:_sync:fp=69d4c22527c9a7f2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate filesystem trust boundary in a rollback path. The primary exception is captured and re-raised; the OSError catch on staged_blob_dir.rename only annotates primary_exc with a recovery-failure note via add_note, then raise \(line 2245\) propagates the original exception. Neither failure is swallowed — both surface to the operator. Catching OSError narrowly at a filesystem-mutation call during compensating action is exactly the offensive-programming-compatible pattern \(record what happened, preserve causal chain, re-raise primary\). |
| `web/sessions/service.py:R7:SessionServiceImpl:archive_session:_sync:fp=8aadfa2550cfc8c9` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate filesystem-boundary suppression. The rmdir targets a parent quarantine root that is shared across sessions; OSError here \(ENOTEMPTY because peer sessions still occupy the root, or transient race under concurrent purge\) is an expected, semantically-meaningless condition that does not affect the archive outcome already committed above. The suppression scope is tight \(single rmdir call, OSError only\), the rationale identifies the specific failure modes, and the alternative — propagating — would falsely signal archive failure after the staged dir was already removed. R7 \(silent exception suppression\) at an external-filesystem boundary on a best-effort cleanup is the textbook acceptable case. |
