# Reaudit report

## Summary

| Divergence | Count |
| --- | --- |
| SOURCE_EXCERPT_REJECTED | 0 |
| FUTURE_DATED_ENTRY | 0 |
| JUDGE_CALL_FAILED | 0 |
| JUDGE_CLASSIFICATION_FAILED | 0 |
| AMBIGUOUS_FINDING_MATCH | 0 |
| WAS_ACCEPTED_NOW_BLOCKED | 0 |
| PRE_JUDGE_FRESH_BLOCK | 272 |
| OVERRIDE_NO_LONGER_NEEDED | 0 |
| ENTRY_OBSOLETE | 0 |
| OVERRIDE_STILL_NEEDED | 0 |
| PRE_JUDGE_FRESH_ACCEPT | 43 |
| STILL_AGREES | 0 |
| _entries dispatched_ | 315 / 315 |
| _entries skipped by rule_filter_ | 0 |
| _judge calls attempted_ | 315 |
| _prompt tokens_ | total=3009843; cached=2504150; uncached=505693 |

## PRE_JUDGE_FRESH_BLOCK (272)

| Entry | Original Verdict | Fresh Verdict | Fresh Model | Cause | Notes |
| --- | --- | --- | --- | --- | --- |
| `contracts/schema_contract_factory.py:R1:create_contract_from_config:fp=caa0070eb7edad8e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) here is being used as a control-flow primitive with a non-None default \(fd.name\) — this is exactly the fabrication/silent-fallback pattern the policy forbids: if a normalized name unexpectedly lacks a reverse mapping, the code silently substitutes the normalized name as if it were the original, masking a mapping invariant break. The data is first-party \(normalized_to_original was just constructed two lines above from field_resolution\), not Tier-3 external data, so no trust-boundary exemption applies. The rationale 'intentional identity fallback' describes the behaviour but does not justify why silent identity substitution is correct rather than evidence of a bug — if field_resolution is provided, every fd.name should map; if it isn't, the branch shouldn't run. The fix is structural \(use dict\[fd.name\] and let it KeyError, or explicitly branch on whether field_resolution was provided\), not an allowlist entry. |
| `contracts/tier_registry.py:R5:_register_with_module_prefix:fp=18d4ee87363f8490` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is on \`cls.__module__\`, a CPython class attribute. Python's data model guarantees \`__module__\` is a string \(or None in pathological cases\) — this is a first-party type contract, not a Tier-3 external boundary. The decorator receives \`cls\` from \`@tier_1_error\` application sites within ELSPETH's own codebase \(already gated by \`_module_is_allowed\(caller_module\)\` above\), not from external/untrusted input. Guarding \`__module__\`'s shape here is exactly the defensive isinstance pattern R5 forbids: if a class somehow has a non-string \`__module__\`, that is a framework-level invariant break that should crash on the downstream \`.strip\(\)\` or \`_module_is_allowed\` call with a clear error, not be pre-validated. The rationale's invocation of 'Tier-1 ownership' describes what the surrounding code does, but does not establish that \`cls.__module__\` is Tier-3 data needing a boundary guard. |
| `contracts/plugin_context.py:R1:PluginContext:record_call:fp=8f7477d482ca1864` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is at line 316 inside record_call\(\), which takes response_data as a parameter carrying an external LLM response snapshot — a Tier-3 external-data boundary. R1 is in the decorator's suppressible set, and the subject response_snapshot.get\("usage"\) is rooted at the response_data parameter \(assigned to response_snapshot at line 305\). All three decorator conditions are met, so the correct remediation is @trust_boundary\(tier=3, source_param='response_data', suppresses=\('R1',\), ...\) on record_call, not a per-line allowlist entry. |
| `core/events.py:R1:EventBus:emit:fp=6f513a170f117689` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an internal EventBus dispatching to an in-process subscribers dict that we control — not a Tier-3 boundary at all. The rationale 'Optional field access at trust boundary' is misapplied: there is no external data here, and \`self._subscribers\` is first-party state. The correct pattern is direct access with a deliberate absence check \(e.g., \`self._subscribers.get\(type\(event\)\)\` is still .get, but the honest fix is to either pre-populate the dict on subscribe and access directly, or make the absence-as-no-subscribers contract explicit in code, not suppress R1\). The decorator does not apply \(no external parameter; R1 subject is not rooted at a parameter\). Rationale is shallow boilerplate that does not address R1's concern at this site. |
| `core/dag/graph.py:R6:ExecutionGraph:warn_divert_coalesce_interactions:fp=ea839197606998d2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale \("DIVERT edge may target non-existent node"\) doesn't match the code: the try block calls \`self._trace_branch_endpoints\(coalesce_nid, branch_name\)\` on internal graph state, not a DIVERT-edge target lookup. This is first-party code operating on first-party data structures — KeyError/GraphValidationError here indicate internal graph invariants that either hold or represent bugs in our code. Silently swallowing them with \`pass\` is the exact silent-except pattern R6 targets, and contradicts the offensive-programming policy. If \`_branch_info\` not being populated is a legitimate state, encode that explicitly \(e.g., a predicate check or an explicit Optional return\), not via exception suppression. |
| `core/retention/purge.py:R4:PurgeManager:purge_payloads:fp=ed928398c920ef43` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-1 \(audit database / Landscape\) operation: update_grade_after_purge mutates the audit trail's reproducibility grade. Per the Auditability Standard and Tier-1 rules, anomalies in our own data must crash, not be swallowed. The 'point of no return' argument is real for the payload deletion, but the correct response to a failed grade update is not a logger.warning — it's the Landscape audit trail recording the failure \(telemetry/logging primacy: audit fires first, sync, crash-on-failure\), and/or quarantining/raising. Diverting a pipeline-relevant Tier-1 failure to slog.warning is itself a defect per the primacy-order policy, not a justification for R4 suppression. Additionally, R4 wants specific exceptions caught; the rationale doesn't identify which failure modes of update_grade_after_purge are legitimately recoverable versus which indicate audit-DB corruption that must surface. |
| `core/checkpoint/recovery.py:R6:RecoveryManager:get_resume_point:fp=c48ba8486b6c2e03` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is checkpoint data — Tier-1 \(Our Data, audit/landscape-adjacent\). Policy is explicit: 'crash on any anomaly — serialization doesn't change trust tier' and 'checkpoints' are named directly. An IncompatibleCheckpointError is exactly the kind of invariant break that must surface, not be swallowed and converted to 'just recompute from scratch'. Silently degrading to None erases the audit fact that a persisted checkpoint existed but was unreadable; an auditor asking 'what happened to run X's checkpoint?' would get no answer. If post-deploy schema migration is a real scenario, the resolution is an explicit migration/quarantine path that records the incompatibility, not a silent except that fabricates a clean-resume state. |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:record_transform_error:fp=b4edfad55e521778` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-1 write boundary into the Landscape audit database, not a Tier-3 boundary. The error_details payload originates from transform results inside the pipeline — that data is Tier-2 \(shape-guaranteed by source validation\) or Tier-1 once we are writing it to the audit trail. The policy is explicit: 'Our data \(Landscape, checkpoints\): crash on any anomaly — serialization doesn't change trust tier.' Silently swallowing ValueError/TypeError and substituting a repr-based fabricated payload is exactly the evidence-tampering pattern the Auditability Standard forbids; NaN/Infinity in error_details is an upstream plugin bug that must surface, not be papered over. The correct remediation is to let it crash \(or quarantine\), not to suppress R6. |
| `engine/dependency_resolver.py:R1:_load_depends_on:fp=0ecc84f5b6b35d41` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary on YAML operator input, and R1 is in the decorator's suppressible set. The function _load_depends_on takes a settings_path parameter and the .get\(\) is on \`loaded\`, which is derived from reading that path. However, the subject \`loaded\` is rooted in a local file-read, not directly in a function parameter named as source_param — the decorator's data-flow check is on names derived from source_param. Even so, the cleaner remediation is structural: extract the parsed dict into a parameter or apply @trust_boundary with source_param pointing to the YAML-loaded dict. More importantly, the rationale defends fabrication-adjacent behavior: substituting \[\] for absent depends_on is meaning-preserving here \(absent = no deps is a documented schema convention, not unknown\), which is legitimate, but the direct fix is \`loaded\['depends_on'\] if 'depends_on' in loaded else \[\]\` or making depends_on required in the schema. Recommend @trust_boundary on this function with source_param naming the loaded dict. |
| `engine/executors/state_guard.py:R5:NodeStateGuard:_extract_audit_evidence_context:fp=81df711769fff87f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance\(context, Mapping\) check guards the return value of a first-party method \(AuditEvidenceBase.to_audit_dict\(\)\) — that is exactly the Plugin-Ownership scenario the policy describes: a value constructed by our own code under a typed contract. The rationale frames this as a 'contractual' requirement but the correct response when a first-party contract is violated is to let it crash \(or assert offensively\), not to shape-guard with isinstance. The data is not Tier-3 external content; to_audit_dict is ELSPETH code returning ELSPETH-shaped data. The remediation is to fix or tighten the AuditEvidenceBase contract \(e.g., a typed return\) rather than mask contract violations here, which is precisely what R5 forbids. |
| `engine/executors/state_guard.py:R4:NodeStateGuard:_extract_audit_evidence_context:fp=d9eb19b04d13628e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims the broad catch protects against broken audit-evidence serialization stranding node_states OPEN, but the code is operating on AuditEvidenceBase — a first-party typed contract under Plugin Ownership, not Tier-3 external data. A defective to_audit_dict\(\) that raises or returns the wrong type is a bug in our code; per policy the correct response is to CRASH, not swallow the exception and substitute None. Furthermore, the inner isinstance/TypeError raise is itself defensive shape-guarding on a first-party return. Suppressing R4 here entrenches silent recovery on system-owned code, which is exactly what the broad-except rule is meant to prevent. |
| `engine/orchestrator/core.py:R6:Orchestrator:_handle_quarantine_row:fp=49d051713c4a7398` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is in the engine orchestrator \(L2\), not at a source/quarantine boundary — by the time _handle_quarantine_row runs, the row has already been quarantined and source_item.row is post-source data. More importantly, the rule_id is R6 \(silent-except\) and the code path diverts a hashing failure to a logger.debug fallback, which violates the telemetry primacy order: the content_hash on RowCreated silently changes semantics \(stable_hash vs repr_hash\) with no Landscape record of which path was taken, contradicting the rationale's claim that 'the audit trail records which hashing path was used' — slog.debug is not the audit trail. If non-canonical floats are a legitimate concern, the fix is to make stable_hash accept them \(or define a single canonical hashing function for Tier-3 quarantine content\) rather than swallow the exception and emit a differently-computed hash under the same field name. |
| `mcp/server.py:R6:_find_audit_databases:fp=badb2efd50be18f6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with the actual code: the except OSError block sets mtime = 0 and continues, which is precisely the silent-recovery pattern R6 forbids — no explicit error result, no quarantine, no re-raise. Substituting 0 for an unknown mtime is also fabrication \(None-like absence replaced with a concrete sort key value\), and it silently degrades the sort ordering without surfacing the filesystem fault. This is not a Tier-3 boundary justification at all; it's swallowing an OS error during a discovery scan. The fix is to let it raise, or to record the file as unstattable explicitly, not to allowlist. |
| `mcp/server.py:R6:_prompt_for_database:fp=250dc301eb3eda7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with the actual pattern: catching ValueError from Path.relative_to\(\) to fall back to displaying the absolute path. While the code's behavior is arguably reasonable \(relative_to raises ValueError when the path isn't under search_path, and falling back to the absolute path is a meaningful UX choice, not silent swallowing\), the rationale must explain THAT — name the specific exception, why falling back is meaningful rather than masking a bug, and why this isn't a defect to surface. A two-word rationale fails the auditability standard regardless of whether the code is defensible. |
| `mcp/server.py:R6:_prompt_for_database:fp=dbbeb81e161a9a1d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is boilerplate and does not engage with the R6 finding's specifics. While the catch of EOFError/KeyboardInterrupt at an interactive prompt is plausibly legitimate \(user cancellation is a normal control-flow signal, and the function returns None to signal cancellation to the caller\), the rationale fails the auditability standard: it does not name the exceptions caught, does not explain why returning None is the correct cancellation contract, and does not justify why those specific exceptions cannot indicate a real defect. A future auditor reading the allowlist entry would have no idea what was suppressed or why. The agent should rewrite the rationale to name the exceptions, explain the cancellation contract \(returning None signals 'user aborted' to the caller\), and confirm no other exception types are being swallowed. |
| `mcp/server.py:R6:_prompt_for_database:fp=b20406c8a83aad1b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Silent recovery with explicit error handling path' is shallow boilerplate that doesn't address R6's specific concern. Looking at the code, the except ValueError block writes to stderr but does not re-raise, return, or loop — it falls through, meaning a user typing a non-number gets an error message and then the function returns None implicitly \(or worse, depending on surrounding loop structure not visible\). This is exactly the silent-swallow pattern R6 targets: the error is caught, a message printed, and control proceeds as if nothing happened. If this is interactive input \(Tier-3\), the correct pattern is to loop and re-prompt, or return an explicit sentinel — not catch-and-continue. The rationale asserts 'explicit error handling' but stderr.write is logging, not an explicit error result per the audit/telemetry primacy order. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=054e76166ade23bc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale talks about csv.Error and malformed preamble corrupting parser state, but the code excerpt shows an \`except ValidationError as e:\` block that records the validation error and yields a quarantined SourceRow — this is exactly the explicit quarantine result R6 asks for, not a silent except. Either the rationale is describing a different except clause than the one flagged \(mismatch between justification and site\), or the flagged handler genuinely does quarantine and no suppression is needed at all. Additionally, the rationale's stated concern \(csv.Error / parser-state corruption\) is not addressed by swallowing ValidationError; that would be a separate handler. The agent should either point the allowlist at the correct line or remove the suppression because the visible handler already satisfies R6 via the quarantine yield. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=0fb2b154fb016195` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale doesn't match the finding's location. R6 is flagged on the StopIteration handler at line 441, but the rationale talks about header discovery and skip_rows — this excerpt is the data-row read loop, not header discovery. More importantly, StopIteration here is the normal end-of-iteration signal from next_nonblank_record\(\); breaking out of a while-True loop on StopIteration is loop control flow, not silent error swallowing. If R6 is genuinely firing on this idiom, the rationale needs to explain why this particular except-and-break is not error suppression \(it's iterator termination\), not invoke an unrelated header-parsing scenario. The mismatch suggests a copy-paste rationale that doesn't address this site. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=bb231ad98805d136` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale doesn't address the R6 finding at all — it talks about ValidationError and schema validation for Tier-3 rows, but the flagged code is an \`except csv.Error\` block, not a ValidationError handler. The excerpt is also truncated mid-block, so I cannot verify whether the handler re-raises, quarantines, or silently swallows the error; the visible code only builds an error_msg string. A rationale that argues a general Tier-3 point without speaking to the specific silent-except pattern the analyzer flagged is shallow and fails the auditability standard. The agent should either show that this handler produces an explicit quarantine/error result \(and write a rationale that says so\) or fix it to do so. |
| `plugins/sources/dataverse.py:R1:DataverseSource:_normalize_row_fields:fp=b9db16c1b599becb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`.get\(\)\` here is on \`mapping\` \(\`self._field_resolution.resolution_mapping\`\), which is first-party code-controlled state built by \`resolve_field_names\`, not the Tier-3 Dataverse row. The rationale conflates the row \(Tier 3\) with the resolution mapping \(first-party\). The pattern is a legitimate 'key may be absent, compute it then' lookup, but R1's remediation is direct indexing with try/except KeyError or \`in\` check — the rationale never addresses why \`.get\(\)\` specifically is the right shape here, and the data being guarded is not Tier 3. The decorator nudge does not apply because the subject is not rooted at an external parameter \(the function param \`row\` is the Tier-3 data, but the flagged expression is on \`mapping\`\). |
| `plugins/infrastructure/manager.py:R4:PluginManager:register:fp=0e88b71c015f8aa0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a plugin-ownership context \(pluggy registration on first-party code\), not a Tier-3 boundary. The rationale invokes 'rollback protection' to justify catching bare Exception, but per policy the correct channel for system events is the Landscape audit trail, not slog.error — diverting plugin-rollback failures to logger and swallowing them silently is itself a defect, not a justification. Additionally, the original exception is re-raised at line 109, so Python's implicit __context__ chaining would already surface both diagnostics if the rollback exception were allowed to propagate; the 'buries the original' framing is incorrect. The fix is to catch a specific exception type \(or restructure to record rollback failure through audit\), not to allowlist a broad-except on first-party plugin code. |
| `plugins/infrastructure/batching/mixin.py:R9:BatchTransformMixin:_release_loop:fp=2f93bd40222e5b89` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is shallow boilerplate \('may not exist in race or optional scenarios'\) and does not establish a Tier-3 boundary — \`self._batch_submissions\` is a first-party in-process dict guarded by \`self._batch_submissions_lock\`, populated by code we control. Per the Defensive vs Offensive Decision Test and Plugin Ownership policy, if the key may legitimately be absent, that invariant should be documented and asserted; if it must be present, a direct \`del\` \(or \`pop\` without default\) should crash to surface the bug. The 'race' hand-wave is precisely the silent-recovery pattern R9 targets — the lock already serialises access, so any missing key is an invariant violation, not a race. Remediation is a code fix \(establish the contract and let it crash on violation\), not an allowlist entry. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=24666a1e729638d7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line is \`except ShutdownError: break\` — this swallows an exception without re-raise or explicit error result, which is exactly what R6 targets. The rationale 'Silent recovery with explicit error handling path' is boilerplate that doesn't engage with whether ShutdownError represents a normal control-flow signal versus an error. If ShutdownError is genuinely the designed exit signal \(as the comment suggests\), the honest fix is structural — either don't model normal shutdown as an exception, or document the invariant in a way the analyzer recognizes — not a per-line suppression with a vague one-liner. The rationale also doesn't address why this differs from the R6 pattern the rule was written to catch. The decorator does not apply \(R6 is not in the {R1,R5} suppressible set, and this is not a Tier-3 external-data parameter site\). |
| `plugins/infrastructure/clients/http.py:R1:AuditedHTTPClient:_parse_response_body:fp=c10e1a372d3013e8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(HTTP response headers from an external server\) and the R1 finding is on response.headers.get\("content-type", ""\) — a classic Tier-3 shape guard. The function _parse_response_body takes the external httpx.Response as a parameter named 'response', and the suppressed expression is rooted at that parameter, so the structural remediation is @trust_boundary\(tier=3, source_param='response', suppresses=\('R1',\), ...\) rather than a per-line allowlist entry. The rationale is correct on substance but the project policy prefers the decorator for function-scoped Tier-3 boundaries on parameters. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=153b600f7b824428` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 external-data boundary: provider usage payloads come from an external SDK/network call, the function takes the external value as parameter \`\`usage\`\`, the rule_id is R1, and the .get\(\) subjects are rooted directly at that parameter. The correct remediation is the @trust_boundary decorator on _extract_usage_from_provider_response with source_param='usage', not a per-line allowlist entry. The rationale itself is honest and accurate — but the structural fix exists and is mandatory per policy. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=54f676db05cacf08` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case for the @trust_boundary decorator: the function _extract_usage_from_provider_response takes external provider usage data as its 'usage' parameter, the rule is R1 \(in the suppressible set\), and the flagged .get\(\) calls are rooted directly at that parameter. The rationale correctly identifies the Tier-3 nature and honest-absence pattern, but the structural remediation is the decorator on the function rather than a per-line allowlist entry. The R5 isinstance\(usage, Mapping\) check on the same parameter would also be covered by the same decorator. |
| `plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=3ce6385129d45fcc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external boundary \(provider usage payload from an LLM SDK\) and R1 is in the decorator's suppressible set, and the subject is rooted at the function parameter \`\`usage\`\`. The correct remediation is to apply @trust_boundary\(tier=3, source_param='usage', suppresses=\('R1','R5'\), ...\) to _extract_usage_from_provider_response rather than a per-line allowlist entry. |
| `plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=d741dbbe7cf1857f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr\), which is NOT in the @trust_boundary decorator's suppressible set \({R1, R5}\), so the decorator nudge does not apply. However, the rationale itself is sound on the merits: the \`usage\` object is a provider SDK response wrapper whose shape is controlled by external SDK versions \(LiteLLM/OpenAI/etc.\), not by any first-party typed contract — this is genuine Tier-3 external data per the Plugin Ownership scope clarification, and shape-guarding with getattr\(..., None\) here is the honest boundary pattern that records absence as None rather than fabricating. The verdict is BLOCKED only because R2 suppressions cannot ride on @trust_boundary; the agent should either \(a\) propose this as a per-file/per-symbol allowlist entry for R2 specifically with this same rationale, or \(b\) if the rule table supports it, request R2 be added to the decorator's suppression set. The rationale correctly identifies honest-absence semantics, but the gate cannot accept an R2 suppression through the decorator-aligned path. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=ac3495bbb7ec9639` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external-data boundary \(Azure Search API response\), R1 is in the decorator's suppressible set, and the flagged subject \(item.get\('@search.score'\)\) is rooted in iteration over response_data\['value'\] — the function's external parameter. The correct remediation is @trust_boundary\(tier=3, source_param='response_data', suppresses=\('R1','R5'\), ...\) on _parse_response rather than a per-line allowlist entry. The rationale's substantive claim \(Tier-3 boundary, absence recorded in skipped_items rather than fabricated\) is correct, but the structural fix is the decorator. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=d3ae8b449f567803` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case suitable for the @trust_boundary decorator: _parse_response processes external Azure Search JSON, the rule is R1 \(in the suppressible set {R1, R5}\), and the subject item.get\("id"\) is rooted in iteration over response_data\["value"\], an external parameter-derived value. The excerpt does not show the function signature or decorators, but the parsing function operates on externally-sourced data and shows multiple R1/R5 patterns at this same boundary — the structural fix is one decorator on _parse_response covering all of them, not per-line allowlist entries. The agent should apply @trust_boundary\(tier=3, source_param=&lt;the response param&gt;, suppresses=\('R1','R5'\), ...\) rather than land this entry. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=1a56c5169fca221b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 external-data boundary case suitable for the @trust_boundary decorator: the function parses an Azure AI Search JSON response \(external API payload, Tier 3\), the finding is R1 \(in the decorator's suppressible set {R1, R5}\), and the subject \`item.get\("content"\)\` is rooted in data flowing from the external response parameter. The code correctly records absence/invalid shape as skipped_items rather than fabricating defaults, which is honest Tier-3 handling. However, the structural remediation is to apply @trust_boundary to the enclosing parsing function \(with source_param pointing to the response/items parameter\) rather than land per-line allowlist entries — multiple R1/R5 sites in this same function \(item.get on id, content, @search.documentId; isinstance on raw_score and content\) confirm a function-scoped boundary pattern. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=46bde1db12749de8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary \(Azure Search API response\) and R1 .get\(\) on external data is the structural pattern the @trust_boundary decorator was built to suppress. The function appears to iterate over external response items and uses item.get\('id'\) to record identifying info on skipped items — a legitimate Tier-3 pattern, but the correct remediation is the decorator, not a per-line allowlist entry. The excerpt does not show the function signature, but the loop variable 'item' is rooted in the external response; the agent should apply @trust_boundary with source_param naming the response parameter \(likely the response/items parameter of _parse_response\) and suppresses=\('R1','R5'\). |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=3c62e2c3ff824fd7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary parsing function operating on Azure AI Search API response items, and R1 is in the decorator's suppressible set. The .get\("id"\) access at line 279 is rooted in an external API response item. If the enclosing _parse_response method takes the response/items as a parameter, the correct structural remediation is @trust_boundary\(tier=3, source_param=&lt;param&gt;, suppresses=\('R1',\), ...\) rather than a per-line allowlist entry, since multiple similar .get\(\) calls exist \(lines 264, 271, 273, 276, 279, 282, 287\). Use the decorator on the enclosing function. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=423ca2aea34a9f56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary case on Azure AI Search API response items \(external data\), R1 is in the decorator's suppressible set, and the subject \`item.get\(...\)\` is rooted in iteration over the external response. The function \`_parse_response\` is the natural trust-boundary site, so the structural remediation is \`@trust_boundary\(tier=3, source='Azure AI Search response', source_param=&lt;response param&gt;, suppresses=\('R1',\), ...\)\` on the enclosing method rather than per-line allowlist entries. Without seeing the function signature in the excerpt I cannot name the exact parameter, but the iteration variable \`item\` clearly derives from a response parameter to \`_parse_response\`; the agent should pass that parameter name. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=8c1f201c7b5ddd49` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This R1 finding is inside a function that parses an Azure AI Search API response — externally-sourced Tier-3 data where item shape is not contractually guaranteed. The .get\(\) chain on 'id' and '@search.documentId' is a legitimate Tier-3 boundary pattern, and absence is honestly recorded \(skipped_items + 'missing_id' reason, no fabrication\). However, this is a structural Tier-3 boundary case: the enclosing _parse_response method should use @trust_boundary\(source_param=...\) rather than per-line allowlist entries, since multiple R1 hits on item.get\(...\) appear in the same function \(lines 271, 273, 276, 279, 282, 287\). The agent should apply the decorator on the parameter carrying the parsed response items. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=f9be71eef8e59cb3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line is \`item.get\("id"\)\` inside \`skipped_items.append\({...}\)\` on line 273 — diagnostic metadata being recorded when content is missing. While \`item\` is Tier-3 Azure Search response data and the function is plausibly a trust boundary, this finding pattern \(R1 on \`item.get\(...\)\` rooted at a Tier-3 parameter\) is exactly what the \`@trust_boundary\` decorator exists to suppress structurally. The excerpt does not show the function signature or decorators, but the symbol \`_parse_response\` suggests the response is passed in as a parameter; the agent should apply \`@trust_boundary\(tier=3, source_param=&lt;response param&gt;, suppresses=\('R1','R5'\), ...\)\` rather than per-line allowlist entries, especially since this file clearly has many such sites \(lines 264, 271, 273, 276, 279, 282, 287 all do the same thing\). |
| `plugins/infrastructure/config_base.py:R1:_format_validation_error_cause:fp=06bc6e6d57f87e71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | Pydantic's ValidationError.errors\(\) is a first-party typed contract from a pinned library dependency, not Tier-3 external data — the 'type' field is part of Pydantic's documented error schema and is always present. The rationale invokes 'across versions' variance as a hedge, but ELSPETH pins its dependencies and has no legacy-compatibility mandate \(No Legacy Code Policy\); if a future Pydantic upgrade changes the error dict shape, that is a contract break that should crash loudly, not be silently swallowed by .get\(\) returning None. The adjacent line uses bare error\['loc'\] and error\['msg'\] without guarding, which is the correct pattern; error\['type'\] should match. This is not a trust boundary — fix by using direct subscript. |
| `plugins/transforms/rag/transform.py:R2:RAGRetrievalTransform:_configured_collection_name:fp=757eb1e99aea1a3e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The provider_config is a first-party validated model \(the rationale itself says 'validated provider-specific model union'\), making this Tier-2 data with a guaranteed shape per its typed contract. Using getattr with a None default to probe for one of two mutually exclusive attribute names is exactly the defensive shape-guarding the policy forbids on Tier-2 data — the correct structure is to dispatch on the concrete model type \(isinstance on the union members, or a property on each model\) and access the field directly, letting AttributeError surface if the contract is violated. The decorator does not apply: provider_config is not an external Tier-3 parameter, and R2 is not in the decorator's suppressible set {R1, R5}. |
| `plugins/transforms/rag/transform.py:R5:RAGRetrievalTransform:_configured_collection_name:fp=b22badfe2569d3ea` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The provider_config is described as a validated provider-specific model — that's a first-party typed contract \(Tier-2 with guaranteed shape\), not Tier-3 external data. If the contract guarantees one of 'collection' or 'index' as a non-empty string, the correct pattern is direct attribute access on the specific model type and let it crash on contract violation. The getattr+isinstance dance suggests the code is treating a union of typed models as if its shape were unknown; the fix is to dispatch on the concrete model type \(or define a protocol exposing the field\) rather than shape-probe with isinstance. This is exactly the contract-violation-masking pattern R5 targets. |
| `plugins/sinks/chroma_sink.py:R6:ChromaSink:write:fp=8501da8c0e5bf4c0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale describes 'optional metadata fields', but the code excerpt shows the KeyError is caught around access to REQUIRED fields \(fm.id_field and fm.document_field\) — the comment on line 292 explicitly says 'Required fields: id and document must be present'. The rationale does not match the code at the flagged site. Furthermore, the exception is not silently swallowed: it routes the row to _divert_row with a specific reason and continues — this is explicit quarantine, which is the R6 remediation. The correct path is either to fix the rationale to describe what the code actually does \(per-row quarantine of malformed rows from Tier-3-ish pipeline data\) or to recognize this is the documented quarantine pattern, but as written the rationale is misaligned with the site and cannot be accepted as an honest audit record. |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=12aaa412bd91a810` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a sink operating on Tier-2 pipeline data — rows that have passed source validation and arrive with a contractually guaranteed shape. The Quick Reference is explicit: 'Sink: no coercion, expect types.' If raw_id is not a str, that is an upstream plugin bug and the correct response is to crash, not to isinstance-guard and divert. Additionally, the rationale claims the guard protects against NaN/Infinity before canonical_json hashing, but the flagged code checks isinstance\(raw_id, str\) — the rationale does not describe the actual pattern the analyzer flagged. The remediation is to remove these shape guards and fix the upstream source/transform if it can produce non-str ids. |
| `plugins/sinks/chroma_sink.py:R5:ChromaSink:write:fp=87156d7ec508a40b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale doesn't match the flagged line. Line 311 checks \`isinstance\(raw_doc, str\)\` — this is a Tier-2 row-shape check on a pipeline field \(document_field\), not a guard against NaN/Infinity floats before canonical_json hashing. The rationale describes a different site \(float metadata validation, presumably further down near line 322+'s metadata block\). A Sink receiving row data should trust the upstream schema per the Tier-2 policy \('Sink: no coercion, expect types'\) — if the document field arrives as non-str, that's an upstream source/transform bug to fix, not something to divert at the sink. The mismatch between rationale and actual flagged line makes this an uninterpretable audit record regardless of whether some nearby isinstance might be defensible. |
| `plugins/sinks/chroma_sink.py:R6:ChromaSink:write:fp=fe6fa1b761d7dd9c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a sink operating on Tier-2 pipeline data \(rows that passed source validation\). Per policy, sinks should expect types and not coerce — if a configured metadata_field is absent from the row, that is an upstream contract violation, not a per-field optional case. The KeyError swallow silently papers over schema/configuration mismatches: an auditor querying 'did metadata field X get written?' will get no signal distinguishing 'field was absent from every row' from 'field was written'. If metadata fields are genuinely optional per-row, the correct pattern is explicit membership check that records absence \(e.g. write None or divert\), not a silent except-continue. The rationale also doesn't engage with R6's specific concern \(swallowing without explicit error/quarantine result\). |
| `plugins/transforms/web_scrape.py:R5:WebScrapeTransform:get_post_call_hints:fp=f919002b3beb8525` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims config_snapshot is 'LLM-supplied' Tier-3 data, but config_snapshot is a Mapping\[str, object\] passed via a first-party typed contract — it's the plugin's config snapshot, not an LLM tool-call payload. Config is operator-authored and validated at source/load time; by the time get_post_call_hints sees it, the shape is established by ELSPETH's own contract. The 'object' value type reflects Mapping's heterogeneous value annotation, not external untrustworthiness. If text_separator must be a str, that should be enforced in the config schema and accessed directly; an isinstance guard here masks a contract violation, which is exactly what R5 targets. The decorator nudge doesn't apply: this is not a Tier-3 external-input boundary function. |
| `plugins/sinks/azure_blob_sink.py:R6:AzureBlobSink:write:fp=304f148f13df7df3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is literally a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) and provides no site-specific justification at all. Per the Auditability Standard, an allowlist entry IS an audit record; a placeholder rationale is a corrupted audit record that future auditors cannot interpret. The code itself appears defensible — catching JSON serialization errors to divert the bad row via the explicit _divert_row quarantine path is the correct Tier-3 handling of per-row data faults, not silent swallowing — but the rationale must say that. The agent must rewrite the rationale to explain that R6's concern \(silent swallow\) is addressed because the except branch records and routes the row via _divert_row before continuing. The decorator nudge does not apply: R6 is not in the {R1,R5} suppressible set. |
| `plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_with_per_row_diversion:fp=9e9d1f67fecbd6f2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent-supplied rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly flags this as unreviewed debt rather than a justification. It does not address the R6 silent-except concern at all — it offers no argument about why catching IntegrityError without re-raise is correct here. While the code itself may well be defensible \(the except clause performs per-row diversion, which is an explicit quarantine path, not silent swallowing\), the policy requires the allowlist entry to be a coherent audit record. A rationale that openly admits it needs review is a corrupted audit record and must be rejected; the agent must write a site-specific justification explaining how per-row diversion satisfies R6's 'explicit error/quarantine result' remediation. |
| `plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_with_per_row_diversion:fp=d64a7ca89b0cfe0e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder template \("ALLOWLIST-FRESH — no stale entry matched...review whether this is new debt"\) and does not justify the suppression at all. The code itself may well be legitimate — IntegrityError from a per-row insert is caught specifically to divert the offending row to a quarantine sink, which is the policy-correct quarantine-row pattern for user data — but the rationale must say that. An audit record reading "ALLOWLIST-FRESH" is exactly the corrupted audit record the Auditability Standard forbids. Resubmit with a site-specific rationale explaining that IntegrityError is caught narrowly to attribute the constraint violation to a single row and route it via _divert_row \(explicit quarantine result, not silent swallow\). |
| `plugins/sinks/csv_sink.py:R6:CSVSink:_stage_rows_per_row:fp=73ebd4f4b271eb4f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) and provides no site-specific justification for the R6 suppression. That said, the code itself appears to handle the exception explicitly by diverting the row via \`_divert_row\` with a reason — this is a quarantine pattern, not silent swallowing, which is the correct response for per-row serialization failures in a sink. However, the agent must supply an actual rationale explaining that the except branch quarantines the row \(explicit error result per R6's remediation\) rather than submitting a fingerprint-rotation placeholder. An empty/boilerplate rationale is a corrupted audit record per the Auditability Standard and must be rejected on that ground alone. |
| `plugins/sinks/json_sink.py:R6:JSONSink:write:fp=70e172c6d4480200` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification was provided — that alone is grounds to block per the auditability standard \(a vague rationale is a corrupted audit record\). On the merits, however, the code itself is actually doing the right thing: it catches ValueError/TypeError from json.dumps and explicitly diverts the offending row via self._divert_row with a reason, which is the 'explicit error/quarantine result' R6 demands — this is not silent-except. The correct remediation is for the agent to write a site-specific rationale explaining that the except branch quarantines via _divert_row \(making it an explicit error result, not a swallow\), not to land a placeholder entry. Block now; a proper rationale would likely be accepted on resubmission. |
| `plugins/sinks/json_sink.py:R6:JSONSink:_write_jsonl_content:fp=7f2a8740a6f881fa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is the placeholder 'ALLOWLIST-FRESH' marker explicitly flagging that no real justification was provided — it is a non-rationale that asks the judge to decide whether this is new debt. On the merits, the code at line 437 catches ValueError/TypeError from json.dumps and routes the offending row to _divert_row with an explicit reason before continuing — this is precisely the 'explicit error/quarantine result' that R6's remediation calls for, not a silent swallow. The exception is narrowly scoped, the row is recorded and routed per the on_write_failure policy, and processing of remaining rows continues per a documented per-row Tier-2 fault model. However, since the supplied rationale text does not articulate any of this, the audit record would be uninterpretable; the agent must resubmit with a site-specific rationale describing the quarantine path. |
| `plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=e59ce88529f38df2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly asking for review, not a substantive justification. It provides no site-specific reasoning addressing R6's concern about swallowed exceptions. While the code excerpt suggests the handler does record a validation error and yield a quarantined row \(which would be legitimate Tier-3 boundary handling\), the judge cannot accept a suppression on a rationale that is a meta-comment about fingerprint rotation rather than an actual argument. A proper rationale would state: this is a Tier-3 external-header fault, the except clause records to the validation error channel and yields a quarantined SourceRow, so the error is neither silenced nor lost. The agent must rewrite the rationale. |
| `plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=7b8583638b096231` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder string \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly asks the reviewer to determine whether this is new debt — it is not a justification at all. The audit trail requires a site-specific rationale explaining why catching ExternalHeaderError without re-raise is correct here; the operator has supplied none. While the code itself plausibly handles a Tier-3 boundary correctly \(records the bad header to the validation-failure channel and quarantines/discards\), an allowlist entry must stand on its written rationale, and 'review whether this is new debt' is a corrupted audit record by the Auditability Standard. The agent must write an actual rationale naming R6 and explaining the record-and-quarantine path. |
| `plugins/infrastructure/validation.py:R1:_extract_errors:fp=9240c6f6517a323b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The surrounding code accesses err\['loc'\], err\['msg'\], and err\['input'\] directly with bracket notation, treating Pydantic's error-dict schema as a guaranteed contract — but then uses err.get\('type'\) defensively on the same dict from the same iteration. This is internally inconsistent: either the Pydantic ValidationError.errors\(\) shape is trusted \(then use err\['type'\]\) or it is not \(then all four field accesses need guarding\). Pydantic is a pinned first-party dependency and its error-dict schema is stable and documented; 'type' is always present. The rationale's appeal to cross-version variance is speculative and contradicted by the code's own treatment of sibling keys. The fix is err\['type'\] == 'missing'; let it KeyError if a future Pydantic upgrade actually breaks the contract, and address it then. Not a trust_boundary candidate — pydantic_error is not an external parameter of this function in the Tier-3 sense; it's a library object from a pinned dependency. |
| `telemetry/manager.py:R6:TelemetryManager:handle_event:fp=64535719a1fd4215` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is telemetry queue handling, which sits below the audit trail in the primacy order — but the rationale 'Silent recovery with explicit error handling path' is exactly the boilerplate the policy warns against. The except block logs and increments a counter, but the event is silently dropped: the caller is not informed, no telemetry-loss audit record is emitted, and the rationale never speaks to R6's specific concern \(swallowing without re-raise or explicit error result\). Telemetry is best-effort by policy, so a case could exist for this swallow, but the rationale doesn't articulate it — it doesn't name telemetry's best-effort tier, doesn't justify why logger \(last-resort channel\) is acceptable here, and doesn't address why the drop counter alone is a sufficient explicit result. A shallow rationale on an R6 finding is a corrupted audit record. |
| `telemetry/manager.py:R6:TelemetryManager:_drop_oldest_and_enqueue_newest:fp=1c75bf1b3c8e4241` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged R6 is the \`except queue.Empty\` at line 384, which sets \`evicted = None; had_evicted = False\` — this is the canonical silent-except defensive pattern: catching an exception and substituting sentinel values to continue execution. The rationale 'queue overflow handling at telemetry backpressure boundary' is generic boilerplate that never addresses why swallowing queue.Empty specifically is correct here, nor does it explain why this isn't simply the Tier-1/internal-invariant case \(the queue is process-local, first-party state, not an external trust boundary\). Telemetry primacy order does not license swallowing — and even if it did, the rationale must speak to the specific swallow site. Per the Decision Test, this is 'silently swallowing an error with a default value' on internally-owned state; the fix is to restructure \(e.g., check qsize, use a different control-flow primitive\) or to honestly document the invariant in code, not to allowlist. |
| `telemetry/manager.py:R6:TelemetryManager:_drop_oldest_and_enqueue_newest:fp=07c7b40d564bb0bc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a single boilerplate phrase \('Queue overflow handling at telemetry backpressure boundary'\) that names a location but does not address the R6 finding: why is swallowing the RuntimeError from sentinel re-insertion the correct semantic? The code itself has a more thorough inline comment than the rationale does — the audit record \(the allowlist entry\) must stand on its own and explain the invariant \(close\(\) independently retries sentinel insertion, the export thread join has a timeout, propagating would corrupt caller accounting\), not gesture at 'backpressure boundary'. Additionally, this is not a Tier-3 external-data boundary; it is internal queue-state management, so 'boundary' is a misapplication of the trust-tier vocabulary. The agent should rewrite the rationale to state the recoverability invariant and reference the close\(\) retry contract, or restructure to return an explicit drop-result. |
| `telemetry/manager.py:R6:TelemetryManager:_requeue_shutdown_sentinel_or_raise:fp=9a2e20048626df2b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale 'Sentinel reinsertion loop handles bounded queue contention' is a one-line label that does not address the R6 silent-except concern. The flagged except clauses \(queue.Full and queue.Empty\) are caught and silently continued without re-raise, explicit error result, or even a comment-level justification tied to the silent-swallow pattern. The function does raise RuntimeError on exhaustion per its docstring, but that's not visible in the excerpt and the rationale doesn't mention it; nor does it explain why swallowing queue.Full/Empty mid-loop is correct rather than offensive. This is not a Tier-3 external-data boundary \(it's internal queue state\), so the @trust_boundary decorator does not apply. The rationale must explicitly justify why each swallowed exception is part of normal control flow and point to the eventual raise. |
| `tui/widgets/lineage_tree.py:R1:LineageTree:_build_tree:fp=60a7a8bf5283d1ba` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The .get\(\) here is on tokens_by_sink, a local dict built moments earlier in this same function \(Step 1\). It is not external Tier-3 data — it is a process-local lookup table whose population invariant is fully controlled by this method. The honest pattern is a direct membership test or a defaultdict, or simply \`\`tokens_by_sink\[sink_node_id\]\`\` inside a conditional; using .get with a fabricated empty-list default to paper over 'sink has no tokens' is exactly the R1 anti-pattern \(silent default hides whether the sink was ever registered\). The rationale \('sparse lookup'\) describes a code-design choice, not a trust-boundary justification, so this is a fix-the-code task, not a suppression. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:decode_token:fp=c10514f46a44024c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary on an externally-supplied JWT, and R1 is in the decorator's suppressible set. The flagged subject \`header.get\("kid"\)\` is rooted at data derived from the \`token\` parameter \(via \`jwt.get_unverified_header\(token\)\`\). All three decorator-nudge conditions are satisfied: external-data function parameter, R1 rule, and clear data-flow from the parameter to the suppressed expression. The correct remediation is \`@trust_boundary\(tier=3, source='JWT header from client request', source_param='token', suppresses=\('R1',\), ...\)\` rather than a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_validate_discovery_document:fp=e21f32481fc9784c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(parsed JSON from an external IdP\) and the R1 finding's subject is rooted at the function parameter \`\`discovery\`\`, which is exactly the structural case the @trust_boundary decorator exists to handle. R1 is in the decorator's suppressible set, the function shape-validates and raises AuthenticationError on malformed input — a clean boundary invariant. The correct remediation is @trust_boundary\(tier=3, source='OIDC IdP discovery document', source_param='discovery', suppresses=\('R1','R5'\), invariant='raises AuthenticationError on malformed shape', ...\) rather than a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_validate_jwks_document:fp=6494c39fcc6d20fc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary \(JWKS document fetched from an external OIDC provider\), and shape-guarding it is legitimate. However, the function takes the external value as a parameter named \`jwks\`, the rule is R1, and the flagged expression \`jwks.get\("keys"\)\` is rooted at that parameter — all three conditions for the @trust_boundary decorator are met. The structural remediation is to decorate \`_validate_jwks_document\` with @trust_boundary\(source_param="jwks", suppresses=\("R1",\), ...\) rather than add a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_token_algorithm:fp=08a628af082bd78b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: _get_token_algorithm takes an external JWT header dict as parameter 'header', the finding is R1 \(in the decorator's suppressible set\), and the subject 'header.get\("alg"\)' is rooted at that external parameter. The correct remediation is @trust_boundary\(tier=3, source='JWT token header', source_param='header', suppresses=\('R1',\), ...\) on the function, not a per-line allowlist entry. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_jwk_algorithm:fp=57b015b09222f296` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: _get_jwk_algorithm takes external JWKS data \(a parameter\) and the R1 finding on raw_key.get\("kid"\) is rooted in that external parameter \(jwks -&gt; raw_key via iteration\). All three decorator-suggestion conditions are met \(external-data parameter, R1 in the suppressible set, subject rooted at the parameter\), so the correct remediation is @trust_boundary\(tier=3, source_param='jwks', suppresses=\('R1','R5'\), ...\) on the function rather than a per-line allowlist entry. The same decorator would also cover the isinstance shape-guard above. Note also: raw_key.get\("alg"\) returning None is treated as 'no advertised alg' which is meaning-preserving recording of absence, not fabrication — but the suppression mechanism should still be the decorator. |
| `web/auth/oidc.py:R1:JWKSTokenValidator:_get_jwk_algorithm:fp=7ad8f0d01efdbd81` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This R1 finding occurs inside a function that takes external JWKS data as a parameter \(jwks, fetched from the OIDC IdP discovery endpoint — Tier-3 external data\). The flagged subject raw_key.get\("alg"\) is rooted at the jwks parameter via iteration and the isinstance-dict guard. All three @trust_boundary conditions are met: external-data parameter, rule R1 is in the suppressible set, and the subject flows from the parameter. The correct remediation is @trust_boundary\(tier=3, source='OIDC IdP JWKS endpoint', source_param='jwks', suppresses=\('R1','R5'\), invariant=..., test_ref=..., test_fingerprint=...\) on the function rather than a per-line allowlist entry. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:authenticate:fp=5b6d0378675eb2f2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 OIDC token payload boundary and R1 is in the suppressible rule set, but the subject is rooted at \`payload\`, which is the function-local parameter derived from the \`token\` argument decoded by the validator. The structural remediation is \`@trust_boundary\(tier=3, source='OIDC token claims', source_param='token', suppresses=\('R1',\), ...\)\` on \`authenticate\`, not a per-line allowlist entry. Additionally, the fallback \`payload.get\('preferred_username'\) or sub\` is arguably fabrication-adjacent \(substituting \`sub\` for an absent username changes meaning\), but the immediate gate decision is structural: use the decorator. |
| `web/auth/oidc.py:R1:optional_profile_claim:fp=4c7ec97e16ccebc9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary case where the function takes an external OIDC token payload as a parameter and the R1 finding is on payload.get\(claim_name\), rooted at that parameter. R1 is in the decorator's suppressible set. The correct remediation is to apply @trust_boundary\(tier=3, source_param='payload', suppresses=\('R1',\), ...\) rather than a per-line allowlist entry. The rationale is otherwise sound about the Tier-3 nature, but the structural fix is required. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=b8b1270a71192d97` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case for the @trust_boundary decorator: get_user_info receives an external OIDC token, decodes it into a payload dict whose shape is controlled by the IdP, and the flagged R1 finding \(payload.get\('groups'\)\) is rooted at that external payload. The function also validates shape \(isinstance check on raw_groups\) and raises on malformed input — exactly the invariant a trust_boundary decorator documents. The structural remediation is @trust_boundary on get_user_info with source_param pointing at the decoded payload, not a per-line allowlist entry. Note: the source_param must name an actual function parameter; since 'payload' is derived from 'token' inside the function, the agent may need to restructure \(e.g. extract a helper that takes payload directly\) to apply the decorator cleanly. |
| `web/auth/oidc.py:R1:OIDCAuthProvider:get_user_info:fp=cd4ba5b028a35026` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 OIDC token boundary and R1 on payload.get is in the decorator's suppressible set, but the subject is rooted at the function parameter \(payload\), which is the external token claims dict. The correct remediation is @trust_boundary\(tier=3, source='OIDC ID token claims', source_param='payload', suppresses=\('R1',\), ...\) on get_user_info rather than a per-line allowlist entry. Additionally, the fallback \`... or sub\` treats empty string as absence, which is a minor concern but acceptable since sub is the OIDC-mandated stable identifier \(not fabrication — sub is provided by the same IdP\). |
| `web/auth/entra.py:R1:EntraAuthProvider:authenticate:fp=564181fecfacb986` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(decoded JWT payload from external IdP\) and R1 is a suppressible rule, but the subject is rooted at \`payload\`, which is a function-local variable derived from \`token\` — not a parameter named in a \`@trust_boundary\` decorator. However, the function does take \`token\` as an external parameter, and the standard remediation is to decorate \`authenticate\` with \`@trust_boundary\(source_param='token', suppresses=\('R1',\), ...\)\` so the suppression covers \`payload.get\(...\)\` derivations. The decorator's data-flow analysis follows derivations from the named source param, so \`payload\` rooted at \`token\` qualifies. The per-line allowlist entry should not land; use the structural decorator instead. |
| `web/auth/entra.py:R1:EntraAuthProvider:get_user_info:fp=9ef9844bc003576e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary \(decoded JWT payload from Entra\), R1 is in the suppressible set, and the subject \`payload.get\("preferred_username"\)\` is rooted in the decoded token. The function takes \`token\` as a parameter and \`payload\` is derived from it — this is exactly the structural case for \`@trust_boundary\` rather than a per-line allowlist entry. The agent should decorate \`get_user_info\` with \`@trust_boundary\(tier=3, source='Entra ID token', source_param='token', suppresses=\('R1',\), ...\)\` instead of adding YAML entries. Note also the helper \`optional_profile_claim\` is used elsewhere for the same pattern, suggesting the decorator is the right structural answer. |
| `web/execution/progress.py:R6:ProgressBroadcaster:_safe_put:fp=b0813b2652b434b9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R6 silent-except on a subscriber queue, but the rationale routes a pipeline-relevant event \(subscriber queue drop\) to slog.warning, which violates the telemetry/logging primacy order: pipeline-observable events belong on the Landscape audit trail \(or at minimum telemetry\), not the last-resort logger. The rationale also doesn't address R6's actual concern — it explains *why* dropping is acceptable but not why swallowing the exception without an explicit error/quarantine result or audit record is honest. 'Event dropped rather than blocking pipeline thread' describes a policy choice that needs to be recorded in the audit trail, not suppressed with a log line. The decorator nudge doesn't apply \(R6 is not in {R1, R5}, and the queue is internal state, not Tier-3 external data\). |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=6529682f4f63b877` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R6 finding is on \`except _RunStatusNotFoundError:\` which closes the websocket with code 4004 and breaks the loop — that IS an explicit error result handled via the websocket protocol, not a silent swallow. However, the rationale supplied \('Idle-timeout status recheck — run may be deleted between heartbeat polls'\) only explains the *situation* \(why the run might be missing\) and never speaks to R6's actual concern: that the handler converts the exception into an out-of-band signal \(websocket close code\) rather than re-raising. A correct rationale must articulate that the close-code-and-break IS the explicit error result and why that channel is the contractually correct disposition here. The current text reads as context, not justification, and does not address the flagged pattern. |
| `web/middleware/rate_limit.py:R9:ComposerRateLimiter:_sweep_stale_entries:fp=50c8c5807a0f8fe3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is internal first-party state \(self._buckets and self._user_locks are process-local dicts owned by ComposerRateLimiter\), not Tier-3 external data. The rationale admits the invariant is 'a bucket entry may exist without a corresponding lock' — if that is a real invariant, the code should reflect it explicitly \(e.g., conditional \`if uid in self._user_locks: del ...\`, or fix _get_user_lock to keep them in lockstep\). Using pop\(uid, None\) silently hides the case where a bucket exists without a lock, which is precisely R9's concern: a missing-key bug masked by an implicit default. The decorator does not apply \(no external parameter; R9 is not in the suppressible set {R1,R5}\). |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=5f1f81fee41e3e1a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`config.get\("sinks"\)\` at line 74, but the rationale \("sink options keys path/file may be absent"\) describes a different location entirely — the path/file keys are accessed via \`key in opts\` membership tests at lines 70-71 and 86-87, not via \`.get\(\)\`. The actual flagged call retrieves the optional top-level \`sinks\` block from a YAML-generated config. While YAML config could plausibly be Tier-3 external data, the rationale never addresses the actual flagged expression nor names the function's external-data parameter, and the function is not obviously a decorated trust boundary from this excerpt. The rationale is shallow and misaligned with the rule's actual subject; remediation is either direct access \(\`config\["sinks"\]\` if the schema guarantees presence\) or a proper boundary treatment with an honest, site-specific justification. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=c876a96667c425f7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line is \`sink_cfg.get\("options"\)\` on a YAML-parsed config dict. While YAML input is externally sourced, this function is navigating a config structure where 'options' is an optional schema key, not a guaranteed field — but the rule R1 concern is precisely that .get\(\) hides missing-key bugs. The honest pattern here, consistent with the surrounding code, is to test \`if "options" in sink_cfg:\` then index directly \(\`sink_cfg\["options"\]\`\), mirroring the \`if key in opts\` pattern already used on lines 71 and 87. The rationale 'keys may be absent' is the textbook defensive-programming justification the policy forbids; absence should be tested explicitly, not papered over with .get\(\). The function does take YAML-derived config but is not a single-parameter Tier-3 boundary decorated with @trust_boundary, and the fix is a small code change, not a suppression. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=42996be95a89dd56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The YAML here is produced by our own first-party YamlGenerator \(the function even type-checks its return value\), making the resulting config a first-party value whose schema we control — not Tier-3 external data. The rationale 'source key may be absent in generated YAML' describes an optional schema field, which is a first-party contract concern: either the schema makes 'source' optional \(in which case direct access with a documented contract, or explicit membership check, is appropriate\) or it doesn't \(in which case missing 'source' should crash\). The .get\(\) pattern here silently swallows a schema-contract question. Additionally, the function does not appear to take external data as a parameter from a Tier-3 boundary, so the @trust_boundary decorator is not the right structural fix either; the right fix is to make the schema contract explicit. |
| `web/middleware/request_id.py:R1:RequestIdMiddleware:dispatch:fp=a17a1317c0fa8e7b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: request.headers is external HTTP input, the rule is R1, and the subject is rooted in the dispatch method's request parameter. The structural remediation is @trust_boundary\(tier=3, source='inbound HTTP request headers', source_param='request', suppresses=\('R1',\), ...\) on dispatch, not a per-line allowlist entry. The rationale itself is sound on the merits — absent header is legitimately optional, empty-string sentinel flows into a validator that regenerates — but policy directs Tier-3 function-scoped R1 suppressions to the decorator. |
| `web/execution/runtime_preflight.py:R9:RuntimePreflightCoordinator:run:_evict:fp=a4269762ca0780b3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The identity guard on line 60 \(\`self._inflight.get\(key\) is finished\`\) already proves the key is present and maps to \`finished\` immediately before the pop. Under the GIL, no other code can intervene between the guard and the pop within this synchronous callback, so the key is guaranteed to exist at pop time — the \`None\` default is dead defensive code hiding a non-existent race. R9's concern \(implicit default hiding a missing-key bug\) applies directly: if the invariant ever breaks, the pop should raise KeyError loudly. Fix is \`del self._inflight\[key\]\` or \`self._inflight.pop\(key\)\` without the default; this is internal first-party state, not a trust boundary. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=fdc4a3946c001515` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being read is from the Landscape audit database \(\`transform_errors_table\` via \`db.read_only_connection\(\)\`\), which is Tier-1 per policy — 'Our Data \(Audit Database / Landscape\) — FULL TRUST'. Tier-1 explicitly forbids defensive \`.get\(\)\` with defaults: 'A defensive .get\(\) on a row read out of the audit database is forbidden — if the field is missing, that is a catastrophic invariant break and must surface.' The rationale's claim that 'shape varies by transform' and substituting 'UnknownError' is a documented fallback is exactly the fabrication pattern the policy prohibits — None→'UnknownError' changes meaning. The decorator does not apply: this is not Tier-3 external data, it's our own audit trail. Fix the schema/writer contract so error_type is always present, or record absence as None and let consumers handle it. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=b09747c7efb37444` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being accessed is \`error_details_json\` read from \`transform_errors_table\` — this is Tier-1 audit-database data, not Tier-3 external data. Policy is explicit: 'A defensive .get\(\) on a row read out of the audit database is forbidden — if the field is missing, that is a catastrophic invariant break and must surface.' The rationale that 'some transforms write reason instead' describes a schema inconsistency in our own first-party code writing to the audit trail; the fix is to normalize what transforms write \(one canonical key\), not to paper over both conventions with a fallback chain in the reader. The two-level fallback to empty string is also fabrication — substituting '' for an absent field changes meaning from 'unknown' to 'empty message'. |
| `web/execution/failure_samples.py:R1:load_top_failure_samples:fp=1f176fb7969fbed2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data being read is \`error_details_json\` from \`transform_errors_table\` — this is Tier-1 audit database data \(Landscape\), not Tier-3 external input. Per policy, Tier-1 data must be 100% pristine and a defensive \`.get\(\)\` on a row read from the audit database is explicitly forbidden \('silently coercing bad data is evidence tampering'\). If the audit trail's error_details JSON lacks an 'error' or 'reason' field, that is a catastrophic invariant break that must surface, not be papered over with an empty string. The rationale invokes 'same justification as the outer .get' — but the outer .get is equally wrong; chaining a second forbidden pattern does not legitimize it. Fix the schema/writer contract so error_details has a guaranteed shape, then access directly. |
| `web/composer/guided/steps.py:R1:handle_step_1_source:fp=d12bd40a848a8b71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 \(.get\) on resolved.options — externally-supplied SchemaForm data, a legitimate Tier-3 boundary case. However, this is the structural decorator pattern: the function handle_step_1_source has external data flowing through a parameter \(likely args/state\), and the rule_id R1 is in the suppressible set {R1, R5}. The correct remediation is @trust_boundary on the enclosing function rather than a per-line allowlist entry. I cannot see the function signature in the excerpt to name the exact parameter, but the data subject 'resolved.options' is rooted at SchemaForm input passed in via the handler's parameters; the agent should apply the decorator with the appropriate source_param. |
| `web/composer/guided/recipe_match.py:R1:_classify_slot_resolver:fp=6726fb5580a0cb92` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims sink.outputs\[0\].options is Tier-3 external data, but the excerpt shows sink is a SinkResolved \(a typed first-party contract object\) — its .options field has a guaranteed shape by the time it reaches this resolver. Even if the original values originated from a user recipe, by the time they're wrapped in SinkResolved they've passed through ELSPETH's contract layer and the dict shape is guaranteed. More importantly, the .get\(\) here substitutes a fabricated default path 'outputs/classified.jsonl' when 'path' is absent — this is precisely the fabrication pattern the policy forbids \(None→default changes meaning from 'unknown' to a concrete value\). The honest fix is either to require 'path' in the predicate gate \(like blob_ref above\) and crash on absence, or to record absence as None and let the recipe_offer turn surface it. The 'propagates to operator confirmation' framing does not redeem fabricating a default silently at this layer. |
| `web/composer/guided/recipe_match.py:R1:_split_threshold_slot_resolver:fp=e71b13ac68a94bb6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims this is a Tier-3 coercion, but the code shows a fabrication pattern: when 'path' is absent from sink.outputs\[N\].options, the code substitutes a synthetic default like 'outputs/above.jsonl' that the external system never asserted. Per the fabrication-decision test, this changes the meaning from 'unknown/absent' to a concrete fabricated path — exactly what the policy forbids \('None -&gt; 0 is fabrication'; here None -&gt; 'outputs/above.jsonl'\). Furthermore, the function just above asserts an explicit InvariantError on missing 'blob_ref', demonstrating that this resolver's contract is to crash-on-missing, not paper over absence. If 'path' is required for the recipe slot, the honest fix is direct subscript access \(or an analogous InvariantError\) — not a silent default. The rationale's appeal to 'operator confirmation downstream' is the Shifting-the-Burden pattern. |
| `web/composer/guided/recipe_match.py:R1:_split_threshold_slot_resolver:fp=fd8beb31159c31ca` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims a Tier-3 boundary coercion, but the .get\(\) with a fabricated default path \('outputs/below.jsonl'\) is exactly the forbidden pattern: substituting a synthetic value for an absent field rather than recording absence as None or crashing on the upgrade to Tier-2. Per the fabrication-decision test, inventing a default output path the LLM/operator never asserted changes the meaning from 'unspecified' to a concrete filesystem path — that is fabrication, not coercion. If sink.outputs\[1\].options lacks 'path', the honest response is either to validate at the boundary and raise \(as the function already does for blob_ref two lines above\) or to propagate None for operator confirmation explicitly — not to silently invent a path. The decorator nudge does not apply: the subject is rooted at \`sink\`, not at an external function parameter named in a @trust_boundary contract, and the value is constructed by upstream first-party code. |
| `web/blobs/service.py:R6:BlobServiceImpl:finalize_run_output_blobs:_sync:fp=0f0584634973a5ac` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R6 \(silent-except\) but the excerpt only shows an inner except for BlobQuotaExceededError that re-finalizes the blob as 'error' — that is an explicit error result, not a swallow. The actual R6 finding must be on a broader outer except that collects errors into the 'errors' list; that code is not visible in the excerpt, so I cannot verify the rationale's claim that 'narrow filesystem/database failures are collected.' The rationale is vague \('narrow filesystem/database failures'\) without naming the exception types caught or explaining how collected errors surface to the caller as an explicit error result per R6's remediation. Without seeing the except clause being suppressed and evidence it is narrowly typed and produces an inspectable error result \(not a bare except swallowing all exceptions\), this is an uninterpretable audit record. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=ebdc5429b73a3656` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale text references variant $defs entries lacking a 'required' list, but the flagged line 467 is \`entry.get\("$ref", ""\)\` — a different .get\(\) call on a different object. The rationale does not address the actual flagged pattern. Additionally, the schema being processed here is produced by Pydantic \(first-party-controlled tooling generating JSON Schema\), not Tier-3 external data; calling it a 'Tier 3 boundary' is a misapplication. If a oneOf entry legitimately lacks $ref, the correct offensive approach is an explicit \`if "$ref" not in entry: continue\` check, not a silent .get\(\) with a fabricated empty-string default that then gets string-prefix-tested. The rationale also fails to justify the two other .get\(\) calls on lines 474-475 that share the same pattern. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=b3f6fc4a8d272276` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The data here is a Pydantic-generated JSON Schema, not Tier-3 external data — Pydantic is first-party code we control, and the schema's shape is a contract. The rationale calls this a 'Tier 3 boundary' but a JSON Schema produced by our own Pydantic models is not external input from an untrusted system. More importantly, JSON Schema semantics make 'required' an optional key \(absent means no required fields\), so the correct offensive idiom is direct access with a typed fallback at the schema-spec level, or an explicit \`if "required" in variant\` check that documents the JSON Schema invariant — not a generic \`.get\(\)\` that R1 flags as hiding missing-key bugs. The rationale also doesn't address R1's specific concern \(distinguishing 'key legitimately absent per spec' from 'key missing due to bug'\); it just asserts the tier label. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=d98fcb19a086f35c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged line is \`field_schema.get\("type"\)\` inside an \`if "anyOf" in field_schema and not field_schema.get\("type"\)\` guard — this is not about the optional 'default' field the rationale mentions, so the rationale doesn't even speak to the flagged pattern. More fundamentally, \`field_schema\` is a JSON-Schema property dict assembled by ELSPETH's own plugin-spec machinery \(first-party typed contract over plugin schemas\), not Tier-3 external data; the \`.get\("type"\)\` here is a redundant shape probe that could be written as direct membership/access. Even granting some Tier-3 framing for arbitrary plugin-author schemas, the rationale is shallow boilerplate that points at the wrong call site. The decorator nudge does not apply: \`field_schema\` is not a function parameter sourced from an external boundary in this static method's contract. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=dbd0c2b47b09652b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`branch.get\("type"\)\` at line 504, where \`branch\` is an element of \`field_schema\["anyOf"\]\`. The rationale invokes 'Tier-3 boundary' and 'JSON schema field default is optional' — but the flagged line is not about a 'default' field, it's about reading 'type' from an anyOf branch. The rationale doesn't speak to the actual flagged pattern. Furthermore, JSON schemas in this codebase are plugin-declared first-party artifacts \(used by CatalogServiceImpl to summarize plugin config schemas\), not external Tier-3 input — these schemas are authored by ELSPETH plugin code. Even if treated as Tier-3, the rationale is shallow and mis-targeted. The decorator nudge doesn't apply: \`field_schema\` is a parameter but R1 here is on \`branch\`, an intermediate value derived by iteration, and the data source is a plugin-supplied schema rather than an external untrusted payload. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=9e86d37d099865b9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on line 505: \`branch.get\("type", "object"\)\`, but the rationale invokes the 'default' field which is on line 512 — the rationale does not address the flagged pattern. Even setting that aside, this is reading a JSON-Schema document used internally to describe plugin config schemas; these schemas are first-party artifacts produced by ELSPETH plugin code, not Tier-3 external data. The 'type' field's presence on an anyOf branch is a schema-shape concern that should either be directly indexed \(and crash if a malformed schema slips in\) or handled via offensive validation, not silently defaulted to 'object'. Substituting 'object' when 'type' is absent is exactly the fabrication pattern the policy forbids. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=811406999d977a9f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on field_schema.get\("default"\) inside a static helper that takes a dict parameter named field_schema. While JSON schemas are externally-influenced, the structural decorator @trust_boundary would be the right remediation if this were truly a Tier-3 boundary — but _field_summary is a private helper, not the boundary itself; the schema should already have been validated/normalised by the catalog loader before reaching here. Either the schema is a first-party typed contract \(in which case use direct access and let it KeyError\) or the boundary is upstream and this helper should rely on that contract. The rationale 'JSON schema field default is optional' is a shape claim, not a trust-tier argument — optionality should be expressed by the contract \(TypedDict/dataclass with Optional\), not papered over with .get\(\). Fix the schema contract or move the boundary; do not allowlist this site. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=cad629e5a725a37c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _field_summary receives field_schema as a parameter, but the three-question decorator test applies: this is R1 on a subscript rooted at a function parameter that carries externally-sourced JSON-Schema data. The structural remediation is @trust_boundary\(tier=3, source_param='field_schema', suppresses=\('R1',\), ...\) on _field_summary, not a per-line allowlist entry. Additionally, the rationale is one-line and shallow — it asserts 'Tier 3 boundary' without explaining what trust has been established or why recording absence as None \(which .get does here, returning None as default\) is the right semantic. The decorator nudge is the correct remediation. |
| `web/composer/state.py:R1:_validate_web_scrape_abuse_contact_not_reserved:fp=1be9831e03d16765` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`node\` as its parameter, not a raw external mapping — \`node.options\` is typed \`Mapping\[str, Any\]\` per a first-party contract. However, the value at \`options\['http'\]\` is unstructured \`Any\` originating from user/LLM composer state, so this is a legitimate Tier-3 boundary read on externally-sourced contents. R1 is in the decorator's suppressible set, and the data-flow path \`node.options -&gt; http -&gt; abuse_contact\` is rooted at the function parameter \`node\`. The structural remediation is \`@trust_boundary\(tier=3, source_param='node', suppresses=\('R1','R5'\), ...\)\` on the enclosing function rather than per-line allowlist entries. |
| `web/composer/state.py:R1:_validate_web_scrape_abuse_contact_not_reserved:fp=cacfb09cb1ee4078` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary case that matches all three decorator criteria: the validator processes operator-supplied node.options \(external configuration\), the rule is R1, and the subject http.get\('abuse_contact'\) is rooted at the external options parameter via node.options\['http'\]. The correct remediation is @trust_boundary on the validator function \(with source_param naming the node/options parameter\) rather than a per-line allowlist entry. The rationale itself is substantively correct about the tier, but the structural fix exists and should be used. |
| `web/composer/state.py:R1:_validate_web_scrape_http_identity_not_placeholder:fp=bea652fff29d7051` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes a NodeSpec parameter whose .options dict carries user/LLM-authored composer state \(Tier-3\), the rule_id is R1, and the flagged subject \(node.options.get\('http'\)\) is rooted at the function's parameter. The correct remediation is @trust_boundary\(tier=3, source_param='node', suppresses=\('R1','R5'\), ...\) on the enclosing function rather than a per-line allowlist entry. The rationale is substantively correct about the tier but should land as the decorator structural fix. |
| `web/composer/state.py:R5:_validate_web_scrape_http_identity_not_placeholder:fp=d7b50b5c57e5c231` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`node: NodeSpec\` as parameter, and the R5 finding is on \`http\`, which derives from \`node.options.get\("http"\)\` — a clear data-flow path from the external composer-state parameter into the isinstance-guarded expression. This is exactly the structural Tier-3 boundary pattern the @trust_boundary decorator exists to handle: a function-scoped guard on names rooted at an external parameter, rule R5 \(in the suppressible set\). The correct remediation is the decorator with source_param='node', not a per-line allowlist entry. |
| `web/composer/state.py:R1:_validate_web_scrape_http_identity_not_placeholder:fp=976e77012c7cb2f6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes a NodeSpec containing operator/LLM-authored options \(external untrusted data\), the finding is R1 \(in the decorator's suppressible set\), and the subject http.get\(field_name\) is rooted in the external node.options data flow. The correct remediation is @trust_boundary\(tier=3, source_param='node', suppresses=\('R1','R5'\), ...\) on the enclosing function rather than a per-line allowlist entry. The rationale itself is substantively correct about the trust tier, but the structural fix supersedes the line-level suppression. |
| `web/composer/state.py:R5:_validate_web_scrape_http_identity_not_placeholder:fp=8e9f7b0aaa21d365` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case: the function takes external NodeSpec configuration data, the rule is R5 \(in the suppressible set\), and the guarded subject \(value from http.get\(field_name\)\) is rooted in node.options, a parameter-derived external mapping. The correct remediation is @trust_boundary\(tier=3, source_param='node', suppresses=\('R5',\), ...\) on the function, not a per-line allowlist entry. The rationale itself correctly identifies the Tier-3 boundary nature, which is exactly the case the decorator was designed to handle structurally. |
| `web/composer/state.py:R1:_check_schema_contracts:_producer_emit_set:fp=553d63b88e277081` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims sources with producer_id=='source' aren't in node_by_id, but the excerpt shows that case is already handled by an explicit early return at line 1372-1373. By the time line 1375 executes, producer.producer_id is NOT 'source', so the stated justification doesn't apply to this site. If producer_id refers to a non-existent node at this point, that's a schema-contract violation and should surface — the policy's offensive-programming stance says detect-invalid-state-and-throw rather than silently fall back to a different code path. node_by_id is first-party data assembled from the pipeline graph, not Tier-3 external data, so R1's concern \(hiding missing-key bugs\) applies directly. Fix the root cause: use direct subscript and let KeyError surface, or assert membership with a meaningful error. |
| `web/composer/state.py:R1:_check_schema_contracts:fp=c09544503f550037` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims node.options is raw Tier-3 user/LLM-supplied data, but node.options is a field on a first-party node object constructed by ELSPETH's own composer/state code — it is pipeline/Tier-2 data with a guaranteed shape \(a dict\), regardless of where its contents originated. Defaulting a missing 'select_only' key to False is exactly the fabrication pattern: it conflates 'absent' with 'explicitly false' for the purpose of gating Rule C, when the honest path is to either trust the schema \(direct access, KeyError if the contract is broken\) or normalise options through FieldMapperConfig once rather than re-implementing its default inline. 'Matches the pydantic default' is not a Tier-3 boundary argument — it is duplicating plugin-internal config semantics in engine code, which is also a layer-discipline smell. Decorator nudge does not apply: this is not inside an external-parameter boundary function and the subject is not rooted at a Tier-3 parameter. |
| `web/composer/_required_paths_validator.py:R5:_optional_ancestor_present:fp=b07f5f9d3d7a04bf` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data flowing from an LLM \(Tier-3\), and the subject \`cursor\` is rooted in the function parameter \`value\`. All three decorator-nudge conditions are met: the function takes external data, the rule is R5 \(suppressible by @trust_boundary\), and the guarded expression traces back to that parameter. The correct remediation is to apply @trust_boundary\(source_param='value', suppresses=\('R5',\), ...\) on the enclosing function rather than a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=129470a225d57f27` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This R2 finding is on \`getattr\(response, "usage", None\)\` inside \`token_usage_from_response\`, which takes the LiteLLM/provider response object as a parameter. The response is externally-sourced Tier-3 data with provider-variable shape, so guarding it is legitimate per the Plugin-Ownership scope carve-out — but the correct remediation is structural: apply \`@trust_boundary\(tier=3, source_param='response', suppresses=\('R2',...\), ...\)\` rather than a per-line allowlist entry. R2 is not in the decorator's currently-suppressible set {R1, R5}, however, so the decorator nudge does not apply here; still, the rationale is generic boilerplate that names a tier but does not engage with R2's specific concern \(why \`getattr\` with a \`None\` default rather than direct access plus a typed Tier-3 normaliser at the boundary\). The honest Tier-3 pattern is to normalise the provider response into a known shape once at the boundary and let downstream code use direct access; suppressing R2 inline perpetuates ad-hoc shape-guarding. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=019b920aea416acb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate Tier-3 external-data boundary \(LiteLLM/provider response, shape not guaranteed by any first-party contract\), and R1 is a suppressible rule. The finding's subject \(\`usage.get\(...\)\`\) is rooted in \`response\`, the function parameter carrying external provider data. Per the three-question test, the correct remediation is the @trust_boundary decorator on \`token_usage_from_response\` with source_param="response" and suppresses=\("R1","R5"\), not a per-line allowlist entry. The excerpt does not show existing decorators, but the function-scoped Tier-3 pattern with multiple R1/R5 sites strongly favors the structural fix. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=8a4d5742fc096a4c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary \(LiteLLM provider response usage mapping is not first-party shape-guaranteed\), and R1 is in the decorator's suppressible set. The function takes \`\`response\`\` as a parameter and the flagged \`\`.get\`\` calls are rooted at \`\`usage\`\` which is derived directly from that external parameter via getattr. All three decorator-nudge conditions are met, so the correct remediation is \`\`@trust_boundary\`\` on \`\`token_usage_from_response\`\` with \`\`source_param='response'\`\` rather than a per-line allowlist entry. The excerpt does not show any existing decorator above the function signature line, so the nudge is appropriate. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=7a51927419dde97b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 external-data boundary \(LiteLLM provider response, externally-sourced shape\) and R1 is in the decorator's suppressible set. The flagged subject \`usage.get\("output_tokens_details"\)\` is rooted at \`usage\`, which derives directly from the function parameter \`response\` via \`getattr\(response, "usage", None\)\`. All three decorator-test conditions are satisfied: external parameter, R1 rule, data-flow rooted at the parameter. The correct remediation is \`@trust_boundary\(tier=3, source='LiteLLM provider response', source_param='response', suppresses=\('R1','R5'\), ...\)\` on \`token_usage_from_response\`, not a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=23654a5020554ff2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external boundary \(LiteLLM provider response usage payload, shape not guaranteed by any ELSPETH contract\), and R1 is in the decorator's suppressible set. The function takes \`response\` as its external parameter and the flagged \`.get\(\)\` calls are rooted in \`usage = getattr\(response, 'usage', None\)\`, a clear data-flow descendant of that parameter. The correct remediation is \`@trust_boundary\(tier=3, source_param='response', suppresses=\('R1','R5'\), ...\)\` on \`token_usage_from_response\`, not a per-line allowlist entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=40e4830b7986f089` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is in a function that takes external/provider response data \(\`response\` parameter from LiteLLM\), R1 is in the decorator-suppressible set, and the subject \`usage.get\("completion_tokens"\)\` is rooted at the \`response\` parameter via \`getattr\(response, 'usage', None\)\`. This is the canonical structural Tier-3 boundary case the @trust_boundary decorator was built for — applying it once with source_param='response' covers all the .get\(\) calls in this block rather than landing per-line allowlist entries. The rationale correctly identifies the trust tier, but the remediation is the decorator, not a YAML entry. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=14c33dfbf868059d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 LLM provider response boundary \(R1 on usage.get of LiteLLM/provider data\), and the function takes the external response as a parameter. All three decorator conditions are met: external-data parameter \(\`response\`\), rule R1 is in the suppressible set, and the subject \`usage.get\("total_tokens"\)\` is rooted in \`response\` via \`getattr\(response, "usage", ...\)\`. The correct remediation is to apply @trust_boundary to the enclosing function rather than allowlist this single line \(which would leave many sibling \`.get\(\)\` calls in the same function unsuppressed anyway\). |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=01435ad22c7f7946` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary \(LLM provider response from LiteLLM, externally-sourced shape\) and R1 is in the suppressible set, so the structural remediation is @trust_boundary, not a per-line allowlist entry. The function token_usage_from_response takes \`response\` as its external-data parameter, and the flagged \`usage.get\(...\)\` chain is rooted at that parameter via \`usage = getattr\(response, 'usage', None\)\`. The correct fix is to decorate the function with @trust_boundary\(tier=3, source_param='response', suppresses=\('R1','R5'\), ...\) rather than allowlisting each .get\(\) line individually. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=fa9224835d150b3f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 boundary case \(LLM provider usage metadata via LiteLLM, shape not guaranteed\) and the rule_id is R1 — exactly what @trust_boundary is designed to cover structurally. The function takes \`\`response\`\` as its external parameter and the .get\(\) calls are rooted in \`\`usage = getattr\(response, 'usage', None\)\`\`, derived from that parameter. The correct remediation is to decorate \`\`token_usage_from_response\`\` with @trust_boundary\(tier=3, source_param='response', suppresses=\('R1','R5'\), ...\) rather than land a per-line allowlist entry. The rationale, while substantively correct about the trust tier, points to a structural fix, not a YAML suppression. |
| `web/composer/llm_response_parsing.py:R1:token_usage_from_response:fp=5cd0d50b12dd73ad` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary \(LiteLLM/provider response usage object\), and R1 is in the decorator's suppressible set. The function takes the provider response as a parameter and the flagged subject \(\`usage.get\("reasoning_tokens"\)\`\) is rooted at that parameter via \`getattr\(response, "usage", None\)\`. The correct remediation is the @trust_boundary decorator on the enclosing function, not a per-line allowlist entry. Note: the excerpt does not show the function signature, so I cannot see the exact parameter name — the agent should confirm it \(likely \`response\`\). |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=c745f85451112e20` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 LiteLLM provider-response boundary and the getattr shape-guarding is legitimate in principle, but R2 is not in the decorator's suppressible set \({R1, R5}\), so the structural fix doesn't apply here. The function name token_usage_from_response and the presence of a 'usage' parameter strongly suggest this IS the appropriate boundary function, but the excerpt does not show the function signature or any existing decorators — I cannot confirm whether 'usage' is a direct parameter or whether decorators already exist. More importantly, the rationale is generic boilerplate that does not engage with R2's specific concern \(getattr hiding missing-attribute bugs on a typed contract\); it does not explain why the provider response object's shape is genuinely unguaranteed at this point vs. why a typed LiteLLM response model couldn't be used. A site-specific rationale naming the provider variance \(e.g., 'OpenAI returns CompletionUsage object, Anthropic returns dict, older providers omit *_details entirely'\) would be acceptable; this generic invocation is not. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=9abdc8a826386eea` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr with default\) on an external LiteLLM/provider response object — this is genuine Tier-3 data with provider-variable shape, so guarding it is honest. However, R2 is NOT in the @trust_boundary decorator's suppressible set \(only R1 and R5\). More importantly, the rationale is generic boilerplate that doesn't address the specific pattern: substituting None for absent provider attributes is the correct Tier-3 absence-recording behavior, but the rationale should say so explicitly per the auditability standard. The deeper issue is that this looks like a structural case where each getattr site needs its own entry — the agent should consider whether a per-file R2 rule scoped to this provider-response parser would be a cleaner audit record than line-by-line entries, or extend the decorator's suppresses set. As written, the one-line rationale is too shallow to serve as a durable audit record for this many sites. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=633b8b555a08a0d5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R2 \(getattr with default\), which is not in the decorator-suppressible set \({R1, R5}\); the decorator cannot help here. The rationale correctly identifies that LiteLLM usage objects are Tier-3 provider-shaped data, which would license shape-guarding — but defaulting absent fields to None and propagating them into usage_data is honest recording-of-absence, so the pattern is defensible on policy. However, the rationale is generic boilerplate that never addresses R2 specifically \(why getattr-with-default rather than direct attribute access on a known provider contract, or coercion-and-record at the boundary\), and the file appears to be a parsing/normalisation module that arguably IS the Tier-3 boundary where shapes should be pinned down per-provider rather than blanket-getattr'd. A site-specific rationale naming the provider variability and the recording-absence-as-None invariant is required; this one does not clear that bar. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=cb1c4be269a3e9f0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 LLM provider response boundary \(LiteLLM usage object whose shape varies by provider\), and guarding it with getattr is legitimate per the Plugin Ownership scope clause. However, the function \`\`token_usage_from_response\`\` takes \`\`usage\`\` \(or a response\) as a parameter and the finding is rule R2 on \`\`getattr\(usage, ...\)\`\` rooted at that external parameter — this is exactly the structural case the @trust_boundary decorator exists to handle, except R2 is not in the decorator's suppressible set {R1, R5}. Even so, the rationale is shallow: it asserts the tier but does not address why R2 specifically is honest here, nor does it commit to recording absence as None \(which the code does appear to do\) versus fabricating. Given R2 is outside the decorator's scope, the agent should either broaden coverage structurally or write a site-specific rationale that names the provider-shape variance and the None-preservation invariant; a generic 'provider metadata boundary' one-liner is insufficient as an audit record. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=2bfecea3bd9c907b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary \(LiteLLM/provider response object whose shape is provider-variable and not guaranteed by any first-party ELSPETH contract\), and the rule R2 getattr-with-default pattern is exactly the boundary-guard idiom here — recording absence as None rather than fabricating values. However, R2 is not in the decorator's suppressible set \({R1, R5}\), so the structural nudge does not apply. The rationale is also thin: it asserts the tier but does not speak to why getattr-with-None-default \(rather than direct access + except\) is the right shape-guard at this specific site, nor does it address each field. A more site-specific rationale tying the pattern to the recorded-as-None contract on this function would be acceptable; as written it reads as boilerplate. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=320755415534b8b2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R2 \(getattr\) finding inside what appears to be a function processing LiteLLM provider response metadata — genuinely Tier-3 external data. However, the @trust_boundary decorator only suppresses R1 and R5, not R2, so the decorator nudge does not apply. The rationale correctly identifies the tier but R2's concern is specifically that getattr-with-default hides missing-attribute bugs; the rationale doesn't engage with why every one of these seven fields needs a silent default rather than, e.g., a single shape check followed by direct attribute access, or explicit per-provider normalization. Recording absence as None is the right policy outcome, but the suppression scope \(a blanket getattr-with-default sweep\) is broader than the rationale justifies and reads as boilerplate Tier-3 invocation rather than a site-specific defense of this pattern against R2's specific concern. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=0d833fdefa90f98f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R2 \(getattr with default\) is not in the @trust_boundary decorator's suppressible set \({R1, R5}\), so the decorator nudge does not apply. However, the rationale correctly identifies this as Tier-3 provider/LiteLLM metadata — provider response shapes are genuinely externally-controlled and not guaranteed by any ELSPETH contract, and recording absence as None \(not fabricating a default\) matches the policy's fabrication-decision test. The getattr-with-None pattern here is the legitimate Tier-3 boundary form: coerce shape, record absence honestly. But the excerpt is truncated and I cannot see whether the enclosing function is already decorated or whether a broader per-file rule would be more appropriate than a per-line entry; the rationale is also somewhat boilerplate \('may be mapping-shaped, object-shaped, absent'\) without specifying why each individual field needs the guard. Leaning BLOCKED on the conservative prior — the agent should consider whether normalising the provider response once at the boundary \(and then using typed access\) would be a structurally better fix than 9 individual getattr suppressions. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=b62e64bc2d61e617` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a genuine Tier-3 external-data boundary \(LiteLLM provider response, shape not guaranteed by any ELSPETH contract\), and guarding the shape with getattr returning None is the honest pattern — absence is recorded as None rather than fabricated. However, the finding is R2 \(getattr\), which is NOT in the @trust_boundary decorator's suppressible set \({R1, R5}\), AND the subject \`usage\` is not rooted at a parameter named in source_param of any visible decorator. The rationale is also shallow boilerplate that doesn't speak specifically to why getattr-with-default is needed here rather than direct attribute access plus a try/except at the boundary. The agent should either restructure to coerce/validate once at the boundary \(so downstream code accesses a typed shape\), or broaden a per-file/per-rule policy for R2 on provider response objects rather than per-line allowlist churn. |
| `web/composer/llm_response_parsing.py:R2:_safe_provider_request_id:fp=25a663b069e6a21e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary \(LiteLLM/provider response object whose shape ELSPETH does not own\), so guarding shape is legitimate in principle. However, the finding is R2 \(getattr-with-default\), and the @trust_boundary decorator only suppresses R1 and R5 — it cannot cover R2. The function takes \`\`response\`\` as an external-data parameter and the flagged subject is rooted at it, but since R2 is outside the decorator's suppressible set, the structural-decorator path does not apply. The rationale is also generic \("may be mapping-shaped, object-shaped, absent"\) and does not speak to why getattr-with-default at this specific site is the right pattern versus, e.g., a single typed normalizer at the border that records absence as None — the auditability standard wants a site-specific justification. |
| `web/composer/llm_response_parsing.py:R1:_response_field:fp=216e94b72103f074` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a generic helper \(\`_response_field\`\) that takes \`value: Any\` and is not itself a trust boundary function — it's a utility called from multiple sites. The three-question decorator test fails on \(1\): \`_response_field\` doesn't receive external data as a named parameter representing a clear Tier-3 source; it's an internal dispatch helper that happens to be used on provider data. The rationale invokes 'Tier-3 LiteLLM/provider metadata boundary' correctly in spirit \(provider responses are Tier 3\), but the suppression should live on the boundary function that first receives the provider response \(with @trust_boundary\), not on every internal helper that pokes at it. Accepting per-helper allowlist entries fragments the boundary and defeats the structural decorator pattern. |
| `web/composer/llm_response_parsing.py:R1:apply_anthropic_cache_markers:fp=37f7de34a9e33a02` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`apply_anthropic_cache_markers\` operates on \`messages\` and \`tools\` — these are request payloads being constructed/transformed to send TO LiteLLM, not provider response objects coming back from an external system. The rationale claims this is a 'Tier 3 LiteLLM/provider metadata boundary' for response objects, but the code is clearly iterating over outgoing messages with role='system'. These messages are first-party constructed dicts under our control; their shape is guaranteed by our own code. The \`.get\("role"\)\` is defensive on data we own — direct indexing \`message\["role"\]\` is correct, and a KeyError would indicate a real bug. The rationale misidentifies the data direction and tier. |
| `web/execution/schemas.py:R1:RunEvent:_resolve_data_from_event_type:fp=946fe25115431a7a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Pydantic before-validator receiving raw input \(\`values: Any\`\) prior to schema validation — a genuine Tier-3 boundary where shape-guarding with \`.get\(\)\` is legitimate. However, all three decorator-nudge conditions are met: \(1\) the function takes external/pre-validated data as its \`values\` parameter, \(2\) the rule is R1, and \(3\) the flagged subject \`values.get\("data"\)\` is rooted at that parameter. The structural remediation is \`@trust_boundary\(tier=3, source_param='values', suppresses=\('R1',\), ...\)\` on \`_resolve_data_from_event_type\`, not a per-line allowlist entry. |
| `web/composer/tools/_dispatch.py:R1:execute_tool:fp=64df0c1899440c17` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook structural Tier-3 boundary case for the @trust_boundary decorator. The finding is R1 \(in the decorator's suppressible set\), it occurs in execute_tool which receives tool_name as a parameter sourced from the LLM, and the flagged expression all_handlers.get\(tool_name\) is rooted in that external parameter. The correct remediation is to apply @trust_boundary\(tier=3, source='LLM tool-call name', source_param='tool_name', suppresses=\('R1',\), ...\) on execute_tool rather than a per-line allowlist entry. The rationale itself correctly identifies the Tier-3 boundary semantics, which is exactly the signal that the decorator — not YAML — is the right tool. |
| `web/composer/yaml_generator.py:R1:_strip_web_metadata:fp=d266b01a6f1f2583` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification has been provided — that alone fails the auditability standard. On the merits, \`options\` is a \`dict\[str, Any\]\` passed by first-party code building YAML from CompositionState; it's not a Tier-3 external boundary. The \`.get\("blob_ref"\)\`/\`.get\("mode"\)\` pattern is checking optional keys in an internal dict, which is exactly the R1 defensive pattern: either the schema guarantees these keys \(use direct access\) or it doesn't \(then the contract should be tightened, e.g. TypedDict or explicit membership check\). The decorator nudge does not apply — this is not a Tier-3 boundary function. |
| `web/composer/yaml_generator.py:R1:_strip_web_metadata:fp=553a2bc2f7828b83` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits it has not been justified — it is asking the reviewer to decide whether this is new debt. That is not an audit-worthy justification. On the merits, \`options\` is a \`dict\[str, Any\]\` passed within first-party code \(composer state serialization\), not external Tier-3 data, and the \`.get\("blob_ref"\)\` / \`.get\("mode"\)\` calls are checking for optional keys that the schema should make explicit. The fix is either to access keys directly if the contract guarantees them, or to make the optionality explicit in the type \(TypedDict / dataclass with Optional fields\), not to suppress R1. The decorator nudge does not apply: this is not a Tier-3 external-data boundary function. |
| `web/composer/yaml_generator.py:R9:_strip_web_metadata:fp=8aa1876af2dcdcd5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) and does not justify the suppression at all — it explicitly asks the reviewer to determine whether this is new debt. R9 flags dict.pop with a default hiding missing-key bugs; here 'mode' is being popped with a None default even though the guard on line 41 already checked options.get\('mode'\) == 'bind_source', so 'mode' is known to be present. The honest fix is \`del stripped\['mode'\]\` \(or restructure the comprehension\), not a suppression. Additionally the options dict is first-party UI/composer state, not a Tier-3 boundary, so no trust-boundary argument applies. |
| `web/sessions/routes/_helpers.py:R1:_patched_routes_attr:fp=468f024091698e3f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`sys.modules.get\("elspeth.web.sessions.routes"\)\` — \`sys.modules\` is a Python runtime dict we control, not Tier-3 external data. The rationale invokes 'HTTP/session route boundary' and 'request payloads', but the actual flagged line has nothing to do with request payloads; it's a module-table lookup used to detect test-time monkeypatching. Worse, the surrounding code pattern \(\`_patched_routes_attr\` returning \`None\` so callers fall back to the real symbol\) reads like a test-seam shim, which would itself be a separate policy concern. The rationale does not address the specific R1 site and is shallow/misapplied; if the module is genuinely expected to be importable, use \`sys.modules\["elspeth.web.sessions.routes"\]\` directly, otherwise the rationale must speak to this exact lookup. |
| `web/sessions/routes/_helpers.py:R2:_patched_routes_attr:fp=7d9b5c1063c91bfc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a getattr-with-default lookup on our own module object \(\`elspeth.web.sessions.routes\`\) to detect test-time monkeypatching of helper names. The rationale invokes 'HTTP/session route boundary' and 'request payloads', but the code at the finding has nothing to do with external request data — it's introspecting a first-party Python module to support a patching shim. That makes the rationale non-responsive to R2 at this site, and the shim pattern itself \('check if something patched us, otherwise fall through to the real impl'\) reads as exactly the kind of transitional/compat scaffolding the No Legacy Code policy forbids. The decorator nudge does not apply: there is no external \`source_param\` and R2 is not in the suppressible set {R1, R5}. |
| `web/sessions/routes/_helpers.py:R2:_get_session_compose_lock_registry:fp=5bf9de9dea5e3690` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a lazy-initialization pattern on Starlette/FastAPI's app.state, not a Tier-3 external-data boundary. The rationale invokes 'HTTP/session route boundary' but the flagged line is checking whether a process-local app-state attribute exists yet — that's first-party code we control, and the contrast at line 284 \(direct attribute access via cast\) shows the same state object is accessed without guarding elsewhere. The honest fix is to initialize session_compose_lock_registry at app startup \(lifespan\) alongside composer_progress_registry, then access it directly; getattr-with-default here is the forbidden 'something might be None' defensive pattern, not a trust boundary. R2 is also not in the decorator's suppressible set {R1, R5}, and the subject is request.app.state, not an external parameter payload. |
| `web/sessions/routes/_helpers.py:R5:_litellm_error_detail:fp=1effdbb33b2605f1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is not at a Tier-3 external data boundary — it is type-discriminating between two internal exception classes \(_BadRequestLLMError vs other LiteLLM exceptions\) to decide which attributes to read. This is exception-type dispatch, which is a legitimate offensive pattern, but the rationale offered \('persisted composer state, request payloads, and audit-drain fallbacks require explicit shape checks'\) is generic boilerplate that does not address THIS isinstance call's actual purpose. The rationale conflates several unrelated concerns and never engages with what R5 flagged at line 488. A correct suppression rationale would explain that exception-class discrimination is necessary because the two carrier types expose redacted-vs-raw provider detail through different attribute surfaces — but that argument isn't made, and arguably the cleaner fix is a polymorphic method on the exception classes rather than isinstance dispatch. Shallow rationale, not site-specific. |
| `web/sessions/routes/_helpers.py:R5:_extract_runtime_model_snapshot:fp=c7a42ffc3485b7c0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code explicitly documents this as Tier-1 data \(composition_state JSON written by our own writer\), and the docstring even justifies using \`type\(value\) is str\` over isinstance precisely so the offensive exact-type check is visible. Yet at line 624 the code uses \`isinstance\(options, Mapping\)\` — the very pattern R5 flags — inconsistent with its own stated discipline. The rationale \("HTTP/session route boundary; persisted composer state... require explicit shape checks"\) misclassifies Tier-1 audit data as a boundary needing shape checks, and is generic boilerplate that does not address why isinstance is correct here when exact-type is used elsewhere in the same function. Remediation is a code change \(use \`type\(options\) is not dict\` or equivalent exact-type check, consistent with surrounding lines\), not an allowlist entry. The decorator nudge does not apply: this is Tier-1, not a Tier-3 external parameter. |
| `web/sessions/routes/_helpers.py:R1:_extract_runtime_model_snapshot:fp=dc84631f2ee5050f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code itself declares this data Tier-1 \(the surrounding raises are 'Tier 1 audit anomaly' errors invoking 'The composition_state writer guarantees'\). Per policy, Tier-1 data must be accessed without defensive coercion — bad data must crash immediately as 'evidence tampering' protection. Using options.get\('model'\) / options.get\('model_version'\) silently substitutes None when the key is absent, which is exactly the forbidden pattern on Tier-1 data; the correct access is options\['model'\] / options\['model_version'\] and let KeyError surface \(or an explicit AuditIntegrityError, matching the sibling shape checks\). The rationale's appeal to 'HTTP/session route boundary' is a tier misapplication — the values being read come from persisted composition_state that the function's own error messages assert is first-party-guaranteed. |
| `web/sessions/routes/_helpers.py:R5:_composer_persisted_validation:fp=92aa5cf0eede32c5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check here is not at a Tier-3 external boundary — runtime_preflight is a first-party value with a typed contract \(either a _RuntimePreflightFailed sentinel object or a preflight result with .is_valid and .errors\). The rationale invokes 'HTTP/session route boundary' and 'request payloads', but the code excerpt shows discrimination between two first-party result types, which is a tagged-union dispatch on internally-produced data, not boundary validation. The correct remediation is to model this as a proper sum type \(e.g., a single result protocol with an is_failed/variant discriminant, or polymorphic dispatch\) rather than isinstance-guarding internal contract shapes. The rationale also leans on 'legacy sentinel preserved for older tests', which brushes against the No Legacy Code Policy — that smell should be resolved, not entrenched via suppression. |
| `web/sessions/routes/_helpers.py:R6:_load_canonical_mapping:fp=3fb4e4fbf22e6de6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder ALLOWLIST-FRESH marker, not a justification — it explicitly asks the reviewer to evaluate whether this is new debt, which is not a defense of the suppression. The R6 finding flags that json.JSONDecodeError is swallowed and silently converted to None, which is the fabrication/silent-recovery pattern: a malformed canonical_payload \(data we presumably wrote ourselves into our own storage\) becomes indistinguishable from a legitimately-absent one. If canonical_payload originated from the Landscape/audit DB \(Tier 1\), corrupted JSON must crash, not return None; if it's Tier-3 external, the boundary must record absence/quarantine rather than swallow. Either way, returning None on decode failure is silent-except, not a documented trust-boundary coercion. |
| `web/sessions/routes/_helpers.py:R5:_load_canonical_mapping:fp=365956fb402548eb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no site-specific justification has been written — it is a stub asking for review, not a defense of the suppression. That alone fails the auditability standard: an allowlist entry must be a self-contained audit record explaining why THIS isinstance check at THIS site is legitimate. Separately, the code is a plausible Tier-3 boundary case \(json.loads output is genuinely unguaranteed in shape, so the isinstance check is honest\), but the function takes a string parameter and parses it locally rather than receiving a pre-parsed external dict, so the @trust_boundary decorator's source_param mechanism does not cleanly apply here. The agent must supply a real rationale identifying canonical_payload as Tier-3 JSON whose decoded shape is unguaranteed. |
| `web/sessions/routes/_helpers.py:R6:_state_data_from_composer_state_impl:fp=ce6f7b423897ff71` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' that does not address the specific R6 finding: a broad except \(ValueError, TypeError, KeyError\) around state.validate\(\) that swallows the exception, fabricates a ValidationSummary\(is_valid=False\) with a generic 'validation_failed' entry, and continues. CompositionState is first-party persisted composer state, not Tier-3 external data; if validate\(\) raises TypeError or KeyError, that is a code/invariant bug that should surface, not be flattened into a generic validation failure. Even if some ValueErrors are legitimately data-shaped, the recovery discards the actual error detail \(replacing it with a hardcoded 'validation_failed' string\), which is precisely the silent-swallow pattern R6 targets and corrupts the audit record. The decorator does not apply: this is not an R1/R5 finding and the subject is not rooted in an external parameter. |
| `web/sessions/routes/_helpers.py:R5:_state_data_from_composer_state_impl:fp=be2139c04c7b110c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and doesn't address this specific isinstance check. The flagged line tests \`isinstance\(runtime, ValidationResult\)\` on a value returned by \`_capture_runtime_preflight_failure\`, which is first-party ELSPETH code — not external/Tier-3 data. If that helper has a union return type, the correct fix is to narrow the contract \(return a discriminated type, raise on the failure path, or split into two functions\) rather than shape-guard with isinstance. This is the exact pattern R5 forbids: using isinstance to dispatch on a first-party return shape instead of fixing the contract. |
| `web/sessions/routes/_helpers.py:R6:_handle_convergence_error:fp=00b7ed549b93bf34` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' that does not engage with the specific R6 pattern at this site: a SQLAlchemyError caught during a partial-state save, logged via slog.error, and converted into a response_body flag with no re-raise and no explicit error/quarantine result returned to the caller. Per the Telemetry and Logging primacy order, slog is the last-resort channel — diverting a DB failure during audit-relevant partial-state persistence to a logger and silently continuing the response is itself a defect, not a justification. The correct remediation is to either re-raise \(let the request fail loudly\) or return an explicit error result that the caller routes through Landscape; the rationale does not argue why neither is possible here. The rationale also bundles three unrelated concerns \('persisted composer state, request payloads, audit-drain fallbacks'\) suggesting it was written to cover a class of sites rather than this one. |
| `web/sessions/routes/_helpers.py:R6:_handle_plugin_crash:fp=10a0a4c1f8e0af26` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and never addresses the specific R6 concern: this except swallows the full SQLAlchemyError family while writing only a slog.error and \(per the truncated comment\) continuing to the 500 path. Per the Telemetry primacy order, slog is the last-resort channel, not a substitute for surfacing a DB failure during audit/state persistence — the partial-state save IS our data \(Tier-1 Landscape-adjacent\), and SQLAlchemy failures here should propagate or be returned as an explicit error/quarantine result, not absorbed with a log line. The decorator nudge doesn't apply \(R6 isn't in the {R1,R5} suppressible set, and save_err isn't rooted in an external parameter\). The agent must either re-raise, convert to an explicit error result the caller branches on, or narrow the recovery so the audit trail records the failure as a fact rather than inferring success. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=a024c22b062f016e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R5 isinstance check is on \`plugin_name = edited\["plugin"\]\` where \`edited\` comes from \`turn_response\["edited_values"\]\` — an HTTP request payload, which is Tier-3 external data. This is a textbook structural boundary case: the function processes external request data, the rule is R5 \(in the decorator's suppressible set\), and the subject is rooted at the external parameter \(turn_response\). The correct remediation is \`@trust_boundary\` on the enclosing function rather than a per-line allowlist entry. I cannot see the function's parameter name in the excerpt, but the data clearly flows from \`turn_response\`, which is the likely source_param. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=cc710463b50e2ed0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data flowing from an HTTP request payload \(edited_values\), which is genuine Tier-3 external data — shape-guarding it and raising HTTPException\(400\) on malformed input is exactly the boundary pattern the policy endorses. However, the function signature is not visible in the excerpt, so I cannot confirm the parameter name carrying the external data, but the pattern \(isinstance on dict values from a parsed request body inside an HTTP route handler\) strongly matches the @trust_boundary decorator's intended use: R5 suppression rooted at an external-parameter-derived name. The structural remediation is to decorate _dispatch_guided_respond_impl with @trust_boundary\(tier=3, source='HTTP composer session request body', source_param=&lt;the request/payload param&gt;, suppresses=\('R5',\), ...\) rather than land per-line allowlist entries for each isinstance check. The agent should identify the actual parameter name from the function signature and apply the decorator. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=f07a10c9d12a6ba4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The R5 isinstance check is on \`observed_columns_raw = edited\["observed_columns"\]\`, which is a field within an HTTP request payload — Tier-3 external data. This is the canonical Tier-3 boundary shape-guard pattern that the @trust_boundary decorator exists to handle structurally. The enclosing function \`_dispatch_guided_respond_impl\` appears to take the request/edited payload as a parameter, and the subject roots back to that external input. The correct remediation is the decorator on the enclosing function, not a per-line allowlist entry. The rationale also lumps together multiple distinct concerns \("persisted state, request payloads, audit-drain fallbacks"\) without site-specific justification for this particular check. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=bab71348af41ba49` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a clear Tier-3 HTTP boundary R5 isinstance check on externally-sourced data \(composer response payload\), and the code's shape-guarding behavior with explicit 400 errors is exactly the structural pattern the @trust_boundary decorator was designed to handle. The finding is on R5, occurs in a function processing external request payload data, and the guarded subject \(sample_rows_raw from edited\[...\]\) is rooted in the external input. The correct remediation is the decorator, not a per-line allowlist entry. I cannot see the function signature in the excerpt to confirm the exact source_param name, but the symbol _dispatch_guided_respond_impl and the pattern of edited\[...\] access strongly suggests an external payload parameter — the agent should identify it and apply @trust_boundary\(tier=3, ..., suppresses=\('R5',\), ...\). |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=248c9c25b0846ebf` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data flowing from an HTTP request payload \(turn_response\['edited_values'\]\['columns'\]\), which is a genuine Tier-3 boundary. The function appears to take request/turn-response data as a parameter, and the isinstance guard validates external shape before use — exactly the pattern @trust_boundary\(tier=3, suppresses=\('R5',\)\) is designed to cover structurally. The rationale is also generic boilerplate covering multiple concerns rather than this site specifically. Per the three-question decorator test, the correct remediation is the decorator on the enclosing function, not a per-line allowlist entry; however, the excerpt does not show the function signature, so the agent must identify the external parameter \(likely 'turn_response' or similar\) when applying it. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=209b201c4d6f6543` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks here operate on values extracted from an external HTTP request payload \(turn_response/edited_values\), which is a legitimate Tier-3 boundary case validating shape before use. However, R5 on a parameter-rooted external value inside a request-handling function is exactly what @trust_boundary\(tier=3, suppresses=\('R5',\)\) exists to cover structurally. The excerpt does not show the function signature or any decorators, so I cannot confirm the source_param name, but the rationale explicitly names 'request payloads' as the source. The correct remediation is the decorator on the enclosing handler, not a per-line allowlist entry. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=22c4187cc4e2b19c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R5 \(isinstance\) on \`options_raw\`, which is rooted at the external \`edited\` payload — a request body parsed from HTTP input. This is a textbook Tier-3 boundary case: external data whose shape isn't guaranteed, validated at the border by raising HTTPException 400 on malformed input. The three-question test is satisfied \(boundary function handling external payload, R5 is in the suppressible set, subject roots at the external param\), so the structural remediation is \`@trust_boundary\` on the enclosing function rather than a per-line allowlist entry. The rationale's content is reasonable but the correct mechanism is the decorator. Note: the excerpt does not show the function signature, so the source_param is inferred from the data flow through \`edited\`. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=d753a4a63fb314d4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks are on values extracted from turn_response\['edited_values'\], which appears to be a client-supplied request payload at an HTTP route boundary — a legitimate Tier-3 location for shape guarding. However, the rationale is generic \('HTTP/session route boundary...'\) and does not engage with this specific R5 pattern. More importantly, this is a textbook @trust_boundary candidate: the function _dispatch_guided_respond_impl handles external request data and the R5 finding is on a value rooted in that external payload. The excerpt does not show the function signature or existing decorators, so I cannot confirm the source_param name with certainty, but the structural remediation \(decorator\) is preferred over a per-line allowlist entry. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=4d71e28876dcb4ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This R5 isinstance check on edited_values\['slots'\] is validating an HTTP request payload — a genuine Tier-3 boundary case. The finding subject \(slots_raw\) is rooted in an external request parameter, and R5 is in the decorator-suppressible set. The structural remediation is @trust_boundary\(tier=3, source_param=&lt;the request-payload param&gt;, suppresses=\('R5',\), ...\) on the enclosing function, not a per-line allowlist entry. The rationale also lumps together multiple unrelated concerns \('persisted composer state, request payloads, audit-drain fallbacks'\) rather than speaking to this specific isinstance site. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=312b6f0675209ff6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance checks here run on fields of guided.step_3_proposal — a previously-persisted composer state object whose shape should be governed by a first-party typed contract, not on the external HTTP request payload. The rationale lumps 'persisted composer state' together with 'request payloads' as if both are Tier-3, but persisted state we wrote is Tier-1/2 and its shape must be guaranteed by the contract, not re-validated with isinstance to raise InvariantError. If step\['plugin'\] can really be a non-string, that's an upstream contract bug to fix at the producer/deserializer, not to guard here. The function signature visible in the excerpt does not show an external parameter that the @trust_boundary decorator could bind to for this subject, so the decorator nudge does not apply. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=16d626de6112ffc8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is on \`options = step\["options"\]\` where \`step\` comes from \`guided.step_3_proposal.steps\[edit_step_index\]\` — this is persisted composer state, which should be governed by a first-party typed contract \(a dataclass/TypedDict\), not an arbitrary mapping requiring shape-guarding. If \`step_3_proposal\` is first-party state, its fields' shapes are guaranteed and isinstance is redundant defensive code; access \`step.options\` directly and let it crash if the contract is broken. The rationale invokes 'persisted composer state... require explicit shape checks' but persisted first-party state IS Tier-1/2 by the policy — serialization doesn't change trust tier. If the state is in fact deserialized from untrusted external input without a normalising contract, the fix is to introduce that contract at the deserialization boundary, not to scatter isinstance guards at every read site. The decorator nudge doesn't apply: the subject isn't rooted at an external parameter of this function. |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=c0f2bbfd5e6c57ed` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) and offers no substantive justification for this specific site. However, the code itself is a textbook Tier-3 boundary case: \`arguments\` is a Mapping\[str, Any\] carrying external tool-call data, the finding is R1 on \`arguments.get\("source"\)\`, and the subject is rooted directly at the \`arguments\` parameter. All three decorator-test questions answer yes, so the structural remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1','R5'\), ...\)\` on the function, not a per-line allowlist entry with a non-rationale. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=7b7d6c8ca4388365` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`arguments: Mapping\[str, Any\]\` — external tool-call data from the composer model — and the R5 isinstance check is rooted in that parameter \(\`source = arguments.get\('source'\)\` then \`isinstance\(source, Mapping\)\`\). This is the canonical Tier-3 boundary pattern that the @trust_boundary decorator was built to handle structurally. The rationale itself is just the fresh-fingerprint placeholder and offers no site-specific justification. Remediation is to decorate the function with @trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1','R5'\), ...\) rather than add a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=feb1bbf03070b119` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`_inline_blob_content_for_proposal\` accepts \`arguments: Mapping\[str, Any\]\` which carries external tool-call data \(composer model arguments\), and the flagged R1 \`.get\(\)\` call is rooted at that parameter via \`source = arguments.get\("source"\)\`. All three decorator-eligibility conditions hold: external-parameter function, rule_id R1 is in the suppressible set, and the subject's data flow originates at \`arguments\`. The correct remediation is \`@trust_boundary\` on the enclosing function, not a per-line allowlist entry. Additionally, the rationale itself \(\`ALLOWLIST-FRESH ... review whether this is new debt\`\) is a meta-note about fingerprint rotation, not a site-specific justification — it does not speak to why this particular \`.get\(\)\` is honest. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=09b43dc60ebd6036` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`arguments: Mapping\[str, Any\]\` which carries external LLM tool-call data \(Tier-3\), and the R5 isinstance check on \`inline_blob\` is rooted at that external parameter via \`arguments.get\('source'\).get\('inline_blob'\)\`. All three decorator-test conditions are satisfied: function takes external data, rule is R5 \(in the suppressible set\), and the subject traces back to the \`arguments\` parameter. The agent's rationale is a placeholder \('ALLOWLIST-FRESH'\) that does not justify the suppression at all. The correct remediation is the @trust_boundary decorator on this function, not a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R1:_inline_blob_content_for_proposal:fp=5e022f0ca07cbf2e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — review whether this is new debt'\) and provides no site-specific justification at all. However, the code itself shows a Tier-3 boundary pattern: \`arguments\` is a tool-call argument Mapping \(external/composer-model-sourced\), and the flagged \`.get\` chain is rooted in that parameter. The function takes \`arguments: Mapping\[str, Any\]\` and the R1 finding on \`inline_blob.get\("content"\)\` traces back through \`arguments.get\("source"\)\` → \`source.get\("inline_blob"\)\` → \`inline_blob.get\("content"\)\`. The structural remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1','R5'\), ...\)\` on the function, not a per-line allowlist entry with an empty rationale. |
| `web/sessions/routes/composer.py:R5:_inline_blob_content_for_proposal:fp=f210c162f5a60357` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly asks for review rather than justifying the suppression — it is not a valid audit record. However, the finding is a structural Tier-3 boundary case: the function takes \`arguments: Mapping\[str, Any\]\` originating from an LLM tool-call payload, the rule is R5 \(in the decorator's suppressible set\), and the flagged \`isinstance\(content, str\)\` traces through \`arguments\['source'\]\['inline_blob'\]\['content'\]\` — rooted at the \`arguments\` parameter. The correct remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R5',\), ...\)\` on the enclosing function, not a per-line allowlist entry. |
| `web/sessions/routes/composer.py:R1:register_composer_routes:accept_composition_proposal:fp=ec8182b9ca85932f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`result.data.get\(_DATA_ERROR_KEY\)\` where \`result\` is a ToolResult returned by a first-party tool execution path — \`result.data\` is a typed contract value produced by ELSPETH's own composer tool code, not external/Tier-3 data. The rationale handwaves about 'HTTP/session route boundary' and 'audit-drain fallbacks' but the specific flagged expression is reading from a first-party plugin return; the isinstance guard plus \`.get\(\)\` plus \`or\` fallback is the textbook defensive pattern Plugin Ownership forbids. The fix is to make \`result.data\` a typed structure \(or fix the tool contract to guarantee the error key when \`not result.success\`\), not to suppress. The decorator does not apply: \`result.data\` is not rooted at an external function parameter. |
| `web/sessions/routes/composer.py:R6:register_composer_routes:accept_composition_proposal:fp=97184ca5456a173f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is R6 \(silent-except\) on a first-party service call, not a Tier-3 boundary. The except block catches KeyError/ValueError from service.reject_composition_proposal and swallows them — only adding a note to the exception object \(which is then discarded, since nothing re-raises rejection_exc\) before raising an unrelated HTTPException about the original validation failure. The rationale invokes 'HTTP/session route boundary' and 'narrow recovery catches' but does not engage with R6's actual concern: that concurrent-rejection or already-terminal states should produce an explicit outcome \(logged via audit, or an explicit branch checking proposal state\), not a silent swallow. If 'proposal already terminal' is a legitimate expected state, it should be detected explicitly rather than caught-and-discarded; if it's unexpected, it must surface. The add_note on a discarded exception is theatre, not auditability. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:get_guided:fp=f09febcda74c85ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | R4 flags broad-except, but the rationale is generic boilerplate \('HTTP/session route boundary... narrow recovery catches'\) that does not justify THIS catch. The code catches \`Exception\` around \`_persist_tool_invocations\` — a first-party audit-persistence call — and swallows the failure after only logging via slog. Per the Telemetry primacy order, audit \(Landscape\) fires first and crashes on failure; diverting an audit-persist failure to slog.error and continuing is exactly the inverted channel the policy forbids. The rationale also claims 'narrow recovery catches' while the code catches bare Exception, contradicting its own justification. Remediation is a code fix \(let the audit failure propagate, or catch a specific exception class\), not an allowlist entry. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:get_guided:fp=a89801c4ea858a56` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a generic boilerplate description of the whole route file \('HTTP/session route boundary; persisted composer state, request payloads, and audit-drain fallbacks require explicit shape checks and narrow recovery catches'\) and never addresses THIS specific R7 site — a contextlib.suppress\(Exception\) wrapping a slog.error call inside an except handler. While the comment in the code claims 'logger-of-last-resort: no safer channel exists if structlog itself raises here,' which is a plausible site-specific argument, the submitted rationale itself does not articulate that reasoning and reads as copy-paste justification spanning unrelated concerns. Per the Auditability Standard, an allowlist entry is itself an audit record; a vague rationale that doesn't speak to the flagged pattern at this exact site is a corrupted audit record. The agent must rewrite the rationale to address the specific terminal-logger pattern here, or restructure \(e.g., a small helper with explicit exception types\). |
| `web/sessions/routes/composer.py:R4:register_composer_routes:get_guided:fp=3596e2d1f66b3f99` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and doesn't address this specific broad-except. The code shows a broad \`except Exception\` around \`_persist_llm_calls\` — a first-party audit persistence call to the Landscape. Per the Auditability Standard, audit writes are sync and crash-on-failure; swallowing exceptions from audit persistence \(even while logging via slog\) inverts the primacy order and corrupts the audit trail. R4 requires catching specific exceptions or re-raising; the rationale never justifies why every Exception subclass must be swallowed here, nor names the concrete failure modes. This is not a Tier-3 external boundary — it's a first-party DB write being silenced. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:get_guided:fp=7357db09796362cd` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a contextlib.suppress around slog.error in an audit-fallback path, not a Tier-3 external-data boundary. Per the Telemetry/Logging primacy order, pipeline-relevant failures belong in the Landscape audit trail; using slog.error as the last-resort channel and then silently swallowing any logging failure is exactly the 'we log it anyway' anti-pattern the policy calls out. The rationale is also generic boilerplate about 'HTTP/session route boundary' that doesn't engage with R7's specific concern \(why is suppressing every Exception from a logging call legitimate here?\). If slog.error can raise, the honest fix is to make logging non-throwing or to narrow the catch to a specific, documented exception class — not a blanket suppress. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_respond:fp=65b078af067fd26a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate \('HTTP/session route boundary... narrow recovery catches'\) that does not address the specific R4 pattern at this site. The code catches bare \`Exception\` around \`_persist_tool_invocations\` — a first-party audit persistence call — and merely logs via slog. Per the Telemetry primacy order, audit \(Landscape\) fires first and must crash on failure; swallowing audit-persist exceptions and diverting to slog.error is itself a defect, not a justification. Additionally, \`_persist_tool_invocations\` is our own code; if it raises, that is a bug to surface, not a Tier-3 boundary to guard. The decorator does not apply \(R4 is not in {R1, R5}\). |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_respond:fp=90d1459942d2afe8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' that does not speak to the specific R7 finding: a contextlib.suppress\(Exception\) wrapping a slog.error call inside an except handler. Per the Telemetry and Logging primacy order, pipeline-relevant audit failures belong in the Landscape, not silently-swallowed slog.error calls — this code is already in the degraded path where audit persistence failed, and suppressing all exceptions from the fallback logger means a future auditor will have no record that the fallback itself failed. The rationale neither identifies what exception types are expected nor justifies why broad Exception suppression is the right scope; 'narrow recovery catches' is asserted but the code shows the opposite \(bare Exception\). This is the defensive 'silently swallow' pattern the policy forbids; if slog.error can raise, the fix is to narrow the except or fix slog, not to blanket-suppress. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_respond:fp=459203baa79b869f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' that does not address the specific R4 broad-except pattern at this site. The code catches bare Exception around _persist_llm_calls — a first-party audit persistence call — and then nests contextlib.suppress\(Exception\) around the logging. Per the Telemetry primacy order, audit \(Landscape\) fires first and must crash on failure; swallowing persistence failures during exception handling silently corrupts the audit trail, which is exactly what the Auditability Standard forbids. The rationale also conflates 'request payloads' \(Tier-3\) with audit-drain persistence \(Tier-1, our data\), which is a misapplication of the trust model. If a narrower catch is genuinely needed \(e.g., a specific DB error\), it must name that exception type; a bare Exception here is not a trust-boundary guard. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_respond:fp=647eaaa3142057a0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a generic boilerplate sentence about 'HTTP/session route boundary' that never addresses the specific R7 pattern flagged here: a contextlib.suppress\(Exception\) wrapping an slog.error call inside an except handler. Per the Telemetry primacy order, audit \(Landscape\) fires first with crash-on-failure, and logging is the last resort — swallowing logger failures with a blanket Exception suppress is exactly the silent-error pattern R7 exists to catch, and the rationale offers no site-specific justification for why this slog.error in particular must not raise. Additionally, this is not a Tier-3 external-data boundary \(no external parameter being shape-guarded\); it is an internal logging call in an exception handler, so the 'boundary' framing is a misapplication of the trust-tier vocabulary. If the intent is truly 'best-effort log during exception cleanup', that needs an explicit narrow catch with a site-specific rationale, not a copy-paste boundary claim. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_chat:fp=4aac0c4c0b044ec2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a broad except wrapping an internal first-party audit persistence call \(_persist_tool_invocations\), not an external Tier-3 boundary. Per the Auditability Standard, the Landscape audit trail fires first and crashes on failure — silently swallowing a persistence exception \(then logging via slog, which is explicitly called out as last-resort, not the channel for pipeline events\) inverts the primacy order and corrupts the audit record. The rationale's invocation of 'audit-drain fallbacks' is precisely the anti-pattern: audit failures must surface, not be downgraded to slog.error. R4's concern \(broad except suppressing bugs\) is not addressed; the fix is to narrow or remove the catch and let audit failures propagate. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_chat:fp=fda975b0625f46e8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and does not speak to the specific R7 pattern flagged: a contextlib.suppress\(Exception\) wrapping a slog.error call inside an except handler. Per policy, audit \(Landscape\) is the primary channel and slog is last resort; silently suppressing all exceptions from a logger call hides defects in the logging path itself and produces a corrupted audit record \('I don't know what happened'\). The decorator nudge does not apply \(R7 is not in {R1,R5}, and the suppressed expression is a logger call, not external-parameter data\). If a narrow recovery is actually needed, the fix is to catch a specific exception type with a meaningful comment, not blanket-suppress Exception — and the rationale must explain why losing the error is acceptable here, which it does not. |
| `web/sessions/routes/composer.py:R4:register_composer_routes:post_guided_chat:fp=6a812675536c1066` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate \('HTTP/session route boundary; persisted composer state, request payloads, and audit-drain fallbacks require explicit shape checks and narrow recovery catches'\) that does not address the specific R4 finding. R4 is about broad-except suppressing bugs; the excerpt shows \`except Exception\` wrapping a call to first-party \`_persist_chat_turns\`, which is internal code, not a Tier-3 external boundary. The policy is explicit: plugin/internal method exceptions should crash, not be swallowed. The rationale mentions 'audit-drain fallbacks' but persistence failures during exception handling should be narrowed to the specific DB/IO exceptions expected, or re-raised after recording. Telemetry-primacy concerns also apply: slog.error is last-resort, not a justification for swallowing the persist failure. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:post_guided_chat:fp=89713c5bac9cbaa5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is generic boilerplate about 'HTTP/session route boundary' and 'audit-drain fallbacks' that does not engage with this specific R7 site. The suppressed call is slog.error — a last-resort logger — inside an except handler for a persistence failure. Per the Telemetry/Logging primacy order, pipeline-relevant events belong in the Landscape audit trail, not logger; swallowing logger failures here masks the fact that even the fallback channel broke. More importantly, the rationale never explains why exceptions from slog.error specifically must be suppressed, what failure mode is anticipated, or why re-raising would be wrong — it is a shallow audit record. R7 is not in the decorator's suppressible set {R1, R5}, so the decorator does not apply. |
| `web/composer/tools/blobs.py:R1:_set_nested_option:fp=9606870cec567eb9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder admitting it's a fresh fingerprint with no site-specific justification — it explicitly says 'review whether this is new debt that needs a source fix'. That is not an audit-grade reason. Separately, the code uses container.get\(head\) where container is a recursively-walked nested dict being mutated by the function itself; absence of the key is a normal expected state \(the function builds the nested structure\), so the correct fix is likely container\[head\] if head in container else None or restructuring, not a suppression. The function is not a Tier-3 boundary on an external parameter — it's an internal helper operating on a dict the caller owns — so the decorator does not apply. |
| `web/composer/tools/blobs.py:R5:_set_nested_option:fp=984ed68ceb1df11f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent-supplied rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly asks for review rather than justifying the suppression — it provides no site-specific reasoning, no tier analysis, and no explanation of why the isinstance check is legitimate here. Per the Auditability Standard, this is a corrupted audit record. Additionally, the isinstance check is on \`child\`, which is read from \`container\` \(a dict being mutated by recursive composer-driven option-setting\); the function does not take an external parameter named in a way that makes the @trust_boundary decorator obviously applicable from this excerpt, and the rationale makes no Tier-3 boundary claim to evaluate. The agent must supply a real, site-specific rationale \(or fix the root cause\) before this can be re-judged. |
| `web/composer/tools/blobs.py:R1:_execute_wire_blob_inline_ref:fp=b158f3692e8bbf5d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on arguments.get\("encoding", "utf-8"\) inside _execute_wire_blob_inline_ref, a tool handler whose \`arguments\` parameter is an LLM tool-call payload — Tier-3 external data, with the subject rooted at the external parameter. This is the canonical @trust_boundary\(source_param='arguments'\) case: function takes external data, rule is R1 \(in the suppressible set {R1,R5}\), and the .get\(\) access flows directly from \`arguments\`. The agent rationale is a stub \('ALLOWLIST-FRESH ... review whether this is new debt'\) and does not justify a per-line allowlist entry. Also note the default 'utf-8' substituted for a missing encoding is borderline fabrication versus coercion — the decorator's invariant field should document that explicitly. Remediation: apply @trust_boundary to the function rather than land this entry. |
| `web/composer/tools/blobs.py:R5:_execute_wire_blob_inline_ref:fp=3658d54a8721ac96` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale text is a placeholder \('ALLOWLIST-FRESH ... review whether this is new debt'\) — it explicitly asks for review rather than justifying the suppression, which is a corrupted audit record by the Auditability Standard. On the merits, this is a textbook Tier-3 boundary case: the function takes external tool-call \`arguments\`, the finding is R5, and the subject \`encoding_value\` is rooted at \`arguments.get\("encoding", ...\)\`. The correct remediation is the \`@trust_boundary\(source_param='arguments', suppresses=\('R5',\), ...\)\` decorator on the enclosing function, not a per-line allowlist entry. |
| `web/composer/tools/blobs.py:R1:_execute_wire_blob_inline_ref:fp=79391f3b16bd3db6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly asks for review rather than justifying the suppression. Substantively, this is a textbook Tier-3 boundary case: the finding is R1 on arguments.get\('sha256_override'\) inside a tool-call handler where 'arguments' is the external composer-model-supplied dict. The correct remediation is the @trust_boundary decorator with source_param='arguments', not a per-line allowlist entry. |
| `web/composer/tools/blobs.py:R1:_state_source_blob_ref:fp=9f3a080fbddec2a0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH...'\) that explicitly admits it has not justified the suppression — it is a prompt to the reviewer, not an audit record. On the merits: state.source.options is a first-party CompositionState field whose shape should be contractually guaranteed; if it is typed as a dict the .get\(\) is the forbidden defensive pattern \(use options\['blob_ref'\] or model the field as Optional on the dataclass\), and if the schema genuinely allows blob_ref to be absent that should be expressed in the type, not papered over with .get\(\). The function takes a CompositionState, not an external parameter, so the @trust_boundary decorator does not apply. The agent must fix the root cause \(schema/contract\), not record this suppression. |
| `web/composer/tools/blobs.py:R5:_state_source_blob_ref:fp=e8ea24c6f35c3e3c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification was supplied — it explicitly says 'review whether this is new debt'. That alone fails the auditability standard: an allowlist entry must be a coherent audit record, not a TODO. On the merits, \`state.source.options\` is a CompositionState field; if its typed contract guarantees \`options\` values' shapes, the isinstance is redundant defensive code \(R5's exact concern\). If \`options\` is a free-form dict whose values come from external composer input, the function takes \`state\` as a parameter and the correct remediation is a @trust_boundary\(tier=3, source_param='state', suppresses=\('R5',\), ...\) decorator on \`_state_source_blob_ref\`, not a per-line entry. Either way the allowlist entry is wrong. |
| `web/composer/tools/blobs.py:R1:_prepare_blob_create:fp=df3f9d3f9790ca88` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on arguments.get\("description"\) inside _prepare_blob_create, which takes external tool-call arguments as a parameter. This is the canonical Tier-3 boundary pattern the @trust_boundary decorator was designed for: external parameter, rule in the suppressible set {R1, R5}, and the subject is rooted at the \`arguments\` parameter. The rationale itself is also shallow — it merely cites an AST shift from a merge and does not justify the suppression on its merits. Remediation: apply @trust_boundary\(tier=3, source_param='arguments', suppresses=\('R1',\), ...\) to the enclosing function rather than a per-line allowlist entry. |
| `web/composer/tools/generation.py:R1:_csv_source_delimiter:fp=a46f1ac16266c1ba` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder fingerprint-rotation note \('ALLOWLIST-FRESH...review whether this is new debt'\), not an actual justification — it explicitly invites review rather than defending the suppression. On the merits, \`options\` here appears to be tool-call/composer-supplied configuration \(Tier-3-ish external input\), and the function does validate the value and reject bad types/lengths, so guarding the optional key is arguably honest at this boundary. But the appropriate remediation is the structural @trust_boundary decorator: this is a function taking an external \`options\` mapping, the rule is R1, and the subject \`options.get\("delimiter"\)\` is rooted at the \`options\` parameter — all three conditions for the decorator nudge are met. A per-line allowlist with a non-rationale should not land. |
| `web/composer/tools/generation.py:R1:_csv_source_skip_rows:fp=5e95739f36d9eda1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder template \('ALLOWLIST-FRESH — no stale entry matched...'\) rather than a site-specific justification. Per the Auditability Standard, an allowlist entry is itself an audit record, and a non-rationale like this is a corrupted audit record that future auditors cannot interpret. Separately, the code reads from an \`options\` Mapping that appears to be externally-sourced configuration — this is a plausible Tier-3 boundary case where \`.get\(\)\` for an optional field with \`None\`-fallback is honest, and the function takes \`options\` as a parameter with R1 in the suppressible set. The correct remediation is the \`@trust_boundary\` decorator on the function \(source_param='options'\), not a per-line allowlist entry with a meta-rationale about fingerprint rotation. |
| `web/composer/tools/generation.py:R1:_csv_source_columns:fp=556a65c1e6ad5068` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification has been written — that alone fails the auditability standard, which requires every suppression to be an interpretable audit record. On the merits: this function parses a user-supplied options Mapping \(config-time external input\), so guarding for an absent 'columns' key is legitimate at the boundary, but the correct remediation is either direct access with a typed-config contract or the @trust_boundary decorator on source_param='options' — not an ad-hoc per-line allowlist entry with no rationale. The agent must either write a site-specific justification or apply the structural fix; the decorator is not suggested here because 'options' is config data rather than the per-call external payload pattern the decorator targets, and the excerpt does not confirm absence of existing decorators. |
| `web/composer/tools/generation.py:R5:_csv_source_columns:fp=f89d44e37975a3cc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent-supplied rationale is the placeholder 'ALLOWLIST-FRESH' boilerplate — it offers no site-specific justification at all, which is by itself a corrupted audit record. Separately, while the function does validate Tier-3-ish options from a user-supplied mapping \(a legitimate boundary use of isinstance to reject malformed input with a clear ValueError\), the proper remediation for repeated R1/R5 patterns on an external-parameter boundary is the @trust_boundary decorator, not a per-line allowlist entry. The finding is R5, the subject 'raw' is rooted in the function's 'options' parameter, and the function's job is exactly the Tier-3 shape-validation pattern the decorator was built for. |
| `web/composer/tools/generation.py:R5:_csv_source_field_mapping:fp=176985867214e183` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is the boilerplate ALLOWLIST-FRESH placeholder \('review whether this is new debt'\) — it explicitly does not justify the suppression and asks the reviewer to do so. The code comment in the excerpt does offer a real argument \(LLM-authored config arrives as MappingProxyType, so isinstance\(Mapping\) is the correct subtype-aware Tier-3 structural guard, while type\(raw\) is dict would wrongly reject it\), and this does look like a legitimate Tier-3 boundary use of R5. However, the agent must rewrite the rationale field to state that justification site-specifically; an audit record reading 'ALLOWLIST-FRESH — review whether this is new debt' is a corrupted audit record per the Auditability Standard. The decorator nudge does not apply: 'options' is config \(not external tool-call arguments\), and the function is module-level, so the @trust_boundary refactor isn't the obvious remediation — fixing the rationale text is. |
| `web/composer/service.py:R5:_request_interpretation_review_kind_from_arguments:fp=ee9dfe639c1746a6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary function: \`arguments\` is a Mapping built from LLM tool-call data, R5 is in the suppressible set, and the flagged isinstance is rooted on \`raw_kind\` which derives directly from \`arguments\["kind"\]\`. The structural remediation is \`@trust_boundary\(tier=3, source_param='arguments', suppresses=\('R5',\), ...\)\` on \`_request_interpretation_review_kind_from_arguments\`, not a per-line allowlist entry. The guard itself is honest \(it raises AuditIntegrityError on malformed shape rather than fabricating\), so the suppression is legitimate in substance — it just needs to land via the decorator. |
| `web/app.py:R1:_BodySizeLimitMiddleware:dispatch:fp=9519401fd195b0ac` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a structural Tier-3 boundary case suitable for @trust_boundary: dispatch\(\) receives an external Starlette request, R1 is in the decorator's suppressible set, and the flagged subject \(request.headers.get\("content-length"\)\) is rooted at the request parameter. The rationale is substantively correct about the trust tier, but the correct remediation is the decorator on dispatch with source_param='request', not a per-line allowlist entry. |
| `web/app.py:R2:create_app:_request_id:fp=318afae72ce2947e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a getattr-with-default \(R2\) on request.state, which is a Starlette framework object whose shape we control via our own middleware contract — not Tier-3 external data. The rationale concedes the precise failure mode: 'if the middleware did not run, the attribute will not exist.' That's an invariant violation in code we own, and per policy the correct response is to let AttributeError surface \(offensive programming: maximally informative crash pointing at the broken middleware contract\), not substitute a fabricated 'unset' sentinel that silently papers over a missing correlation id in the audit/log trail. Substituting 'unset' for an absent value is exactly the fabrication pattern \(None→default\) the manifesto forbids: future auditors querying request_id='unset' cannot distinguish 'middleware skipped' from any other cause. The decorator nudge does not apply — request.state is not external input and R2 is not in the decorator's suppressible set {R1, R5}. |
| `web/composer/source_inspection.py:R1:_facts_from_objects:fp=839f884f581b26d0` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R1 finding inside a function \(_facts_from_objects\) that processes operator-supplied JSON objects — a plausible Tier-3 boundary case. The subject obj.get\(h\) is rooted at an iteration variable derived from the function's \`objects\` parameter, and R1 is in the decorator's suppressible set. The correct remediation is to apply @trust_boundary\(tier=3, source_param='objects', suppresses=\('R1',\), ...\) on the enclosing function rather than landing a per-line allowlist entry. |
| `web/composer/source_inspection.py:R1:_declared_field_name:fp=ec99754b8b1fc32a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes a \`DeclaredFieldSpec\` parameter \(operator-supplied declared-field, externally sourced/Tier-3\) and the R1 finding on \`field.get\("name"\)\` is rooted directly at that parameter. This is exactly the function-scoped Tier-3 boundary pattern the @trust_boundary decorator was built to handle: R1 is in the suppressible set, the function is the boundary, and the subject roots at the parameter. The excerpt does not show any existing decorator above the function, so the structural remediation is to apply @trust_boundary\(tier=3, source_param='field', suppresses=\('R1',\), ...\) rather than land a per-line allowlist entry. |
| `web/composer/source_inspection.py:R5:_declared_field_is_required:fp=61f9e4be883aff78` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no site-specific justification was provided — it is not an honest audit record and cannot stand on its own. Separately, the isinstance check here is on a union-typed parameter DeclaredFieldSpec \(str \| dict-like\), which is a legitimate type-discrimination pattern, but the agent has not made that argument. Without a real rationale tying the suppression to the union-type discrimination, the entry must be rejected; the agent should either write a proper rationale explaining the union dispatch or restructure \(e.g., normalize DeclaredFieldSpec at the parse boundary so downstream code sees one shape\). The function does not take an external parameter routed through a Tier-3 boundary decorator pattern, so @trust_boundary is not the right remediation. |
| `web/composer/source_inspection.py:R1:_declared_field_is_required:fp=431bc71e5d5e137e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder boilerplate \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits it is not a justification and asks for review. It provides no site-specific reasoning about why .get\(\) is appropriate here. Separately, DeclaredFieldSpec appears to be a typed union \(str \| dict-like\) where the dict branch's schema is first-party controlled; if 'required' is optional in the contract, direct access with a typed check or a TypedDict.get with documented semantics could be argued, but the rationale does not make that case. The agent must either justify the suppression site-specifically or fix the schema/access pattern. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=3aba7cf3d76c43d9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`node.options.get\("schema"\)\`, where \`node\` is a pipeline-config object iterated from a collection — not a function parameter named in scope that the decorator could bind to. The excerpt does not show the enclosing function signature, so I cannot confirm a \`source_param\` mapping for a @trust_boundary nudge, and the rationale itself is a generic 'Tier-3 config boundary' line that does not address why this specific access needs \`.get\(\)\` rather than direct subscript with a schema-validated dataclass. If \`node.options\` is a typed mapping from validated config, the shape should be guaranteed and direct access is correct; if it truly is raw operator dict, the function should be decorated as a trust boundary \(covering both this and the sibling \`sink.options.get\`, plus the \`isinstance\` guards\) rather than allowlisted line-by-line. Agent should either restructure to use a typed options contract or apply @trust_boundary to the enclosing function with the appropriate source_param. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=ec32acfc57602caa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`schema_block.get\("fields"\)\` inside \`_find_identity_node_advisories\`. The excerpt does not show the function signature or its parameters, so I cannot confirm the external data is rooted at a named parameter — but the rationale describes user-authored pipeline dictionaries flowing in as Tier-3, and R1 is in the suppressible set. This pattern \(operator-supplied pipeline config, repeated \`.get\(\)\` + \`isinstance\` dispatch on schema/options blocks\) is the canonical @trust_boundary case: the decorator should suppress R1/R5 on the external-rooted parameter rather than landing per-line allowlist entries \(this excerpt alone shows four such accesses\). The per-line allowlist entry is the wrong remediation shape even though the Tier-3 reasoning is sound. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=a1d34b9af2f7e3db` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding sits inside a function \(_find_identity_node_advisories\) that processes operator-authored pipeline config — a Tier-3 source. The flagged expression sink.options.get\("schema"\) is a classic R1 Tier-3 boundary access on externally-sourced data whose shape is not contractually guaranteed. This is exactly the structural case the @trust_boundary decorator exists to handle: per-line allowlist entries for R1/R5 on Tier-3 parameters should be replaced by decorating the enclosing function. The excerpt does not show the function signature or any existing decorator, but the subject roots in a parameter-derived value \(sink/node options from the validation input\), so the correct remediation is to apply @trust_boundary with source_param naming the external-config parameter rather than land a YAML entry. |
| `web/execution/validation.py:R1:_find_identity_node_advisories:fp=7022ce32f26888c2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is R1 on \`sink_schema_block.get\("mode"\)\` inside \`_find_identity_node_advisories\`. The excerpt does not show the function signature or its parameters, but the data flow comes from \`sink.options\` — accessed via the enclosing function's iteration over pipeline nodes/sinks, which are operator-authored config \(Tier-3\). This matches the three-question test: external/untrusted data flowing from a function parameter, rule R1 is in the suppressible set, and the subject is rooted in that external structure. The correct remediation is \`@trust_boundary\` on the enclosing function rather than a per-line allowlist entry. Since the excerpt doesn't reveal the parameter name, I infer it from \`sink.options\`/\`node.options\` usage — the agent should identify the actual parameter \(likely the pipeline config\) and apply the decorator. |
| `web/execution/validation.py:R1:validate_pipeline:fp=733611406e3fefe3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | source_options originates from state.source.options, a typed SourceSpec.options field \(Mapping\[str, Any\]\) — this is first-party Tier-2 pipeline data with a guaranteed shape, not Tier-3 external input. The rationale's claim of a 'Tier 3 path-allowlist boundary' misapplies the tier model: user-authored YAML was already parsed and validated upstream into the typed CompositionState. The correct pattern for optional keys in a typed mapping is direct membership testing \(\`if key in source_options: value = source_options\[key\]\`\) rather than \`.get\(\)\`, which is the R1 defensive pattern. The decorator nudge does not apply because \`source_options\` is not a function parameter rooted in external data — it is derived from a typed domain object. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=a12ab183df5dc425` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a bare AST-shift / merge note — it documents that the pattern was preserved across a refactor, but it does not justify why the isinstance check is legitimate here. The code shows isinstance\(\) being used as a discriminated-union dispatch on the return value of a first-party helper _resolve_source_blob, which apparently returns either a ToolResult \(failure\) or a resolved-source object. That is a return-type-union design smell that R5 is specifically designed to flag: the fix is a typed result \(e.g., separate success/failure return paths, an exception for failure, or a sealed result type with explicit discriminator\) rather than runtime type-sniffing. The data is not Tier-3 — it's a first-party function's return value — so no trust-boundary exemption applies, and the decorator would not help \(R5 here is not rooted at an external parameter\). |
| `web/composer/tools/sessions.py:R1:_execute_set_pipeline:fp=a9b4b8adc81c4f9b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged .get\(\) is on _MIME_TO_SOURCE, a process-local lookup table owned by ELSPETH — not external data and not rooted at a Tier-3 function parameter. The rationale is purely procedural \('AST shift after refactor merge'\) and never argues why the defensive pattern is legitimate at this site; it just asserts the pattern is 'preserved'. Per the R1 remediation, the correct fix is direct subscript with an explicit membership check, or restructure to a sentinel-returning lookup — not a suppression. The decorator does not apply because the subject is not derived from an external parameter \(the prepared_inline_blob.mime_type key is used against an internal mapping, and the .get\(\) target is the internal dict itself\). |
| `web/composer/tools/sessions.py:R1:_execute_set_pipeline:fp=dbb2f95a63783167` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 LLM-tool-call boundary read: \`\`args\`\` is the raw tool-call payload, R1 is in the decorator's suppressible set, and the flagged subject \`\`args.get\("outputs"\)\`\` is rooted at the function parameter \`\`args\`\`. The structural remediation is \`\`@trust_boundary\(tier=3, source_param='args', suppresses=\('R1',\), ...\)\`\` on \`\`_execute_set_pipeline\`\`, not a per-line allowlist entry. The rationale itself describes the boundary correctly but proposes the wrong mechanism \(entry preservation across an AST shift\) instead of the decorator. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=8a8cb7d027f0f2f1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary isinstance check on data rooted at the external LLM tool-call parameter \(\`args.get\("outputs"\)\` → \`raw_outputs\`\), and R5 is in the decorator's suppressible set. The function \`_execute_set_pipeline\` takes the external \`args\` parameter, so the structural remediation is \`@trust_boundary\(tier=3, source='LLM tool-call arguments', source_param='args', suppresses=\('R1','R5'\), ...\)\` rather than a per-line allowlist entry. The rationale also fails on its own terms — it merely cites an AST shift after a refactor and notes the pattern is preserved, without explaining why suppression at this site is legitimate; that is a corrupted audit record. Use the decorator and delete related per-line entries. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=f8d3f1fc01dbefb4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is an R5 isinstance check on data derived from an external tool-call arguments dict inside \`\`_execute_set_pipeline\`\` — a textbook Tier-3 boundary case rooted in an external parameter. The structural remediation is the @trust_boundary decorator on the enclosing function with source_param pointing at the external arguments parameter and suppresses=\('R1','R5'\), not a per-line allowlist entry. Additionally, the rationale only describes an AST shift from a refactor merge and does not engage with why this specific isinstance is the right pattern at this site; that is a shallow audit record. Note: the excerpt does not show the function signature, so the exact parameter name \(likely \`\`args\`\` based on line 417\) should be confirmed by the agent. |
| `web/composer/tools/sessions.py:R5:_execute_set_pipeline:fp=87b1f78f92cca424` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a changelog note \('AST shift after ToolContext refactor merge'\) that documents what the code does without justifying why isinstance\(\) is legitimate here. It does not address R5's concern — whether this isinstance\(\) guards a genuine Tier-3 boundary or masks a contract violation. The iterated value \`validated.nodes\` appears to be a Pydantic-validated model \(\`_PipelineNodeModel\`\), so \`n.branches\` should have a contractually-guaranteed shape from the validation layer; branching on isinstance\(Mapping\) vs other-iterable suggests the Pydantic model permits a union type, in which case the correct fix is to tighten the model or dispatch via typed fields, not suppress R5. Even if this function is the Tier-3 boundary for LLM tool arguments, the rationale never explains why the shape isn't already normalized by \`validated\`. A merge-conflict-flavored 'pattern preserved' note is not an audit-grade justification. |
| `web/composer/tools/sessions.py:R5:_is_full_state_component_alias:fp=a583fa7b91f2f5b4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is non-substantive — it merely notes an AST shift after a refactor merge and restates the flagged line, without addressing why isinstance\(\) is justified here. The function takes \`component: Any\` and is a helper, not itself decorated as a Tier-3 boundary; the excerpt does not show that \`component\` is rooted in an external parameter of an enclosing @trust_boundary function. Even if \`component\` ultimately originates from a tool-call argument, the correct remediation is to validate/normalize at the boundary \(the tool entry point\), not to scatter isinstance\(\) shape guards into helpers. The rationale fails the Decision Test: it neither establishes a trust boundary at this site nor explains why the contract permits Any here. |
| `web/composer/tools/sessions.py:R1:_execute_get_pipeline_state:fp=53cdd7b125696d4a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: _execute_get_pipeline_state receives args \(an LLM tool-call arguments dict\) as its first parameter, the finding is R1 \(in the decorator's suppressible set\), and the flagged expression args.get\("component"\) is rooted directly at that external parameter. The correct remediation is the @trust_boundary decorator on the function, not a per-line allowlist entry. The rationale itself is also shallow — it merely notes an AST shift after a refactor and does not justify the suppression on its merits. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=b19e372e1ba24e9a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is non-substantive — it describes an AST shift from a merge and quotes a pattern about 'prompt_template' that doesn't match the actual flagged line \(which is options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\). It never engages with R1's concern at this site. Code context shows that just four lines above, the code already checks \`INTERPRETATION_REQUIREMENTS_KEY not in state.source.options\` and raises — so the .get\(\) is redundant defensive access where direct subscript \`state.source.options\[INTERPRETATION_REQUIREMENTS_KEY\]\` would be correct and informative. This is not a Tier-3 boundary \(state.source.options is internal pipeline state\), the decorator does not apply, and the remediation is a code fix, not a suppression. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=9821fdd3b0a0e36a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a meta-comment about a merge/refactor \('AST shift after ToolContext refactor'\) rather than a substantive justification of why isinstance\(\) is appropriate here. It never explains the trust tier of \`state.source.options\` or why shape-guarding is needed. \`state.source.options\` appears to be first-party pipeline state, not external tool-call arguments; if \`INTERPRETATION_REQUIREMENTS_KEY\` has a contractual shape, the isinstance checks are masking a contract violation per R5's definition. If the options dict legitimately holds Tier-3-derived content, that must be argued explicitly with reference to where the data enters and why it isn't normalized at the boundary. The finding is not rooted at an external function parameter, so the @trust_boundary decorator does not apply. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=cddef38c47eddcee` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder admitting no real justification was supplied \('ALLOWLIST-FRESH — review whether this is new debt'\). That is not an audit-grade reason. Separately, the code reads from state.source.options — a first-party config/state structure whose shape should be guaranteed by ELSPETH contracts, not an external-parameter Tier-3 boundary; if \`requirements\` items are expected to be Mappings with specific keys, the contract should make that explicit and direct access should crash on violation. The function does not take an external-data parameter that the flagged isinstance is rooted in, so the @trust_boundary decorator is not the appropriate remediation here. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=a3aa2e4be368b676` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) admitting this is unreviewed debt rather than a justification. No argument is made about the data tier, the source of \`requirements\`, or why \`.get\(\)\` is appropriate here. The code reads from \`state.source.options\`, which is internal pipeline state — its shape should be governed by a typed contract, not shape-guarded with \`isinstance\` + \`.get\`. Even if there's a Tier-3 boundary argument to be made, the rationale doesn't make it, and an audit record consisting of 'review whether this is new debt' is exactly the corrupted audit record the policy forbids. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=afcde30a5ec360ea` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) admitting no real justification has been written — that is by definition a corrupted audit record. The code accesses \`requirement.get\("user_term"\)\` where \`requirement\` comes from \`state.source.options.get\(INTERPRETATION_REQUIREMENTS_KEY\)\`; this is pipeline/source state, not data flowing from an external parameter of this function, so the @trust_boundary decorator does not apply. If the requirements list has a contractual shape, direct subscript is correct; if it doesn't, validate at the source boundary that produced \`state.source.options\`, not here. Either way, a per-line allowlist with no site-specific reasoning must not land. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=d92cc8f903f46fb1` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) explicitly asking for review, not a substantive justification. It does not identify the trust tier of the data, does not address R1's specific concern about hiding missing-key bugs, and does not explain why .get\(\) is appropriate here. The code operates on \`state.source.options\` and a \`requirements\` list/tuple of Mappings — this looks like internal/Tier-2 state data whose shape should be contractually defined; if so, direct subscript access is the correct fix. Even if \`state.source.options\` is externally-sourced configuration, the rationale never argues that and never describes the trust boundary or invariant. The decorator nudge does not apply: the finding is not rooted at a function parameter of \`_assert_affected_component\` but on iterated mapping items derived from \`state\`. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=fcc8acd21c8d9c6b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder boilerplate \('ALLOWLIST-FRESH — review whether this is new debt'\) that explicitly admits it has not justified the suppression for this site. It does not engage with the R1 finding at all, does not identify the trust tier of \`requirement\`, and does not explain why \`.get\("draft"\)\` is legitimate here. On the code itself, \`requirement\` originates from \`state.source.options\` — pipeline/source configuration data whose shape is being shape-guarded with isinstance just above; if the shape is guaranteed by contract, direct subscript is correct, and if it's Tier-3 config the boundary should be structural. Either way, a non-rationale cannot be accepted as an audit record. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=b3cc1f6e14caea35` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder marker \('ALLOWLIST-FRESH'\) that explicitly admits no justification was provided — it asks the reviewer to assess whether this is new debt. That is not a rationale; it is a TODO. Additionally, the data being isinstance-checked \(state.source.options.get\(INTERPRETATION_REQUIREMENTS_KEY\) and its elements\) is not rooted at an external parameter of this function — it comes from session state.source.options, whose shape should be governed by a first-party contract. If the contract guarantees the shape, the isinstance guards are redundant defensive code; if it doesn't, the fix is to tighten the contract or validate at the source boundary, not to allowlist here. Decorator nudge does not apply because the subject is not rooted at an external parameter of _assert_affected_component. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=42309d8e9c36b9ae` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder ALLOWLIST-FRESH marker, not a justification — it explicitly asks the reviewer to determine whether this is new debt. That is not an audit record a future auditor could interpret; per the Auditability Standard it must be refused. Additionally, node.options is pipeline/configuration data whose shape is governed by ELSPETH's own contracts \(Tier-1/2\), not external Tier-3 input; the surrounding code piles isinstance/.get guards on top of it, which is exactly the forbidden defensive pattern. The fix is to make node.options a typed structure and access fields directly, not to suppress. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=5e4977581a3e3e2f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder stub \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits no site-specific justification has been written; it does not engage with the R5 isinstance pattern at all. The code is operating on \`node.options.get\(...\)\` where \`node\` and \`node.options\` are ELSPETH-internal pipeline state, not data sourced directly from an external parameter of this function — the isinstance guards on \`requirements\`, \`requirement\` \(Mapping\), and \`draft_value\` look like defensive shape-checking on first-party state rather than a Tier-3 boundary. The agent must either prove these \`options\` values are Tier-3 \(with a written rationale tied to the source-of-truth\) and structurally guard at the actual ingestion boundary, or fix the contract so the shape is guaranteed and let the access crash. No decorator nudge: the subject is rooted at \`node.options\`, not at an external parameter of this function. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=23a8268fd0c90047` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) — it explicitly asks the reviewer to determine whether this is real debt, which is not a justification. It provides no site-specific reasoning about why isinstance is appropriate here. The code reads from node.options, which is internal pipeline state \(Tier 1/2\), not from an external parameter at this function's boundary; shape-guarding requirements/requirement/draft_value with isinstance looks like defensive programming masking unclear contracts on node.options. The agent must either tighten the typed contract on node.options.INTERPRETATION_REQUIREMENTS_KEY and let access crash, or provide a real rationale showing this data is genuinely Tier-3. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=100a8d01f438c0e6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no justification has been written for this site — it is not a substantive defense of the suppression. Substantively, the code is iterating over \`node.options\`, which is first-party state data on a node retrieved via \`_find_node_or_raise\`; this is Tier-1/Tier-2 data under a typed contract, not a Tier-3 external boundary. The defensive \`isinstance\` + \`.get\(\)\` cascade on \`requirement\` fields suggests the schema for \`INTERPRETATION_REQUIREMENTS_KEY\` is either contractually defined \(in which case direct access should be used and crashes are correct\) or undefined \(in which case the fix is to define it\). Either way, the remediation is a code/schema fix, not an allowlist entry, and the decorator does not apply since the data is not rooted at an external parameter. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=d4327137d876f74a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH ... review whether this is new debt'\) — it is explicitly an unjustified entry asking for review, not a substantive justification tied to this site. Policy requires the allowlist entry itself to be an interpretable audit record; a meta-comment about fingerprint rotation is not. Separately, the code operates on node.options requirements which appear to be pipeline/state data rather than a clearly external Tier-3 payload, and the surrounding code already shape-guards with isinstance\(requirement, Mapping\); whether the .get\(\) calls on requirement are honest Tier-3 boundary handling or redundant defensive code on Tier-2 state data cannot be confirmed without a real rationale. The agent must supply a site-specific justification or fix the root cause. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=2a12bf7ffce7858d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is the placeholder 'ALLOWLIST-FRESH' boilerplate — it provides no site-specific justification at all and explicitly flags itself as needing review. Per the Auditability Standard, a vague/placeholder rationale is a corrupted audit record and must be refused. Separately, the code reads \`requirement.get\("status"\)\` from \`node.options\` — pipeline node options are first-party configuration data with a typed contract, not Tier-3 external input, so the defensive \`.get\(\)\` is forbidden Tier-2 shape-rechecking; the fix is to define a typed schema for interpretation requirements and access fields directly. The decorator nudge does not apply: this is not a function whose parameter carries external data flowing into the subject. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=87a636f73cd97447` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no site-specific justification has been written — this is exactly the corrupted-audit-record case the policy forbids. Separately, the code is iterating over node.options requirements, which appears to be pipeline/checkpoint state rather than a freshly-received external payload at this function's parameter boundary; if it is first-party state, the .get\(\) is defensive on Tier-1/2 data and should be a direct key access. Even if requirements entries are externally-influenced, the subject is not rooted at a parameter of this function, so the @trust_boundary decorator path does not cleanly apply from what is visible. The agent must write a real, site-specific rationale or fix the root cause. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=32dc09ac1df53ab3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH... review whether this is new debt'\) and provides no site-specific justification for the isinstance check. While the code operates on node.options data which may have varying provenance, the agent has not articulated why this is a legitimate trust boundary, what the data's tier is, or why isinstance is the right tool here rather than contract enforcement. A rotation-stub rationale is itself a corrupted audit record per the Auditability Standard — future auditors cannot interpret it. The agent must supply a real justification or fix the root cause. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=316d0487bb1d8b7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) admitting no real justification has been provided — that is by definition a corrupted audit record. On the merits: \`options\` is \`node.options\` from a composer-state node, which is first-party pipeline data with a typed contract, not Tier-3 external input. The R1 \`.get\("prompt_template"\)\` here is defensive shape-guarding of a first-party structure; the fix is to access the field directly per the contract \(or define the schema such that prompt_template's optionality is explicit\) and let the code crash on contract violation. The decorator nudge does not apply: the subject is rooted at \`node.options\`, not at an external parameter of this function. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=fd42b91d6bc188f8` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is literally a placeholder marker \('ALLOWLIST-FRESH — no stale entry matched...'\) admitting that no site-specific justification has been written. That is a corrupted audit record by the Auditability Standard — a future auditor reading 'review whether this is new debt' cannot determine why the suppression was granted. Separately, the code reads options.prompt_template off a node whose shape is a first-party typed contract \(node.options is our own structure\), so isinstance-guarding it is the redundant defensive pattern R5 targets, not a Tier-3 boundary check; the honest fix is to tighten the node-options contract \(typed dataclass / validated at construction\) so the field's type is known, not to suppress the finding. The function parameter is an internal state/id, not external Tier-3 input, so the @trust_boundary decorator does not apply. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=400e7fa8db4f1ead` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely procedural \('AST shift after refactor merge'\) and provides no policy justification for the isinstance check. The function operates on 'nodes', a mapping whose values' shape determines downstream handling — but the excerpt does not establish that 'nodes' is Tier-3 external data; the companion '_typed' variant directly below accepts Sequence\[NodeSpec\], suggesting these nodes are first-party pipeline structures with a typed contract. If nodes come from a first-party typed source, the isinstance guards are redundant defensive code and the fix is to type 'nodes' properly, not allowlist. If they are genuinely Tier-3 \(parsed external JSON\), the remediation is @trust_boundary on the function with source_param='nodes', not a per-line entry — but the rationale never makes that case. Either way the suppression is unjustified as written. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=edf7fab42da3610b` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a non-justification — it merely notes that an AST shift occurred after a refactor and preserves the pattern, without arguing why .get\(\) is appropriate at this site. The code operates on \`nodes\` which appears to be a dict of node specs; there is a parallel typed function \`_detect_unresolved_interpretation_placeholders_typed\` taking \`Sequence\[NodeSpec\]\`, suggesting this untyped variant exists for raw/external composer config. Even granting a Tier-3 boundary, the function takes \`nodes\` as a parameter and the flagged access is rooted at that parameter, so the structural remediation is \`@trust_boundary\`, not a per-line allowlist. The rationale also fails the auditability standard — 'AST shift after merge' is not a trust-tier argument a future auditor could interpret. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=bd3440d42f497594` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a merge-bookkeeping note \('AST shift after ToolContext refactor'\) and does not justify the suppression on its merits — it doesn't identify the data's trust tier, explain why .get\(\) is honest here, or address R1's concern. The code operates on a \`nodes\` mapping whose values are being shape-guarded with \`isinstance\(node, Mapping\)\`, suggesting these nodes come from an untyped/external structure, but the rationale never argues this. Even if a Tier-3 boundary argument applies, the correct remediation is @trust_boundary on the enclosing function \(with source_param='nodes'\), not a per-line allowlist entry. However, since \`nodes\` is a parameter to the enclosing function and R1 is in the suppressible set, and the subject \`node\` derives from iterating \`nodes.values\(\)\`, the structural fix is the decorator. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=d9b8b97d7f2fd4e7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely procedural \('AST shift after refactor merge, pattern preserved'\) and does not justify why isinstance\(\) is needed here. The function operates on a \`nodes\` mapping whose shape should be governed by a first-party contract \(there's even a typed sibling \`_detect_unresolved_interpretation_placeholders_typed\` operating on \`Sequence\[NodeSpec\]\`\), suggesting this untyped variant is shape-guarding internal composition state rather than Tier-3 external data. The excerpt shows no decorator and no boundary framing; preserving a pre-existing defensive pattern across a refactor is not a policy-grade justification. If \`nodes\` is genuinely Tier-3, the remediation is to coerce/validate at the boundary or use the typed sibling; if it's first-party state, the isinstance guards are forbidden defensive code. |
| `web/composer/tools/sessions.py:R1:_detect_unresolved_interpretation_placeholders:fp=ff5b63e0153dcdb3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a meta-note about an AST shift after a refactor merge — it doesn't justify the suppression on its merits, it merely says the pattern was preserved. The function operates on a \`nodes\` mapping that, judging by the typed sibling function \`_detect_unresolved_interpretation_placeholders_typed\` operating on \`Sequence\[NodeSpec\]\`, represents composition node data; the untyped variant is shape-guarding with \`isinstance\(node, Mapping\)\` and \`isinstance\(options, Mapping\)\` because the input shape is not contractually guaranteed at this entry. However, R1 flags the \`.get\('prompt_template'\)\` call: once \`isinstance\(options, Mapping\)\` is true, options is a Mapping but its internal schema \(whether 'prompt_template' is present\) is not contract-guaranteed here either — yet the rationale never speaks to why \`.get\` is appropriate vs direct access, and the 'AST shift / pattern preserved' justification is exactly the kind of audit record a future auditor cannot interpret. The function does not take an external parameter named in a \`@trust_boundary\` decorator signature in a way that R1 on \`options.get\` would be cleanly rooted at it \(subject is rooted at \`options\`, derived from \`node\`, derived from \`nodes.values\(\)\`\), so the decorator nudge does not cleanly apply. Block; the agent should either justify the suppression site-specifically \(why is this Tier-3-ish dict-of-dicts and not a typed NodeSpec contract\) or migrate to the typed sibling. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders:fp=329fb69e5f766ce6` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is procedural metadata about an AST shift after a merge — it explains why the line moved, not why isinstance\(\) is legitimate here. The function operates on a \`nodes\` mapping whose shape should be governed by an ELSPETH contract \(NodeSpec/CompositionState as the typed sibling makes clear\); if \`nodes\` values aren't Mappings or \`options\` isn't a Mapping or \`prompt_template\` isn't a str, that's a contract violation that should surface, not be silently \`continue\`d past. Even if the agent wants to argue this is a Tier-3 boundary, the rationale never makes that argument — it only notes the pattern was 'preserved' across a refactor, which is exactly the kind of audit record a future reviewer cannot interpret. The excerpt does not show a @trust_boundary decorator and the parameter is \`nodes\` \(not obviously an external untrusted payload at this layer given the typed sibling exists\), so I am not nudging toward the decorator. |
| `web/composer/tools/sessions.py:R5:_detect_unresolved_interpretation_placeholders_typed:fp=b1ca3b580e951eeb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function iterates over \`nodes\` which appears to be a typed structure \(each \`node\` has \`.plugin\` and \`.options\` attributes\), suggesting the shape upstream is contractually established. However, \`node.options\["prompt_template"\]\` is a dict value whose type is not statically guaranteed — it likely originates from external tool-call arguments. If this function is the boundary that validates external tool input, the correct remediation is \`@trust_boundary\(tier=3, source_param=&lt;param&gt;, suppresses=\('R5',\), ...\)\` on the enclosing function, not a per-line allowlist entry. The rationale itself is non-substantive: it cites an 'AST shift after refactor merge' rather than explaining why the isinstance check is legitimate at this site — that is an audit-record failure regardless of the underlying merit. Cannot confirm the parameter name from the excerpt \(the function signature is not shown\), so a decorator nudge would be speculative. |
| `web/composer/tools/generation.py:R1:_schema_required_fields:fp=170da3b1e8b91a7c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent's rationale is a placeholder \("ALLOWLIST-FRESH — review whether this is new debt"\) and provides no site-specific justification. It does not engage with R1's concern at this location. Additionally, the function _schema_required_fields takes a \`schema: Mapping\[str, Any\]\` parameter which appears to be external/LLM-authored config \(per the comment context at line 114\), making this a candidate for the @trust_boundary decorator on parameter \`schema\` — the .get\(\) is rooted at the external parameter and R1 is in the suppressible set. The structural remediation is the decorator, not a per-line allowlist entry with no rationale. |
| `web/composer/tools/generation.py:R5:_schema_required_fields:fp=42a2049f51f41ad5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale text is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) admitting it is not a real justification — it explicitly asks for review rather than explaining why the suppression is legitimate. An allowlist entry without a site-specific justification is a corrupted audit record per the Auditability Standard. Separately, while the isinstance check here does appear to validate external schema config at a Tier-3-ish boundary \(raising on bad shape, which is the honest pattern\), the function takes a \`schema: Mapping\[str, Any\]\` parameter and the correct structural remedy if this is truly a boundary is \`@trust_boundary\`, not a per-line entry — but I cannot confirm the parameter is externally sourced versus a first-party typed config contract from the excerpt alone. Regardless, the placeholder rationale fails the auditability bar on its own. |
| `web/composer/tools/generation.py:R1:_execute_get_plugin_assistance:fp=be409e72085e2f59` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: the function takes external tool-call \`\`args\`\` \(as evidenced by sibling direct accesses \`\`args\["plugin_type"\]\`\` and \`\`args\["plugin_name"\]\`\`\), the finding is R1 on \`\`args.get\("issue_code"\)\`\`, and the subject is rooted at the external \`\`args\`\` parameter. All three decorator conditions are met, so the correct remediation is \`\`@trust_boundary\`\` with \`\`source_param='args'\`\`, not a per-line allowlist entry. The rationale itself is also shallow — it only describes an AST shift after a merge and does not justify why \`\`.get\`\` \(treating absence as optional\) is correct here versus direct subscript. |
| `web/composer/tools/generation.py:R1:_execute_list_models:fp=abc6ef78029ec544` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: \`_execute_list_models\` is an MCP/composer tool handler whose \`args\` parameter carries the model's tool-call arguments \(external, untrusted shape\), and the finding is R1 on \`args.get\("provider"\)\` rooted at that parameter. All three decorator-nudge conditions are met \(boundary function, R1 is in the suppressible set, subject rooted at \`args\`\), so the correct remediation is \`@trust_boundary\(tier=3, source_param='args', suppresses=\('R1','R5'\), ...\)\` on the function, not a per-line allowlist entry. The rationale itself is also non-substantive — it describes an AST shift from a merge rather than justifying the suppression on trust-tier grounds. |
| `web/composer/tools/generation.py:R1:_execute_list_models:fp=1f07af64510ad515` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case suitable for the @trust_boundary decorator: the function processes external tool-call arguments \(\`args\`\), the rule is R1, and the subject \`args.get\('limit', 50\)\` is rooted at the external parameter. The rationale itself is non-substantive — it describes a merge/AST shift rather than justifying the suppression on policy grounds, and merely restating the flagged pattern is not a defense. The correct remediation is to apply @trust_boundary\(tier=3, source_param='args', suppresses=\('R1',\), ...\) to the enclosing handler rather than adding a per-line allowlist entry. Note also that defaulting a missing \`limit\` to 50 is borderline fabrication; if absence is meaningful, record None and let downstream decide. |
| `web/composer/tools/generation.py:R5:_execute_list_models:fp=2d3a8b87d9ca51f3` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function \`\`_execute_list_models\`\` takes \`\`args\`\` \(an external tool-call argument dict from the composer model\), and the R5 finding is on \`\`isinstance\(limit, int\)\`\` where \`\`limit\`\` is rooted at \`\`args.get\("limit", 50\)\`\`. All three conditions for the structural @trust_boundary remediation are met: external-data parameter, R5 is in the suppressible set, and the subject flows from that parameter. Additionally, the rationale itself is shallow — it cites an AST shift from a refactor merge and \`\`pattern preserved\`\` rather than justifying the suppression on policy grounds. The remediation is the decorator on the enclosing function with \`\`source_param='args'\`\`, not a per-line allowlist entry. |
| `web/composer/tools/generation.py:R5:_execute_list_models:fp=9e1243b28dd8b86f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding meets all three decorator-test conditions: \(1\) the function takes external tool-call arguments as the \`args\` parameter, \(2\) the rule is R5 \(isinstance shape guard\), and \(3\) the flagged subject \`provider\` is rooted at \`args\` via \`args.get\("provider"\)\`. The correct remediation is \`@trust_boundary\(tier=3, source_param='args', suppresses=\('R1','R5'\), ...\)\` on \`_execute_list_models\`, not a per-line allowlist entry. The rationale itself is also shallow — it merely notes an AST shift after a refactor without justifying why this site needs a line-level suppression rather than the structural decorator. |
| `web/composer/tools/generation.py:R1:_source_schema_mode:fp=e0bc2bf3936637b4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This finding is a structural Tier-3 boundary case: the function \`_source_schema_mode\` takes \`source: SourceSpec\` as a parameter whose \`.options\` mapping carries LLM-supplied values \(per the rationale\), the rule is R1 \(in the decorator's suppressible set\), and the subject \`source.options.get\("schema"\)\` is rooted at the external parameter via attribute access. The correct remediation is \`@trust_boundary\(tier=3, source='LLM-authored SourceSpec.options', source_param='source', suppresses=\('R1','R5'\), invariant='returns None for missing/non-Mapping schema or non-string mode', test_ref=..., test_fingerprint=...\)\` on the function, not a per-line allowlist entry. Note the function also contains an R5 isinstance shape guard on the same data flow, which the decorator would cover uniformly. |
| `web/composer/tools/generation.py:R5:_source_schema_mode:fp=a198571c1b9ad526` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _source_schema_mode takes a SourceSpec parameter, not an external LLM payload directly — source.options is read from a first-party SourceSpec contract. The rationale claims this is LLM-authored data but the excerpt shows access through a typed SourceSpec.options.get\('schema'\), which is ELSPETH's own configuration object. If options\['schema'\] originates from LLM tool-call arguments upstream, the correct remediation is to validate/coerce at that Tier-3 boundary \(where the LLM JSON enters\), not re-guard the shape every time we read from SourceSpec. Additionally, source_param cannot be applied here since 'source' is a SourceSpec wrapper, not the raw external dict. The rationale conflates origin-of-contents with current-trust-tier without showing the boundary. |
| `web/composer/tools/generation.py:R5:_sample_csv_rows:fp=20dc866451ce18cc` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 CSV boundary \(csv.DictReader output on raw bytes\), and the R5 isinstance check on \`key\` is a legitimate shape-guard against ragged rows where DictReader buckets extras under a None key. However, the finding is inside \`_sample_csv_rows\`, which takes the external \`content: bytes\` as a parameter — this is exactly the structural case the @trust_boundary decorator exists to handle. R5 is in the decorator's suppressible set, and the guarded subject \(\`key\` from \`row.items\(\)\`\) is rooted in data derived from the external \`content\` parameter. The correct remediation is \`@trust_boundary\(tier=3, source='uploaded CSV/TSV bytes', source_param='content', suppresses=\('R5',\), ...\)\` on the function, not a per-line allowlist entry. |
| `web/composer/tools/generation.py:R1:_value_transform_preserves_field:fp=e2665599a03a84d5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes \`\`node: NodeSpec\`\` as parameter and the R1 finding is on \`\`node.options.get\("operations"\)\`\` — the subject is rooted at an external/LLM-supplied parameter, R1 is in the decorator's suppressible set, and the function shape-guards untrusted nested values. This is the canonical structural Tier-3 boundary case; the remediation is \`\`@trust_boundary\`\` on the function with \`\`source_param='node'\`\`, not a per-line allowlist entry. |
| `web/composer/tools/generation.py:R5:_value_transform_preserves_field:fp=7236ffc455b6c7be` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function takes a NodeSpec and reads node.options — this is not a function whose parameter is a fresh Tier-3 payload from an external system. The rationale claims the operations value is 'LLM-authored', but the data flowing through node.options at this point has already been ingested into a NodeSpec structure within the composer, not handled at the external boundary. If node.options carries unvalidated LLM-authored content, the correct fix is to validate/coerce at the actual ingestion boundary \(where the LLM tool call is parsed\) and give NodeSpec.options a typed contract — not to scatter isinstance shape-guards throughout downstream analysis helpers. The decorator nudge does not apply either: the external value is not rooted at a parameter of this function. |
| `web/composer/tools/generation.py:R5:_value_transform_preserves_field:fp=aa5808f615d3af9a` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _value_transform_preserves_field takes a NodeSpec \(a first-party typed contract\), not an external parameter. node.options is project-owned configuration data that should have a guaranteed shape per the NodeSpec contract. The rationale invokes 'LLM-authored operations' but the excerpt shows no evidence this parameter is freshly Tier-3 — it's a NodeSpec field accessed via .get\(\) already \(which is itself suspicious\). If operations entries can genuinely be malformed at this layer, the contract is wrong and should be fixed upstream where the LLM output is normalized into NodeSpec, not shape-guarded here. The decorator nudge doesn't apply since the function parameter is a typed NodeSpec, not an external dict. |
| `web/composer/tools/generation.py:R1:_value_transform_preserves_field:fp=c1946cc969215bd2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function does not take an external parameter directly — it receives a NodeSpec, and the operations list is being read from node.options which is composer/LLM-authored config. While the data is plausibly Tier-3, the function signature is \(node, field_name\), so the @trust_boundary decorator's source_param mechanism doesn't cleanly apply to a nested attribute. More importantly, the rationale is shallow: it asserts Tier-3 status but doesn't address why the surrounding isinstance guards establish shape-trust for the list/Mapping but then operation.get\('target'\) is still needed — if operation is a Mapping with a guaranteed schema, direct access with KeyError-on-miss is the offensive choice; if the schema permits absence, the honest pattern is 'target' in operation or direct access with a documented contract. The agent should either tighten the schema contract upstream or justify per-key absence explicitly. |
| `web/composer/tools/generation.py:R1:_numeric_aggregation_diagnostics_for_observed_csv:fp=f179b12ee165ade2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a \`.get\(\)\` on \`inferred_types\`, a first-party Mapping computed by our own type-inference code over Tier-2 pipeline data. A missing key is a well-defined condition \(inference produced no signal for that column\), which is exactly the semantics \`Mapping.__getitem__\` would NOT support — but the honest expression of "key may legitimately be absent" against a first-party mapping is membership check + direct access, or a typed structure with an explicit absent sentinel, not a silent \`.get\(\)\` that R1 is designed to catch. The rationale also conflates None-as-absence-signal with Tier-3 boundary semantics; there is no external data here. Fix the contract \(e.g., make \`inferred_types\` always present and use \`value_field in inferred_types\` + direct indexing, or model 'no signal' explicitly\), don't suppress R1. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=78003676a01b62e7` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims this is Tier-3 \(LLM-authored source.options.schema\), but the inline comment in the code itself contradicts that: it states 'source.options is Tier-1 \(Mapping\[str, Any\]\)'. Whether this is Tier-1 config or Tier-3 LLM-authored data is load-bearing and the rationale is inconsistent with the code's own annotation. If the schema field truly originates from an LLM tool-call argument, the structural remediation is @trust_boundary on the boundary function where the LLM payload enters — not a per-line R1 suppression deep inside compute_proof_diagnostics, which appears to operate on an already-constructed source object. The agent must reconcile the tier claim and, if Tier-3, push the boundary upward; if Tier-1/2, the .get is defensive and should be direct access. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=65bfa9c0c7279e67` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is the placeholder ALLOWLIST-FRESH marker — it explicitly says no justification was supplied and asks the reviewer to determine whether this is new debt. That is not an audit-quality rationale; per the Auditability Standard, a vague or absent rationale is a corrupted audit record and must be refused. Separately, the code comment claims source.options is Tier-1, which is suspect — source.options is plugin/user configuration, not the Landscape audit DB — but even setting that aside, the suppression cannot land without a site-specific justification that speaks to R1 at this exact call. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=fd05b8a30100c68c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits it has not justified the suppression and asks for review. That is not a rationale at all; it is a TODO marker. Per the Auditability Standard, an allowlist entry is itself an audit record, and a non-rationale cannot land. Separately, the R6 finding concerns a try/except ValueError that appends a diagnostic and sets a failure flag — whether that constitutes a legitimate explicit error result or silent swallow requires a site-specific argument, which the operator has not provided. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=0939dce5f06ef7cb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no site-specific justification has been provided — it is itself a request for review, not a defense of the suppression. Per the Auditability Standard, an allowlist entry with a vague/non-explanatory rationale is a corrupted audit record and must be refused. On the merits, the code does appear to convert a ValueError from derive_required_header_mismatch_risk into a recorded diagnostic and a field_resolution_failed flag, which is a plausible quarantine pattern, but the agent must articulate that explicitly \(what raises, why it is expected, that the diagnostic is the explicit error result\) before R6 can be honestly suppressed. The decorator nudge does not apply: R6 is not in the suppressible set {R1, R5}. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=2d23380aa7ea7a24` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH'\) that explicitly admits no justification has been provided and invites review. That is not a valid audit record. The code uses schema.get\("mode"\) on a 'schema' object whose tier and shape contract are not visible in the excerpt, but no rationale at all has been offered to establish that this is a Tier-3 boundary or that the .get is otherwise warranted. Per the Auditability Standard, vague or absent rationales must be rejected; the agent should either access the key directly and let it crash, or supply a site-specific justification tied to the actual data's provenance. |
| `web/composer/tools/generation.py:R6:compute_proof_diagnostics:fp=2dadfe65b888003f` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \("ALLOWLIST-FRESH — no stale entry matched..."\) that explicitly admits it has not analyzed whether this is legitimate or new debt. It provides no site-specific justification for why R6 \(silent-except\) is appropriate here. While the code excerpt suggests the except block does append a diagnostic \(which may legitimately convert the exception into an explicit error result\), the agent must articulate that reasoning. A placeholder rationale cannot serve as an audit record per the Auditability Standard. The decorator nudge does not apply: R6 is not in the suppressible set {R1, R5}. |
| `web/composer/tools/transforms.py:R5:_execute_upsert_node:fp=28b82b3b788af6e4` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a changelog entry \('AST shift after ToolContext refactor merge into RC5.2'\) — it documents that the pattern was preserved across a merge, not why the isinstance check is legitimate at this site. The subject is \`validated.branches\`, which is a field on a \`validated\` object \(presumably a typed/validated structure produced by upstream parsing\), not directly on an external \`arguments\` parameter — so the Tier-3 boundary argument is not self-evident from the excerpt, and the rationale never makes it. If \`validated\` is a typed contract object, the branches field's shape should be guaranteed and the isinstance is redundant defensive code; if it genuinely carries unnormalised external data, that should be stated and the boundary should be structural. Either way, 'pattern preserved across a refactor' is not an audit-grade justification. |
| `web/composer/tools/transforms.py:R1:_execute_upsert_edge:fp=8e25103e55154751` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a change-log note \('AST shift after refactor merge'\) and does not justify the suppression on its merits. The flagged expression \`routes.get\(route_key\)\` operates on \`routes = dict\(node.routes or {}\)\` — a dict built from a first-party typed node field \(Tier-2 data with guaranteed shape\), not external/Tier-3 data. Per policy, \`.get\(\)\` on a dict whose shape is contractually guaranteed is the forbidden defensive pattern; the correct fix is direct access \`routes\[route_key\]\` with a membership check \(\`route_key in routes\`\) or restructuring the comparison, not an allowlist entry. The decorator nudge does not apply: although the enclosing function takes external \`args\`, the flagged subject is rooted at \`node.routes\` \(first-party state\), not at the external parameter. |
| `web/execution/service.py:R5:ExecutionServiceImpl:set_openrouter_catalog_snapshot:fp=f622220671ecdcda` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder admitting it is a fresh, unjustified entry \('ALLOWLIST-FRESH ... review whether this is new debt'\). That is not a justification at all — it is a request to suppress the finding without analysis. On the merits: the function's parameters are typed as \`str\` via a first-party typed contract \(the docstring states the caller is the FastAPI lifespan reading from a function that never returns None\), so \`isinstance\(sha256, str\)\` is checking against a guaranteed shape — this is the redundant defensive pattern R5 targets. The correct fix is to delete the \`isinstance\` check and rely on the type contract \(the \`is_valid_sha256_hex\` value check is fine to keep\). No decorator nudge: this is not a Tier-3 boundary function and the parameter is not externally sourced. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=d458fff4dd2be32e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on output.options.get\(key\) where output.options is a typed config dict from user-authored composition state. This is Tier-3 external data \(user config from /execute API\), and the function likely handles externally-supplied composition_state. The R1 rule and the .get pattern on a parameter-rooted external dict is exactly what @trust_boundary\(source_param=...\) is designed to suppress structurally. The rationale 'optional keys in user-authored config' identifies the Tier-3 boundary correctly but the remediation should be the decorator on _execute_locked covering composition_state, not a per-line allowlist entry. The fingerprint-shift note also suggests this is churn maintenance on an existing per-line entry — exactly the case the decorator was introduced to replace. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=f6eeabcecd608c77` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims \`options\` is user-authored config where a key may be absent, but \`composition_state.source.options\` is a first-party typed contract field accessed via attribute, and the code is reading a well-known optional key \`blob_ref\`. If the schema declares \`options\` as a dict that may or may not contain \`blob_ref\`, the correct pattern is direct access with explicit absence handling — and crucially, this is not a Tier-3 external boundary inside this function \(no external \`arguments\`-style parameter; \`composition_state\` is a typed first-party object\). The rationale also leans on a fingerprint-shift note rather than addressing R1's concern \(hiding missing-key bugs\). If \`blob_ref\` is genuinely optional in the contract, the schema/typing should express that \(e.g., a typed model with an optional field\) so direct access is honest; if not, the access should crash. Per-line allowlist for \`.get\(\)\` on a first-party config dict is the forbidden defensive pattern. |
| `web/execution/service.py:R6:ExecutionServiceImpl:validate_state:_blob_get_metadata:fp=07f7b1d9599f9e03` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder boilerplate \('ALLOWLIST-FRESH — no stale entry matched...'\) that explicitly admits it has not justified the suppression; it asks the reviewer to decide whether this is debt or needs justification, which is not a justification at all. The auditability standard requires the allowlist entry itself to be a meaningful audit record, and this text fails that test. Independent of the rationale, the code pattern \(catching BlobNotFoundError and returning None\) may well be a legitimate domain-specific 'not found means absent' translation, but accepting that requires the agent to say so explicitly and tie it to the function's contract. Without a site-specific justification, the suppression cannot land. |
| `web/execution/service.py:R1:ExecutionServiceImpl:cancel:fp=3195518183f4604c` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is shallow and does not justify the suppression. \`self._shutdown_events\` is a first-party internal dict owned by ExecutionServiceImpl, not Tier-3 external data — using \`.get\(\)\` here to test membership is exactly the R1 defensive pattern the rule targets. The rationale's reference to a fingerprint shift and 'live shutdown event check' describes intent but doesn't establish a trust boundary or explain why direct access \(\`dict\[key\]\` with KeyError handling, or \`key in dict\`\) is wrong. The correct fix is structural: use \`self._shutdown_events.pop\(str\(run_id\), None\)\` only if the absence is semantically meaningful, or rewrite as a membership test — not an allowlist entry. The decorator does not apply \(no external parameter; R1 subject not rooted in untrusted input\). |
| `web/execution/service.py:R4:ExecutionServiceImpl:_run_pipeline:fp=d291d0da191e7e8e` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-1 audit DB read \(load_top_failure_samples on Landscape data\). Policy is explicit: 'Bad data in the audit trail = crash immediately. No coercion, no defaults, no silent recovery.' The rationale argues that enrichment is less important than run-status persistence and so degradation is acceptable — but that is precisely the silent-recovery pattern the Auditability Standard forbids for Tier-1 reads. A malformed audit row is a catastrophic invariant break and must surface, not be swallowed with a slog.warning \(logging is last-resort, not a substitute for crashing on Tier-1 corruption\). If enrichment must be decoupled from persistence, the structural fix is to order operations so persistence happens first and then let the enrichment read crash loudly — not a broad except that hides audit-DB shape violations behind a warning. |
| `web/execution/service.py:R6:ExecutionServiceImpl:_finalize_output_blobs:fp=76aac7aeed5d2790` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is shallow boilerplate that does not address R6's specific concern: this except block catches exceptions and only logs via slog.error without re-raising, recording to audit, or returning an explicit error result. Per the Telemetry primacy order, slog is last-resort — pipeline-relevant events like blob finalization failure must hit the Landscape audit trail, not just the logger. The claim 'failure must not mask run outcome' is precisely the Shifting-the-Burden pattern: swallowing the exception silently degrades the audit trail to preserve flow. The fix is to record the failure to the audit trail \(or return an explicit error result\), not to suppress the finding. The fingerprint-shift note is housekeeping, not justification. |
| `web/composer/service.py:R1:_last_mutation_was_pending_proposal:fp=b8c97a159c375db2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a merge/AST-shift note \('pattern preserved'\) and does not justify the suppression on its merits. The code parses \`invocation.result_canonical\` via \`json.loads\` — this is a serialized tool result whose shape originates from a tool execution our code controls, but even granting Tier-3 framing, the function already shape-guards with \`type\(payload\) is not dict\` then uses \`.get\("data"\)\` followed by another \`type\(data\) is dict\` check. The R1 finding is on \`payload.get\("data"\)\`, which is a defensive read of an optional key; the honest fix is direct access with a contract \(or \`"data" in payload\`\) rather than silent \`.get\`. Also, the enclosing function \`_last_mutation_was_pending_proposal\` does not take the external value as a parameter — the data is loaded from \`invocation.result_canonical\` inside the function — so \`@trust_boundary\` with a \`source_param\` does not structurally apply here. A rationale describing only the refactor provenance is not a valid audit record. |
| `web/composer/service.py:R1:_last_mutation_was_pending_proposal:fp=2a6ac3e449ff4daa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is purely a provenance note \("AST shift after ToolContext refactor merge"\) and never argues why this specific .get\(\) is legitimate. The code parses \`invocation.result_canonical\` via json.loads — that field's contents originate from an LLM tool-call result, so the parsed payload is Tier-3 external data, and shape-guarding it is honest. However, this is a module-level function \(not a method\) that takes \`tool_invocations\` as its parameter; the external payload is obtained by json.loads inside the function body, not via a named source_param, so the @trust_boundary decorator's source_param mechanism does not cleanly apply. More importantly, the rationale itself fails the audit standard: "AST shift, pattern preserved" is not a justification — it's a migration note that an auditor cannot interpret as evidence the suppression is correct at this site. Resubmit with a site-specific rationale explaining the Tier-3 boundary and the invariant on malformed payloads. |
| `web/composer/service.py:R5:ComposerServiceImpl:_serialize_response_via_walker:fp=cd655256ca15f7bb` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder fingerprint-rotation note \("ALLOWLIST-FRESH"\) that explicitly admits no site-specific justification has been written; this is exactly the corrupted audit record the policy forbids. The isinstance\(response, Mapping\) check discriminates between two first-party shapes \(a Mapping vs a ToolResult\) on outcome.response, which is a first-party typed outcome object, not Tier-3 external data — so the R5 flag is a real defensive-pattern signal that the dispatch should be expressed via a typed union/contract rather than runtime shape-sniffing. No external parameter is in scope here, so the @trust_boundary decorator does not apply. |
| `web/composer/service.py:R1:ComposerServiceImpl:_cached_runtime_preflight:fp=4ba60774ae10f0f5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is pure boilerplate about an AST shift after a merge and does not address why .get\(\) is appropriate here. The code shows cache.get\(key\) on a local _RuntimePreflightCache — a process-local, first-party cache, not Tier-3 external data. Cache miss is a normal control-flow case, but the rationale never argues that; it just records a refactor. R1's concern \(hiding missing-key bugs\) is not addressed — if the intent is 'None means cache miss', that should be explicitly stated and the cache's contract documented, or the cache should expose a typed lookup method. The @trust_boundary decorator does not apply \(no external parameter; cache is internal\). |
| `web/composer/tool_batch.py:R5:run_tool_batch:fp=9a7b25e2fc96514d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a textbook Tier-3 boundary case: decoded_arguments comes from JSON-decoding an LLM tool-call response, the rule is R5 \(in the suppressible set\), and the subject is rooted at external data flowing through the function. The structural remediation is @trust_boundary on run_tool_batch rather than a per-line allowlist entry. I cannot see the function signature in the excerpt to confirm the exact parameter name, but the decoded_arguments value originates from the LLM tool-call arguments processed by this function; the agent should identify the appropriate source_param \(likely the tool_call or arguments-bearing parameter\) and apply the decorator. |
| `web/composer/redaction.py:R5:_coerce_stringified_json_object:fp=4f5bfe819cd1532d` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The agent rationale is a placeholder \('ALLOWLIST-FRESH — no stale entry matched...'\), not a substantive justification for this specific suppression. Policy requires every allowlist entry to be a self-contained audit record; a fingerprint-rotation marker is not a rationale and future auditors cannot interpret it. The code itself appears defensible \(a Tier-3 boundary coercion validator with documented meaning-preserving semantics\), but the docstring's reasoning must be restated in the allowlist entry's own rationale before acceptance. The decorator nudge does not apply: this is a module-level Pydantic BeforeValidator, not a function taking a named external parameter the decorator could bind to. |
| `web/composer/redaction.py:R5:_coerce_stringified_json_object:fp=22c2fc298648b291` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is a placeholder \('ALLOWLIST-FRESH ... review whether this is new debt'\) — it does not justify the suppression at all, which fails the Auditability Standard for allowlist entries. The code is a Tier-3 boundary coercion function for LLM-supplied JSON \(the value parameter is external data\), and R5 is in the decorator's suppressible set, so the structural remedy is @trust_boundary\(source_param='value', suppresses=\('R5',\), ...\) on _coerce_stringified_json_object rather than a per-line allowlist entry. |
| `web/composer/redaction.py:R1:redact_source_storage_path:fp=2d2e6b24c7aafd25` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function's docstring describes it as operating on a 'serialized state dict' — a first-party shape produced by our own to_dict\(\) serialization. The rationale admits the function is called with multiple shapes \(to_dict output AND component dicts from _execute_get_pipeline_state\) and uses .get\(\) to paper over that polymorphism. That is exactly the schema/contract ambiguity R1 is meant to surface: the function has no typed contract for its input. The remediation is structural — either give the function a single typed input contract \(and have callers project to it\), or split it into two functions with clear shapes — not suppress the analyzer. This is internal first-party data, not a Tier-3 boundary, so the decorator does not apply. |
| `web/composer/redaction.py:R1:redact_source_storage_path:fp=f98ce159ca5d32fa` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims state_dict.source is 'raw JSON from the DB', but per policy our audit/landscape data is Tier-1 \(full trust, crash on anomaly\) — serialization doesn't change trust tier. If source dicts legitimately lack 'options' as a schema variant, the honest pattern is direct access with a documented schema contract, or an explicit key membership check \('options' in source\) that surfaces intent, not a silent .get\(\) that conflates 'missing key' with 'None value'. Additionally, the function is not decorated as a Tier-3 boundary and the parameter is a serialized state dict produced by our own code, so @trust_boundary is not the right structural fix either. The rationale also doesn't address R1's specific concern \(hiding missing-key bugs in a schema we control\). |
| `web/sessions/service.py:R6:SessionServiceImpl:persist_compose_turn:fp=1708f112cf7702e2` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale is entirely non-responsive to the flagged pattern. R6 fires on the \`except RuntimeError: pass\` block at line 1791-1794, which is a deliberate control-flow check for whether an event loop is running — it has nothing to do with 'session persistence boundary', 'optional envelope, fork, archive, or lock state'. The rationale appears to be boilerplate describing the function's broader purpose rather than justifying why this specific silent-except is legitimate. A correct rationale would explain that \`asyncio.get_running_loop\(\)\` is being used as a probe whose RuntimeError is the expected negative-result signal, not a swallowed error; absent that site-specific reasoning, the audit record would be uninterpretable to a future auditor. |
| `web/composer/tools/sessions.py:R1:_assert_affected_component:fp=bd8888c405f0f2b5` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The rationale claims options\['model'\] is Tier-3 because the LLM is 'still authoring the node', but options is the node's stored configuration on an existing pipeline node — first-party state, not the LLM tool-call argument. The LLM-supplied Tier-3 inputs here are affected_node_id and llm_draft; options is read from the node the operator is constructing/holding in session state. If the schema for an LLM_MODEL_CHOICE node guarantees options.model, this should be direct subscript and crash on absence; if the schema permits absence during authoring, the fix is to make that explicit in the contract and check membership offensively, not to .get\(\) it. Either way the decorator nudge does not apply because the subject is not rooted at an external parameter of this function. |
| `web/composer/tools/sessions.py:R5:_assert_affected_component:fp=d096c7dfd60bc4c9` | <pre-judge> | BLOCKED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is on \`options.get\('model'\)\` where \`options\` is a node's configuration dict — first-party pipeline state \(CompositionState/node.options\), not Tier-3 external data. The rationale conflates the LLM tool-call \`llm_draft\` argument \(which IS Tier-3\) with \`current_model\`, which is read from our own composition state and should have a guaranteed shape per the node-options contract. If \`options\['model'\]\` can be a non-string or missing, that is a contract/invariant question to fix at the node-options schema level, not to paper over with isinstance here. The decorator nudge does not apply: the subject is rooted at \`options\`/\`self\`-state, not at an external function parameter. |

## PRE_JUDGE_FRESH_ACCEPT (43)

| Entry | Original Verdict | Fresh Verdict | Fresh Model | Cause | Notes |
| --- | --- | --- | --- | --- | --- |
| `core/canonical.py:R6:_normalize_value:fp=b26d50562c16f852` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The try/except wraps np.isnan/np.isinf, which raise TypeError on non-numeric dtypes \(e.g., string arrays\) — this is a documented NumPy API behavior used here as a dtype dispatch mechanism, not silent error swallowing of an unknown failure mode. The suppressed exception is narrowly typed \(TypeError only\) and the semantic claim is correct: non-numeric arrays cannot contain NaN/Inf, so skipping the finite-value check is sound. The alternative \(pre-checking dtype with np.issubdtype\) is functionally equivalent; the try/except is an idiomatic way to handle NumPy's polymorphic numeric APIs. The data is a NumPy array being normalized for the audit trail — not a Tier-3 boundary, but the suppression is offensive in spirit \(it validates finite values for numeric arrays and crashes on NaN/Inf\) rather than defensive. |
| `core/landscape/data_flow_repository.py:R6:DataFlowRepository:create_row:fp=90984799b1de3e99` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except handles a genuine Tier-3 boundary case: quarantined row data originated from external sources and may contain NaN/Infinity or other non-canonical values that canonical_json rejects. The handler does not silently swallow — it falls back to a repr-based representation that preserves the data for audit purposes, which is the documented quarantine contract \(record what we got, even if malformed\). This is bounded to the quarantined branch only; the non-quarantined path correctly lets canonical_json raise. The fallback is meaning-preserving \(repr of actual data, not fabrication\) and the payload is still persisted to the audit store. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=188d682d00e17ea1` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged except clause catches TimeoutError from a 1.0s poll and continues the release loop — this is not silent error swallowing but the documented control-flow mechanism of a polling loop. TimeoutError here is a signal \('no result ready yet'\), not an error condition; the loop's job is to retry. The excerpt also shows the surrounding handlers \(ShutdownError for clean exit, generic Exception with explicit re-raise logic\) demonstrating that genuine errors ARE propagated. R6's concern \(swallowing errors without explicit handling\) does not apply to expected polling timeouts. |
| `plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=92c580e9594a61bd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The usage object is a provider SDK response wrapper — Tier-3 external data per the Plugin Ownership scope clause \('LLM provider's reply... is Tier-3 external data'\). The function explicitly normalizes through TokenUsage.from_dict, recording absence as None rather than fabricating zeros, which satisfies the Tier-3 boundary discipline \(coerce/validate at the border, record what we got\). R2 is not in the decorator-suppressible set {R1, R5}, so the structural alternative doesn't apply here. The rationale correctly identifies the shape-unguaranteed external origin and the honest-absence preservation. |
| `plugins/infrastructure/clients/retrieval/azure_search.py:R1:AzureSearchProvider:_parse_response:fp=866a8f498d10e0fe` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on \`item.get\("id"\)\` where \`item\` is an element of the Azure AI Search API response's 'value' array — Tier-3 external data per the Quick Reference \('Transform on external calls: external response is Tier 3'\). The code is recording what was received at the boundary in a skipped_items audit list; using \`.get\("id"\)\` correctly records absence as None rather than fabricating an ID, which aligns with the fabrication-decision test. The decorator nudge does not apply here because \`_parse_response\` takes \`response_data\` \(not \`item\`\) as its external parameter, and the subject is rooted at the loop variable \`item\`, not at a function parameter — a function-scoped @trust_boundary on response_data wouldn't cleanly cover this subject. The rationale correctly identifies the tier and the pattern matches honest Tier-3 absence-recording. |
| `plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_complete_ticket:fp=9b67bbe4194832ff` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The suppression sits at a known race-window: a worker thread completes a ticket that may already have been evicted by timeout or invalidated by buffer shutdown. The except clause catches a narrowly-scoped pair \(KeyError, ShutdownError\) — not a bare except — and records the discard via structured debug logging with token_id and state_id, which is the documented and expected behaviour \('discarding late results after timeout eviction' per the docstring\). This is not silent swallowing of arbitrary errors; it is the explicit handling of two well-defined concurrent-state outcomes that are semantically equivalent to 'ticket no longer exists to complete'. R6's concern \(errors hidden without a result path\) does not apply: there is no result to deliver because the ticket has already been resolved by another path \(eviction/shutdown\). |
| `plugins/transforms/web_scrape.py:R5:WebScrapeHTTPConfig:_validate_allowed_hosts:fp=922ac0bb4c76376b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The field is typed \`str \| list\[str\]\` and the validator must discriminate which branch to take — the isinstance check is a legitimate union-type discriminator at the Pydantic config boundary \(Tier-3 operator-supplied config\). This is not a defensive shape-guard hiding a bug; it's the only honest way to dispatch on a union type, and the function correctly validates both branches and raises on malformed input. The rationale's mention of ip_network is slightly misaimed \(the isinstance is for union discrimination, not CIDR validation\), but the suppression target is correct: R5 on a union-type discriminator at a config boundary is the rule's intended exception. The decorator is not applicable here — Pydantic validators aren't the @trust_boundary pattern, and source_param would not bind to \`v\` cleanly across classmethod validator signatures. |
| `telemetry/manager.py:R6:TelemetryManager:close:fp=79190bde3fa222f5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except queue.Full block is not silently swallowing an error — it implements an explicit retry-with-drain loop to guarantee sentinel insertion during shutdown, with a final explicit error path \(logger.error at line 600\) if the drain attempts fail. This is offensive programming at a concurrency primitive boundary: queue.Full is the documented signaling mechanism of Queue.put with timeout, not an unexpected fault. The inner queue.Empty handler similarly handles a documented race between put and get. R6's concern \(swallow without re-raise or explicit error result\) is directly addressed by the sentinel_sent flag and subsequent error logging. |
| `telemetry/manager.py:R6:TelemetryManager:close:fp=c87058caa78a1776` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except queue.Empty branch is a legitimate race-handling case in a concurrent shutdown drain: between the failed put \(queue.Full\) and the get_nowait, another consumer may have drained the item, making the queue empty. The 'pass' is not swallowing an error — it's the explicit, correct response to that race \(loop around and retry the put\). The outer loop bounds retries and logs an error if the sentinel ultimately fails to send, so failure is surfaced. This is concurrency control on a process-local queue, not Tier-3 data handling, and the decorator does not apply. |
| `web/catalog/routes.py:R1:get_schema:fp=bc80b0854f0cb34e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The \`plugin_type\` parameter is Tier-3 external data from a URL path, and the code uses \`.get\(\)\` on a constant lookup dict precisely to detect unknown values and raise a 404 — this is the honest Tier-3 boundary pattern: validate at the border, crash/reject on failure, do not fabricate. The \`.get\(\)\` here is not silent recovery; the immediate \`if singular is None: raise HTTPException\` makes the absence explicit and rejects the request. R1's concern \(hiding missing-key bugs\) doesn't apply because the missing key is the expected failure mode being explicitly handled. The decorator nudge doesn't fit cleanly: \`plugin_type\` is a scalar path param, not a dict/structure that the @trust_boundary decorator's source_param normalization targets. |
| `web/execution/progress.py:R8:ProgressBroadcaster:subscribe:fp=80dd3c1d05f93c94` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The setdefault here is a legitimate lazy-initialization pattern for a first-party in-process data structure: the outer dict maps run_id to a per-run subscriber dict, and the first subscriber for a run must create that inner dict. This is not the R8 anti-pattern of hiding missing-key bugs — there is no schema contract that run_id must pre-exist; absence is the expected first-subscription state. The data is Tier-1 internal process state, not external input, and the rationale honestly describes the lazy-mapping semantics. Direct access would require an explicit if-absent-create dance with identical semantics. |
| `web/execution/routes.py:R6:create_execution_router:websocket_run_progress:fp=03f80783c2d49864` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The except clause catches ValidationError and _RunStatusIntegrityError and responds with an explicit WebSocket close using code 1011 \(internal error\) and a clear reason, then returns. This is not silent swallowing — it converts the exception into an explicit error result on the WebSocket protocol channel, which is the appropriate way to signal an internal-accounting failure to a connected client rather than sending a malformed terminal event. The rationale accurately describes the R6 concern \(explicit error result vs swallow\) and the code excerpt confirms the explicit close-with-error-code is the chosen error surface. |
| `web/execution/preflight.py:R1:resolve_runtime_yaml_paths:fp=d0e7e2507b117f2d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function parses YAML produced by a generator and navigates the resulting dict structure. YAML content is Tier-3 external/untrusted shape \(yaml.safe_load returns arbitrary structure\), and the code correctly validates shape with isinstance checks raising TypeError on violations. The specific .get\("options"\) call is navigating a parsed YAML dict where 'options' is genuinely optional in the schema — absence is meaningful, not a bug. The code crashes loudly on shape violations and handles absence by skipping the block, which is honest Tier-3 boundary handling. The function is not parameterized on the external data via a single named source_param \(it takes pipeline_yaml as a string and parses it internally\), so the @trust_boundary decorator's source_param mechanism doesn't cleanly apply. |
| `web/composer/implicit_decisions.py:R5:_flatten_options:fp=24bbd76d54ccfe33` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function recursively flattens a Mapping\[str, Any\] where values are typed as Any by contract — the isinstance\(value, Mapping\) check is not a defensive shape-guard against untrusted data but a structural type-narrowing to distinguish nested mappings \(recurse\) from leaf values \(thaw and append\). This is a legitimate use of isinstance for runtime type dispatch on a heterogeneous Any-typed value space, which the Decision Test treats as offensive structural discrimination rather than defensive shape-guarding. The data is post-validation config, and there is no fabrication or silent recovery — both branches handle the value honestly. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_fields_from_discriminated:fp=627b5114d5635ef7` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code processes a JSON Schema produced by Pydantic, where a \`$defs\` variant entry legitimately may omit \`properties\` and \`required\` per the JSON Schema spec \(a variant with no required fields, or no declared properties\). This is a shape-variability case at a schema-introspection boundary: the absence of these keys is a valid JSON Schema state, not a bug to surface. The \`.get\(\)\` with an empty default preserves meaning \(no required fields = empty set; no properties = empty dict\), which is not fabrication since JSON Schema semantics define omission as equivalent to empty. The rationale correctly identifies the specific R1 concern \(missing key\) and explains why absence is legitimate at this site. |
| `web/catalog/service.py:R1:CatalogServiceImpl:_field_summary:fp=0fcb6d233deaafbe` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function processes JSON-Schema property entries, where 'default', 'description', and 'type' are genuinely optional per the JSON-Schema spec — absence is semantically meaningful and distinct from presence. JSON schemas here come from plugin manifests \(external/declarative configuration\), not a first-party typed contract guaranteeing these keys exist. Using .get\(\) with None/sensible fallback at this schema-introspection boundary is the correct pattern: missing 'default' means 'no default', not a bug. The rationale correctly identifies the boundary nature, though it's terse. The decorator nudge doesn't fit cleanly — field_schema is not the function's external-source parameter root in the @trust_boundary sense \(it's a nested schema fragment passed by an internal caller\), and there are multiple .get\(\) sites that are all legitimate JSON-Schema optional-field reads. |
| `web/composer/llm_response_parsing.py:R2:_provider_details_payload:fp=2a1833c0a52a5e5a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _provider_details_payload normalizes provider response metadata from LiteLLM/provider SDKs — Tier-3 external data per the Plugin Ownership scope clause \(external system controls the shape\). The getattr with None default is the correct boundary pattern: it records absence as None \(not a fabricated zero\), preserving the audit-honest distinction between 'attribute missing' and 'attribute present with value'. The surrounding code already handles Mapping and None shapes explicitly, and the getattr branch handles object-shaped provider responses; this is coerce-at-the-border into a known shape. R2 is not in the decorator's suppressible set {R1, R5}, so the structural decorator path does not apply here. |
| `web/composer/llm_response_parsing.py:R2:token_usage_from_response:fp=b05d403d618526a2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The finding is on getattr\(\) applied to a LiteLLM/provider usage object — an externally-sourced wrapper whose shape is not guaranteed by any first-party ELSPETH contract. Per the Plugin Ownership scope clarification, LLM provider replies wrapped by third-party SDKs like LiteLLM are explicitly Tier-3, and shape-guarding their fields is the legitimate boundary pattern. The else-branch handles the object-shaped case \(the if-branch already handled Mapping\), and absent fields are recorded as None rather than fabricated defaults, consistent with the Tier-3 absence-recording rule. R2 is not in the decorator's suppressible set \(only R1/R5\), so the decorator nudge does not apply here. |
| `web/composer/llm_response_parsing.py:R2:_provider_cost_from_response:fp=00217f7aa707a30d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on a LiteLLM provider response object — externally-sourced Tier-3 data per the Plugin Ownership scope clarification \(external system controls the shape, no ELSPETH contract guarantees it\). The getattr on 'usage' guards a shape that genuinely varies across providers \(mapping-shaped, object-shaped, or absent\), and the function honestly records absence as None with a NOT_AVAILABLE source sentinel rather than fabricating a default value. Value-level threats \(negative, non-finite, bool-typed\) are separately checked. The decorator nudge does not apply: R2 is not in the decorator's suppressible set {R1, R5}. |
| `web/composer/llm_response_parsing.py:R2:_provider_cost_from_response:fp=e68305badf66bd65` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on a LiteLLM response object whose shape is provider-controlled Tier-3 external data, explicitly excluded from Plugin-Ownership per policy \('an LLM provider's reply wrapped by a third-party SDK such as LiteLLM' is Tier-3\). The getattr on usage.cost guards a provider-variable attribute that may be absent or differently-shaped across providers, and the function honestly records absence as None rather than fabricating a default \(returns PROVIDER_COST_SOURCE_NOT_AVAILABLE\). The rationale correctly identifies the boundary and the code matches the claim. Note: R2 is not in the @trust_boundary decorator's suppressible set {R1, R5}, so the decorator nudge does not apply here. |
| `web/composer/llm_response_parsing.py:R2:safe_response_model:fp=0c7de3a07a40310f` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function safe_response_model operates on a LiteLLM/provider response object whose shape is not guaranteed by any first-party ELSPETH contract — per policy, provider SDK wrappers are Tier-3 external data even when our code holds the reference. Guarding the shape with getattr\(response, 'model', None\) at this boundary is the legitimate Tier-3 pattern, and the code correctly validates the value \(isinstance str + non-empty\) rather than fabricating a default. The decorator nudge does not apply cleanly here: the parameter is named 'response' but is typed Any\|None and represents a provider object rather than the typical arguments-dict shape; still, this is a per-line suppression and the rationale honestly identifies the boundary. |
| `web/composer/llm_response_parsing.py:R2:_response_field:fp=66a71df341185085` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function _response_field is a Tier-3 boundary helper for LiteLLM/provider response objects, which the policy explicitly identifies as external data whose shape ELSPETH does not guarantee \(provider SDKs return mapping-shaped or object-shaped responses depending on provider\). The getattr with None default is the correct coerce-to-known-shape pattern at the boundary — absence is recorded as None rather than fabricated, matching the Tier-3 quick reference. R2 flags getattr-with-default as potentially hiding contract bugs, but here there is no first-party contract to violate; the dual mapping/object handling directly addresses the provider-variability the rule would otherwise mask. The @trust_boundary decorator nudge does not apply because R2 is not in the decorator's suppressible set {R1, R5}. |
| `web/composer/llm_response_parsing.py:R2:_json_safe_provider_artifact:fp=a50249b4c425b80e` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function normalizes arbitrary provider artifact objects from LiteLLM/SDK responses into JSON-safe form — Tier-3 external data per the Plugin Ownership scope clarification \(LLM provider replies wrapped by third-party SDKs are externally-sourced, shape-unguaranteed\). The getattr\(\) probes for model_dump/to_dict/dict are the standard duck-typing pattern needed because different providers expose different serialization methods; this is shape-discovery at a Tier-3 boundary, not defensive suppression of a first-party contract bug. The function falls through to repr\(\) rather than fabricating a default, which preserves the 'record what we got' discipline. The decorator nudge does not apply cleanly here: the parameter is typed Any \(not an external-payload dict\), the rule is R2 \(not in the {R1, R5} suppressible set\), and the pattern is method-probing rather than key/shape guarding. |
| `web/composer/llm_response_parsing.py:R2:_json_safe_provider_artifact:fp=c958b58e1cfbd5e2` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Tier-3 boundary case: _json_safe_provider_artifact normalises provider response objects from LiteLLM/SDK wrappers whose shape is not guaranteed by any first-party contract \(per policy, external-API response bodies are Tier-3 even when wrapped by our client code\). The getattr probes for model_dump/to_dict/dict are duck-typing across provider-specific shapes — exactly the shape-guarding pattern policy permits at a Tier-3 boundary. The decorator nudge does not apply: R2 is not in the decorator's suppressible rule set \({R1, R5}\), and the function parameter is a recursive Any value, not a named external source_param the decorator would scope to. |
| `web/execution/schemas.py:R1:RunEvent:_resolve_data_from_event_type:fp=020df37cc90810ad` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a Pydantic before-validator operating on raw deserialization input \(\`values: Any\`\) — genuinely Tier-3 data whose shape is not yet guaranteed by any contract \(that's precisely what the validator establishes\). The code first guards with \`isinstance\(values, dict\)\` and then uses \`.get\(\)\` to read fields that may be absent in malformed input, falling through to let Pydantic's normal validation reject the payload. R1's concern \(hiding missing-key bugs\) doesn't apply: missing keys here are an expected Tier-3 condition, and the subsequent \`isinstance\(data, dict\)\` and dict-membership checks ensure the fabrication path isn't taken when fields are absent. The decorator nudge doesn't fit cleanly — \`values\` is the parameter but it's \`Any\`, and this is a classmethod model_validator where the @trust_boundary pattern is awkward; per-line suppression is reasonable here. |
| `web/sessions/routes/_helpers.py:R2:_litellm_error_detail:fp=6eff26e727be1d1a` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The getattr is on a LiteLLM-native exception object whose shape is not guaranteed by any first-party contract — different provider exception classes from the LiteLLM SDK may or may not carry a status_code attribute. This is the Tier-3 external-boundary pattern per the Plugin Ownership scope caveat \(externally-sourced wrapper objects are Tier-3 regardless of who called them\). The code also correctly validates the value's type afterward \(type\(...\) is int else None\) rather than fabricating a default, recording absence as None. R2 is not in the decorator's suppressible set \({R1, R5}\), so the decorator nudge does not apply. |
| `web/sessions/routes/_helpers.py:R1:_extract_runtime_model_snapshot:fp=a1dd46422475f434` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code is reading optional keys 'model' and 'model_version' from a Tier-1 audit options mapping where absence is a legitimate, expected state \(str-or-absent shape, per the AuditIntegrityError messages\). The .get\(\) here is not silently swallowing a missing required key — the shape invariant \(options is a Mapping\) is already enforced offensively above with an explicit isinstance/raise, and the type of any present value is then asserted with exact-type checks that raise AuditIntegrityError. Using .get\(\) to distinguish 'absent' from 'present-with-wrong-type' is the correct expression of the documented optional-key contract; direct subscripting would conflate absence with a bug. The rationale's framing \(HTTP boundary\) is weak, but the code independently supports the suppression under the Tier-1 optional-field pattern. |
| `web/sessions/routes/_helpers.py:R5:_dispatch_guided_respond_impl:fp=d6b20d8b8dacf40d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The isinstance check is at a genuine HTTP/Tier-3 boundary: sample_rows comes from an external request payload where any element could be arbitrary JSON. The guard validates shape before calling dict\(r\), and on shape failure it raises HTTPException 400 \(loud failure / record-what-we-got\), not silent recovery with a fabricated default. This matches the Tier-3 boundary pattern R5 explicitly permits: validate at the boundary and crash/quarantine on shape mismatch. The decorator nudge does not apply cleanly here — the function is an HTTP route handler whose external data flows through multiple nested dicts rather than a single source_param dict, and trust_boundary suppression on a deeply-nested element check would be over-broad. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:recompose:fp=65afef4550e989c5` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is R7 \(contextlib.suppress\) on an asyncio.CancelledError around an asyncio.shield\(\)ed persistence call in a cancellation-handling path. The suppression is narrowly typed to CancelledError only, scoped to the shielded persist/publish call, and exists because the surrounding handler is itself running under cancellation — re-raising CancelledError here would abort the shielded persistence of llm_calls and progress events, which is exactly what the shield is meant to prevent. The rationale identifies the cancellation-mirror pattern and an explicit cross-reference exists in the code comment to send_message's documented rationale. This is offensive handling of a known structural concern \(cooperative cancellation during audit-critical persistence\), not silent error swallowing of unknown failures. |
| `web/sessions/routes/composer.py:R7:register_composer_routes:recompose:fp=b22f01dc72b4660b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a CancelledError handler doing best-effort shielded persistence/publish during request cancellation. The contextlib.suppress\(asyncio.CancelledError\) narrowly catches re-cancellation of the shielded task during shutdown — the outer \`raise\` immediately re-raises the original CancelledError, so the suppress does not swallow the cancellation signal, only prevents the cleanup from masking it. This is a legitimate, narrow exception class \(not bare Exception\) at a cancellation boundary where the alternative is losing the audit/progress event entirely. R7's concern \(silent broad suppression\) does not apply: the suppression is class-narrow, intentional, and the cancellation propagates via the explicit raise. |
| `web/app.py:R1:create_app:fp=7ce3ee8e08561e45` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged call is os.environ.get\("ELSPETH_ENV"\) — reading an optional environment variable, which is a genuine Tier-3 external boundary \(process environment is not under ELSPETH's typed contract\). The .get\(\) correctly treats absence as 'not test' rather than crashing, and the comparison to "test" means a missing var falls through to the safe \(crash\) branch — no fabrication. The R1 pattern \(direct dict access\) would be wrong here since env vars are inherently optional. Rationale is terse but accurate and site-specific. |
| `web/app.py:R1:create_app:fp=f228499fcfff4222` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged call is os.environ.get\("WEB_CONCURRENCY", "1"\) — reading an optional environment variable at a genuine Tier-3 external boundary \(process environment supplied by the deployment platform\). The default "1" is not fabrication: it expresses the documented semantic that absent WEB_CONCURRENCY means single-worker, and the string is immediately validated/coerced by _parse_worker_count. The rationale, while terse, correctly identifies the tier and the pattern is the canonical legitimate use of dict.get on os.environ. The decorator nudge does not apply: os.environ is a module global, not a function parameter, so source_param has nothing to bind to. |
| `web/composer/source_inspection.py:R5:_declared_field_name:fp=c2d07dc1c5c6d068` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on DeclaredFieldSpec which is operator-supplied configuration \(a union accepting either a string or a dict\). The isinstance check at line 773 is shape validation on a Tier-3 value \(a field within an externally-sourced dict whose 'name' value's type is not contractually guaranteed\). The guard correctly returns None on the non-str path rather than fabricating — letting the caller handle absence honestly. This is the legitimate Tier-3 boundary pattern: validate shape, record absence as None. The decorator nudge does not cleanly apply here because the function takes a DeclaredFieldSpec union \(not a clearly-named external param dict\) and is a helper called from the boundary rather than the boundary function itself; suggesting source_param would be awkward. |
| `web/execution/validation.py:R1:validate_pipeline:fp=0c7d6399be7e8ecd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code iterates over optional keys \('path', 'file'\) in user-authored pipeline YAML sink options — Tier-3 external configuration. Neither key is required \(they're mutually exclusive alternatives\), so .get\(\) returning None for the absent key is the correct way to probe optional fields at the configuration boundary, and the immediate \`if value is not None\` check handles absence without fabrication. This is a legitimate Tier-3 boundary guard on user-supplied config, and the rationale correctly identifies the trust tier and the mutual-exclusivity contract. The decorator nudge does not apply cleanly because the external data is reached via \`state.outputs\[\].options\`, not a direct function parameter named in the loop. |
| `web/composer/tools/generation.py:R5:_source_schema_mode:fp=30e852fafe54278d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on source.options\['schema'\], which originates from LLM-authored tool-call arguments — Tier-3 external data with no shape guarantee. The isinstance\(schema, Mapping\) check is a legitimate Tier-3 boundary shape guard before subscripting, and the function correctly returns None on shape mismatch rather than fabricating a value. The R5 rule explicitly permits isinstance at Tier-3 boundaries, and this matches that pattern. Decorator nudge does not apply: the parameter is a typed SourceSpec \(first-party contract\), not a raw external dict, and the external-ness enters via .options.get\('schema'\), not via a function parameter the decorator could bind to. |
| `web/composer/tools/generation.py:R1:_source_schema_mode:fp=23513e8813c297f6` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The function operates on \`source.options\`, which is operator/LLM-authored configuration data — Tier-3 external content whose internal shape is not guaranteed by any first-party contract. The code has already narrowed \`schema\` to a Mapping at line 848 and uses \`.get\("mode"\)\` to handle the legitimately optional key, then validates the value's shape with isinstance before normalizing. This is the honest Tier-3 boundary pattern: record absence as None rather than fabricating a default. The function is small and not decorated, but the suppression is for a single legitimate optional-field lookup; the decorator would also be reasonable but the per-line entry is defensible here since \`source\` is a typed SourceSpec parameter, not a raw external payload — the Tier-3 surface is specifically \`source.options\`'s contents. |
| `web/composer/tools/generation.py:R1:_numeric_aggregation_diagnostics_for_observed_csv:fp=9c1695c48f0efa04` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows \`options\` is returned from \`get_aggregation_contract_options\` on LLM-authored node configuration — Tier-3 data whose shape is not contractually guaranteed at this point. The code immediately follows the \`.get\(\)\` with a strict shape/value check \(\`type\(value_field\) is not str or not value_field.strip\(\)\`\) and abstains from emitting a diagnostic on malformed input, which is the honest Tier-3 boundary pattern \(guard the shape, fail-safe by skipping\). The rationale correctly identifies the tier and the abstention behaviour, and the surrounding line directly substantiates it. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=0945f207a98cbbff` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged \`.get\('blob_ref'\)\` accesses \`source.options\`, which is LLM-authored content from set_source/set_pipeline tool calls — Tier-3 external data per the policy's 'Transform on external calls' clause. The field is schema-optional: present only when a blob was wired in, absent for path-based sources. The function correctly handles absence by returning an empty diagnostics list rather than fabricating a default, matching the Tier-3 boundary pattern \(record/handle absence honestly\). Since this is not inside a function that takes the external data as a parameter \(it's reached via \`state.source.options\`\), the @trust_boundary decorator nudge does not apply. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=f0e165ac927832cd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The flagged .get\('mode'\) is operating on a value \(schema\) that has just been narrowed to Mapping but whose contents originate from LLM-authored source.options\['schema'\] — an unstructured, externally-influenced payload with no first-party shape contract over its inner fields. Probing 'mode' with .get and comparing to a discriminator set is the correct Tier-3 boundary pattern: it neither fabricates a default nor silently coerces; absence simply fails the discriminator and skips the diagnostic branch. The inline code comment explicitly identifies the trust-tier transition, and the rule R1 concern \(hiding missing key bugs\) does not apply because there is no upstream contract guaranteeing 'mode' exists. |
| `web/composer/tools/generation.py:R1:compute_proof_diagnostics:fp=157afff4c5b89efd` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The excerpt shows schema is read from source.options at "schema" as an unstructured Mapping whose contents are LLM-authored — the surrounding comment explicitly identifies it as Tier-3 shape-unguaranteed data, and the preceding isinstance\(schema, Mapping\) check guards it as such. The flagged .get\("fields"\) accesses an optional field within this unstructured external mapping, and the subsequent isinstance check at line 1219 validates the shape before use. This is the legitimate Tier-3 boundary pattern: coerce absent/falsy to empty tuple, then shape-check. The function appears too large for a function-scoped @trust_boundary decorator to be the right structural fit, and schema is not a direct parameter of the enclosing function. |
| `web/execution/service.py:R8:ExecutionServiceImpl:execute:fp=ae8955d9fc074acc` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | This is a legitimate and idiomatic use of setdefault: lazily creating a per-session asyncio.Lock on first access to serialize concurrent execute\(\) calls \(the TOCTOU comment makes the intent explicit\). The R8 rule targets setdefault as a way to hide missing-key bugs in schema-bearing dicts; here the dict is a process-local lock registry where 'key missing' is the expected first-call state, not a bug. The atomic get-or-create semantics of setdefault are exactly what's needed to avoid a race between check and insert. No external data, no trust-tier confusion, no fabrication. |
| `web/execution/service.py:R1:ExecutionServiceImpl:_execute_locked:fp=a91add5d5970814d` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The code is performing a path-allowlist security check on user-authored composition state options — a Tier-3 external boundary. The options dict is user-supplied configuration where 'path' and 'file' keys are genuinely optional \(only one or neither may be present depending on source type\). The .get\(\) correctly returns None for absent keys, and the immediate \`if value is not None\` guard handles absence honestly without fabrication — no default substitution, no inference. This is the legitimate Tier-3 boundary pattern: probe for an optional key, skip validation cleanly if absent. The decorator nudge does not apply because the data is accessed via \`composition_state.source.options\`, not rooted at a function parameter named in a source_param. |
| `web/composer/service.py:R2:ComposerServiceImpl:_call_text_llm:fp=9475aed316c9be0b` | <pre-judge> | ACCEPTED | anthropic/claude-4.7-opus-20260416 | PRE_JUDGE_BASELINE | The getattr\(\) target is a LiteLLMBadRequestError exception instance from the LiteLLM SDK — a third-party library wrapping an external LLM provider response. Per the Plugin Ownership scope clarification, exception objects from external SDKs are not first-party typed contracts; the provider's status_code attribute is not guaranteed to exist on every BadRequest variant. Guarding the shape of an externally-sourced exception attribute with getattr\(..., None\) at this Tier-3 boundary is the honest pattern, and recording absence as None \(rather than fabricating a status code\) matches the fabrication-decision test. The decorator nudge does not apply: R2 is not in the decorator's suppressible set {R1, R5}, and the subject is an exception object, not a source_param. |
