{
  "knob_schema": {
    "fields": [
      {
        "description": "Schema configuration for data validation. Use 'schema: {mode: observed}' to infer types from data, or provide explicit field definitions with mode (fixed/flexible).",
        "kind": "json-value",
        "label": "schema_config",
        "name": "schema_config",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "required_input_fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Name of the field containing prompt/treatment variant labels",
        "kind": "text",
        "label": "variant_field",
        "name": "variant_field",
        "nullable": false,
        "required": true
      },
      {
        "description": "Name of the numeric outcome field to compare",
        "kind": "text",
        "label": "score_field",
        "name": "score_field",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Optional baseline variant label. When omitted, the first-seen variant is the baseline.",
        "kind": "text",
        "label": "baseline_variant",
        "name": "baseline_variant",
        "nullable": true,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "batch_experiment_compare"
}
