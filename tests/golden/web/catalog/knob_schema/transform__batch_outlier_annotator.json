{
  "knob_schema": {
    "fields": [
      {
        "description": "Schema configuration for data validation. Use 'schema: {mode: observed}' to infer types from data, or provide explicit field definitions with mode (fixed/flexible).",
        "kind": "json-value",
        "label": "schema_config",
        "name": "schema_config",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "required_input_fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Name of the numeric field to annotate",
        "kind": "text",
        "label": "value_field",
        "name": "value_field",
        "nullable": false,
        "required": true
      },
      {
        "default": "outlier",
        "description": "Prefix used for emitted annotation fields",
        "kind": "text",
        "label": "output_prefix",
        "name": "output_prefix",
        "nullable": false,
        "required": false
      },
      {
        "default": 3.0,
        "description": "Absolute sample z-score threshold for outlier annotation",
        "kind": "number-float",
        "label": "z_threshold",
        "name": "z_threshold",
        "nullable": false,
        "required": false
      },
      {
        "default": 3.5,
        "description": "Absolute modified z-score threshold based on median absolute deviation",
        "kind": "number-float",
        "label": "robust_z_threshold",
        "name": "robust_z_threshold",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "batch_outlier_annotator"
}
