{
  "knob_schema": {
    "fields": [
      {
        "description": "Schema configuration for data validation. Use 'schema: {mode: observed}' to infer types from data, or provide explicit field definitions with mode (fixed/flexible).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema_config",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "Required Input Fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Mapping from existing input field names to output field names.",
        "kind": "json-object",
        "label": "Mapping",
        "name": "mapping",
        "nullable": false,
        "required": false
      },
      {
        "default": false,
        "description": "When true, emit only fields named in the mapping.",
        "kind": "checkbox",
        "label": "Select Only",
        "name": "select_only",
        "nullable": false,
        "required": false
      },
      {
        "default": false,
        "description": "When true, fail if any mapped source field is missing from an input row.",
        "kind": "checkbox",
        "label": "Strict",
        "name": "strict",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "field_mapper"
}
